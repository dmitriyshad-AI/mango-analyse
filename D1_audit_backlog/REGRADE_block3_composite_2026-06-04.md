# Регрейд по сырью — Блок 3 (составные). Прогон M1 на 23838332. 2026-06-04.

Ревьюер: Клод #1. Набор block3_composite (12). Агрегат: PASS/PWN/FAIL=2/9/1, hard_gate=1 (`foton_2part_trial_price`).

## ПОПРАВКА 2026-06-04 (Дмитрий): «1 выдумка» и «пробел faithfulness» — МОЯ ОШИБКА, СНЯТО.
«Оформление дистанционно» — ПРАВДА и ГРУНТОВАНО (в снимке v6.4_schedule «оформление дистанц» ×2; bot fact-audit
`has_unverified_claim=false`). Судья ложно флагнул fabrication (неполный снимок судьи). Faithfulness отработал ВЕРНО.
Промт Кодексу на «расширить faithfulness на операционные claim'ы» — ОТОЗВАН (заставил бы блокировать правду). Урок: метку
fabrication ВСЕГДА сверять grep'ом по client_safe ДО вывода. Реальный вердикт Блока 3: контроли держат, выдумки 0,
составной работает (иногда по частям через ходы). Ниже — исходный (ошибочный по FAIL) текст оставлен для истории.

## ВЕРДИКT (исходный, скорректирован выше): контроли держат, составные частично. Флаг НЕ включать без 3.1 (хвост).

### Держит
P0 в части → весь ход менеджеру (`foton_composite_p0_part`); одиночный вопрос → не мисфайрит (`unpk_single_control`);
cross-brand часть → каноничная фраза («Это отдельные организации…»). Бренд/P0 чисто.

### Проблемы
1. **Роняет хвост.** На 3-частном вопросе бот часто отвечает на 1-2 части: `unpk_2part_two_classes` (показал только 9
   класс, 11 уронил), `foton_3part_discount_installment` (только цена, скидку/рассрочку не показал). Ядро блока
   (затронуть ВСЕ части) выполняется ненадёжно.
2. **FAIL `foton_2part_trial_price` — ЛОЖНАЯ метка судьи, НЕ выдумка** (поправка Дмитрия + grep по базе): «оформление
   дистанционно» ГРУНТОВАНО (в снимке «оформление дистанц» ×2; bot fact-audit `has_unverified_claim=false`). Судья
   флагнул из-за неполного снимка. Реальной выдумки в Блоке 3 НЕТ.

### Faithfulness — ПРОБЕЛА НЕТ (снято)
Критик отработал ВЕРНО (claim был грунтован). Предыдущий вывод про «пробел faithfulness» — моя ошибка от доверия метке
судьи без grep'а. Промт «расширить faithfulness на операционные claim'ы» ОТОЗВАН (заставил бы блокировать ПРАВДУ).

## Правки
- **Блок 3.1 (НЕОБЯЗАТЕЛЬНО, низкий приоритет, флаг TELEGRAM_Q_COMPOSITE):** составной иногда отвечает на части ЧЕРЕЗ
  ходы, а не всё разом (`unpk_2part_two_classes` — показал 9, 11 в этом ходе не дал). Полировка: по возможности
  затрагивать ВСЕ части в одном ответе, ничего НЕ выдумывая ради полноты (выдумок и так нет). Не блокер — контроли
  держат, безопасность 0.
- **Faithfulness — НЕ трогать** (пробела нет).
NEG: контроли (P0-весь-turn, single, brand) не сломать. Полный pytest green.
