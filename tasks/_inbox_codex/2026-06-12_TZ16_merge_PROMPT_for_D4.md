# Промт для D4: мерж ТЗ-16 + перенос артефактов + уборка дерева (выдаёт Дмитрий)

Ты — D4. Регрейд ТЗ-16 пройден (`D1_audit_backlog/existing_clients/REGRADE_TZ16_2026-06-12.md`, PASS_WITH_NOTES). Закрой хвост тремя действиями, строго в этом порядке:

1. **Перенос артефактов в канон (ДО любой уборки).** Папку `Mango-analyse-tz16/product_data/customer_profiles/tz16_profiles_v7_20260612/` целиком перенести в `product_data/customer_profiles/` ГЛАВНОЙ папки (`Mango analyse`). Проверка: число и размеры файлов совпадают, sqlite открываются, в git ничего не добавилось (папка под ignore). Вспомогательные `-wal/-shm` файлы корректно закрыть (checkpoint) перед переносом.

2. **Мерж.** Одобрение Дмитрия = выдача этого промта. `codex/tz16-d4-profiles-v7-rerun-tail` → `main` отдельным merge-коммитом. Приёмка: дифф пре-мерж..main = только файлы ТЗ-16; полный pytest на main зелёный; запретных путей нет; `git status` чистый.

3. **Уборка.** Удалить рабочее дерево `Mango-analyse-tz16` штатной командой git worktree (только после пунктов 1–2). Главную папку (старая ветка ТЗ-14, чужие незакоммиченные правки draft_loop) НЕ переключать и НЕ чистить.

Запрещено: запись в AMO/Tallanto/CRM; полный перепрогон 77 (отдельное «да» Дмитрия после ревью малого батча); этап Б карточек.

Отчёт в `tasks/_done/` канона: хэш merge-коммита, вывод проверки диффа, подтверждение переноса (счётчики файлов), подтверждение удаления дерева.
