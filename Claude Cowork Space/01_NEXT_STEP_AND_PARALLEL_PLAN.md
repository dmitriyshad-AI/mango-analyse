# Следующий шаг + план параллельной работы (после ТЗ v2)

Пин проверки: git `48935d60`. Claude — read-only по репозиторию.

## A. Независимая проверка билда Кодекса (что не доведено)

Фундамент заложен: `answer_contract.py` (все поля), `answer_safety_classifier.py`, фикс инструкции судьи,
финальный P0-override. НО три глубоких пункта ОТКРЫТЫ (evidence на HEAD 48935d60):
1. **P0-recall дыра открыта.** «мошенн/незаконн/жалова(ться)» нет в channels; новый классификатор зовёт
   старый `_has_complaint_signal` (answer_safety_classifier.py:121,203), который их не ловит. → safety.
2. **Консолидация наполовину:** новый классификатор обёртка над старыми `_has_*_signal`
   (conversation_intent_plan.py:269/282/290), а не замена. Дублирование осталось.
3. **Зелёные safe-template не убраны, только загейчены:** 29 вызовов `_safe_template(` в subscription_llm
   (88 упоминаний). Branch-count, который должен был ПАСТЬ, не упал.

Вывод: не строить новое поверх — сначала добить эти 3 пункта и честно замерить.

## B. ГЛАВНЫЙ диалог Кодекса (channels — один поток, не параллелить)

Задача (она и есть «большая»): закрыть 3 пункта → измерить.
1. P0-recall в ЕДИНОМ классификаторе: `жалоб\w*`→`жал(?:об|у|ова)\w*`, добавить `мошенн\w*`,`незаконн\w*`;
   recall-набор реальных P0 в тесты рядом с benign (ложный P0=0, recall=100%).
2. Завершить консолидацию: старые `_has_*_signal` должны остаться ОДНИМ источником внутри классификатора
   или быть удалены; не два параллельных пути. Branch-count зафиксировать ДО/ПОСЛЕ — он должен упасть.
3. Реально убрать зелёные safe-template-подмены (по теме за раз, под флагом), а не только гейтить;
   контракт+факты+few-shot+генерация вместо подмены.
4. Затем замер: targeted16 (dev-сигнал) → разбор PASS_WITH_NOTES ПО КЛАССАМ → архитектурные правки →
   **чистый holdout от Claude (eval-only, не тюнить)** → full v8.
Анти-хардкод и метрика числа веток — по ТЗ v2, раздел 2.

## C. Параллельные диалоги Кодекса (только независимые файлы, низкий риск коллизии)

Правило: параллельные диалоги НЕ трогают `channels/` одновременно (это территория главного диалога).
- **Диалог B — гигиена KB:** прогнать smoke v6.3; свести сборку KB к ОДНОМУ источнику (убрать хардкод-патчи
  цен в `build_kb_release_v6_1...`); файлы: `knowledge_base`/build-скрипты + данные KB. Не channels.
- **Диалог C — housekeeping:** пометить SaaS/agent_runtime/mail_archive как parked, снять с обязательного
  зелёного прогона; один живой roadmap; ретайрнуть `TZ_FULL_VISION` как исторический. Только конфиг/доки,
  без удалений (удаления — отдельно, с подтверждением).
- НЕ параллелить: подключение `customer_timeline` (коллизия с context builder = channels). Готовить
  внутри модуля (ослабить пере-строгий гейт, read-only provider), финально подключать ПОСЛЕ замера бота.

## D. Что делает Claude параллельно (read-only / артефакты, не трогая репозиторий)

1. Проверить билд (сделано: 3 пункта выше) + после прогонов независимо переграйдить выборку holdout (real vs strict).
2. Собрать **DEV-набор** для итерационного цикла (чтобы holdout оставался чистым; Кодекс тюнит на dev, не на holdout).
3. Вести этот план + сверять branch-count и метрики при каждом отчёте Кодекса.
4. Дописать детальный план подключения parked-модулей (раздел E) и forward-roadmap (раздел F).

## E. Parked-модули: куда, когда, как подключать

| Модуль | Когда | Как |
|---|---|---|
| customer_timeline (15K, построен, off) | СРАЗУ после замера бота (highest-ROI) | ослабить soft-гейт `p1_missing_recommended_action` (82% manual самоналожено); read-only provider → в Answer Contract как `known_client_history`; preview под флагом → основное чтение с fallback |
| read_only_customer_context | уже в работе (Кодекс вернул в промпт) | проверить, что реально доходит и используется в ответах |
| insights/ROP-паки | как оффлайн-материал для gold/few-shot | не в реальном времени; кандидаты в корпус через approval |
| amocrm/Tallanto read | при персонализации | read-only контекст; запись — позже, через одобрение менеджера |
| deal_aware / agent_runtime / AMO writeback | поздно, по готовности | держать выключенными; включать по одному, с dry-run→canary |
| productization/SaaS-ядро | при продуктизации (последним) | разморозить ядро захвата; остальной каркас — когда есть внешний клиент |

## F. Forward-roadmap к «человечному ИИ-сотруднику» (после доказанного качества)

1. **Качество доказано** на чистом holdout + «отправил бы без правок %» + ежедневное использование менеджерами.
2. **Персонализация историей:** включить customer_timeline → бот отвечает с учётом реальной истории клиента
   (вся премиса проекта; актив уже построен).
3. **Рабочее место менеджера:** черновик → отправка по одобрению одной кнопкой; P0/feedback/daily-цикл как рутина.
4. **Проактивность:** мягкие напоминания/догрев после утверждения правил частоты (не раньше).
5. **Ширина каналов:** форма сайта, почтовые черновики, потом Max.
6. **AMO/Tallanto-запись:** через одобрение → точечная автоматизация безопасных полей.
7. **SaaS для других организаций — последним**, при неоспоримой внутренней пользе.

Принцип на всём пути (антидот переусложнению): доводить до пользы один слой, мерить на чистом holdout,
не строить ширину вперёд; строки-без-потребителя не должны расти.
