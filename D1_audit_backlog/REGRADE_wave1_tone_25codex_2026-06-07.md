# РЕГРЕЙД — волна 1 тона: 20260607_wave1_tone_25_codex vs базлайн v2 (M1, GPT, parallel 4, один набор)

**Вердикт: PASS_WITH_NOTES — волна 1 работает (дампы 7→2, закрытия срабатывают, судья 0 FAIL, безопасность чиста), но найден 1 реальный баг закрытий — фикс до волны 2.**

## Победы (по сырью, не по сводке)

| Что | Базлайн v2 | Волна 1 |
|---|---|---|
| Ходы с роботскими дампами | 7 | **2** (оба — НЕ детерминированные: модель сама пишет «По подтверждённым ценам…» в прозе) |
| Судья PASS/PWN/FAIL | 4/20/1 | **7/18/0**, hard_gate 0 |
| Close-детект | — | fired 7; подавления корректны: P0 ×2, pending-менеджер ×1, handoff ×3 |
| Закрытия без повтора цен | — | подтверждено (шаги-тексты без чисел) |
| P0-деферралы | 9 | 8 (на месте) |
| Верификатор | 3.95% | 5.48% — в бюджете ≤10%, unavailable 0 |
| tone_score | 59.0 | 58.2 — **предсказанная** лёгкая просадка (старые дампы случайно матчили warm-регексы); НЕ провал |

## Баг (реальный, до волны 2)

**ov_price_close T3→T4: повторный дожим контакта после явного отказа.**
- T3 (закрывашка) → контакт-шаг: корректно.
- T4 клиент: «**Нет, пока не нужно**, мы просто хотели понять условия. Спасибо» → бот ДОСЛОВНО повторил тот же контакт-шаг.
- Три нарушения ТЗ сразу: (1) `contact_requested` НЕ пишется в память — после каждого хода None (поле добавлено, запись не реализована); (2) дедуп `previous_bot_texts` не применён к закрытиям (дословный повтор); (3) явный отказ от шага должен давать завершение БЕЗ дожима (NEG-4 класс — сработал в живую).
- Мелочь: счётчик `by_step contact=10 > fired=7` — шаг считается и на подавленных ходах (косметика).

## Остаточное (не баг волны 1)

«По подтверждённым ценам…» ×2 — модель сама пишет это семейство в прозе (рамки заменяют только детерминированные префиксы). Лечение: запрет семейства в промпте черновика (волна 2) + опционально паттерн в санитайзер.

## Решения

1. Микрофикс Кодексу до волны 2: запись contact_requested при fired+contact; дедуп закрытий; отказ-детект («нет, не нужно» после предложения) → тёплое завершение без CTA; косметика счётчика.
2. В промпт волны 2 — запрет семейства «по подтверждённым/проверенным…» для модели.
3. Замер волны 2 — против ЭТОГО прогона (та же машина/набор).
