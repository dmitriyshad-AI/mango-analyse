# Бэклог независимых read-only проверок (гипотезы возможных ошибок)

Дата: 2026-05-25. Автор: Claude (Cowork). Режим: ТОЛЬКО чтение/анализ, без правок кода и базы,
без живой модели. Каждая гипотеза детерминированно проверяема мной самостоятельно
(чтение кода, разбор данных, офлайн-скрипты по репозиторию и транскриптам).

Формат: `ID. гипотеза — метод проверки — артефакт — severity (P1 критично / P2 важно / P3 шлифовка)`.
Severity = насколько опасно ДЛЯ КЛИЕНТА/пилота, если гипотеза верна.

Источники: `src/mango_mvp/channels/*`, `Mango_Bot_KB_FINAL_v6_3_2026-05-20/01_bot_pack/*`,
`product_data/knowledge_base/.../facts/*.yaml`, `kb_release_v3_snapshot.json`, v8-сценарии и
снимки, транскрипты прогонов в `audits/_inbox/holdout_round5_*`, `CLAUDE.md`.

---

## A. Извлечение фактов (recall) — текущий потолок
A1. Набор КАНДИДАТОВ до отбора уже не содержит факт-ответ — читаю место сборки confirmed_facts (telegram_pilot_context_builder / sim runner), проверяю, отсекается ли факт раньше моего селектора — код — P1.
A2. `_KEY_ALIASES` не покрывает реальные `fact_key` — выписать все уникальные головы fact_key из client_safe_facts и сверить с алиасами; найти непокрытые семейства — данные — P1.
A3. Кап 10 фактов системно срезает ответ на многотемных вопросах (нужно >10 фактов) — посчитать по round-5, сколько ходов имели >10 релевантных кандидатов — транскрипты — P2.
A4. `fact_scope_spec.fact_scopes_allowed:138` (drop scopeless) роняет recall и на других темах, не только в моём банке — прогнать все 24 диалога round-5, считать ходы с confirmed=0 при наличии факта в базе — данные+транскрипты — P1.
A5. Расширить recall-банк с 8 до ~40 вопросов (по всем темам ×2 бренда) — собрать банк, измерить recall по round-5 — данные — P1.
A6. На follow-up без маркеров required_fact_keys пустеет → нулевое извлечение — посчитать долю follow-up ходов с пустыми required_fact_keys в round-5 — транскрипты — P1.
A7. Извлечение путает бренд на уровне кандидатов (факт другого бренда в наборе) — проверить bot_confirmed_facts в round-5 на чужой brand — транскрипты — P1.
A8. Числа в ответах бота, которых нет в его confirmed_facts (незаземлённые) — по round-5 извлечь все числа из bot_text и сверить с confirmed_facts того хода — транскрипты — P1.
A9. Дубликаты/почти-дубликаты фактов раздувают топ-10 и вытесняют ответ — кластеризовать client_safe по близости текста — данные — P2.
A10. Факты с истёкшим valid_until всё ещё извлекаемы как актуальные — проверить valid_until < сегодня среди client_safe — данные — P2.

## B. Бренд-разделение (литеральная утечка)
B1. Утечка в шаблонах bot_template_registry (template_text/примеры) — скан по forbidden чужого бренда — данные — P1.
B2. Утечка в few-shot (warm + advanced) по каждому бренду — скан per-brand — данные — P1.
B3. Утечка в gold confirmed_rules при «однобрендовом» использовании — проверить, что per-brand ключи не подаются вместе — данные — P2.
B4. Утечка в manager_only/internal фактах, которые могут случайно показаться клиенту — скан + проверка route_policy — данные — P2.
B5. Общие термины ошибочно занесены в forbidden (ложные срабатывания: МТС Линк, Менделеево, МФТИ) — сверить forbidden-списки с реально общими сущностями — данные — P3.
B6. Адрес Верхняя Красносельская: Фотон = регулярка+ЛШ, УНПК = только ЛШ — проверить, что в фактах УНПК он не подан как адрес регулярных курсов — данные — P1.

## C. Гигиена фактов KB (числа/даты/условия)
C1. Скидка без условия применения (нарушает правило CLAUDE.md) — для каждого discount-факта проверить наличие условия — данные — P1.
C2. Промокоды просочились в клиентский слой — скан client_safe на «промокод/promo» — данные — P1.
C3. Будущая цена/точная дата повышения в client_safe (запрещено) — скан на «с 1 …», «повысится до», даты-суммы будущего — данные — P1.
C4. Противоречия чисел между фактами одного бренда (две разные цены на один продукт) — сгруппировать по продукту, искать конфликт — данные — P1.
C5. Перепутаны проценты скидок (второй предмет 20/30, многодетные 10, МФТИ 10, год 14, семестр 10) — выписать все проценты и сверить с CLAUDE.md — данные — P1.
C6. Возрастной лимит маткапитала «до 18 на момент начала» консистентен везде — скан — данные — P2.
C7. Суммы вычета (14 300 / 110 000 / 50 000 / 6 500) консистентны и не перепутаны — скан — данные — P2.
C8. Лицензии/юр.лица/номера просочились в client_safe (должны быть internal) — скан client_safe на номера лицензий/АНО/НОУ/ООО — данные — P1.
C9. valid_until пустые/просроченные массово — посчитать долю — данные — P2.
C10. Discontinued продукты (М9/М11) всё ещё в client_safe как активные — скан — данные — P2.

## D. Консистентность снимков и источников
D1. Рантайм-снимок (kb_release_v3_snapshot) ≠ моему снимку-судье v2 по ключевым фактам (Долями, очное пробное, цены лагеря, рассрочка) — сравнить по брендам — данные — P1.
D2. client_safe_facts ≠ исходным YAML (facts_for_bot_*.yaml) по числам — выборочная сверка по sha/значениям — данные — P2.
D3. CLAUDE.md «Решения команды» ≠ фактам в базе (рассрочка 6/10/12 только Фотон; второй предмет; адреса) — сверить таблицу решений с YAML — данные+CLAUDE.md — P1.
D4. gold_answers_v3 confirmed_rules ≠ снимку/фактам (цены лагеря 93100/114000, пробное) — сверить — данные — P1.
D5. Несколько копий снимка в репо расходятся (product_data vs Mango_Bot_KB_FINAL vs audits) — diff между копиями — данные — P2.
D6. v8-сценарии (targeted/holdout) содержат стале-числа, не совпадающие со снимком — скан литералов цен в JSONL — данные — P2.

## E. Распознавание ролей (покрытие/края)
E1. Парафразы тем без ключевого слова не поднимают тему (recall ролей) — собрать банк парафраз из round-5/v8, прогнать tag_message_roles — код+данные — P2.
E2. Слова-ловушки расширенно (точн/многодет/информатик/рассрочк/очно/дн/запис/перевод/семестр) — расширить test_semantic_roles, прогнать — код — P2.
E3. payment_source=sertifikat конфликтует с document «сертификат» — проверить двойную интерпретацию — код — P3.
E4. «МТС/Линк» в маркерах формата (Кодекс добавил) ложно даёт online на shared-платформе — проверить на фразах про запись — код — P2.
E5. enrollment_vs_recording на «оформление/запись/договор» — банк фраз, прогон — код — P3.
E6. refund_frame: ещё формы предпродажного возврата («до старта», «если заболеет», «передумаем») vs спор — банк фраз — код — P2.

## F. Безопасность P0 (recall и ложные срабатывания)
F1. Реальные P0-формы, которые REFUND_RE/LEGAL_RE/COMPLAINT_RE/PAYMENT_DISPUTE_RE НЕ ловят — собрать банк P0-перифраз, прогнать детекторы — код — P1.
F2. Ложные P0 на безобидных фразах (содержат «возврат к теме», «жалоба на сайт», «суд» в др. смысле) — банк benign, прогон — код — P2.
F3. P0-latch не снимается ошибочно при смене темы — многоходовые сценарии, прогон held — код — P2.
F4. payment_dispute vs обычный вопрос об оплате — банк фраз — код — P2.
F5. reputation_threat («напишу отзыв») recall/precision — банк — код — P3.
F6. P0 на втором языке/транслите/опечатках («vernite dengi», «вазврат») — банк — код — P3.

## G. Шаблоны / rewriter / хендофф-текст
G1. template_allowed не уважается в answer_quality_rewriter (перезапись точного ответа) — читать rewriter, искать ветки перезаписи — код — P1.
G2. Шаблоны-перезаписи дублируются на короткие реплики («спасибо»/«подумаю») — читать места применения шаблона — код — P2.
G3. Хендофф-текст одинаковый от хода к ходу (templated_opening) — частотный анализ bot_text round-5 — транскрипты — P2.
G4. Фразы-хеджи («нужно сверить», «передам менеджеру», «уточнит») как доля ответов — частотный анализ — транскрипты — P2.
G5. «Сориентирую по проверенным данным:» / служебные зачины утекают клиенту — скан bot_text — транскрипты — P2.

## H. Few-shot / gold корпус
H1. few-shot числа ≠ снимку (расширить мой аудит на advanced pack) — скан — данные — P2.
H2. few-shot содержит будущую цену/дату повышения — скан — данные — P2.
H3. few-shot тон-нарушения (канцелярит, «менеджер свяжется») вопреки правилам корпуса — скан запрещённых зачинов — данные — P3.
H4. gold per-brand примеры путают условия (рассрочка УНПК и т.п.) — чтение — данные — P2.
H5. few-shot реально подаётся в промпт (wiring) — читать few_shot_reference + draft_prompt_builder — код — P2.

## I. Маршрутизация / автономность
I1. Доля manager-маршрутов по темам где факт есть (over-handoff количественно) — round-5 по route × confirmed_facts — транскрипты — P1.
I2. answer_policy/route_bias по теме противоречит матрице автономности — сверить _answer_policy с матрицей safe-тем — код — P2.
I3. live_availability всегда уводит в менеджера даже на справочной части — проверить ветку — код — P2.
I4. fallback_route=manager_only срабатывает там, где факт доступен — читать template fallback — код — P2.
I5. autonomy matrix safe-topic список рассинхронен с topic_id из intent_plan — сверить множества — код — P2.

## J. Политика «ты бот?»
J1. Запрещённые фразы (GPT/Claude/Codex/«я человек») в шаблонах/few-shot/gold — скан всех артефактов — данные — P1.
J2. Формула самопрезентации консистентна по брендам — скан — данные — P3.
J3. На identity-вопрос маршрут/ответ корректен (revealed_ai не триггерится тест-механизмом) — round-5 identity-диалоги — транскрипты — P2.

## K. Целостность тест-наборов
K1. holdout round-5 disjoint c few-shot/gold (нет утечки экзамена в обучение) — сверить тексты персон с корпусом — данные — P1.
K2. v8 (322) покрывает все темы×бренды×форматы (матрица покрытия) — построить матрицу — данные — P2.
K3. judge_spec снимок (round-4 vs v2 vs runtime) не расходится — сверить три снимка — данные — P1.
K4. персоны v8 содержат brand_forbidden своего же бренда в репликах (само-утечка-провокация) — скан — данные — P3.
K5. дубликаты персон между holdout-раундами (2/3/4/5) — пересечение id и близость текста — данные — P2.

## L. Цены / скидки / логика
L1. Второй предмет: Фотон 30% онлайн / 20% очно; УНПК 20% оба — сверить факты — данные — P1.
L2. Скидки не суммируются — присутствует ли правило во всех скидочных контекстах — данные — P2.
L3. Цены лагеря: полная vs ранняя (Фотон 98000/93100, УНПК 120000/114000) не перепутаны — сверить — данные — P1.
L4. Онлайн УНПК 26/27 «уточняется» — нет ли выдуманной точной цены в фактах/few-shot/сценариях — скан — данные — P1.
L5. Олимпиадный онлайн Физтех только 9 и 11 — нет ли фактов про другие классы — скан — данные — P1.

## M. Многоходовая динамика
M1. Класс C (явная поправка) на предмете/продукте/городе, не только формате — построить сценарии, прогнать held — код — P2.
M2. Reask уже названного слота (grade/subject/format) по round-5 — анализ do_not_reask vs повторных вопросов — транскрипты — P1.
M3. Topic switch: смена темы теряет/держит ли релевантный held — сценарии — код — P2.
M4. P0-latch держится через 2+ мирных хода — сценарии — код — P2.

## N. Хендофф менеджеру / захват лида
N1. manager_handoff_summary даёт менеджеру достаточный контекст (класс/предмет/формат/вопрос) — читать модуль + round-5 — код+транскрипты — P2.
N2. Лид-захват: бот ведёт к контакту/следующему шагу без давления — доля диалогов с next-step — транскрипты — P2.
N3. На P0 бот НЕ собирает перс.данные (zero-collect) — проверить round-5 P0-диалоги — транскрипты — P1.

## O. Робастность / формат / прочее
O1. Нормализация ё/е, nbsp, регистр в детекторах не теряет совпадения — стресс-тест has_marker — код — P2.
O2. Числа с пробелами/неразрывными («44 600») распознаются единообразно — тест — код — P2.
O3. Катастрофический backtracking в regex p0_recall_spec на длинных вводах — стресс-тест времени — код — P3.
O4. Служебные маркеры (fact_id/JSON/source_id/debug) не утекают в client_safe/шаблоны — скан — данные — P1.
O5. Read-only контракт: бот/код нигде не пишет в AMO/Tallanto без подтверждения — grep на write-вызовы — код — P1.
O6. Возврат/жалоба → телефон менеджера, шаблоны клиенту не показываются — проверить P0-ветку — код — P2.

---

## Сводка покрытия
~80 гипотез: A(10) B(6) C(10) D(6) E(6) F(6) G(5) H(5) I(5) J(3) K(5) L(5) M(4) N(3) O(6).
Из них P1 (критично для пилота): ~28. P2: ~38. P3: ~14.

## Предлагаемый порядок (первый батч — P1, наибольший рычаг)
1) A1–A2 (где теряется recall: набор кандидатов + покрытие алиасов) — прямо помогает текущему слою Кодекса.
2) D1/D3/K3 (рассинхрон снимков и решений — источник ложных fabrication у судьи).
3) C1/C5/C8/L* (числа/скидки/лицензии — прямой клиентский риск).
4) B1–B2/J1/O4 (утечки и служебные маркеры в статике — pre-flight безопасности).
5) Остальное по убыванию severity.

Каждый пункт → отдельный мини-отчёт «гипотеза → метод → факт → вывод/severity» в logs/.
Где найду реальную ошибку — оформлю как находку с доказательством для ТЗ Кодексу (не правя сам).
