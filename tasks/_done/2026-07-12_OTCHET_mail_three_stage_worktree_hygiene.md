# Отчёт: три стадии почты и порядок worktree

Ветка: `codex/mail-three-stage-worktree-hygiene`.

Коммиты:

- `6eb5407e` — основной трёхстадийный конвейер;
- `d17e56af` — проверка ignored-пути по worktree данных;
- `d18f2804` — существующий mail-link enrich в составе импорта;
- `70b5fe3b` — самостоятельный запуск nightly CLI без внешнего PYTHONPATH;
- `b3947096` — стабильные timestamps при повторном IMAP fetch.

Результат:

- `СКАЧАЛ -> ОБРАБОТАЛ -> ВЛИЛ` реализовано как три отдельные fail-loud задачи;
- курсоры, SHA, lock, fresh-manifest и HEAD/worktree gates работают;
- три ручных цикла на SQLite-копии получили formal PASS;
- полный тест: `4675 passed, 5 skipped`;
- `DECISIONS_LOG`: 91 запись, 91 уникальный номер;
- активные worktree зарегистрированы, ничего не удалено;
- launchd-шаблоны подготовлены, но не установлены.

Независимый аудит: GPT-5.5 high, PASS по коду, документам и трём циклам.

Не сделано: host cutover/установка расписания. Это отдельное решение Дмитрия.
