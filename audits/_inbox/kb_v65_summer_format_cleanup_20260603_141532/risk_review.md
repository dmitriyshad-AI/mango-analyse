# Risk review

- `DEFAULT_KB_SNAPSHOT_PATH` не менялся; бот не переключён на v6.5 до PASS Claude #1.
- Старый builder напрямую не запускался; использован только wrapper `build_kb_release_v6_1_team_answers.py`.
- Из-за устройства v6.1 wrapper импортирует модуль `build_kb_release_v3_from_claude_handoff.py`; там изменён только renderer `objection_responses`, чтобы убрать внутренний префикс и сохранить старые fact_key.
- Бренд-разделение новых фактов проверено regex-контролем по запрещённым токенам.
- Старые структурные значения, конфликтующие с v6.5, синхронизированы, чтобы в релизе не было двух разных цен/скидок по ЛШ.

