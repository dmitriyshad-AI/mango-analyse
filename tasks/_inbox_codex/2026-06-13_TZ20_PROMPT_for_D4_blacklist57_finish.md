# Промт для D4 — ТЗ-20: добить blacklist (57 звонков) + подготовить их вливание (выдаёт Дмитрий)

Ты — D4. Смысловое ревью батча-15 пройдено (`D1_audit_backlog/existing_clients/REVIEW_blacklist_batch15_2026-06-13.md`): фикс работает, полный перепрогон одобрен. Закрой blacklist целиком.

## Блок А. Прогон оставшихся 57

1. 77 минус 5 (микропроба ТЗ-16) минус 15 (батч) = ровно 57 call_id; список и счёт в отчёт, расхождение = стоп.
2. Локальный прогон тем же контуром, что батч-15: gpt-5.4-mini, full v7-исправленная (sha256 сверить: `12718ea6…`), `llm_calls_total=57`.
3. Собрать ЕДИНЫЙ результат всех 77 (5+15+57) в `product_data/customer_profiles/tz20_blacklist77_results/` (ignored): jsonl.gz + сводная таблица (call_id, длительность, класс до/после, target до/после, needs_review).
4. NEG: 3 контрольных автоответчика (15717, 16565, 24790) детерминированно остаются `non_conversation` без LLM.

## Блок Б. Вливание blacklist-результатов (КОД + dry-run, БЕЗ --apply)

1. Расширить `scripts/import_tz19_analyze_tail_results.py` (или отдельный скрипт) режимом `--blacklist-override <файл со списком 77>`: вливать blacklist-строки можно ТОЛЬКО по явному списку; без флага — прежний отказ.
2. Правило потребления из ревью: needs_review сохраняется в выжимке как есть; строки non_conversation+needs_review в счётчике отчёта выделить отдельно (не «исправлено»).
3. Прогнать DRY-RUN вливания всех 77 на копии среза/синтетике: счётчики (обновится / отклонено / needs_review) в отчёт. РЕАЛЬНОЕ вливание (--apply с бэкапом) — отдельная команда после моего регрейда цифр.
4. NEG: не-blacklist строки через override не проходят; повторный dry-run идемпотентен; вне вайтлиста колонок записи нет.

## Границы и приёмка

Запись в CRM запрещена; настройки М1 не менять (станция занята/ждёт хвост — не трогать её задачу); фиче-ветка от main, коммиты по блокам, полный pytest, git чистый. Отчёт в `tasks/_done/`: список 57, метрики прогона, сводная таблица 77 (агрегаты), счётчики dry-run, llm_calls_total=57, путь к результатам для ревью Claude.
