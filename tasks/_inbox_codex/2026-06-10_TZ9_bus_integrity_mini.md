# ТЗ-9 (мини): целостность шины М1 + эталонное окружение прогонов

Повод: сегодня обе smoke89-задачи молча легли в _failed (`ready_sha_mismatch`: в .ready положили sha НАБОРА вместо sha task.yaml), а сбой CLI (`service_tier=default` из личного ~/.codex/config.toml) чинился обходом. Три дешёвые правки, без влияния на бот:

1. **Самодиагностика вотчера:** в отчёт ready_sha_mismatch писать `expected_sha` (из .ready) и `actual_sha` (от task.yaml) + одну строку «как класть правильно». В watcher_status.txt добавить счётчик `failed_total` и имя последней failed-задачи — мой ежечасный монитор это ловит.
2. **Конвенция документирована + хелпер:** скрипт `scripts/m1_make_task_ready.py <task.yaml>` — генерит .ready с правильным sha; README шины (3 строки) рядом с _inbox_m1. Главный Кодекс при закладке задач пользуется только им.
3. **Эталонное окружение прогона:** шаблон `configs/codex_home_template/` в репо (config.toml с service_tier=fast и пр.); раннеры/вотчер/ручные README используют `CODEX_HOME=<копия шаблона>` — личный ~/.codex больше не влияет на замеры. Auth подкладывается локально, в шаблоне его НЕТ (секреты не в репо).

NEG: вотчер с корректным .ready берёт задачу как раньше; полный pytest не задет (скрипты). Приоритет: после live-пробы, до следующей закладки задач на М1.

## Аддендум по аудиту: + bundle_sha256 и auth-класс

4. В task.yaml обязательное поле `bundle_sha256` (сумма по списку файлов бандла или по архиву — как проще); вотчер сверяет до запуска → ловит и подмену бандла, и путаницу «sha чего» (инцидент 10.06). Хелпер из п.2 генерит оба: sha задачи в .ready и bundle_sha внутри yaml; порядок записи: task.yaml → fsync → .ready.
5. Отдельный failed-класс `auth_error` (протухший codex-auth на М1) — отличать от инфраструктурных, в watcher_status строкой.
