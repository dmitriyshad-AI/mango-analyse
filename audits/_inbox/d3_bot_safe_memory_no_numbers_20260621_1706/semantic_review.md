# Semantic review

Verdict: `PASS_WITH_NOTES`

## What passed

Память теперь сохраняет контекст разговора, но не передаёт модели точную числовую конкретику как готовый факт.

Проверенный пример:

- память: `2025/26`, `12:15-14:15`, `94 500 ₽`;
- prompt: эти детали заменены на `<точная деталь из памяти скрыта>`;
- нить `обсуждали расписание` сохранена;
- цена из блока «Факты» `47 250 ₽` сохранена.

## Blocking issues

Нет.

## Non-blocking risks

Адреса без чисел пока не маскируются детерминированно; их запрещает prompt-инструкция. Если D7 покажет, что модель всё равно цитирует адреса из памяти, нужен отдельный адресный санитар.

## Missing checks

Не запускался D7-замер с моделью на кейсе 16.

## Required regression checks

Добавлены тесты:

- память с расписанием/годом/ценой скрывает точные детали;
- факт-блок сохраняет цену;
- нить памяти остаётся.

## Recommended next action

Claude #1 регрейдит по сырью, затем D7 делает единое OFF/ON переизмерение: судья + гейт свежести + запрет чисел из памяти.
