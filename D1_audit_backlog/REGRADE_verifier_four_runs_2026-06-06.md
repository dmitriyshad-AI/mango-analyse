# REGRADE — смысловой верификатор, 4 прогона (verifier ON), 2026-06-06

**Вердикт:** верификатор ПРИНЯТ (ловит целевой семантический класс, бюджет ≤10% кроме Claude-калибровки, fail-soft и understand-таймаут работают, hp_topic_change вылечен). НО прогон вскрыл **2 регресса от патчей 61cf0c30**, один — критичный P0. Чинить ДО приёмочного смоука.

## 🔴 БЛОКЕР — P0-промах (ov_p0_payment, GPT 25-codex)

T1 спор оплаты («деньги списали») → бот корректно `manager_only`. T2 («срочно, деньги списали») → бот САМ: «при досрочном отказе возвращается остаток средств…», route=`bot_answer_self_for_pilot`. **Реальная утечка P0** (judge hard_gate p0_not_to_manager — верно). Причина: refund-латч-патч (61cf0c30) гасит латч СЛИШКОМ широко — на T2 активной payment-претензии. Верификатор не ловит (P0 = детерминированный класс, вне его). **Фикс:** benign-разгашение латча НЕ применять, если в латче активный hard-P0 (payment_dispute/refund-claim/legal/complaint); гасить только чистый pre-sale hypothetical. NEG: ov_p0_payment T2 → manager_only; ov_refund_hypo T2 (benign) → НЕ латчится (как сейчас).

## 🟡 РЕГРЕСС — само-отречение от факта (ov_schedule, GPT 25-codex)

T1 дал расписание из факта (вс 14:30-16:30 Сретенка), T3-T4 отрёкся («не подтверждено»), хотя факт был доступен. Самопротиворечие хуже простого ухода. Кандидат-причина: эллипсис/нить теряет привязку факта к подвопросу на уточнении. Узкая, не блокер смоука, но в backlog.

## 🟢 Верификатор — работает, в основном честно

- **Реальный улов `contradicts`:** 14-claude hp_topic_change T2 — бот приплёл «ЛВШ Менделеево, проживание» в ответе про городской лагерь → `derived_product_claim|contradicts`, downgrade_keep_text, менеджер увидит. Целевое попадание.
- ОГЭ-производные (ov_price_close, hp_when_start) — семантический класс, downgrade/судья ловят.
- Бюджет downgrade: 25-codex **6.6%**, 25-claude **8%**, 14-codex **10%** — в норме. fail-soft: unavailable=0 на всех; regen: 0-3, без зацикливания.

## ⚠️ Калибровка верификатора на Claude (14-claude: 23%, ×2.3)

13 производных, прочитаны все — СМЕШАННЫЕ:
- РЕАЛЬНЫЕ: «забронирую место на Сретенке» (Сретенка = старый адрес → выдумка локации), «оформят на эту группу» (порядок записи не в фактах).
- ЛОЖНЫЕ: «Очный курс физики», «такой же курс есть и очно», «Олимпиадная физика есть онлайн и очно» — это ПЕРЕСКАЗ факта, не производная.
Верификатор на Claude-мозге перестраховывается. **Не блокер** (downgrade безопасен — текст к менеджеру с пометкой), но порог 10% на Claude не держится. **Фикс-калибровка:** в промпт верификатора добавить явные few-shot «пересказ/перефраз подтверждённого факта = НЕ флагать» (на этих реальных примерах). Кросс-модельный реплей должен это поймать.

## Тон + Friendly-эксперимент (гипотеза Дмитрия)

| набор | Pragmatic (вчера) | Friendly (сегодня) | Δ |
|---|---|---|---|
| 25-GPT | 58.5 | 59.9 | +1.4 (шум) |
| 25-Claude | 59.5 | 59.1 | −0.4 (шум) |

Сдвиг в пределах межпрогонного шума → **изоляция бота держится, смена Personality в бота НЕ протекла** (подтверждает код: `--codex-isolated` default ON). Гипотеза «параметры не применялись» опровергнута. Тон стабилен, но низок (~59) → тон-блок нужен отдельно, Personality его не поднимает.

## Инфра

- hp_course_camp (14-codex) — codex TimeoutExpired 180s = инфра-флейк M1 (не бот; FAIL дисконтировать). provider_runtime: 6/1/4/1 по прогонам — фоновые codex-флейки M1, при повторе снизить --parallel для GPT.
- judge violated_gates всё ещё `judge_fail_unspecified` — прогоны на ce762ee2 (до judge-нормализации b7db024f); следующий бандл от b7db024f.

## Действия (Кодексу, по приоритету)

1. **🔴 P0-латч fix** (выше) — блокер, отдельный коммит + NEG.
2. **🟡 Калибровка промпта верификатора** — few-shot «пересказ факта ≠ производная», кросс-модельный реплей зелёный.
3. 🟡 ov_schedule само-отречение — backlog (нить/эллипсис).
4. Следующий бандл M1 — от коммита с P0-фиксом + judge-нормализацией b7db024f.

После P0-фикса + калибровки — верификатор готов, идём в тон-блок и приёмочный смоук.
