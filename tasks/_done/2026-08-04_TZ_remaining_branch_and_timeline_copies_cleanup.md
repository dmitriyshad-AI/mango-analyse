> DONE 2026-08-04 20:27 | ветка main | codex

> TAKE 2026-08-04 19:50 | ветка main | codex

Ветка: main
Зоны: docs/worktrees_registry.md, tests/test_bot_safe_runtime_context.py, tasks/, audits/_inbox/, /Users/dmitrijfabarisov/.mango_local/quiet_window_20260717/, /Users/dmitrijfabarisov/.mango_local/customer_timeline_twopass_20260727/, /Users/dmitrijfabarisov/Backups/codex_cleanup_manifests/
Тест-команда: PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m pytest --collect-only -q tests/test_project_now.py
Семантический-аудит: да

# Последняя ветка и старые копии Customer Timeline

## Разрешение владельца

Дмитрий 2026-08-04 явно разрешил проверить, выборочно принять полезное из
оставшейся ветки и удалить доказанно заменённые старые копии. Текущие базы,
работающие процессы и внешние системы не менять.

## Образ результата

1. Для каждой старой копии известна разница с текущей Timeline по ключам и
   содержимому, а не только по числу строк.
2. Единственные полезные данные не удалены: они либо уже присутствуют в более
   новом каноне, либо перед удалением восстановлены существующим штатным путём.
3. Полезное из `claude/timeline-final-20260803` принимается только выборочно,
   если не дублирует `main` и проходит архитектурный, ломающий и бизнес-аудит.
4. Работающие calls A/B и настроенная Timeline nightly не переключаются и не
   останавливаются этим ТЗ.
5. После удаления сохранён манифест; Git и оставшиеся SQLite проверены.

## Порядок

1. Read-only inventory ветки и обеих копий.
2. Сверка происхождения лишних purchase-строк, event/customer/family связей,
   manifests, cursors и ссылок runtime.
3. Независимые архитектор, ломатель, бизнес-аудитор и уборщик.
4. Удалить только доказанно заменённое. При неоднозначности оставить и назвать
   точный недостающий указатель.
5. Проверить Git, свободное место и сохранённые канонические базы.

## Приёмка

- Для каждой копии зафиксированы размер, схема, ключевые множества и различия
  `customer_purchases_v1` с текущей базой.
- Ни одна уникальная подтверждённая оплата или связь клиента не удалена.
- Открытых файлов и runtime-указателей на удаляемый путь нет.
- Из активной ветки не принят код, который уже есть в `main` или нарушает
  адресную привязку клиента; каждое принятое изменение имеет сквозной тест.
- После удаления оставшаяся SQLite читается в immutable/query-only режиме,
  Git чист, манифест удаления сверяется по SHA.
- Отчёт содержит добавленные/удалённые строки кода и освобождённое место.

## Стоп

- Требуется запись в текущую Timeline, AMO, Tallanto, CRM или Wappi.
- Нужно остановить calls A/B или менять launchd.
- Уникальные финансовые строки не объяснены.
- Полезный код ветки конфликтует с красной линией «не писать клиенту о другом».

## Итог 2026-08-04

- Все 15 уникальных коммитов ветки разобраны. Полное слияние отклонено; нового безопасного production-кода в ней нет.
- Полезный NEG-сценарий перенесён в существующий тест `main`; 64 теста зелёные.
- Удалены три доказанно заменённые большие SQLite; освобождено 23 243 964 416 байт (21,65 ГиБ).
- Уникальные малые AMO/mail хвосты и все отчёты сохранены. Исторические производные `twopass` сохранены в компактном архиве 181 МиБ.
- Текущие prod и staging не менялись; обе: `quick_check=ok`, FK violations `0`.
- Добавлено 11 строк теста, удалено 0 строк production-кода; новых файлов кода 0, флагов 0, зависимостей 0.
- Более простой вариант выбран: не сливать второй payment pipeline, а закрепить уже верную архитектуру одним тестом.
