# Стартовый промт — Кодекс main (фикс волны 1). 2026-05-30.

> Скопировать в окно Кодекса main.

---

Ты — Кодекс main, ветка `codex/m1-intermediate-20260529-final`, HEAD `fc8ef9be`.

Claude #1 прогнал волну 1 в песочнике: **425 passed, 2 failed.** В main НЕ мержить до зелёного.

**Прочитай:** `D1_audit_backlog/TZ_codex_main_volna1_FIX_2026-05-30.md` (там точные корни, проверенные по сырью).

**Два фикса:**
1. **Тест щита** `test_volna_peresborki_safety_shield_blocks_core_autonomy_risks` (стр. 3021): cross-brand
   дал `manager_only` вместо ожидаемого `draft_for_manager` — это строже и безопаснее, защита сработала.
   Ослабить ожидание до «маршрут не автономный» (manager_only ИЛИ draft_for_manager). Не ослабляя смысл щита.
2. **Код** `_fact_match_anchors`: добавить смысловые якоря расписания по ОСНОВАМ слов
   (вечерн*→condition:evening, утренн*→morning, дневн*→day, выходн*/суббот*/воскресен*→weekend,
   будн*→weekday), чтобы текстовый claim покрывался фактом независимо от словоформы. subset-логику,
   unit-якоря, передачу фактов и verified-путь НЕ трогать.

**Негативный контроль обязателен:** утренние vs вечерние / будни vs выходные → блок; существующий
`..._blocks_wrong_scope_schedule_claim` зелёный; promise-гвард (числа/проценты/баллы) не ослаблять
(NEG «25%»/«25 баллов» = не подтверждён).

**Ограничения:** тесты не гонять (лимит) — Claude #1 прогонит щит + smoke. Хирургично: падение 1 —
правка теста, падение 2 — только `_fact_match_anchors`. Отчёт: что изменено + тексты NEG-тестов.
