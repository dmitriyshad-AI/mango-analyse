> DONE 2026-09-05 17:43 | ветка codex/customer-timeline-source-gates-20260902 | codex

> TAKE 2026-09-05 16:49 | ветка codex/customer-timeline-source-gates-20260902 | codex

Ветка: codex/customer-timeline-source-gates-20260902
Зоны: src/mango_mvp/productization/mail_archive.py, tests/test_productization_mail_archive.py, docs/DECISIONS_LOG.md, docs/worktrees_registry.md, tasks/_running/2026-09-05_TZ_TIMELINE_R5_RELEASE.md, tasks/_done/2026-09-05_TZ_TIMELINE_R5_RELEASE.md
Тест-команда: PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m pytest -q tests/test_productization_mail_archive.py tests/test_subscription_llm_draft_provider.py tests/test_customer_timeline_codex_task.py tests/test_customer_timeline_contracts.py
Семантический-аудит: да
Feature-ID: feature.customer_timeline.stable_source_gates
Problem-ID: problem.customer_timeline.m4_first_cycle_source_failures
Исход: attempt_complete
Изменение: fix
Ключевые-символы: known_mailbox_uids,BatchedUidFetchClient,ingest_one_message
Ключевые-слова: IMAP UID account identity,batch fetch fallback

# Минимальное исправление двух дефектов после узкого аудита

ПРЕДКОДОВОЕ РЕВЬЮ: сейчас оценивается корректность ПЛАНА исправления в разделе "Уточнение после независимого ревью", а не готовность пока не написанного исправления и не результаты будущих r5-циклов. Два дефекта существующего кода подтверждены и являются входом этой задачи. PASS плана означает: выбран правильный существующий владелец, предлагаемые изменения закрывают оба дефекта, тесты достаточны, границы соблюдены. После кода будет отдельное ревью результата. Не требуй исправить код до обязательного предкодового preflight: это создало бы круговую зависимость.

Экономия: достаточно прочитать релевантные функции known_mailbox_uids, BatchedUidFetchClient, ingest_one_message, init_mail_archive_db, upsert_message и тесты UID/batch. Не повторять большой аудит Timeline. Весь структурированный отчёт с обязательными строками из review_prompt.md должен быть в ОДНОМ финальном ответе; промежуточные ответы CLI не входят в JSON result.

План уже получил независимый Claude PASS на том же HEAD (05.09, r5_mail_fix_plan). Сейчас изменены только процессные метаданные: зоны включают собственное ТЗ и локальный реестр; в реестр внесена фактически существующая ЧУЖАЯ ветка Tallanto без её изменений. Обновлён inventory после окончательного текста ТЗ. Достаточно узкой проверки этих уточнений и отсутствия ухудшения плана. Финальный ответ до 400 слов; не повторять весь прежний аудит.

Затем принятая цель: существующий r5, два pass, compact-reader, одно расписание.

HEAD bbf6eaca3d56e042f7914ac768585dc343072cc1; базовый 82c4d4f623415dd720d5a3872841435960809778 уже принят отдельным аудитом. НЕ повторять аудит всей базы/архитектуры.

Дельта: 5feda4a1 (UIDVALIDITY и reuse сохранённых писем), 7a3873cd (batch BODY.PEEK[]), 1b30fca4 (UTC в тестах и bytes-counter redaction), bbf6eaca (D-150). Владелец хочет продолжить существующий r5 и два ручных nightly цикла.

Проверь исходники и тесты: состав UID ключа account/mailbox/epoch, неполные batch-ответы, fallback, отсутствие IMAP STORE/изменения Seen, сохранность поздних писем, реальные регрессии, а не совпадение теста с кодом. Дата обработки не должна подменять дату письма. Не считать потерей данные, которые остаются retryable в следующем цикле, но проверить продвижение курсора.

Роли: architect-auditor/breaker; business-auditor/optimizer/cleaner оценивают риск неполной истории и лишней повторной работы. Никаких API, клиентских данных, DB, runtime-файлов, env/секретов, изменений. Вывод до 1000 слов: P0/P1 с точным исходником и минимальным классом воспроизведения либо PASS с ограничениями. Переиспользуй прежнюю архитектуру, не предлагай новую инфраструктуру.
## Уточнение после независимого ревью

Узкий аудит завершён: 538 тестов PASS; Claude PASS с замечаниями. Ломатель воспроизвёл два дефекта, подлежащих исправлению ДО почтового инкремента. Предыдущий запрет изменений относится к роли ревьюера, не к исполнителю цели.

1. Пропуск по UID привязать к реальному серверу/порту/логину, а не только произвольному account_label. Минимум: account_key в существующей message_sources, SHA JSON [host,port,login], без пароля; использовать в source_key и known_mailbox_uids. Старые строки без доказанного аккаунта не повышать автоматически: один раз повторно прочитать только текущее временное окно, сохранив дедуп по SHA тела и исторические файлы.
2. В существующем BatchedUidFetchClient: IMAP4.error на пакетный FETCH и частичный OK без запрошенного UID переводить в одиночный FETCH. IMAP4.abort по-прежнему уходит штатному механизму восстановления соединения. BODY.PEEK/readonly не менять.
3. Тесты: одинаковые label/UID/epoch при смене аккаунта или сервера; смена пароля не требует повторного чтения; старая схема; batch BAD/NO/частичный OK, разрыв соединения, отрицательный чужой UID; повтор без изменений пустой.

Бритва: (а) ничего не менять оставляет доказанный тихий пропуск; (б) отключить UID/batch заставляет повторно читать почту; (в) расширить существующий ключ и fallback. Выбрано (в): один владелец, без нового загрузчика, флагов и зависимостей, до 50 строк нетестового кода. Эта часть закрывает ошибки загрузчика, но не объявляет завершёнными два ночных цикла и установку расписания.

## Приёмка

Указанные регрессионные тесты зелёные; независимый аудитор проверил diff. Затем два ручных цикла подтверждены полными отчётами: overall_status=ok И data_quality_status=pass. Compact читается штатным читателем; дубли и ошибочные разрешения не растут. Только после этого одно расписание, покрывающее подготовку почты и writer. Свежесть Calls оценивать отдельно, не скрывать её успешным обновлением Timeline.

## СТОП

Не менять production/live, внешние CRM/Tallanto, клиентские отправки и ASR. Не понижать гейты ради зелёного результата. Не восстанавливать seed поверх начатого writer. При partial сохранить прогресс и причину, расписание не устанавливать до двух pass.

## Итог кодовой попытки 05.09

Два дефекта исправлены минимально (+32/-7 нетестовых строк); 550 профильных и 5738 полных тестов PASS, 4 skipped. Независимые архитектор/ломатель и бизнес/оптимизатор/уборщик не нашли новых P0/P1. Кодовая попытка завершена, но перенос writer НЕ объявлен завершённым.

Следующий шаг: после финального коммита зафиксировать HEAD; продолжить активную цель по audits/_inbox/customer_timeline_r5_resume_20260905/CONTINUATION.md: mail-chain, ownership, два pass, compact-reader, одно расписание. Этот исход attempt_complete не закрывает исходную проблему и не разрешает пропустить runtime-приёмку. Результаты циклов остаются локальными, чтобы не менять HEAD активированного writer ради отчётов.
