# ТЗ-121 — Группа 4: остатки доделок (B/E/C/A) одним пакетом + замер

- **Дата:** 2026-06-16. Заказчик: Дмитрий. Постановщик: Claude #1. Исполнитель: D4. Основание — замеры ТЗ-116/117/118 + follow-up gold.
- **Контекст:** D (роли) уже primary. Остаются 4 блока. Делаем одним ТЗ, каждый за режимом/флагом default off→shadow→primary, primary поблочно ТОЛЬКО после моего регрейда. Только Codex, без ключей; без записи в AMO/Tallanto/CRM; бренд `unknown`→fail-closed, числа/status_id не ослаблять. Микро-наборы для проверки, не полные сеты.

## B — исход сделки: ВЫБОРОЧНЫЙ primary
- Включить primary ТОЛЬКО для flip `won_paid_or_active → known_student_or_lead` (подтверждён 8/10).
- Flip `→ payment_pending` НЕ применять (legacy), он плох (5 ложных).
- Замер: на gold-срезе исходов — доля верных «снятий ложного won» по разрешённому типу; ноль регрессий на остальных. NEG: off = legacy.

## E — бренд: фикс морфологии Фотона → primary
- Матчер на ПОДСТРОКУ-корень (нормализация регистра + ё→е): `фотон` ловит склонения и склейки («Фотоны/Фотону/в Фотоне/ЦДПФОТОН/ЦИДПОФОТОН»); УНПК — корни `унпк`/`мфти`.
- cross-brand (есть оба корня) → `unknown` (fail-closed), не угадывать. unpk-сторона уже чистая.
- Замер: на gold-срезе — доля `foton→unknown` ложных падает; ноль новых смешений брендов. Потом cyrillic_v2 primary.

## C — категории вопросов: ГИБРИД → primary
- Гибрид-страж: где старое правило уверенно попадает в служебные классы (S1 «не вопрос»/S2 «неясно»/S4/S5), оставляем правило; иначе модель. Опц.: уточнить определения служебных корзин в промпте.
- Замер на 100-наборе: точность модели vs правило (цель >72% без потерь на реальных темах). Основной каталог НЕ пересобирать. Потом primary для офлайн-классификации.

## A — качество сделки: ЭТАЛОН → замер → решение
- Сначала разметить небольшой gold по 24-набору (черновик модели + ручная сверка) по полям verdict/risk/next_step.
- Измерить модель vs эвристика по эталону. primary вслепую НЕЛЬЗЯ — решаем по результату.

## Дисциплина / приёмка
- Каждый блок: реализация → shadow/замер на микро-наборе → мой регрейд по сырью → (если чисто) primary этого блока → следующий.
- Трассы ТЗ-117 включены. Ветка от main, полный pytest зелёный, отчёты в `_done`. primary/writeback нигде до регрейда.

## Порядок (по готовности)
B (выборочный flip — самый дешёвый) → E (морфология Фотона) → C (гибрид) → A (эталон).
