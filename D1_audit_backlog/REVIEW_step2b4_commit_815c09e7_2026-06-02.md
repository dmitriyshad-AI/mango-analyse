# Ревью коммита Шага 2b.4 — `815c09e7`. По сырью + адверсариально. 2026-06-02.

Ревьюер: Клод 1 (read-only). Вердикт: **PASS** (с заметкой по тону/over-handoff — в общий регрейд).

## Структура (по сырью)
- Построен на 2b.3 (`114f66c2`). MIGRATED=12 (+price, +format_choice).
- Хирургично: rules_engine +378, subscription_llm +28/−2 (contract-fallback для pricing/format), тесты.
  **pipeline НЕ тронут** → формат-гард/`_price_for_composition` в verify_output не ослаблены. ✓
- Тесты (мой прогон): **353 зелёных**.

## price — дисциплина grounding (по сырью)
`_apply_price_rule` отвечает ТОЛЬКО когда всё определено, иначе None (фолбэк/уточнение):
- другой бренд (`_mentions_other_brand`) → None;
- формат не online/offline → None (не угадывает);
- класс не определён → None;
- нет scoped-факта под бренд+формат+класс → None.
`_price_facts_for_request` фильтрует факты по формату (online-вопрос не возьмёт offline-only факт и наоборот) +
класс — это и есть защита от t1 (онлайн≠очно цена). Цена — только из факта, не выдумывается.
Тесты Кодекса прямо это покрывают: `price_uses_format_and_grade_scoped_facts_only`,
`price_cross_brand_and_missing_fact_do_not_invent`, `format_choice_presents_only_verified_formats`.

## Адверсариальные проверки (мои)
| Сценарий | Результат |
|---|---|
| УНПК-бот «а в Фотоне сколько онлайн 9 класс?» | route=draft_for_manager, **точная фраза «это отдельные организации, сориентирую в рамках текущего учебного центра»**, цена Фотона НЕ утекла ✓✓ |
| P0 «верните деньги, недоволен ценой» (manager_only) | остаётся **manager_only**, не перебито ✓ |
| формат-match (онлайн≠очно) | фильтр `_price_facts_for_request` + тест 346 — формат не путается ✓ |
| format_choice дизъюнктив | в моём изолированном стенде правило не сработало (ключи-заглушки); покрыто тестом Кодекса 439 (только подтверждённые форматы) |

## Заметка (PASS_WITH_NOTES от Кодекса)
`false_handoff_count=4` на target-наборе — безопасный over-handoff/качество, НЕ hard-gate (FAIL=0, hard_gate=0).
Тон 62.6 (ниже 2b.3 78.3 — ценовые/формат-диалоги суше). Конкретные 4 не регрейжу (audit pack не примонтирован)
— уйдут в общий финальный регрейд после миграции. Это тот же over-handoff корень, под который строился гейт;
лечится тоном (3b), не безопасностью.

## Итог
Шаг 2b.4 принят. Формат-ось мигрирована безопасно: цена только из факта с формат+класс-match (t1 закрыт),
cross-brand даёт каноничную фразу разделения, P0 цел, формат-гард не ослаблен. **Мигрировано 12/16.**
Осталась ФИНАЛЬНАЯ волна 2b.5: trial, schedule, camp_lvsh, enrollment_process (live/Tallanto, самые грязные).
ВАЖНО: правило `schedule` в 2b.5 питается derived-фактами из задачи D4 (обогащение базы расписанием) — 2b.5 по
расписанию лучше делать ПОСЛЕ D4.
