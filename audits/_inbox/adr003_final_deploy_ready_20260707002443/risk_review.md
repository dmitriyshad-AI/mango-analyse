# Риски

- Клиентский риск: основной исправленный риск был `route safe, text wrong` — безопасный маршрут к менеджеру, но неверный возвратный/консультационный текст. Закрыт регрессиями и smoke.
- Деньги/P0: маршрут не ослаблен. Детерминированные полы могут только оставить/усилить `manager_only`; клиентский смысловой шаблон выбирается по валидному SemanticFrame или нейтрально. Edge-case "P0-frame без legacy-P0 metadata" закрыт отдельной регрессией: route ужесточается до `manager_only`.
- Данные/записи: live-write не запускался, AMO/CRM/Tallanto/Wappi не трогались.
- Откат: изменения изолированы в `text_hygiene.py` и тестах; профильный флаг PaymentFix остается управляемым через существующий профиль/override.
- Остаточный риск: replay exam на реальных обезличенных диалогах еще не пройден; до него нельзя считать качество полностью доказанным за пределами smoke-наборов.
