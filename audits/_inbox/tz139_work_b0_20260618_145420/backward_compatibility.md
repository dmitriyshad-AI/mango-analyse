# Backward Compatibility

- Non-family `mango_client_phone` remains `strong_unique/0.95`.
- Family `phone` behavior is unchanged from Work A real-data fix: `ambiguous/0.55`.
- Family `mango_client_phone` intentionally changes from `strong_unique/0.95` to `ambiguous/0.55`.
- Schema unchanged.
- Existing source matching and customer_id mapping behavior unchanged.
