# Приёмочный референс Волны 1 (Claude-сторона). 2026-05-26

Подготовлено заранее, пока Codex кодит Волну 1. Это эталоны/ожидаемые результаты, по которым я сверю
гейт и метрики Codex (навык №1 «верификация», №7 «объект vs измеритель»). Инструменты — мои независимые
(в рабочей папке Claude): `wave1_acceptance_integrity_check.py`, `wave1_m3_number_levels_golden.py`.

## A. Целостность базы (B2/B3) — независимый чекер
- Синтетика: ловит порчу 6/6 (год-в-частоте, weeks вне диапазона, %>100, класс>11, кол-во против
  структуры, деньги<=0); ложных блоков 0/6 (даты, классы 9/11, телефон, «2026/27», URL — чисто).
- Реальный снимок v6.3 (472 client-safe факта): **найдена реальная порча того же класса —**
  `discounts.multichild.note` (Фотон И УНПК): `structured_value.percentage = 2026.0` (год из даты
  «2026-05-18» в raw_value утёк в поле процента; в raw_value верно «скидка 10%»). Клиентский текст
  сейчас чист (число не показывается), но структура испорчена — **гейт B3 Волны 1 ОБЯЗАН это ловить, а
  значение поправить на 10%.** Узкий weekly-чек это пропускал.
- Ложные срабатывания МОЕГО чекера (калибровка, не порча): `individual_lessons_foton ...lesson_45min/
  session_90min` — «45/90 минут» это реальная длительность в ценовом факте, просто не в числовом поле
  структуры. Вывод и для B2 Codex: длительность-атрибут в ценовых фактах блокировать нельзя.

**Критерий приёмки гейта Codex:** на синтетике — те же 6/6 и 0/6; на реальном снимке — ловит
`multichild percentage=2026` (оба бренда) и НЕ блокирует длительности/даты/классы/телефоны.

## B. M3 — уровни совпадения чисел (золотой ответ)
Прогон-источник: `targeted_riskzones_v2_x2_all_eligible_20260526_161859`, автономные ходы.
Ожидаемое распределение (number_audit Codex должен воспроизвести):
- `retrieved_match`: 23 (цены 44 600/74 500/49 000/82 000, классы 5-11 — все подтверждены retrieved).
- `service(year)`: 10 (2026, «2026/27»→27).
- `client_echo`: 5 (класс ребёнка из вопроса клиента — 9/8/7/10).
- `same_brand_global_match` / `other_brand_match` / `no_match`: **0**.

Вывод: на этом прогоне числовой выдумки нет (`no_match=0`) — совпадает с моим прежним регрейдом.

## C. Важный вывод: два слоя дополняют друг друга
M3 ловит ВЫДУМКУ (`no_match`), но НЕ ловит порчу вида «2 026 раз» — для классификатора это `service(year)`.
Порчу-год-в-количестве ловит именно B2/B3 (целостность базы). Значит **оба слоя нужны**: M3 — против
выдумки в ответе, B2/B3 — против порчи факта в базе. Один другой не заменяет.

## Что отдать Codex
1. Находку `multichild.note percentage=2026` (оба бренда) — как тест-кейс гейта Волны 1 + поправить на 10%.
2. Калибровку: длительности в ценовых фактах не блокировать.
3. Ожидаемое распределение M3 — для сверки его `number_audit`.
