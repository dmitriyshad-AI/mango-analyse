# TZ-121 Block C Hybrid Semantic Review

## Verdict

`PASS_WITH_NOTES`

## Что прошло

- Гибрид не ухудшает подтвержденную победу модели: `80/100` против `72/100` у модели и `37/100` у правила.
- Все 8 ранее разобранных регрессий модели закрыты guard-условием.
- Основной каталог не пересобирался, live-бот и клиентский путь не менялись.
- Новых LLM-вызовов нет: использованы сохраненные Codex-предсказания из предыдущего принятого shadow-замера.

## Неблокирующие риски

- Guard сейчас опирается на follow-up regression list. Для будущего primary нужно решить, оставляем ли это как allowlist регрессий или превращаем в обобщенные правила по классам ошибок.
- Осталось `20/100` ошибок hybrid; перед primary нужен регрейд по сырью и разбор уверенно неправильных случаев.
- Это shadow-измерение, а не включение поведения.

## Новые регрессии / gate

- Добавлен тест, что runner:
  - применяет follow-up regression guard;
  - не включает primary;
  - не пишет raw-фрагменты по умолчанию;
  - не вызывает модель.

## Следующее действие

Дождаться регрейда C shadow. До регрейда `primary` для C не включать.
