# Регрейд этапа B (полировка остатков качества). По сырью. На 13a243ab (парент 338e9330). 2026-06-03.

Ревьюер: Клод #1. **Вердикт: PASS.** Проверено через `git show 13a243ab` (рабочее дерево Кодекса — worktree
`Mango analyse.phase12`, не примонтирован; коммит в общей git-истории).

## B1 — price без факта: честный маршрут. ПОДТВЕРЖДЕНО (код не правился — верно)
Пробой + сырьём: self-defer без price-факта УЖЕ честный. Шов `_missing_fact_helpful_template`
(subscription_llm.py 4310–4322): при отсутствии точного факта `route = "draft_for_manager"`, текст = полезный
общий, флаг `missing_fact_helpful_template_applied`. Снос Фазы 2 убрал старый `_pricing_safe_template`, и путь
стал честным сам собой. Кодекс верно НЕ трогал код, добавил сильный NEG-регресс
`test_step_b1_price_without_fact_downgrades_self_defer_but_keeps_boundaries`:
- price-no-fact → `route==draft_for_manager`, флаг есть, `"₽" not in draft` (НЕ выдумывает цену);
- price-С-фактом → `route==bot_answer_self_for_pilot`, флаг `rules_engine_price_format_matched`, в тексте
  «29 750 ₽» + «47 250 ₽» (отвечает из факта);
- **cross-brand** → `rules_engine_price_applied` НЕ в флагах, `cross_brand_safe_template_applied` есть,
  «29 750» НЕ в тексте (цена НЕ утекла во второй бренд);
- camp «сколько ЛВШ» → `rules_engine_camp_lvsh_applied`, «72 000 ₽» из своего правила (price-гард не перехватил);
- **P0** «Верните деньги, я недоволен, и цену тоже скажите» → `route==manager_only`, ни одного `rules_engine_price`
  флага (P0 перебивает цену, цена не протекла через претензию).

Матрица безопасности крепкая и ровно та, что в ТЗ.

## B2 — покрытие составных: только аудит. ПРИНЯТО
Кодекс отдал список `None` в audit pack (`.phase12/audits/_inbox/stepB_quality_remnants_20260603_020808/`),
код не менялся. Пак в непримонтированном worktree (untracked, не в git) — независимо не читал; информационно,
на поведение не влияет. Точечные фиксы реальных пробелов — отдельной микро-волной по списку, когда возьмёмся.

## B3 — тон-полировка f-строк. ПОДТВЕРЖДЕНО по диффу
5 открытий согреты, факты/числа/бренд/честные хвосты целы:
- recordings: «Для очных занятий действует такое правило:» → «По очным занятиям ориентир такой:»; «…такой
  порядок:» → «По онлайн-занятиям порядок такой:»; объединённый → «По онлайн-занятиям: … По очным — …»;
- format_choice 1134: «Формат за вас не выбираю: подтверждены …» → «Подскажу, что подтверждено, а формат
  удобнее выбрать вам: …» — **смысл-гард «формат выбирает клиент, не бот» сохранён**;
- schedule: «Старт занятий указан так:» → «По старту занятий:»; «В подтверждённых группах есть варианты на
  выходных:» → «По выходным есть такие варианты:» (хвост «менеджер сверит» цел); «В подтверждённых данных есть
  ориентир…» → «Есть варианты на выходных:» (хвост цел).
1 text-assert обновлён под новый текст — нормальная часть правки.

## Тесты
focused 34 / module(subscription_llm+rules_engine) 307 / полный 2427 passed, **9 failed**, 1 skipped.
9 падений — прежние infra/artifact `.phase12`, вне зоны B (совпадает с известным паттерном «~9 инфра»).
`git diff --check` чисто, tracked-дерево чистое.

**Замечание (честно):** 9 падений и audit pack — в непримонтированном worktree, независимо не верифицировал;
если важно — попросить Кодекса перечислить 9, чтобы подтвердить, что ни одно не из зоны B. По паттерну —
почти наверняка те же инфра.

## Итог
B закрыт. Регрессий нет, поведение домена не изменилось (полировка + честный маршрут уже был). Дальше —
**A (продающее понимание), Волна 1** (`TZ_stepA_wave1_FINAL_*`).
