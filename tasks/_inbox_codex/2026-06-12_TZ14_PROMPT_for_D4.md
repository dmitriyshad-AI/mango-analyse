# Промт для диалога D4 (выдаёт Дмитрий, автор — Claude-архитектор трека)

Ты — D4, исполнитель трека «действующие клиенты». Твоя задача — полностью выполнить наряд ТЗ-14: `tasks/_inbox_codex/2026-06-12_TZ14_D4_crm_amo_tallanto.md` (Шаги 1–4; Шаг 0 делает серверный Кодекс, Шаг 5 не делать). Нормативный текст ТЗ: `D1_audit_backlog/existing_clients/TZ14_FINAL_crm_amo_tallanto_2026-06-12.md`. Не останавливайся, пока наряд не выполнен и не отчитан.

## Порядок работы

1. **Сначала думаем.** Прочитай целиком: наряд, нормативный текст ТЗ-14 (включая §«Решения Дмитрия»), `D1_audit_backlog/existing_clients/DESIGN_crm_contact_card_2026-06-11.md` (§4–5, §7), `D1_audit_backlog/DELTA_crm_track_live_priorities_2026-06-12.md` (§2 контракт live_card, §3 таблица дедлайнов). Read-only разведка до первой правки: build_live_tallanto_context, контур amo_duplicate_after_staff_done, паттерн morning scan, utils/phone, механизм примечаний пилота. План коммитов — первым сообщением, потом код.

2. **Зависимость от Шага 0.** Массовые AMO-выгрузки — только после подтверждения Дмитрием, что серверный Кодекс закрыл Шаг 0 (смоук amo_api_get users прошёл). До того: код и тесты на синтетических фикстурах + Tallanto-дорожка.

3. **Параллель: до 4 субагентов, у всех максимальный уровень рассуждения.** Две независимые дорожки: A = AMO-контур (Шаг 1 → Шаг 2 → Шаг 3 этап А, последовательно); B = Tallanto-контур (Шаг 4, начинается сразу с пробы формата фильтра active). Плюс C = ревьюер-адверсарий (полный pytest + NEG + границы после мержа каждой дорожки). Общие файлы (.gitignore, conftest, регистрация CLI) правит только главный поток; мерж дорожек — по одной через главный поток.

4. **Дисциплина (стандарт проекта, без исключений):** каждый Шаг — отдельный коммит со своими тестами и NEG; хирургические правки; полный pytest (не подмножество) перед каждым отчётом; `git status` чистый после любых прогонов (ПДн!); фиче-ветка от main.

5. **Жёсткие границы (нарушение = стоп дорожки, не «обойти»):** ни одной пишущей операции в AMO/Tallanto — все пишущие контуры за флагами, default OFF / dry-run, включает только Дмитрий; pilot-инвариант (бот байт-в-байт, потребление live_card в промпте — не твоя зона); filial=shd — скип со счётчиком; ПДн и реальные выгрузки — не в git и не в тесты (только синтетические фикстуры); ФИО преподавателей фильтруются на источнике; CRM-запросы — постранично limit 50, пауза ≥0,5 с, ретраи на 429; перед массовым прогоном — микропроба 3–5 записей.

6. **Не виси на вопросах.** Открытые мелочи не блокируют: код Красносельской/Менделеево в справочнике филиалов → выяснить пробой и зафиксировать в отчёте; тип поля «ИИ: профиль клиента» → проверить, что текстовая область, при несовпадении — в отчёт и работать в dry-run. Полная остановка ТОЛЬКО при: красном полном pytest, который не чинится твоими правками; риске утечки ПДн; необходимости пишущей операции в CRM.

7. **Отчётность:** промежуточный отчёт в `tasks/_done/` после Шага 1 (счётчики дублей/общих телефонов/многодетных, пути CSV, база метрики «до» = ~11 дублей/нед); финальный отчёт после Шагов 2–4: коммиты, счётчики, перечень NEG, llm_calls_total по ролям (если LLM звался), путь к пакету 20 семей этапа А — его ревьюит Дмитрий, решение по этапу Б принимает он, НЕ ты.

Критерий завершения: Шаги 1–4 закоммичены, полный pytest зелёный, оба отчёта в `_done`, git чистый, в CRM не записано ничего. До этого — не останавливаться.
