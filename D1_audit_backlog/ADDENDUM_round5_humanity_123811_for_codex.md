# Аддендум для Кодекса — независимый регрейд round-5 humanity (123811). 2026-05-25

Claude перепроверил лично (вердикты, recall, транскрипты, RUN-снимок). Read-only.

## Подтверждено (хорошо)
- Безопасность закрыта: FAIL=0, hard-gate=0 (второй чистый прогон подряд). P0-перифразы 18/18, мтс убран, KB пересобран.
- Фикс «спасибо/поняла → не handoff» РАБОТАЕТ: на завершающих репликах бот даёт тёплое закрытие, без нового «передам менеджеру».
- «отправил бы без правок» растёт: 12.5%→21%→29%.

## Что НЕ закрыто — и точные причины

### 1. recall 6/8 — оба промаха это KB-ПРОБЕЛЫ, НЕ извлечение
Проверено по RUN-снимку (`kb_release_20260520_v6_3_team_answers`):
- foton «запись урока сохраняется» → в снимке 0 фактов [запис+сохран];
- unpk «олимпиадный онлайн только 9 и 11» → 0 фактов [9 и 11 + олимпиад]; в прошлом прогоне (101407)
  извлекался — пересборка KB, видимо, убрала/изменила этот факт.
Селектор отработал верно (olymp_19 получил required_fact_keys=olympiad_online.current + scope=olympiad_online).
РЕШЕНИЕ: добавить/восстановить 2 client-safe факта в ИСТОЧНИК (не в jsonl): foton recording-saved;
unpk «онлайн Физтех только 9 и 11». После этого recall_probe должен дойти до 8/8.

### 2. Человечность не сдвинулась (приёмка не выполнена)
ignored_question 10, over_handoff 9, templated 9 — НЕ упали vs прошлый прогон. Гварды подключены, но
`should_answer_not_handoff` по факту НЕ переворачивает маршрут: в 9 диалогах бот при наличии факта всё
равно «лучше проверить у менеджера» (perevod_money_04, matkap_05, multitopic_06, discount_09, presale_02/14,
zapis_enroll_15, olymp_19, addr_22).
РЕШЕНИЕ: сигнал should_answer_not_handoff должен РЕАЛЬНО переопределять route на bot_answer_self и
запускать регенерацию «ответь из факта», а не просто помечаться. И ответ должен закрывать ДЕЛЬТУ нового
вопроса (unanswered_direct_question → регенерация под него). Сейчас это сигналы-no-op.

## Честный предел
Дальше «человечность» — это уже не гварды, а КАЧЕСТВО ГЕНЕРАЦИИ/ПРОМПТА: гвард ловит «надо ответить»,
но текст всё равно хеджит. Рычаг — промпт генерации + few-shot (отвечать из факта без «проверит менеджер»,
когда факт есть; не хеджить на простом отвечаемом). Это и есть потолок самой модели, о котором говорили.

## Шум измерения (не переоценивать)
тон 71.8→64.7 и over_handoff 6→9 при N=24 на стохастике gpt-5.5 — в пределах разброса. Стабильны только:
безопасность (FAIL0/hard-gate0) и рост «отправил бы». Для надёжной оценки человечности нужен либо больший
набор, либо 2-3 прогона и медиана.

## Следующий шаг (приоритет)
1. Добавить 2 KB-факта в источник → recall к 8/8.
2. Сделать should_answer_not_handoff и unanswered_direct_question ДЕЙСТВЕННЫМИ (override route + регенерация под дельту), усилить промпт «отвечай из факта, не хеджи».
3. Прогнать round-5 2-3 раза, смотреть медиану ignored_question/over_handoff/«отправил бы», а не один прогон.
