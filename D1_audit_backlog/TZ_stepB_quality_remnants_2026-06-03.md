# ТЗ — Этап B (точечные остатки качества). Для Кодекса. На коммите 338e9330. 2026-06-03.

Автор: Клод #1. Основание: `MAP_quality_to_pilot_2026-06-03.md`, пункт B. Чистая архитектура готова (монолит
снесён, 338e9330). B = мелкая полировка ПЕРЕД главным рычагом A (продающее понимание). Делать хирургически.

## Главное про замер
**Отдельный дорогой LLM-прогон ради B НЕ нужен.** Критерий выхода = `pytest` зелёный (детерминированно, без
вызовов модели) + НЕГ-контроль (ниже). Живую LLM-проверку B объединим со следующим прогоном (A или финальная
приёмка) — не плодим замеры. Если по ходу добавляешь точечный прогон — счётчик `llm_calls` в summary уже есть,
число должно попасть в отчёт.

Все якоря — на 338e9330 (рабочее дерево в папке стоит на старом 36e23cb8, читай через `git show 338e9330:...`).

---

## B1 — price без факта: честный маршрут (подтвердить пробой → чинить, НЕ вслепую)

**Гипотеза (подтвердить, не принимать на веру).** До сноса при отсутствии факта цену «закрывал»
`_pricing_safe_template` (route мог оставаться answer_self с текстом «менеджер уточнит» — противоречие: маршрут
говорит «бот сам», текст говорит «передам менеджеру»). После Фазы 2 этот шаблон удалён; `_apply_price_rule`
возвращает `None` при отсутствии факта (нет grade / нет format / `scoped_facts` пуст / `selected_amount is None`
/ `price_text` пуст — rules_engine.py @338e9330, строки 1037–1090), и поток уходит в общий композер
(`_apply_migrated_rules_engine` → None на 1324–1325, subscription_llm.py).

**Входной тест (обязателен ДО любой правки).** Офлайн-проба: задать боту ценовой вопрос БЕЗ факта в базе
(например онлайн-цена по классу/предмету, которого нет в KB по active_brand). Зафиксировать ФАКТИЧЕСКИЕ
`route` + `draft_text`. Два исхода:

1. **Уже честно** (route=`draft_for_manager`, текст не делает вид, что бот сам назвал цену) → B1 закрыт сносом,
   ничего не правим, фиксируем в отчёте «проверено пробой, self-defer не воспроизводится».
2. **Self-defer** (route=`bot_answer_self_for_pilot` + текст вида «цену уточнит менеджер / не могу назвать») →
   добавить УЗКИЙ детерминированный гард: *ценовое намерение + нет grounded суммы* → `route=draft_for_manager`
   + честный текст («Точную цену по этому классу/формату подтвердит менеджер — пришлёт актуальную сумму»). Гард
   ставить в шве, где рождается ответ на ценовой интент без факта, не в самом правиле (правило корректно
   возвращает None).

**НЕГ-контроль (защитный слой — без отрицательного теста правка не считается завершённой):**
- price С фактом → по-прежнему `bot_answer_self_for_pilot`, цена из факта (НЕ сломать рабочий путь 2b.4);
- cross-brand ценовой вопрос → каноничная фраза разделения (не утечь второй бренд);
- camp/ЛВШ-цена → прежнее поведение (не перехватывать ценовым гардом);
- P0 (реальная претензия по оплате) → `manager_only` (гард цены не глотает P0).

---

## B2 — покрытие составных вопросов: АУДИТ, не слепой фикс

Не вводить новые ветки наугад. Обработчик второго предмета в скидке УЖЕ есть
(`_asks_second_subject_discount`, rules_engine.py @338e9330 ~854–872) — это образец, что часть составных уже
покрыта.

**Задача — только отчёт:** прогнать правила (офлайн, без LLM, через `apply_rule`/диспетчер на готовых
intent+facts) на реальных составных и «примерных» вопросах из набора `data/` (реальные клиентские вопросы;
если набора под рукой нет — взять 15–20 типовых: «сколько два предмета», «а примерно?», «онлайн и очно
вместе», «физика и математика вместе сколько»). Выписать, на каких именно правило вернуло `None` (тема + текст
вопроса + почему None). **Код в этой волне НЕ менять** — отдаёшь список, мы решаем точечные фиксы отдельной
микро-волной. (Цель — не плодить впрок: чиним только подтверждённые реальные пробелы.)

---

## B3 — тон-полировка сухих f-строк (чистый текст, без изменения смысла)

После согрева 6f44188e часть открытий ещё канцелярские. Согреть ТОЛЬКО ОТКРЫТИЕ фразы; факт, числа, бренд,
смысл-гард и честный хвост («менеджер уточнит/сверит») — НЕ трогать. Не удлинять, не добавлять обещаний.

Конкретные строки (rules_engine.py @338e9330) и ориентир (формулировку финально выбираешь сам, контракт выше):

| Строка | Сейчас (сухо) | Ориентир (теплее) |
|---|---|---|
| 350 | «Для очных занятий действует такое правило: …» | «По очным занятиям ориентир такой: …» |
| 354 | «Для онлайн-занятий действует такой порядок: …» | «По онлайн-занятиям порядок такой: …» |
| 358 | «Для онлайн-занятий действует такой порядок: … Для очных — …» | «По онлайн-занятиям: … По очным — …» |
| 1134 | «Формат за вас не выбираю: подтверждены …» | «Подскажу, что подтверждено, а формат удобнее выбрать вам: …» (СОХРАНИТЬ смысл-гард «формат не выбираю за клиента») |
| 1496 | «Старт занятий указан так: …» | «По старту занятий: …» |
| 1506 | «В подтверждённых данных есть ориентир на слоты по выходным: …» | «Есть варианты на выходных: …» |
| 1514 | «В подтверждённых группах есть варианты на выходных: …» | «По выходным есть такие варианты: …» |

(1139/1144/1152/1155 «Онлайн/Очный формат подтверждён: …», 1482 «Подтверждённый график сейчас такой: …» —
приемлемы, низкий приоритет; трогать по желанию, тем же контрактом.)

**Важно:** строки 1134 и format_choice кодируют ГАРД (бот не выбирает формат за клиента) — тёплая
переформулировка обязана сохранить этот смысл. Текстовые assert-тесты, проверяющие старые открытия, обновить
под новый текст (это нормальная часть правки, не ослабление).

---

## Тест выхода (КРИТИЧНО)
1. **`pytest` полностью зелёный**, включая обновлённые text-assert тесты f-строк (кроме известных ~9 инфра, если
   ещё актуально — указать какие).
2. **НЕГ-контроль B1** (см. выше) проходит: price-с-фактом отвечается сам, cross-brand→разделение, camp-цена не
   тронута, P0→manager_only.
3. **Дифф f-строк B3** = только ОТКРЫТИЯ фраз; факт/числа/бренд/смысл-гард/честный хвост не изменены (показать
   дифф до/после по каждой строке).
4. **B2** = приложен список None-составных (без правок кода).
5. Поведение домена не изменилось (B — полировка, не переписывание логики).

## Что НЕ делать
- НЕ трогать safety-шаблоны (cross_brand/terminal/result_guarantee/admission_guarantee), выходной гейт, планнер,
  p0_pregate, stable_runtime, KB.
- НЕ делать слепой фикс B2 (только аудит-отчёт).
- НЕ запускать отдельный большой LLM-замер ради B.
- НЕ git reset/checkout/clean; рабочее дерево чистое после коммита; не-tracked документы Клода/планы не трогать.

## Отчёт
- B1: результат пробы (был ли self-defer); если правил — где гард, текст, почему безопасно + НЕГ-результаты.
- B2: список правил→None на реальных составных (тема/вопрос/причина).
- B3: дифф открытий до/после по каждой строке.
- Ревизия коммита для моей проверки по сырью.

После B — главный рычаг A (продающее понимание из playbook Тропиной/Козловой в композер-промпт + NEG-гейт против
выдумок). См. `MAP_quality_to_pilot_2026-06-03.md`.
