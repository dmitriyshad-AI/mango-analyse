# Ревью коммита Шага 2b.1 — `7b28fa28`. По сырью в песочнике. 2026-06-02.

Ревьюер: Клод 1 (read-only). Вердикт: **PASS**. Безопасно, хирургично, фолбэк цел, баг адреса закрыт.

## Что проверено по сырью (не по отчёту Кодекса)
- **Реальность/ancestry:** коммит существует, построен прямо на гейте Шага 1 (`96b14560 Add authoritative
  output safety gate`). 10 файлов.
- **Хирургичность:** `subscription_llm.py` +162/−0 — чисто добавление, ни одна safe_template не удалена →
  фолбэк 13 правил цел. Анкеры: intent-plan +7, pipeline +2, fact_retrieval +1, context_builder +4. Каркас
  `rules_engine.py` 401 новый. Без расползания в другие правила.
- **MIGRATED = {teacher, recordings, contact_address}** — `select_rule` возвращает None для остальных →
  фолбэк. Диспетчер встроен РОВНО на месте старого safe_template (`apply_dialogue_contract_v2_template_
  dispatcher`), наследует те же P0-гарды + после него `_reverify_text_change` + выходной гейт Шага 1.
- **Исключение адреса НЕ ослаблено:** «Адресный факт нельзя выдавать как ответ на неадресный вопрос»
  на месте (pipeline:3802). Фикс — расширение детекции адреса на `площадк` (не ослабление).
- **Тесты (мои прогоны):** `test_rules_engine.py` 3 зелёных, `test_subscription_llm_draft_provider.py`
  317 зелёных (вкл. политику C для «ты бот?» и сохранение P0 manager_only).

## Адверсариальные проверки (мои, сверх тестов Кодекса)
- **P0-жалоба со словом «преподаватель»** ("преподаватель плохо ведёт, верните деньги") → route остаётся
  `manager_only`, правило НЕ срабатывает. Перебивания P0 нет. ✓
- **Позитив — правила реально работают (не инертны):**
  - teacher: регалии из факта (МФТИ/МГУ/ВШЭ), ФИО → менеджер уточнит;
  - recordings: «онлайн доступны» из факта;
  - **Фотон адрес → «Москва, Скорняжный»** — баг Прогона 1 закрыт; ✓
  - УНПК адрес → все 3 площадки + уточнение.

## Одно наблюдение (не блокер, в план 3b)
Правила сейчас выдают ДЕТЕРМИНИРОВАННЫЙ текст f-строкой, grounded в факте (не старая константа, но и не
LLM-композиция). Это безопасно и верно для 2b.1; «живость» тона — это слой X2 (сейчас выключен, Шаг 3b).
Поэтому точечный прогон дал PWN=11/12 (безопасно, но тон плоский). Ожидаемо.

## Рекомендация для 2b.2 (дисциплина негатив-контроля)
Добавить явный NEG-тест: «правило НЕ перебивает P0/manager_only, даже когда topic-regex
(`_migrated_rule_intent_from_dialogue_contract`) ловит „преподав/адрес/запись“». Я это проверил вручную —
держится за счёт гейта/гардов, но в сьюте Кодекса именно такого теста нет. Это его правило #4 (контрольный
отрицательный тест на каждую правку защитного слоя).

## Итог
Шаг 2b.1 принят. Каркас миграции работает, 3 правила перенесены сквозь, фолбэк и безопасность целы, баг
адреса Фотона закрыт. Можно идти в 2b.2 (следующая волна правил) или сначала включить разбор тона (3b).
