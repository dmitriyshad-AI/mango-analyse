# ТЗ-14 (D4): CRM-контур действующих клиентов (AMO + Tallanto) — Шаги 1–4

Дата: 2026-06-12. Постановка: Claude (архитектор трека). Принято Дмитрием 12.06 (решения вписаны в нормативный текст).
Нормативный текст: `D1_audit_backlog/existing_clients/TZ14_FINAL_crm_amo_tallanto_2026-06-12.md` — источник правды по содержанию Шагов, NEG и решениям Дмитрия. Этот файл — наряд на исполнение, при расхождении побеждает нормативный текст.

## Объём наряда

- Шаги 1, 2, 3 (только этап А — 20 семей на ревью), 4 — целиком по нормативному тексту.
- Шаг 0 — НЕ в этом наряде: исполняет серверный Кодекс (api.fotonai.online) по отдельному промту; для D4 это внешний блокер AMO-контура.
- Шаг 5 (запись в Tallanto) — НЕ делать: отдельное «да» Дмитрия, отдельный наряд.
- Этап Б Шага 3 (раскатка карточек после ревью) — НЕ в этом наряде.

## Порядок и зависимости

- До подтверждения Шага 0: только код и тесты на синтетических фикстурах, Tallanto-дорожка (Шаг 4) и точечные read-only пробы (до ~20 запросов).
- После Шага 0: Шаг 1 → Шаг 2 → Шаг 3 этап А (после цифр Шага 1). Шаг 4 — параллельно, Шагов 1–3 не ждёт.
- Массовые AMO-выгрузки: постранично limit 50, пауза ≥0,5 с, ретраи на 429. Перед любым массовым прогоном — микропроба 3–5 записей (правило проекта).
- Формат фильтра active в tallanto_select — подтвердить живой пробой в начале Шага 4 (NEG нормативного текста).

## Жёсткие границы

- Пишущих операций в AMO/Tallanto НЕТ. Все пишущие контуры (примечания Шага 2, задачи «перезвоните», карточки Шага 3) — за флагами, default OFF / dry-run. Включение любого контура — только Дмитрий.
- Pilot-инвариант: поведение бота байт-в-байт без изменений. Потребление live_card в промпте бота — зона главного архитектора, не трогать.
- filial=shd (Школа Дизайна «ЖАКО») — скип со счётчиком везде: live-карточка, карточки CRM, бренд-метки. Не «Фотон».
- ПДн: выгрузки, срезы, журналы — не в git (расширить pii-gitignore-тест, NEG: `git check-ignore` rc=0); ФИО преподавателей фильтруются на источнике.
- Телефоны нормализуются единым utils/phone.
- Старое поле «Авто история общения» не трогать (решение Дмитрия).

## Отчётность и приёмка

- Промежуточный отчёт в `tasks/_done/` после Шага 1: счётчики (живые дубли, общие телефоны 2+ семей, многодетные), пути CSV-пакетов, база счётчика метрики «новых дублей/неделю» (точка «до» = ~11/нед).
- Финальный отчёт в `tasks/_done/`: коммиты по Шагам, счётчики, перечень NEG, llm_calls_total по ролям (если LLM звался), путь к пакету 20 семей этапа А на ревью Дмитрию.
- Приёмка — §«Приёмка ТЗ-14» нормативного текста: полный pytest зелёный + NEG всех шагов; в AMO/Tallanto не записано ничего; git status чистый.
