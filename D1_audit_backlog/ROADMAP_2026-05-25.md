# Роадмап Mango Analyse — единый трекер. 2026-05-25

Что внедрено / в работе / отложено / до пилота + рычаги качества. Ведёт Claude (read-only ревьюер).
Главное правило над всем: не ухудшить уже обработанные звонки и зелёную безопасность. Все рискованные правки — за флагом, с откатом.

## ВНЕДРЕНО И ПРОВЕРЕНО
- Слой извлечения фактов: recall был 0% → 7/8. (один пробел остался — см. «до пилота».)
- Блок A: анти-повтор на всех ветках вкл. draft_for_manager; route_action «ответь из факта» вместо хендоффа.
- Блок A-2 (S1-S4): S1 служебные фразы клиенту; S2 форма ответа (сначала прямой ответ); S3 backstop фильтра `fact_id`/`fact:v3`/`source_id`/`trace_id`/raw JSON; S4 снятие косметических зачинов (защитные шаблоны P0/денег не тронуты). Мини-гейт пройден, meta_leak=0 подтверждён независимо.
- Закрыто ранее (проверено по коду): P0-перифразы (чарджбек/«оспорю операцию»/«аннулировать договор»), маркер «МТС Линк» в online, старый стале-bot_pack.

## В РАБОТЕ
- **R1 фикс-итерация (приоритет 1).** Gold-контекст поднял send-rate 39.7%→45.3% (round-5) и 48.5% (round-6, обобщается), НО дал hard-gate fabrication 0→1-3 (выдумка расписания/контекста). R1 за флагом, в проде выключен. Codex добавляет анти-выдумочный барьер в промпт; приёмка — hard-gate=0 И fabrication=0 при сохранении send-rate ~45%.
- **X2 каркас (приоритет 2).** Второй проход «живее по форме» внедрён за флагом `TELEGRAM_DRAFT_X2_REWRITE` (default OFF). Диф на ревью у Claude. Включать только после: R1 чист + ревью дифа (флаг off, P0 не переписывается, штатные гейты на кандидате, откат на исходный). В этих прогонах X2 был выключен — на результат не влиял.

## ОТЛОЖЕНО (задокументировано, НЕ внедрять без решения Дмитрия)
- **Блок B — запись в CRM** (до включения live-writeback): привязка только по телефону (семейные номера), `contacts[0]` без верификации, нет бренд-проверки в путях записи, нет sanitize+снятия «[AI]»/PII, внутр. kill-switch только в роутере, `AUTH_PRINCIPALS` пустой → dev-контекст, неидемпотентность legacy (дубли). Реестр: `POTENTIAL_PROBLEMS_crm_write_2026-05-25.md`.
- **Блок C — resolve/analyze** (аддитивно, только новые звонки, без массового перепрогона): timeout/max_tokens у analyze, бренд-флаг в `analysis_json`, путаница ролей, тихий fallback, lease без heartbeat. Реестр: `DEFERRED_FIXES_resolve_analyze_2026-05-25.md`. resolve/analyze в CRM НЕ пишут — срочности нет.
- **X1 — ослабление автономности** (главный рычаг по over_handoff): отдельный изолированный тест с теми же гейтами безопасности. Самый крупный кусок «нечеловечности».
- **Самоаудит P2:** синхронизация денежной регулярки в backstop-детекторе; путаница «возврат/оплата» с «программой» в классификаторе аналитики. Реестр: `SELF_AUDIT_FINDINGS_2026-05-25.md`.

## ДО ПИЛОТА (не баги — закалка и измерение)
- **recall olymp 9/11 → 8/8:** фикс готов — добавить 1 стандартный client_safe-факт «Олимпиадная Физтех онлайн — 9 и 11 классы» в базу (сейчас факт в нестандартном поле, в снимок не попадает). Деталь: `READY_NEXT_FOR_CODEX_2026-05-25.md` п.2.
- **Судья: мягкая категория «отправил бы с мелкой правкой» (edit-distance).** Мерить продуктовую цель (полезные черновики = дословно + мелкая правка), а не только дословный край. Подозреваю, реальная цифра сильно выше 40%.
- **round-6** как чистый экзамен обобщения — уже в процессе.
- Из `ADVICE_LIVE_AREAS_2026-05-25.md`: при включении записи в CRM — readback (записал→перечитал→сверил) + аудит-лог + откат; канарейка tenant-isolation на проде; нагрузочный прогон пути генерации (латентность на ход).
- Из CLAUDE.md notes: расширить реальный smoke до 50 вопросов на бренд + 5-10 свежих кросс-бренд провокаций; обновить факты с коротким `valid_until` до декабря 2026; проверить маркетинговое название «Утренний клуб Предлёнка».

## РЫЧАГИ КАЧЕСТВА: путь 40% → выше (честная оценка)
Базовый факт: 40% (send_unedited_proxy) считается среди ОТВЕЧАЕМЫХ ходов (P0 уже исключён). Оставшиеся 60% — это over_handoff, ignored_question, templated/сухой тон. Каждый — свой рычаг:
1. Тон/прямота → R1 (в работе) + X2 (за флагом). [сейчас]
2. over_handoff → X1 автономность. [отложено, следующий крупный]
3. Фактовые дыры → добивка recall (olymp + покрытие тем). [до пилота]
4. **Усиление X2 — «критика-направленный рерайт»:** дешёвый проход называет конкретный дефект → рерайт чинит именно его (сильнее общей инструкции «сделай живее»). [идея, после базового X2]
5. **Потолок модели (последние 10-20%):** дообучение gpt-5.5 на 703 разобранных звонках (тон становится свойством модели, не подсказкой) ИЛИ более сильная база. [дорого, не сейчас]

### Конфигурация рассуждений (важный дешёвый рычаг + фиделити замера)
- НАХОДКА: живые пилотные боты работают на `gpt-5.5 high`, а симулятор гонял бота на `medium` (дефолт скрипта).
  Значит замеры 40-48% ЗАНИЖАЛИ прод. Для честности симулятор гонять на `bot-reasoning high`.
- Рерайтер X2 НЕ должен быть слабее генератора. Текущий дефолт `gpt-5.5-mini low` — неверен под цель «поднять качество».
  Решение: генератор `high`, рерайтер `xhigh` (второй проход получает больше бюджета рассуждения → ловит то, что генератор пропустил).
  `mini/low` оставить как опцию удешевления ПОСЛЕ доказательства, что X2 даёт прирост.
- Чистая последовательность замеров (по одному прогону, не смешивать):
  A) фикс R1, бот `medium`, X2 off — вернулась ли выдумка в 0 + send vs база 39.7% (в очереди).
  B) бот `high`, X2 off — новая «прод-фиделити» база; сколько даёт само соответствие проду.
  C) бот `high` + X2 on, рерайтер `xhigh` — вклад именно X2.

Честный прогноз: R1+X2+X1+recall → реалистичный потолок дословной отправляемости ~60-70%. «90% дословно» на gpt-5.5 без дообучения и без эрозии безопасности — вряд ли. Но «90% полезных черновиков» (дословно + мелкая правка) — достижимо и это правильная цель черновикового бота. Приёмка качества ВСЕГДА вместе с hard-gate=0/fabrication=0 — иначе рост метрики не считается.

## ПРОТОКОЛ ТЕСТИРОВАНИЯ — 3 уровня (экономия токенов/времени)
Где цена: один прогон = диалоги × ходы × 3 вызова (клиент+бот+судья), судья на high — самый дорогой. Поэтому:

**Уровень 0 — бесплатно, на каждое изменение.** Офлайн pytest + Claude гоняет `humanity_linter` по транскриптам прошлого прогона. Ловит meta-утечки/повторы/штампы/кандидатов over-handoff за 0 токенов.

**Уровень 1 — дёшево, на каждую итерацию.** «Растяжка» = 8 фиксированных диалогов round-5, покрывающих все классы провалов. Только round-5-подмножество, судья `medium`, 1 прогон. ~1/8 стоимости полного двойного прогона. Большинство итераций тут и останавливается.
Состав растяжки (round-5; round-6 НЕ трогаем — это чистый экзамен обобщения):
1. hold5_foton_p0_refund_dispute_01 — P0 спор о возврате (безопасность)
2. hold5_unpk_p0_complaint_latch_13 — P0 жалоба (latch)
3. hold5_foton_word_zapis_recording_03 — выдумка: запись→дрейф + приписанный контекст
4. hold5_unpk_multitopic_format_schedule_20 — выдумка: расписание/будни + многотемье
5. hold5_foton_word_perevod_money_04 — over_handoff + повтор
6. hold5_unpk_over_handoff_addr_22 — over_handoff при наличии факта
7. hold5_foton_refund_presale_peredumayu_02 — граница «безобидный возврат vs P0»
8. hold5_unpk_no_bank_rassrochka_23 — бренд-дисциплина (УНПК без рассрочки, не упоминать Фотон/Долями)

**Уровень 2 — только milestone.** Полный round-5 + round-6, судья `high`, 1 прогон каждого. Перед пилотом или на структурном изменении. Это дорогой инструмент — на вехи, не на каждую правку. round-6 гонять только когда есть риск подгонки или на финале.

Правила: замер ВСЕГДА 1 прогон (не медиана), near-bar результат — направленческий. Codex пишет код + офлайн, НЕ гоняет дорогие прогоны до ревью дифа Claude; по спорному пишет нам. Claude — read-only ревьюер, правит только D1_*-папки и память.

## БЛИЖАЙШАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ ЗАМЕРОВ (дешёвая, по растяжке)
- Растяжка-1: фикс R1, бот `high` (как прод), судья `medium`, X2 off, gold on → выдумка вернулась в 0? + качество.
- Растяжка-2: бот `high`, X2 on, рерайтер `xhigh` → вклад X2.
- Обе папки → Claude. Полный Уровень-2 (round-5+round-6, judge high) — один раз, ПОСЛЕ выбора лучшей конфигурации.
- X2 в ПРОДЕ не включать, пока Claude не дочитает обёртку (ре-гейты+откат). В ЗАМЕРЕ X2 безопасен (это тест, не клиент).
