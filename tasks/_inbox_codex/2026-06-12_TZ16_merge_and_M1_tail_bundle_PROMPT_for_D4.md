# Промт для D4: мерж ТЗ-16 + бандл «хвост 3 439» для M1 (выдаёт Дмитрий; заменяет промт чистого мержа от 12.06)

Ты — D4. Два блока строго по порядку. Сначала сними залипший замок в каноне: `rm ".git/index.lock"` (проверь, что git-процессов нет).

## Блок 1. Мерж ТЗ-16 (одобрен Дмитрием выдачей этого промта)

1. Перенос артефактов ДО уборки: папку `Mango-analyse-tz16/product_data/customer_profiles/tz16_profiles_v7_20260612/` целиком в `product_data/customer_profiles/` главной папки. Перед переносом закрыть sqlite (checkpoint -wal/-shm). Проверка: число/размеры файлов совпадают, базы открываются, в git ничего не добавилось.
2. `codex/tz16-d4-profiles-v7-rerun-tail` → `main` отдельным merge-коммитом (main ушёл вперёд после гигиены — при конфликте НЕ решать самовольно: стоп и доклад). Приёмка: дифф пре-мерж..main = только файлы ТЗ-16; запретных путей нет; полный pytest зелёный; `git status` чистый.
3. Удалить дерево `Mango-analyse-tz16` штатной командой git worktree. Главную папку не переключать.

## Блок 2. Бандл хвоста для M1 → Яндекс (по образцу analyze_rerun_20260611)

Цель: перепрогон выжимок 3 439 звонков (зона активных клиентов, старая выжимка, дата < 2025-06-01, длительность ≥ 60 сек, БЕЗ blacklist_77) на gpt-5.4-mini, полная подсказка v7 С АНТИ-«АВТООТВЕТЧИК» правкой из ТЗ-16 (поэтому строго после Блока 1).

1. Селектор хвоста — те же критерии, что в `rerun_tail_report.json` ТЗ-16 (квадрант old_ge_60 без blacklist): ровно 3 439 call_id; число в манифесте обязано совпасть, расхождение = стоп и доклад.
2. Состав бандла (структура как у analyze_rerun_20260611): манифест (call_id, длительность, символы); транскрипты data/; подсказка v7-исправленная с фиксацией версии и хэша; конфиг прогона: модель gpt-5.4-mini, профиль full, `--parallel 4`; blacklist_77.txt для контроля невключения; README_RUN с командой запуска и ожидаемым временем (~4,7 ч).
3. Разместить: `OpenClaw/Actual Mango Tests/analyze_tail_20260612/` + задача в `tasks/_inbox_m1` (шина: лимит 3 задачи без подтверждения не исчерпан). В task.yaml — только прогон и выгрузка результатов JSONL; ВЛИВАНИЕ в каноническую базу в задачу НЕ включать (отдельное ТЗ с приёмкой, как у v7-импорта).
4. ПДн: транскрипты только в data/ бандла на Яндексе (прецедент analyze_rerun); по завершении всех работ напомнить Дмитрию удалить data/ с диска — строкой в отчёте.

NEG: blacklist-звонки в манифест не попали (пересечение = 0); повторная сборка бандла идемпотентна (тот же манифест); в git транскрипты не попали.

## Отчёт

В `tasks/_done/` канона: хэш merge-коммита и проверки Блока 1; подтверждение переноса среза v7; число звонков/символов в манифесте, хэш подсказки, путь бандла и задачи M1; перечень NEG. Запись в AMO/Tallanto/CRM запрещена; полный перепрогон blacklist-77 в этот бандл НЕ входит (отдельное «да» после ревью малого батча).
