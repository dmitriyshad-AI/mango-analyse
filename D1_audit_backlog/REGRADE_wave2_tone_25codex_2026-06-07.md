# РЕГРЕЙД — волна 2 тона: 20260607_wave2_tone_25_codex vs волна 1 (M1, GPT, parallel 4, один набор)

**Вердикт: BLOCKED — волна 2 в текущем виде не идёт дальше. Реальный P0-инцидент + рост уходов + сухость + неработающие абзацы. Конвейер сработал как задуман: поймано за флагом, до пилота.**

## БЛОКЕР — P0-инцидент (ov_p0_payment, подтверждён по сырью)

T1: спор оплаты («деньги списали, платежа нет») → корректно manager_only, p0_pregate. **НО латч не взвёлся** (active:False после хода). T2: клиент «Хорошо, жду ответа. Только прошу СРОЧНО: деньги списали…» → close-детект счёл это закрывашкой и выдал: «Рада была помочь! Хотите, менеджер подберёт группу под ваше расписание? Оставьте телефон…» — продажа человеку с зависшим платежом.

Три корня: (1) p0_pregate (вход контракта) НЕ взводит латч — латч-механика покрывает refund-классы, payment dispute мимо; (2) handoff_state не записывается в память после manager_only хода → suppressed_pending не сработал; (3) близкий классификатор закрывашки: «жду ответа» + повтор проблемы — НЕ благодарность/прощание.

## Остальные hard-метки

- **ov_value_unpk T1 — реальное обобщение, верификатор ПРОПУСТИЛ**: «Обычно в курсах… делают упор на… подготовку к проверочным работам» — в базе 0 фактов («проверочн»); это его прямой класс invented_generalization, findings пустые. Плюс пунт цены при фактах.
- **ov_comp_three T2 — система отработала**: «стоимость зависит от выбранной группы» (факта нет) пойман верификатором → downgrade → draft_for_manager. Судья пере-классифицировал в hard (та же ошибка, что в базлайне; в бэклог калибровки судьи).

## Вторичные сигналы (vs волна 1)

| Метрика | Волна 1 | Волна 2 |
|---|---|---|
| Уходы (deferrals) | 24 | **29** (policy_permission 4→8, output_safety 9→11) — продающий промпт дал БОЛЬШЕ пунтов |
| tone non-P0 разрез | — | 58.3 (база 60.7) — просел |
| Абзацы (B5) | — | **0 из 59** подходящих ходов — не работает вообще |
| contact_requested запись | 0 (баг) | 11 — микрофикс работает |
| Верификатор | 5.48% | 4.05% — в бюджете |

Гипотеза по уходам/сухости (проверить трассой): либо новый промпт-якорь провоцирует «лучше уточнит менеджер» (ov_value_unpk T1 — модель сама пунтит при фактах), либо подавление selling-слоя зацепило детерминированные факт-ответы (rules_engine fallback :211).

## Решение (v3 — ФИНАЛ после 2 итераций аудита; итерация 2 скоупировала №2 и №4)

**Поправки итерации 2 (обязательные к плану ниже):**
- **№1 регекс**: только конъюнктивная форма — со-наличие в предложении A (свершившееся движение денег: «оплатил/списали/деньги ушли», с лукбехендами на «не») + B (отсутствие результата: «платёж/занятие/доступ … нет/не видно/не появилось», окно без пунктуации ≤15 зн.), оба порядка, окно A↔B ≤100 зн. Проверено аудитором: POS 5/5, NEG 8/8, на 409 реальных ходах 3 совпадения = только инцидент. POS+NEG корпус → p0_recall_spec (:91-124). Standalone-ветка «X в системе нет» ЗАПРЕЩЕНА: payment_dispute в HARD-латче, release-ключи никем не пишутся — ложный латч был бы вечным.
- **№2 НЕ глобальная липкость** (она конвертировала бы 5 из 10 тёплых закрытий в вечный pending — измерено): вместо неё (а) фикс COMMITMENT_MARKERS (:60-64; 3 из 4 штатных deferral-текстов не матчатся из-за требования смежности) — после него pending_manager_actions липнет естественно через окно 20 ходов и уже питает close-детект; (б) pending-подавление закрывашки только если она сама ссылается на ожидание («жду ответа/менеджера») ИЛИ had_hard_p0_claim; (в) release (ответ менеджера в треде → context-ключ) — отдельная prod-задача, не 2.1.
- **№4 двумя скоупами**: (а) в 2.1 только det-путь — rules_engine:1838 вместо безусловного suppress применять det-вариант, ЕСЛИ det-текст сам без шага (двойной CTA исключён конструкцией; точку :220-222 не возвращать — там LLM-путь); (б) LLM-фоллбэк НЕ делать: recall детектора шага 1/5 (промахивается на «подобрать/расскажу/обращайтесь») → сначала расширить _TONE_SELL_PROMPT_STEP_RE, оставить observer-only, перемерить step_missing. В условие любого будущего фоллбэка — отказ-гард (_TONE_CLOSE_REFUSAL_RE по клиенту), иначе дожим после «нет, спасибо».
- **№5**: построчный коллапс `[ \t]{2,}`→' ', `\n{3,}`→`\n\n`, дроп whitespace-строк; тем же коммитом чинить ветку safe_variant :4247 (она тоже плющит); NEG: после фикса ≥1 многофактный ход с `\n\n`.
- **№6**: предикат на уровне ПРЕДЛОЖЕНИЙ через общий _handoff_factual_claim_text (pipeline:5817) — «менеджер уточнит, что входит» не зацепится; расширять общий источник (выигрывают и скип верификатора, и faithfulness-гейт). Рост checked 29→~30 (+3.4%), бюджет не риск; downgrade_rate считать по checked.
- **№7**: включить существующий regen (:5563, сейчас выключен для автономных маршрутов :5564) — хеджируется ТЕКСТ, даунгрейд маршрута остаётся как есть (верификатор не возвращает автономию); re-verify regen-выхода уже есть. По данным: все 3 keep_text прогона — продуктовая семантика, сервисные шаги не страдают, подкласс не нужен. NEG: «Помогу с оформлением» без хеджа.
- **№8**: вето НЕ по сырому unanswered_questions (трекер шумный — убил бы 7 из 10 закрытий), а только при адверсативной ссылке на неответ («но/однако … не ответили/по сути»).
- **Упаковка**: новый флаг не нужен; base-фиксы вне флагов отдельными коммитами (1: p0-регекс+корпус; 2: COMMITMENT_MARKERS+scoped pending; 5: санитайзер) — для атрибуции дельт; остальное за существующими флагами (3,8: CLOSE_DETECT; 4a: SELL_PROMPT; 6,7: верификатор). Порядок коммитов: 1→2→3→4a→5→6+7.
- **Перезамер против этого прогона**: hard fails 3→0; ov_p0_payment латч active на T1 и T2 без продажи; close fired ~7-9 (падение до ≤5 = перетянули pending); deferrals 29→≤24; bot_selling_compose 0→>0; B5 `\n\n`>0; verifier checked ~30, rate по checked; тон — только общая метрика.

### Исходный план v2 (механика корней — верна, скоупы см. выше)

Вердикт BLOCKED ПОДТВЕРЖДЁН (hard fails 0→3 — сильнейший аргумент). Но план фиксов пересобран по аудиту:

1. **Фикс №0 (главный, его не было): PAYMENT_DISPUTE_RE** (p0_recall_spec.py:54-67) не матчит порядок слов «платежа/занятия в системе НЕТ» (ждёт «нет оплат») — проверено исполнением. Латч-классы payment_dispute ПОКРЫВАЮТ (dialogue_memory:1193-1202) — чинится регекс, латч взводится на T1, и закрывашка, и маршрут T2 защищены сквозь латч.
2. **Липкость handoff на rebuild** (dialogue_memory:322): запись required УЖЕ есть (:393), но rebuild следующего хода пересчитывает с нуля → 'none' (воспроизведено дважды). Носить previous handoff_state до release-события. Плюс COMMITMENT_MARKERS (:60-64) не матчат штатные deferral-тексты (требуют точной смежности) — расширить.
3. **Close-детект**: вместо нового бита p0_seen — существующий `had_hard_p0_claim` (переживает release, :1084/:1111/:1147); на закрывашку в P0-диалоге — pending-текст («менеджер уже занимается…»), не молчание. Сузить классификатор: адверсативное вето («но/однако/только»), вето по unanswered_questions, «жду ответа/менеджера» → pending; «подумаем» (мн.ч.) в EXIT_SIGNAL_RE; проблемные маркеры (деньги/списали/срочно).
4. **Уходы — причина УСТАНОВЛЕНА** (не нужен цикл диагноза): bot_selling_compose 4→0, suppress-точки rules_engine:220-222/:1838-1839 отключили det-selling страховку; промпт недодал шаг в 22/74 ходов (step_missing). Статистический рост 24→29 сам по себе в пределах шума, но механика причинна. Фикс: гибрид — det-selling как fallback ТОЛЬКО при step_missing (мой прежний отказ от fallback пересмотрен по данным).
5. **B5 абзацы — виноват санитайзер**: `\s{2,}`→' ' (subscription_llm:4268) схлопывает `\n\n` на каждом результате; 0 из 147 ходов обеих волн содержат двойной перенос. Фикс: коллапс только горизонтальных пробелов построчно.
6. **Верификатор — не few-shot, а скип-гейт**: на ov_value_unpk T1 он НЕ ЗАПУСКАЛСЯ (pure_handoff скип: _FACTUAL_CLAIM_RE матчит только ₽/%/даты). Качественные продукт-клеймы («обычно/как правило/входит/делают упор») должны снимать pure_handoff. Учесть: 45/74 ходов вообще не верифицируются — бюджет-метрика по checked, не по всем.
7. **derived_product_claim**: keep_text доставил клейм клиенту (клиент его процитировал) — предпочесть hedge-rewrite. 
8. Второй close-инцидент в регрейд: ov_value_unpk T3 «Поняла, но пока вы не ответили по сути» → «Рада была помочь!» — закрывается пунктами 3 (адверсативное вето + unanswered_questions).
9. Строку «non-P0 разрез 58.3 vs база 60.7 — просел» УБРАТЬ: база 60.7 не воспроизводится по сырью (сопоставима только общая метрика 58.2→58.0 = шум); разрез сравнивать начиная с перезамера.

Перезамер после фиксов против ЭТОГО прогона. NEG: сценарий ov_p0_payment дословно + ov_value_unpk T3 + «подумаем».
