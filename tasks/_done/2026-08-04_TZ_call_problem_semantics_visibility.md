> DONE 2026-08-04 05:28 | ветка main | codex

> TAKE 2026-08-04 04:58 | ветка main | codex

Ветка: main
Зоны: scripts/export_daily_mango_calls_resolve.py, scripts/publish_daily_mango_calls_google.py, tests/test_export_daily_mango_calls_resolve.py, tests/test_publish_daily_mango_calls_google.py, docs/MANGO_CALLS_TWO_PROCESSES_RUNBOOK.md, docs/worktrees_registry.md, tasks/
Тест-команда: PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m pytest -q tests/test_export_daily_mango_calls_resolve.py tests/test_publish_daily_mango_calls_google.py tests/test_dialogue_format.py tests/test_resolve.py
Семантический-аудит: да

# ТЗ: полезная выжимка спорного звонка только на листе проблем

## Образ результата и бизнес-польза

РОП видит уже рассчитанные тему, краткое содержание, продукт, возражения и следующий шаг спорного звонка на листе «Проблемы данных», поэтому может понять, что нужно проверить. Такой звонок не попадает на лист «Звонки», не учитывается в оценке менеджера, роли остаются нейтральными, а рядом видны причины недоверия.

## Доказательство отсутствия дубля

Полезное ядро старой ветки `codex/call-dialogue-timing-20260728` уже поглощено коммитом `1863b413`. Нового экспортёра и новой схемы не создавать. Исправить существующий `normalize_row` и существующий сквозной тест.

## Минимальная правка

1. Не обнулять `base` только из-за неподтверждённых ролей или хронологии.
   Для такого звонка показывать только тип, краткое содержание, продукт, возражения и следующий шаг; остальные смысловые поля не раскрывать.
2. Не менять вычисление `manager_ready`, нейтрализацию ролей, причины проверки и фильтр листа «Звонки».
3. В каждой проблемной строке до смысловых полей показать причину проверки и статус «гипотезы, не поручение и не KPI».
4. В сквозном тесте доказать, что разрешённые смысловые поля видны на листе «Проблемы данных», а звонок отсутствует на листе «Звонки» и содержит явное предупреждение о непроверенных ролях.
5. Обновить реестр worktree: донор обработан, прямое слияние отклонено, полезное поведение поглощено выборочно.
6. Поднять контракт экспортёр → Google-публикатор до одной схемы `v4` и закрепить равенство тестом.

## Приёмка

- целевой тестовый набор зелёный;
- отрицательный контроль существующего теста краснеет при возврате обнуления `base`;
- `manager_ready_rows=0`, лист «Звонки» пуст, спорный звонок есть на листе «Проблемы данных»;
- роли в тексте нейтрализованы, предупреждение и причины проверки сохранены;
- автоматическая выжимка явно описана как неподтверждённая подсказка, а не факт для KPI;
- независимые архитектор, ломатель и бизнес-аудитор не находят блокирующих дефектов.

## Ограничения

Не запускать ASR, Resolve+Analyze и live/write. Не менять базы, runtime и внешние системы. Не ослаблять строгий допуск `manager_ready`.

## СТОП

Остановиться без изменения кода, если для показа смысловых полей требуется ослабить `manager_ready`, вернуть приблизительные роли либо писать в runtime/реальные базы.

## Бритва

Ожидаемый production diff: удалить одну строку. Новых файлов рабочего кода, флагов, зависимостей и абстракций: 0.
