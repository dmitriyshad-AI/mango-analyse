# Промт MAIN — фикс ожидания high_risk.veto_category в 4b-тесте. 2026-05-30.

> Скопировать в окно MAIN Кодекса. Правка В ТЕСТЕ, код не трогать.

---

Claude #1 прогнал правку 5 (`48505700`) в песочнике. **Правка 5 корректна:**
- faithfulness-тесты (wrong_scope / contradicted / негативный контроль) — зелёные;
- промпт усилен правильно; smoke 434 passed.

**Одно падение** — `test_pravka4b_default_autonomy_flip_is_flagged_and_bounded` (~3234):
`assert high_risk.veto_category == "high_risk"`, реально `''`.

**Диагноз (проверил): это НЕ дыра и НЕ перебор строгости.**
- `assert high_risk.route == "manager_only"` (строкой выше) ПРОШЁЛ — P0-кейс «верните деньги»
  по-прежнему уходит к менеджеру, безопасность цела.
- Правка 5 (строже faithfulness) забраковала черновик чуть раньше, резолюция ушла в ранний high-risk
  гвард, где `veto_category` не проставляется (его ставит только `decide_route` на своём слое).
  high_risk может резолвиться разными слоями — поэтому жёсткое ожидание именно метки хрупко.

**Фикс — в ТЕСТЕ (код decide_route / faithfulness / маршрут НЕ трогать):**
для `high_risk` проверять `route == "manager_only"` + наличие защитного флага
(`"high_risk_manager_only" in high_risk.safety_flags` или `autonomy_blocked_high_risk`), а строгий
`veto_category == "high_risk"` либо убрать, либо заменить на «метка пустая ИЛИ 'high_risk'». Суть щита —
что high_risk уходит к менеджеру, а не какой слой это сделал. Остальные категории
(`force_manager_only`, `unknown_brand` через decide_route) — оставить как есть, они проходят.

Тесты не гонять (лимит) — Claude #1 перепрогонит весь набор. После фикса правка 5 закрыта → новый
прогон на M1 (правка 5 меняет поведение: проверить, что выдумки клиенту упали И автономия не рухнула).