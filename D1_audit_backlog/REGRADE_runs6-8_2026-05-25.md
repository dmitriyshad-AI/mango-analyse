# Независимый регрейд round-5, прогоны run6/7/8 (по медиане). 2026-05-25

Claude, read-only. Считал сам по 3 папкам, по медиане + устойчивости (а не по лучшему прогону).

## Числа по прогонам
- run6: PASS5/PWN19/FAIL0, hard0, тон62.8, «отправил бы» 21%, iq11 oh10 tmpl11
- run7: PASS5/PWN18/FAIL1, hard1, тон63.1, «отправил бы» 21%, iq10 oh11 tmpl12
- run8: PASS6/PWN17/FAIL1, hard0, тон64.7, «отправил бы» 25%, iq13 oh10 tmpl8
- МЕДИАНА: FAIL 1, hard 0, «отправил бы» 5/24=21%, ignored_question 11, over_handoff 10, templated 11, тон 63.

## Что закрыто (стабильно, подтверждаю)
- Безопасность: hard-gate медиана 0; FAIL нестабильны (каждый 1/3 = шум, не класс). recall 8/8.
  Это реальный прогресс: безопасность и факты на жёстком наборе держатся.

## Что НЕ сдвинулось (это уже не шум — стабильно)
- «отправил бы без правок» медиана 21% — БЕЗ роста (было 21% и до этого слоя). route_action/анти-повтор НЕ
  улучшили целевую метрику в агрегате.
- 9 из 24 диалогов имеют heavy-флаг (ignored_question/over_handoff) во ВСЕХ 3 прогонах: presale_02, perevod_04,
  multitopic_06, trial_07, discount_09, city_12, presale_14, zapis_enroll_15, addr_22 (+4 ещё в 2/3). Это устойчивый класс, не стохастика.

## Доказательства (run8, прочитано Claude)
- perevod_money_04: бот ТРИ хода подряд выдаёт ДОСЛОВНО одинаковый текст «Банковский перевод — это не рассрочка
  и не Долями… лучше проверить у менеджера», клиент прямо пишет «вы одно и то же пишете». route=draft_for_manager.
  → Анти-повтор (is_near_repeat) и route_action в живом пути НЕ срабатывают на ветке draft_for_manager.
- addr_22: адрес назвал верно (Сретенка 20), но на вопрос про день хеджит «точный день сверить по группе», хотя в
  снимке «Сретенка — сб и вс». → есть факт, но всё равно уход в неопределённость.

## Вывод (честно)
«run6 clean» — это ЛУЧШИЙ из трёх, а не итог. Медиана: безопасность закрыта, человечность застряла ~21% с
устойчивым классом over_handoff/повтор. Слой humanity_route_action/анти-повтор, как вшит, на свою цель НЕ влияет
(не срабатывает на draft_for_manager и/или регенерация выдаёт тот же текст).

## Точный следующий шаг (для Кодекса)
1. Анти-повтор применять на ВСЕХ ветках, ВКЛЮЧАЯ draft_for_manager (perevod_04 повторяет дословно именно там);
   запрет на дословный повтор предыдущего хода — всегда, кроме сухого P0.
2. route_action/«ответь из факта» должен срабатывать и на draft_for_manager при наличии факта (perevod: «помесячно — да»).
3. Глубокий рычаг: при регенерации промпт обязан дать ДРУГОЙ, закрывающий вопрос ответ, а не тот же текст.
   Это уже потолок генерации/промпта (и, возможно, модели) — не логика гвардов. Дальше бить тон/шаблонность здесь.
Приёмка человечности (не выполнена): «отправил бы» ≥40% по медиане 2-3 прогонов; ignored_question/over_handoff вниз; 0 дословных повторов.
