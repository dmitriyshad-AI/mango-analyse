# ТЗ-19 (D4): бандл хвоста для М1 + малый батч blacklist + подготовка вливания

Дата: 2026-06-12. Постановка: Claude (архитектор трека). Номера 17/18 заняты задачами главного трека — не путать.
Контекст: ТЗ-16 смержено (b309a951), срез v7 в каноне. Блок «бандл для М1» из промта мержа НЕ выполнен — он переносится сюда Блоком А. Исправленная анти-«автоответчик» подсказка v7 теперь в main.

## Блок А. Бандл «хвост 3 439» для М1 → Яндекс

1. Селектор: критерии квадранта `old_lt_2025_06_01_ge_60` без blacklist из `rerun_tail_report.json` ТЗ-16 — ровно 3 439 call_id; иное число = стоп и доклад.
2. Состав (по образцу analyze_rerun_20260611): манифест (call_id, длительность, символы); транскрипты `data/`; подсказка v7 с анти-«автоответчик» правкой — версия и sha256 в манифест; конфиг: gpt-5.4-mini, профиль full, `--parallel 4`; `blacklist_77.txt`; README_RUN (команда, ожидание ~4,7 ч).
3. Разместить `OpenClaw/Actual Mango Tests/analyze_tail_20260612/`; задача в `tasks/_inbox_m1` в принятом формате шины (`<имя>.task.yaml` + маркер `.ready`, как у существующих задач). В задаче только прогон + результаты JSONL; вливание НЕ включать.
4. NEG: пересечение манифеста с blacklist = 0; повторная сборка даёт тот же манифест; транскрипты не в git.

## Блок Б. Малый батч blacklist (LLM разрешены ТОЛЬКО здесь: 15 вызовов)

1. 15 звонков из blacklist_77, НЕ входивших в микропробу ТЗ-16 (12617, 14115, 14327, 15112, 16146 исключить) — выбрать детерминированно (например, наименьшие call_id), список в отчёт.
2. Локальный прогон на исправленной подсказке (та же, что в бандле, сверка по sha256). ПОЛНЫЕ выжимки сохранить для смыслового ревью Claude: `product_data/customer_profiles/tz19_blacklist_batch15/` (ignored, ПДн не в git).
3. В отчёт: 15 строк — call_id, длительность, call_type до (старая выжимка) / после, target_product_present до/после; `llm_calls_total=15`.
4. Полный перепрогон 77 НЕ запускать — после смыслового ревью батча и «да» Дмитрия.
5. NEG: 3 контрольных настоящих автоответчика из ТЗ-16 — повторить детерминированную проверку, остаются `non_conversation` (без LLM).

## Блок В. Скрипт вливания хвоста (КОД, БЕЗ запуска)

1. Скрипт вливания результатов `analyze_tail_20260612` в каноническую базу звонков — по образцу v7-импорта (идемпотентность по call_id+версии подсказки, бэкап базы до вливания, blacklist не перезаписывается, счётчики до/после).
2. Тесты ТОЛЬКО на синтетике. Реальное вливание ЗАПРЕЩЕНО этим ТЗ — отдельная команда после регрейда прогона М1.
3. NEG: повторное вливание тех же результатов = 0 изменений; запись вне вайтлиста полей выжимки невозможна; blacklist-строки не тронуты.

## Границы

Запись в AMO/Tallanto/CRM запрещена; этап Б карточек не начинать; настройки М1 не менять; фиче-ветка от main, каждый блок отдельным коммитом с тестами; полный pytest перед отчётом; `git status` чистый.

## Приёмка

Отчёт в `tasks/_done/`: путь бандла + задача шины (имя файла), число звонков/символов манифеста, sha256 подсказки; таблица 15 звонков батча; путь к выжимкам для ревью; коммиты и NEG всех блоков; `llm_calls_total` по ролям (= 15).
