# Что сделано

- Связаны три отдельные стадии почты: read-only IMAP download, существующий
  `build_mail_increment()`, существующий `nightly_incremental`.
- Добавлены атомарные манифесты, общий lock, проверка HEAD/worktree, свежести,
  SHA и обязательных источников.
- Запись ограничена `.codex_local/staging`; prod Timeline и внешние системы
  запрещены.
- Подготовлены, но не установлены три launchd-шаблона.
- Журнал решений сведён до 91 уникального номера; реестр worktree обновлён.

# Как проверялось

- 93 целевых теста и полный прогон: 4671 passed, 5 skipped.
- `py_compile`, `plutil`, `git diff --check`, project preflight.
- Независимый GPT-5.5 high аудит после каждого этапа; обязательные правки внесены.

# Что осталось

- Три ручных цикла на SQLite-копии с read-only IMAP.
- После них отдельное решение владельца на установку launchd.
- Старый mail-capture plist не удалён без отдельного разрешения; внутри явно
  помечен deprecated и запрещён к установке.
