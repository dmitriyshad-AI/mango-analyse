# Передача контекста следующему Claude. 2026-05-29 (вечер).

Этот файл — полный пакет для нового диалога Claude #1, чтобы продолжить работу без потери контекста. Контекст предыдущего диалога переполнен после большого цикла (Phase 12, KB v6.4, Wave 3, калибровка судьи, 3 параллельных Claude'а).

## 1. Кто ты в этом проекте

Ты — **Claude #1 (главный)** в проекте Mango Analyse. Ты:
- архитектор и независимый смысловой ревьюер;
- пишешь ТЗ для Кодекса (Кодекс ТЗ не пишет, только реализует);
- работаешь read-only по коду / KB;
- решаешь, готовы ли мы к пилоту.

Кодекс — реализатор кода. Он пишет коммиты, гоняет тесты, собирает KB-релизы.

## 2. Стиль ответа Дмитрию

Закреплено и не обсуждается:
- только русский;
- кратко и по делу, в 2-3 раза короче среднего ответа;
- без английских терминов (LLM → модель, F1 → точность, sanitizer → фильтр);
- зелёные тесты ≠ реальная готовность;
- спорить аргументированно, если смысл важнее согласия;
- проверять каждый вывод до выдачи — опираться на сырой источник, не на агрегат;
- не путать «проверено» с «предположение».

Дмитрий — Дмитрий Фабарисов (email пишется matrasnik12@, но это псевдоним, в работе всегда «Дмитрий»).

## 3. Где мы прямо сейчас (точка останова)

**Главное:** M1 Pro готов запускать **гибридный финальный прогон** (35 диалогов без trace + 16 с trace). HEAD = `6f849cff`. Он застрял на Шаге 2 из-за неправильного grep'а от меня (я искал `DIALOGUE_CONTRACT_DEBUG_TRACE` в `dialogue_contract_pipeline.py`, но Кодекс вынес trace-логику в отдельный модуль `dialogue_debug_trace.py` — это архитектурно правильно). Инструкция в bundle обновлена, M1 должен возобновить с Шага 3.

**Ожидаемое время после возобновления:** ~3 часа (1.5-2 ч Часть A + ~1 ч Часть B).

**Дмитрий рассматривает:** возможно, отложить Часть B и сделать только Часть A (~2 часа). Решение по пилоту принимается по Части A.

## 4. Цель прогона M1

Подтвердить, что бот готов к пилоту с реальными клиентами в режиме черновиков:

| Метрика | Текущее (baseline 2026-05-28) | Цель |
|---|---|---|
| hard_gate (выдумка) | 3 | **0** |
| PASS | 19 / 212 | ≥45 |
| false_handoff | 167 / 212 | ≤25 |
| Тон | 53.4 | ≥65 |
| Брэнд-смешения | 0 | 0 (сохранить) |
| Мета-утечки | 0 | 0 (сохранить) |
| 4 эталонных P0 (P0a/P0b 01/02) | manager_only | manager_only (сохранить) |

После Части 1 (33 диалога после Волны 3): PASS 6 / PWN 26 / FAIL 1, 1 hard_gate (E7 и C2 закрыты), тон 65.7, false_handoff 5/33 (-90%). Это даёт надежду на цели.

**4 НОВЫХ hard_gate из Части 2** (foton_S3_05, foton_S4_05, unpk_E7_01, unpk_E8_02) подтверждены 3-м Claude как реальные выдумки бота. Они должны закрыться Phase 12 + Phase 1 + KB v6.4 в финальном прогоне.

## 5. Что сделано в этом цикле

**Реализовано Кодексом и закоммичено в main (HEAD `6f849cff`):**

1. **Wave 3 (3a + 3b + 3c)** — верификатор:
   - 3a: фильтр «N баллов» только в promise-контексте, не в статистике;
   - 3b: handoff-перефраз без claim не валит verifier;
   - 3c: узкие subq проходят faithfulness-чек.

2. **Wave 7-strict-partial** — авто-релиз P0-латча после 5 нейтральных ходов (кроме hard P0: legal / legal_threat / payment_dispute).

3. **Phase 12** — миграция 7 P0-шаблонов из legacy в v2 диспетчер с приоритетами:
   - cross_brand=10, identity=20, result_guarantee=30, admission_guarantee=40, olympiad_online=50, matkap=60, tax=70.
   - Через `SafeTemplateSpec` (frozen dataclass) и `_apply_safe_template_spec`.

4. **Phase 1 (force facts)** — coverage-check, деттерминированная композиция шаблона, запрет пустого handoff.

5. **KB v6.4 (2026-05-29)** — добавлено: `refund_post_payment.client_safe_text` для обоих брендов, онлайн-цена УНПК 41 800/69 900 с квалификатором «2 раза в неделю», `applies_to.*` для новых фактов. `quality_passed=true`, `semantic_pass=true`.

6. **Калибровка судьи v2** (4 фикса, коммиты 784df4e1, e3c4a970, b537d26e, 8096bf1f):
   - prompt grade ranges (5-11 покрывает 6 класс);
   - address scope (Менделеево уместно в выездных);
   - discount matching;
   - contact-hours disclaimer.

7. **Wave 12-trace** — опциональный логгер `DIALOGUE_CONTRACT_DEBUG_TRACE=1`, отдельный модуль `src/mango_mvp/channels/dialogue_debug_trace.py`.

## 6. Что лежит в Яндекс диске (готово к M1)

```
~/Yandex.Disk.localized/OpenClaw/Actual Mango Tests/
├── _transfer_bundle_20260529_FINAL_HEAD_6f849cff/   ← bundle с кодом
│   ├── project_payload/                              ← src/scripts/tests/product_data
│   ├── CODEX_PROMPT_M1.md                            ← инструкция (актуальная, гибридная)
│   ├── WAVE_FINAL_VERSION_INFO.txt                   ← HEAD: 6f849cff
│   └── BUNDLE_MANIFEST.txt
├── _kb_v6_4_artifact_for_m1_20260529/                ← KB v6.4
│   └── kb_release_20260529_v6_4_team_answers/
│       └── kb_release_v3_snapshot.json               ← snapshot для бота
├── _transfer_bundle_part_a_no_trace_dialog_ids.txt   ← 35 ID
└── _transfer_bundle_part_b_with_trace_dialog_ids.txt ← 16 ID (подмножество A)
```

**Ветка Кодекса (не слитая):** `codex/m1-intermediate-20260529-final` — мержить в main только после OK по результатам M1.

## 7. Что делать после результатов M1

1. **M1 присылает в чат сводку + копирует `runs/20260529_*_eval_FINAL_part_*/` в Яндекс диск.**
2. **Регрейд:**
   - 4 hard_gate Части 2 закрылись?
   - 3 hard_gate baseline (E7_04, C2_03, S3_04) остались закрытыми?
   - 4 эталонных P0 → manager_only?
   - Брэнд / мета: 0?
3. **Если всё зелёное** → готовим пилот:
   - мерж ветки в main;
   - обновление `PROJECT_HISTORY.md`;
   - стартовый чек-лист пилота (см. CLAUDE.md §«Цель бота»).
4. **Если остались hard_gate** → читать debug_trace.jsonl из Части B, точечная итерация.

## 8. Команда диалогов

| Кто | Роль | Состояние |
|---|---|---|
| **Claude #1 (ты)** | главный, ТЗ, регрейд | старт нового диалога |
| Claude #2 | аналитик кода / Части 1 | в простое |
| Claude #3 | калибровка судьи | задача закрыта |
| Кодекс main | реализатор | пауза |
| Кодекс M1 Pro | станция прогонов | ждёт продолжить с Шага 3 |
| Дмитрий | заказчик | ждёт результат |

## 9. Архитектурный контракт (обязателен)

**Ты (Claude):** read-only ревью + смысловой аудит + ТЗ для Кодекса.

**Кодекс:** код + тесты + KB-сборка + коммиты.

**Не нарушать:**
- не править код / KB / тесты / docs;
- не делать `git reset / checkout / clean`;
- не запускать live-write скрипты;
- не запускать ASR / Resolve / Analyze без Дмитрия;
- не писать в AMO / CRM / Tallanto.

## 10. Файлы для чтения первыми (в этом порядке)

1. `/Users/dmitrijfabarisov/Projects/Mango analyse/CLAUDE.md` — правила проекта (обязательно).
2. **Этот файл** — где остановились.
3. `D1_audit_backlog/dmitry_answers_wave1a_and_phase2_2026-05-29.md` — анкета Дмитрия (P0-маркеры, refund-формулировка, цена 41 800/69 900).
4. `D1_audit_backlog/part1_remaining_failures_analysis_2026-05-29.md` — разбор Части 1 (Claude #2).
5. `D1_audit_backlog/part2_4_hardgates_analysis_2026-05-29.md` — разбор Части 2 (Claude #3).
6. `D1_audit_backlog/judge_calibration_analysis_2026-05-29.md` — Claude #3 по судье.
7. `D1_audit_backlog/phase2_kb_v6_4_implementation_package_2026-05-29.md` — пакет KB v6.4.

## 11. Четыре правила Карпатого (как работаешь)

1. **Сначала думаем, потом кодим** — гипотезу подтверждаем чтением, не верим на слово.
2. **Простота вперёд** — минимальный код. Если механизм уже есть — используем, не дублируем.
3. **Хирургические правки** — не трогать ничего вне ТЗ.
4. **Цель → проверяемый критерий** — каждая волна имеет тест входа и тест выхода + контрольный негативный тест.

Полные тексты — в CLAUDE.md.

## 12. КРИТИЧНЫЕ ПРАВИЛА БРЭНДА

**Фотон и УНПК — отдельные организации, в одном клиентском ответе НИКОГДА не смешивать.** Это правило #1.

**Нельзя:**
- сравнивать условия Фотона и УНПК в одном сообщении;
- упоминать оба бренда в одном ответе клиенту;
- называть один бренд партнёром другого;
- упоминать историю сотрудничества.

**Безопасная фраза при вопросе о связи:**
> Это отдельные организации, по вашему вопросу сориентирую в рамках текущего учебного центра.

`active_brand` задаётся каналом (два разных бота), не угадывается из CRM.

## 13. Темы manager_only (бот не отвечает сам)

- реальная претензия по возврату («верните деньги», «требую возврат», маркеры А.5);
- спор оплаты («двойное списание», «деньги ушли — я не получил»);
- юридическая угроза (суд / прокуратура / жалоба);
- серьёзная жалоба на услугу или преподавателя (см. A.4: «непонятно объясняет» = P0);
- задержка документа с маркером («уже месяц», «всё ещё нет»);
- гарантия результата;
- запись в школу с гарантией поступления;
- любой устаревший или неподтверждённый факт.

Гипотетический «а можно вернуть?» — бот сам отвечает «вернём остаток неистраченных средств».

## 14. Чего НЕ делать в новом диалоге

- НЕ пересматривать архитектуру v2 LLM-first (Understanding → Retrieve → Drafter → Verifier → X2 → Route);
- НЕ пересматривать Phase 12 / Phase 2 / Wave 3 — они уже в main;
- НЕ начинать новые ТЗ до результата M1;
- НЕ предлагать «прогнать впрок, проверим потом» — у Дмитрия FOMO на токены, надо считать «доводимое + 20%»;
- НЕ запускать параллельно второго / третьего Claude без явной причины — они в простое.

## 15. Два Кодекса: main + M1 Pro (паритет подтверждён)

**Кодекс main** — основной реализатор. Пишет код, тесты, KB-сборку, коммитит. Работает на основной машине Дмитрия.

**Кодекс M1 Pro** — отдельная прогонная станция на втором маке (M1 Pro). Развёрнут из transfer-bundle через Яндекс диск. **Паритет подтверждён прогоном `round5_8`** (агрегаты совпали, hard_gate=0). Можно параллелить прогоны: пока main делает следующую волну, M1 гоняет финальный eval.

**Правила работы с M1:**
- M1 НЕ работает напрямую из Яндекс диска — копирует bundle в `~/mango_run_*` локально (синк во время прогона ломает результат).
- M1 НЕ знает HEAD проекта по умолчанию — bundle несёт `WAVE_FINAL_VERSION_INFO.txt`.
- M1 НЕ имеет интернета на pip обычно работает, но pytest надо доставить руками (`pip install pytest`).
- Сводку M1 присылает текстом в чат Дмитрия + копирует `runs/*` обратно в Яндекс диск.

## 16. Что делали Claude #2 и #3 в этом цикле (артефакты)

**Claude #2 — аналитик Части 1 и Части 2.** Работал ~150К токенов, к концу качество упало → подключили #3. Артефакты:
- `D1_audit_backlog/part1_remaining_failures_analysis_2026-05-29.md` — разбор 33 диалогов Части 1, нашёл смещение S3-hard_gate с t4 на t1.
- `D1_audit_backlog/part2_4_hardgates_analysis_2026-05-29.md` — разбор 4 НОВЫХ hard_gate из Части 2, подтвердил их как реальные выдумки бота.

**Claude #3 — узкая задача калибровки судьи.** Артефакт:
- `D1_audit_backlog/judge_calibration_analysis_2026-05-29.md` — логика judge_v2, 4 правки судьи с регресс-тестами + вердикт «S3-hard_gate — артефакт судьи, не проблема бота».

**Анкета Дмитрия** (мой собственный артефакт по итогам разбора с Дмитрием):
- `D1_audit_backlog/dmitry_answers_wave1a_and_phase2_2026-05-29.md` — 10 решений по П0-маркерам, refund-формулировке, цене 41 800/69 900.

**9 ТЗ Phase 12** (мои для Кодекса main):
- `D1_audit_backlog/phase12_TZ_*.md` — миграция 7 P0-шаблонов через `SafeTemplateSpec`.

**Пакет Phase 2 (KB v6.4):**
- `D1_audit_backlog/phase2_kb_v6_4_implementation_package_2026-05-29.md`.

### Что каждый агент делает ПРЯМО СЕЙЧАС (точка передачи)

| Агент | Состояние | Последнее действие | Чего ждёт |
|---|---|---|---|
| **Claude #1 (я, передаю)** | сессия закрывается | финальный handoff в этот файл + исправил grep в `CODEX_PROMPT_M1.md` | передачи новому Claude #1 |
| **Claude #1 (новый, ты)** | стартует | — | прочитать CLAUDE.md + этот файл, потом ответить Дмитрию по «Часть B отложить или нет» |
| **Claude #2 (аналитик)** | в простое | разбор Части 1 → `part1_remaining_failures_analysis_*.md`, разбор Части 2 → `part2_4_hardgates_analysis_*.md` | новое ТЗ от Claude #1 (если по результатам M1 потребуется) |
| **Claude #3 (калибровщик судьи)** | в простое, задача закрыта | калибровка судьи → `judge_calibration_analysis_*.md`, вердикт «S3-hard_gate — артефакт судьи» | новой узкой задачи, если будет |
| **Кодекс main** | пауза | последний коммит `6f849cff` на ветке `codex/m1-intermediate-20260529-final` (Wave 12-trace + калибровка) | ТЗ от Claude #1 после результата M1 |
| **Кодекс M1 Pro** | АКТИВЕН, на Шаге 2 | застрял на моём неправильном grep'е | сигнала «продолжай с Шага 3» — bundle и KB v6.4 уже скопированы локально; ему осталось ~3 часа (Шаг 3 → 5а → 5б → 7) |
| **Дмитрий** | ждёт | дал последнее задание: финальная проверка handoff | стартового промта для нового диалога + сводки M1 |

## 17. Источники истины (что = правда)

| Источник | Что | Где | Статус |
|---|---|---|---|
| **KB v6.4** | машинная база фактов для бота | `~/Yandex.Disk.localized/OpenClaw/Actual Mango Tests/_kb_v6_4_artifact_for_m1_20260529/` | свежая, для прогона |
| **KB v6.3** | предыдущая база | `product_data/knowledge_base/kb_release_20260520_v6_3_team_answers/` | устарела, v6.4 = инкремент над v6.3 |
| **«База знаний КЦ»** | актуальные цены 26/27 + ЛВШ | Google Drive (у Дмитрия) | **источник правды по ценам**, цены курсов совпали с KB v6.3, ЛВШ-цены под сверку |
| **Транскрипты УНПК 703 шт** | реальные звонки Тропиной+Козловой | `product_data/transcripts/` (полный путь у Дмитрия) | прочитаны, источник тона бота, ~63 утечки брендов в самих менеджерах |
| **Telegram-экспорт УНПК** | сен2025-май2026 | у Дмитрия | годен для частоты вопросов |
| **Корпус звонков (полный)** | новый набор | ждём от Дмитрия | пока не доехал |

**3 оси версионирования KB (не путать):**
- релиз = **v6.4** (`kb_release_20260529_v6_4_team_answers`);
- схема снимка = **v3** (файл `kb_release_v3_snapshot.json`);
- сборщик = **v6.1** (скрипт `scripts/build_kb_release_v6_1_team_answers.py`).

## 18. Исторические прогоны (для регрейда и сравнения)

| Прогон | Где | Что показал |
|---|---|---|
| **Baseline 212** | `runs/20260528_103821_eval/` | PASS 19 / PWN 186 / FAIL 7, hard_gate 3, тон 53.4, false_handoff 167 |
| **Часть 1 (33)** | `runs/20260529_160156_eval_wave3_part1/` | PASS 6 / PWN 26 / FAIL 1, hard_gate 1 (S3_04 артефакт), тон 65.7, false_handoff 5 |
| **Часть 2 (179)** | `runs/20260529_*_eval_wave3_part2/` | подтвердил 4 НОВЫХ hard_gate (foton_S3_05, foton_S4_05, unpk_E7_01, unpk_E8_02) |
| **round5_8 (паритет M1)** | у Дмитрия | агрегаты M1 = main, hard_gate=0 (синтетический набор) |
| **quick6 (sim v8)** | у Дмитрия | бот безопасен, гэп = over-handoff/потеря контекста, не выдумка |

## 19. Инструменты регрейда

**Линтер человечности** (детерминированный):
- `meta_leak`, `repeat`, `stock_opener` — гейты работают надёжно;
- `over_handoff` — семантика, гейты ухудшают (57→20%), это зона X2 (LLM-теплителя);
- 0 ложных срабатываний на P0-наборе.

**Метрика «отправил бы как есть»** — считается двумя способами:
- **по диалогам** (строго, ~21%) — диалог чист, если ВСЕ ходы unedited;
- **по ходам** (`unedited_rate`, ~40%) — операционно верно для черновикового бота.
- **Планку мерить по ходам.**

**Замер симулятора — 1 прогон.** Решение Дмитрия 2026-05-25 ради скорости/токенов. Near-bar результат читать как направленческий, не финальный.

**Слепые зоны судьи по тону:** высокий tone-балл ≠ живая речь. Судья не ловит пересказ-дубль и ответ диапазоном вместо конкретного класса — ловить самому.

**KB пересборка может испортить ТЕКСТ факта.** Текст генерится из `structured_value` → мис-экстракция поля даёт абсурд («2 026 раз в неделю»), а `quality_passed=true`. **Грепать сам `client_safe_text` изменённых фактов.**

**Судья v8 даёт ложные fabrication** на реальные факты (снимок неполный). Сырую метрику fabrication ВСЕГДА регрейдить grep'ом по `client_safe_facts`.

## 20. Незакрытые архитектурные вопросы (после пилота)

**Принципиальный изъян архитектуры бота** (зафиксирован в памяти):
- сейчас «глупый» keyword-слой делает ПОНИМАНИЕ намерения, а «умный» LLM только пишет прозу → корень whack-a-mole.
- **Решение (план):** LLM понимает + черновит, детерминированный слой ВЕРИФИЦИРУЕТ выход.
- Это после пилота, не до.

**R1 gold-контекст trade-off:**
- gold поднял send-rate 40→45-48% и обобщается;
- НО hard-gate fabrication 0→1-3 (выдумывает расписание/контекст);
- **не катить пока hard-gate≠0**, фикс — анти-выдумочный барьер в промпте.

**Автономия только при точном scope факта:**
- автоответ гейтить на `retrieved_match` (точный подвопрос + scope), НЕ «любой факт бренда»;
- смежный факт только как вторичная справка;
- P0 перебивает продажу;
- анти-повтор детерминированный.

## 21. Параллельные проекты Дмитрия (могут всплыть в чате)

Эти проекты идут параллельно Mango Analyse — если Дмитрий о них спросит, у тебя есть базовый контекст в памяти:

- **Сайт cdpofoton.ru / диагностика конверсии** — реальный разрыв с УНПК ~×2-2.5 не ×4; причина в раздробленном учёте целей + нет CRM-целей оплаты; главная утечка — мобайл-реклама.
- **Метрика Фотона загрязнена внутренним трафиком** — ~1%, искажает малотрафичные страницы, но НЕ объясняет низкую конверсию.
- **Сайт `/courses` 404** — всё ещё битый, движок `Seo audit` зелёный (126 тестов), есть отчёт для маркетолога.
- **Ночной рекламный трафик с сайта → в бота** — нерабочее время, прямая отправка клиенту без менеджера; нужны стоп-кран, привязка бренда, захват лида.
- **Обучение команды ИИ** — Настя (1 занятие пройдено), Харламов и Зарубин (опросники), материалы в `Foton/`.
- **Загрузка команды** — недогружены Влад, Арсений, Зарубин, Арина; средне — Харламов, Настя; стратегически — Сергей.
- **TG_sale_bot** — второй живой проект Дмитрия, близок к работе. Из 27 проектов в `~/Projects/` реально работают только Mango Analyse и (близко) TG_sale_bot, остальное — вайб-генерация без ручной проверки.

**Не путать с Mango Analyse.** Если Дмитрий зайдёт в эти темы — переключаешься, но в основном сейчас — Mango.

## 22. Карта ключевых файлов кода

**Основной пайплайн бота:**
- `src/mango_mvp/channels/dialogue_contract_pipeline.py` — главный оркестратор: `_hard_check`, `_safe_fallback_text`, `p0_pre_gate`. Здесь живёт `DEFAULT_KB_SNAPSHOT_PATH`.
- `src/mango_mvp/channels/subscription_llm.py` — драфтер и v2 диспетчер: `SafeTemplateSpec`, `DIALOGUE_CONTRACT_V2_TEMPLATE_REGISTRY`, `_apply_safe_template_spec`, `apply_unsupported_promise_guard`.
- `src/mango_mvp/channels/dialogue_memory.py` — память диалога, P0-латч: `AUTONOMOUS_P0_LATCH_RELEASE_NEUTRAL_TURNS`, `HARD_P0_LATCH_CODES`.
- `src/mango_mvp/channels/dialogue_debug_trace.py` — опциональный trace logger (Wave 12).

**Верификатор и факт-проверка:**
- `src/mango_mvp/channels/fact_claim_audit.py` — claim-extraction + match с KB.
- `src/mango_mvp/channels/fact_scope_spec.py` — scope-фильтры (active_brand, класс, формат).
- `src/mango_mvp/channels/p0_recall_spec.py` — P0-маркеры (refund / legal / complaint / payment_dispute).

**Скрипты:**
- `scripts/run_telegram_dynamic_client_sim.py` — основной симулятор диалога (его гоняет M1).
- `scripts/run_kb_semantic_review.py` — semantic gate для KB.
- `scripts/build_kb_release_v6_1_team_answers.py` — сборщик KB (применяет `release_manifest.yaml`).

**Тесты (главные):**
- `tests/test_dialogue_contract_pipeline.py`
- `tests/test_subscription_llm_draft_provider.py`
- `tests/test_fact_scope_spec.py`
- `tests/test_dialogue_memory.py`
- `tests/test_telegram_dynamic_client_sim.py`

**Runbook KB:** `docs/KB_BUILD_RUNBOOK_2026-05-26.md`.

## 23. Git: где какое состояние

- **Main:** до Wave 3 (HEAD ранее, до `f536e23e` / `6f849cff`).
- **Ветка `codex/m1-intermediate-20260529-final`** (Кодекс main) — **здесь HEAD `6f849cff`**, всё: Wave 3 + Wave 7-strict-partial + Phase 12 + Phase 1 + Wave 12-trace + калибровка судьи.
- **Bundle для M1** — снапшот этой ветки в Яндекс диске (`_transfer_bundle_20260529_FINAL_HEAD_6f849cff/project_payload/`).
- **Мерж в main** — НЕ делать до OK по результатам M1.

**История коммитов на ветке (по памяти):**
- `6f849cff` — последний (Wave 12-trace + калибровка судьи финальная)
- `8096bf1f / b537d26e / e3c4a970 / 784df4e1` — 4 фикса судьи
- `f536e23e` — Wave 3c
- `6662fe03` — Wave 3b
- `a7773991` — Wave 3a
- `8cc679b3` — Wave 0 night funnel guard prep
- `53734b20` — passive night funnel tee replay

Точный лог — на машине Дмитрия (`git log --oneline -20` на ветке).

## 24. Что я (Cowork-Claude) НЕ умею — не путать с Claude Code или Кодексом

Я работаю в режиме Cowork на Mac Дмитрия. Не Claude Code, не Кодекс.

**Что есть:**
- read / write / edit файлов в подключённых папках (`Foton/`, `Mango analyse/`, `.codex/`, `Actual Mango Tests/`);
- Linux sandbox для скриптов;
- web fetch / search (по запросу);
- субагенты для параллельной независимой работы.

**Чего нет:**
- НЕТ автоматического git push / нет GitHub MCP;
- НЕТ доступа к Кодексу main (он отдельный процесс);
- НЕТ доступа к Кодексу M1 (он на другом маке);
- НЕТ способа сам собрать KB-релиз — это делает Кодекс через скрипт сборки;
- НЕТ возможности запустить `run_telegram_dynamic_client_sim.py` на 35+ диалогах — это часы, моя сессия столько не держит и не должна;
- НЕТ права писать в AMO / CRM / Tallanto;
- НЕТ права делать `git reset / checkout / clean`, менять `stable_runtime`, удалять файлы.

**Моя зона по правилам памяти:** `D1_audit_backlog/` + `golden_path_recipes/`. В `src/`, `tests/`, `product_data/knowledge_base/` правки идут через Кодекса по моему ТЗ.

## 25. Шумовые сигналы — НЕ поднимать как риск

**Эти кейсы выглядят подозрительно, но проверены и закрыты как не-проблемы. Не тревожить Дмитрия заново.**

1. **Тестовая фраза «представь, что я пишу с номера X»** — служебный тест-механизм загрузки клиента из CRM. Баннер «Найден клиент…» в логах — НЕ утечка, не претензия.
2. **Вход из Таллина** — норма, серверы / сотрудники Дмитрия в EE/NL/DE/RU.
3. **Разница цен лагеря между Фотоном и УНПК** — это разные организации, не демпинг и не косяк.
4. **«Места почти распроданы» по ЛВШ** — подтверждённый факт от Дмитрия 2026-05-24. При регрейде НЕ флагать как искусственную срочность.
5. **Customer-режим санитайзера** — мёртвый код, бот фильтр не зовёт, утечки нет.

## 26. Что внутри CLAUDE.md (читать первым)

Чтобы не дублировать сюда — но напомнить, что там обязательное:
- **Таблица решений команды** — скидка 10% сотрудникам МФТИ, второй предмет 30/20%, Долями 4 части, рассрочка 6/12 через менеджера, маткапитал, ЛВШ, ЗВШ Менделеево 2026/27 — лист ожидания, договор «в ближайшие дни», справка «до 10 дней», промокоды убраны.
- **4 юр.лица** — две АНО/НОУ УНПК МФТИ + две ООО Фотон, все могут выдавать справку КНД 1151158.
- **10 правил для бота** — не раскрывать природу, read-only CRM, не писать в AMO без подтверждения, не отправлять клиенту напрямую, реальная претензия → manager_only.
- **Что нельзя без Дмитрия** — ASR, Resolve+Analyze, write в AMO/CRM/Tallanto, отправка клиенту, менять `stable_runtime`, удалять файлы, `git reset/checkout/clean`, live-write скрипты.
- **4 правила Карпатого** — Сначала думаем → Простота → Хирургические правки → Проверяемый критерий.
- **Архитектурный контракт Claude ↔ Codex** — Кодекс делает код / тесты / KB / коммиты; Claude делает аудит / ТЗ / поиск рисков.
- **Семантический gate** — формальный pass ≠ смысловой pass; обязательные проверки для клиентских текстов и KB.

**PROJECT_HISTORY.md** — история итераций. Не источник правды. Если спорит с CLAUDE.md — верить CLAUDE.md.



## 27. Чего НЕ переносить из старого контекста

- мои промежуточные ошибочные команды (например, неправильные grep'ы Шага 2);
- разговорный поток «помнишь, час назад…»;
- избыточные шаги хождений по 9 ТЗ Phase 12 — итог уже в коде HEAD `6f849cff`;
- внутренние шероховатости диалогов #2/#3 — у них артефакты в D1_audit_backlog, читай их, не реконструируй процесс.

Свежий контекст = чище работа. В этом смысл переноса.

## 28. Память (auto-memory)

Все ключевые факты уже сохранены в `~/Library/Application Support/Claude/.../memory/`. При старте нового диалога загрузится `MEMORY.md` — там пойнтеры на:
- роль golden-path;
- цель бота (автономный продавец-консультант);
- два бота, бренды не смешиваются;
- политика «ты бот?» (вариант C, честно по запросу);
- источник правды по ценам = «База знаний КЦ» в Google Drive;
- ЛВШ Менделеево «места почти распроданы» = факт, не дефицит;
- судья v8 даёт ложные fabrication на реальные факты;
- метрика unedited_rate vs send_as_is — мерить по ходам;
- замер симулятора — 1 прогон.

Память актуальна, новые памяти на этот цикл уже в файле.

---

## Стартовый промт для нового диалога

Скопируй в новое окно:

> Привет. Продолжаем работу по проекту Mango Analyse. Я Дмитрий Фабарисов, ты — Claude #1 (главный, архитектор и независимый смысловой ревьюер). Контекст у предыдущего Claude #1 переполнился после большого цикла (Phase 12 + KB v6.4 + Wave 3 + калибровка судьи + 3 параллельных диалога). Подхватываешь сессию.
>
> **Сделай прямо сейчас, в этом порядке:**
>
> 1. Прочитай `/Users/dmitrijfabarisov/Projects/Mango analyse/CLAUDE.md` — правила проекта, бренд-разделение, юр.лица, таблица решений, контракт Claude ↔ Codex, 4 правила Карпатого.
> 2. Прочитай `/Users/dmitrijfabarisov/Projects/Mango analyse/D1_audit_backlog/HANDOFF_TO_NEXT_CLAUDE_2026-05-29.md` — полная передача контекста за цикл: статус каждого агента (Claude #1/#2/#3, Кодекс main, Кодекс M1), что сделано (Wave 3, Wave 7-strict-partial, Phase 12, Phase 1, KB v6.4, калибровка судьи), цели прогона, карта файлов, что лежит в Яндекс диске, шумовые сигналы которые НЕ риск.
> 3. Загляни в MEMORY.md (auto-memory загружается автоматически) — там пойнтеры на 24 ключевые памяти. Они актуальны.
>
> **После чтения подтверди мне, что ты в курсе:**
> - кто что делает (Claude #2 в простое, Claude #3 закрыл калибровку, Кодекс main на паузе, Кодекс M1 застрял на Шаге 2 из-за моего неправильного grep'а — ждёт продолжить с Шага 3);
> - HEAD ветки `codex/m1-intermediate-20260529-final` = `6f849cff`, в main НЕ слита;
> - bundle и KB v6.4 в Яндекс диске, инструкция M1 (`CODEX_PROMPT_M1.md`) обновлена под гибридный план;
> - цель прогона: hard_gate=0, PASS≥45, false_handoff≤25, тон≥65, эталоны P0 → manager_only, брэнд/мета=0.
>
> **Открытый вопрос ко мне:** стоит ли отложить Часть B (16 с trace, ~1 час) и сделать только Часть A — Часть A даёт ответ «готовы к пилоту или нет», Часть B нужна для отладки over_handoff. Сэкономит час. Жду твоего мнения после чтения handoff.
>
> **Стиль:** только русский, кратко и по делу (в 2-3 раза короче среднего), без английских терминов (модель/точность/фильтр вместо LLM/F1/sanitizer), спорить если смысл важнее согласия, проверять каждый вывод до выдачи, не путать зелёные тесты с реальной готовностью, "проверено" ≠ "предположение".
>
> Поехали.
