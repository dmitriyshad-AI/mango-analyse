# Стартовый промт — Кодекс M1 Pro (правка 4: Router + умолчание-вето). 2026-05-30.

> Скопировать в окно Кодекса M1 Pro.

---

Ты — Кодекс на M1 Pro. Bundle: `~/Yandex.Disk.localized/OpenClaw/Actual Mango Tests/_transfer_bundle_20260530_HEAD_fc8892ab/`.
HEAD `fc8892ab` (волна 1 + правка 2, 430 тестов зелёные по прогону Claude #1).

**Шаг 0 — скопировать локально** (не работать из Яндекс диска):
```
mkdir -p ~/mango_run_pravka4 && cp -R "<bundle>/project_payload/." ~/mango_run_pravka4/project/
cd ~/mango_run_pravka4/project
grep -n "_fact_match_anchors" src/mango_mvp/channels/subscription_llm.py | head   # проверка свежести
```

**Прочитай:** `D1_audit_backlog/TZ_pravka4_router_2026-05-30.md` (в payload) — это ТЗ, в нём всё.

**Суть:** свести ~41 точку решения маршрута в одну `decide_route` и перевернуть умолчание: бот
отвечает сам, к менеджеру — только при конкретной опасности (сейчас наоборот). Бот отвечает клиенту
напрямую → ошибка переворота уйдёт живому клиенту, поэтому:

- **Часть A первой:** инвентаризировать ЧТЕНИЕМ все точки ухода (правило #1), собрать полный
  вето-набор (16 категорий в ТЗ), на КАЖДУЮ — негативный тест. Без зелёного щита Часть B не начинать.
- **Часть B:** единый `decide_route`; умолчание = `bot_answer_self`, только если НИ одно вето не
  сработало И есть покрывающий факт И автономия по теме разрешена.

**Не трогать:** KB, верификатор, тёплый слой, матрицу тем, флаг автономии, P0-спеки, покрытие волны 1,
unstated правки 2.

**Тесты:** прогнать щит + smoke, если лимит позволяет; иначе Claude #1 прогонит. В main не мержить до
зелёного. Если в один заход не влезает — резать 4a (Router 1:1) + 4b (переворот), щит до переворота.

**Отчёт:** где `decide_route`, список категорий-вето, что изменил переворот, тексты негативных тестов.
