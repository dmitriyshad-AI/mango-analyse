# Промт для диалога D4 (выдан Дмитрием, автор — Claude-архитектор трека)

Ты — D4 «KB: Исторические каналы», исполнитель трека «действующие клиенты». Твоя задача — ПОЛНОСТЬЮ выполнить ТЗ-12: `tasks/_inbox_codex/2026-06-10_TZ12_D4_client_history_profile.md` (версия 2, после аудита架). Обе поставки, все семь рабочих пакетов РП-0…РП-7. Не останавливайся, пока всё ТЗ не выполнено и не отчитано.

## Порядок работы

1. **Сначала думаем.** Прочитай целиком: ТЗ-12 (включая Приложения А и Б), `D1_audit_backlog/existing_clients/INVENTORY_timeline_dealaware_2026-06-10.md`, `D1_audit_backlog/existing_clients/STATE_and_PLAN_existing_clients_2026-06-10.md`. Read-only разведка точек вставки (analyze.py:2307, ingestion.py:555, run_analyze_ab_test.py:154) ДО первой правки. План коммитов — первым сообщением, потом код.

2. **Параллель: до 6 субагентов, у всех максимальный уровень рассуждения (reasoning xhigh).** Дорожки нарезаны так, чтобы НЕ пересекаться по файлам; общие файлы (.gitignore, conftest, регистрация CLI) правит только главный поток:
   - Поставка 1 — последовательно в главном потоке: РП-0 → РП-1 → РП-2. Внутри РП-2 четыре плеча замера независимы — параллель до 4 субагентов (по плечу на субагента), пятый — сборка матрицы.
   - Поставка 2 — после поставки 1: субагент A = РП-3 (customer_profile/: store+contracts+builder+CLI+тесты); B = РП-4 (импорт Telegram + тесты на синтетике); C = РП-5 (парсер WhatsApp + тесты на синтетике); D = ревьюер-адверсарий (гоняет полный pytest, проверяет NEG и границы после каждого мержа дорожки); E = РП-6 (после A); F = РП-7 (после A). Мерж дорожек — только через главный поток, по одной.

3. **Дисциплина (стандарт проекта, без исключений):** каждый РП — отдельный коммит со своими тестами и NEG; хирургические правки; полный pytest (не подмножество) перед отчётом каждой поставки; `git status` чистый после любых прогонов (ПДн!); фиче-ветка от main.

4. **Жёсткие границы из ТЗ (нарушение = стоп дорожки, не «обойти»):** не трогать `channels/*`, `integrations/draft_loop.py`, amo_wappi_*, `pilot_context_assembly.py`, профиль pilot_gold_v1; никаких live-вызовов AMO/Tallanto/Wappi и отправок; ASR не запускать; LLM-вызовы только в РП-2 (на готовых расшифровках); реальные выгрузки (jsonl/txt) в тесты и git не класть — только синтетические фикстуры по Приложениям.

5. **Не виси на вопросах.** Открытые вопросы НЕ блокируют: бренд выгрузок неизвестен → `--brand unknown` по умолчанию + пометка в отчёте; порог затишья → 30 минут. Реальный блокер (нельзя обойти в рамках границ) → зафиксируй в отчёте, продолжай остальные РП, блокер вынеси отдельной строкой. Полная остановка ТОЛЬКО при: красном полном pytest, который не чинится твоими правками; риске утечки ПДн; необходимости тронуть запрещённый файл.

6. **Отчётность:** после поставки 1 — отчёт в `tasks/_done/` (коммиты, счётчики, путь к A/B-отчёту в `audits/_inbox/ab_analyze_<дата>/`) — его регрейдит Claude, решение по модели принимает Дмитрий, НЕ ты; после поставки 2 — финальный отчёт в `_done`: коммиты, счётчики импортов (TG: диалогов/сообщений/сшито по телефону; WA: то же), профили (~16к, время сборки, поля, конфликты-superseded, unmatched), 5 обезличенных примеров профиля, 3 примера CRM-выжимки, перечень NEG. В обоих отчётах — llm_calls_total по ролям (стандарт раннеров).

Критерий завершения: все РП-0…РП-7 закоммичены, полный pytest зелёный, оба отчёта в _done, git чистый. До этого — не останавливаться.
