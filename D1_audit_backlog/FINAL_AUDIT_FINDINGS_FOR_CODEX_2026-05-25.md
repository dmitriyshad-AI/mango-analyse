# Финальный сводный аудит (read-only) — находки для Кодекса. 2026-05-25

Claude (Cowork) прошёл полный план из ~80 гипотез (15 кластеров A–O), всё read-only, код/базу не правил.
Доказательства по каждому пункту — в `D1_audit_backlog/logs/cluster_*.md` и `logs/MASTER_FINDINGS.md`.
Каждая находка проверена Claude напрямую (не только субагентом). Формат: проблема → доказательство → решение.

## P1 — критично (до пилота)

### 1. Извлечение фактов: факт-ответ вытесняется из топ-10 (recall=0/8)
Доказ.: кандидаты ранжируются по fact_type (+20 «нужному» типу), факт-ответ ДРУГОГО типа
вытесняется однотипными; «за год 14%» под `payment_options...discount_extra` (course_parameter)
не попал в топ-10 discount-фактов. telegram_pilot_context_builder.py:684,1090,719; кап 10 в draft_prompt_builder.py:378.
Решение: уже в `D1_fact_retrieval_findings/` (reference/fact_retrieval.py — гарантия факта-ответа + алиасы ключей + тема из held). Это текущая задача Кодекса.

### 2. Расхождение KB-артефактов: bot_pack отстал от исправленного источника
Доказ. (Claude): RUN-снимок `kb_release_20260520_v6_3_team_answers` исправлен (Долями без числа частей; лагерь 93100/114000), а
`Mango_Bot_KB_FINAL_v6_3_2026-05-20/01_bot_pack/client_safe_facts_*.jsonl` СТАЛЕ и client-safe:
«Долями — 4 равные части», «Т-Банк до 500 000 ₽/36 мес» (НЕ в подтверждённых — у нас 6/10/12),
лагерь Фотон 75000 / УНПК 83800 / 89900, онлайн УНПК 41800/69900. CORE-снимок (716) ≠ RUN (829).
В RUN — битый client-факт «срок рассрочки 2026-05-22 месяцев» (дата вместо числа).
Решение: зафиксировать ОДИН авторитетный снимок (RUN), пересобрать bot_pack/CORE из него, убрать «Т-Банк 500000/36мес», починить битую дату. РИСК: если деплой грузит bot_pack — клиент слышит запрещённое.

### 3. P0-безопасность: детектор пропускает реальные возвраты/споры (8/11 перифраз)
Доказ. (Claude, codes_from_text): пропуск — чарджбек/«оспорю операцию в банке»; «деньги ушли, а доступа нет»,
«списали, а курс не открыли» (обратный порядок PAYMENT_DISPUTE_RE); «не буду платить, это развод»;
«отдайте оплату обратно»; «аннулировать договор» (ловит только «расторг»); «нарушение МОИХ прав потребителя»
(regex рвётся на «моих»). Ложно: «вернуться к теме цены»→refund.
Смягчение: поверх есть answer_safety_classifier+topic; жёсткие P0 в round-5 устояли. Но это самый опасный класс.
Решение: расширить REFUND_RE/PAYMENT_DISPUTE_RE (чарджбек, оспорить операцию, не буду платить, отдайте оплату, аннулировать договор, двунаправленный порядок списания); починить «наруш…прав» через «моих/наших».

### 4. Over-handoff в коде: «машина отказа»
Доказ. (Claude): apply_unsupported_promise_guard (subscription_llm.py:~1376) форсит manager_only при
message_type ∈ {non_question, context_update, wait_for_more}; force_manager_only (:890) — при «неподтверждённой» цифре;
fresh-fact матчинг хрупкий (:4985/:5036). 18/98 ходов round-5 — manager_only при наличии факта.
Решение: не форсить manager на benign context_update (реплика-уточнение клиента — не повод эскалации);
если факт извлечён (после фикса #1) — отвечать; чинить проброс freshness, чтобы цифра не была «неподтверждённой» зря.

### 5. Ложный формат online из «МТС Линк»
Доказ. (Claude): semantic_roles.py:19 — «мтс/линк/link» в маркерах online; «это очно, но записи на мтс линк» → training_format=online (перебивает «очно»). МТС Линк — общая платформа.
Решение: убрать мтс/линк/link из FORMAT_MARKERS online.

## P2 — важно

### 6. held не держит предмет/город/scope (класс C неполон)
subject (физика→химия) и город (Москва→Долгопрудный) теряются; осей нет в MessageRoles/HeldState. Объясняет провал персоны 20 (город) и часть assumed-subject. Добавить оси subject и place/venue в роли+held.

### 7. Дыры recall ролей: нет оси «дни/расписание» («по выходным/субботам» не поднимают тему; «занятия по субботам»→ложно recording через омоним «заняти»); нет оси предмет/программа; промокод не флагается; дизъюнкция формата гасит и тему format.

### 8. Тон/повторы: P0-ветка обходит антиповтор (дубль ходов 2-5); ~20/98 одинаковых зачинов; 49% ходов с хедж-фразой; служебные префиксы клиенту («Приняли обращение», «Сориентирую по проверенным данным:»). [G1 OK — rewriter не перезаписывает.]

### 9. Числа с пробелом и без не взаимозаменяемы в has_marker («44 600» ≠ «44600»). Нормализовать разрядные пробелы/nbsp в числах.

## P3 — шлифовка
- Опенеры персон 05/22 совпадают с few-shot-триггерами (переформулировать; не утечка экзамена).
- 21 почти-дубликат client_safe фактов (раздувают выдачу точечно).
- Транслит/опечатки P0 не ловятся (низкий риск для кириллицы).
- Сводка менеджеру не показывает valid_until/канал связи.

## [OK] Подтверждено чистым (хорошие новости)
Бренд-разделение во всех клиентских артефактах (шаблоны/few-shot/gold/client_safe) — утечек 0.
Запрещённых фраз про природу бота клиенту нет. Служебных маркеров в client_safe нет.
zero-collect на P0 (бот не собирает ПД). НЕТ write-пути в AMO/Tallanto из бота (двойной гейт + dry-run).
few-shot реально подключён в промпт (живой тон включён). rewriter не перезаписывает точный ответ.
Гигиена RUN-снимка: промокодов/будущих цен/перепутанных процентов/юр.данных нет; valid_until заполнены.
Целостность тестов: покрытие v8 полное, раунды дизъюнктны, round-5 помечен holdout. Нормализация ё/е/регистр ок; ReDoS не воспроизведён.

## Приоритет внедрения
#1 (recall, уже в работе) → #2 (зафиксировать снимок + пересобрать bot_pack — иначе клиенту уйдут «4 части/старые цены») →
#4 (калибровка хендоффа, связана с #1) → #3 (расширить P0-детекторы) → #5 (мтс из формата) → P2 → P3.
