# Стартовый промт — MAIN Кодекс (калибровка 5.1 критика). 2026-05-30.

> Скопировать в окно MAIN. Полуфабрикат — проверить и перенести.

---

Ты — MAIN Кодекс, HEAD `b36a016b`.

**Прочитай:** `D1_audit_backlog/TZ_pravka5_1_calibrate_critic_2026-05-30.md` — там разбор и готовый промпт.

**Контекст (разбор прогона правки 5 по сырью):** правка 5 закрыла ~14/26 выдумок, перебора строгости
НЕТ (контрольные просели по тону, не по faithfulness). Осталось ~10 реальных выдумок 4 типов, которые
критик не ловит (ставит supported, найдя похожий факт).

**Правка:** заменить промпт `build_faithfulness_prompt` (dialogue_contract_pipeline.py ~575) на
версию из ТЗ — добавлены 4 типа:
1. преждевременный выбор формата («онлайн» на «онлайн или очно?» без факта);
2. расписание/дни без факта («в будни», «по вторникам»);
3. смена/лагерь ≠ обычный курс ≠ олимпиада (wrong_scope даже при совпадении предмета/класса);
4. отрицание/полнота («других нет») и выдуманная специфика («фокус на ОГЭ»).

`check_claim_faithfulness` (wrong_scope/contradicted в unsupported) — НЕ трогать, уже есть.

**Тесты:** по одному на каждый из 4 типов + негативный контроль (правильный ответ в теме → supported).

**P0b-жалоба** (2 FAIL p0_mishandled: сбор данных / нерелевантный ответ на жалобе) — это отдельная
правка 5.2 P0-слоя, НЕ критик, приоритет ниже. Сейчас не трогать.

**Ограничения:** тесты не гонять (лимит) — Клод 1 прогонит faithfulness + smoke + перепрогон целевого
45. В main не мержить до зелёного. Правило #1: промпт и место подтвердить чтением. Отчёт: что изменено,
тексты тестов.