# Backward Compatibility

- Existing LLM JSON contract is unchanged.
- Existing topic confidence logic is unchanged.
- Neutral pricing questions are not forced to `manager_only` by the new input guard.
- High-risk input only makes routing more conservative.
