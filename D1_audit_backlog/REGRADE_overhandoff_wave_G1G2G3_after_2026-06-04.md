# Регрейд по сырью — волна over-handoff G1+G2+G3 ПОСЛЕ. Прогон M1 на 1deebc09 (v6.5). 2026-06-04.

Ревьюер: Клод #1. Наборы: travel (levelA_estimate_acceptance), partial (block1_partial_yield). Сравнение с базой ДО (6788a2f5, v6.4).
Снимок ПОСЛЕ = v6.5 (другой KB) → читаю МЕХАНИЗМ (guard/recover/partial/эхо), не дельту вердиктов.

## ВЕРДИКT: G1 — ПОБЕДА; G2 — частично; G3 — НЕ РАБОТАЕТ (несмотря на зелёный тест). Безопасность чиста. Волна НЕ принята — нужна v2.

Безопасность: **выдумок 0** (оба), **бренд не смешан 0/0**, P0 маршрутизирован (travel p0×3, partial p0×6). Регрессий безопасности нет.

## G1 — РАБОТАЕТ (победа)
| сигнал | ДО (v6.4) | ПОСЛЕ (v6.5) |
|---|---:|---:|
| `estimate_guard_failed` | 12 | **7** |
| `estimate_pre_handoff_estimate` | 17 | **19** |
| уход при фактах (travel) | 17 | **12** |

Оценка дороги теперь проходит, а не роняется в хендофф. Это ровно цель G1.
- **НОВЫЙ гап (побочка G1):** hard_gate `estimate_without_marker` (`unpk_advice_when_start` ход4: «лучше начинать в 8 классе… старт с 9 тоже нормально» — совет БЕЗ пометки «ориентировочно/обычно»). Раньше бот это прятал в хендофф, теперь даёт — но без маркера. Нужна правка: при пропуске оценки/совета ВСЕГДА вешать маркер.

## G2 — ЧАСТИЧНО
- Победа: `unpk_course_plus_camp` FAIL→**PWN**, camp-mix hard_gate УШЁЛ; грунтованный курс теперь выдаётся (ход3: «Да, годовой курс по физике 9 класса есть. Группы: …Сретенка…»).
- Осталось: `unpk_multi_three` **FAIL** — грунтованную часть (дни/время/контакты) НЕ выдал, дал нерелевантное; плюс смешение «запись НА занятие» (enrollment) ↔ «записи занятий» (recordings). partial-yield срабатывает нестабильно.

## G3 — НЕ РАБОТАЕТ (ключевая находка регрейда)
Эхо сырого подвопроса в выводе ОСТАЛОСЬ: «менеджер уточнит именно про **<сырой вопрос>**» — **23 раза travel, 6 partial**.
- Корень в коде: шаблон `dialogue_contract_pipeline.py:212` подставляет сырой подвопрос в `{detail}`.
- Кодекс добавил только ДЕТЕКТОР (`has_meta_leak`/`detect_meta_leak`) — он не вырезает и не блокирует фразу. Зелёный тест проверял лишь `detect_meta_leak(...)==true`, а не реальный текст хендоффа. Классика «зелёный тест ≠ работает».

## Узкие гапы — для волны v2 (на той же ветке)
- **G3 (приоритет):** срезать эхо В ИСТОЧНИКЕ — не класть сырой подвопрос в `{detail}`; чистая метка темы ИЛИ общий шаблон («точную информацию уточнит менеджер»). Детектор завязать на БЛОК/замену до вывода. КОНТРОЛЬ: тест на РЕАЛЬНЫЙ текст билдера хендоффа (сырой подвопрос отсутствует), не только `detect_meta_leak`.
- **G1-маркер:** при пропуске оценки/совета через гейт ВСЕГДА вешать «ориентировочно/обычно». КОНТРОЛЬ: `unpk_advice_when_start`-совет несёт маркер, `estimate_without_marker`=0.
- **G2-надёжность:** partial-yield стабильно выдаёт грунтованную под-часть (multi_three: дни/время/контакты); чинить enrollment↔recordings смешение. КОНТРОЛЬ: multi_three выдаёт дни/время.

## Рекомендация
Волна v2 (G3-срез + G1-маркер + G2-надёжность) на ветке `codex/overhandoff-G1G2G3-20260604`, NEG целы (число-гард, P0, бренд — сейчас чисто, не ослабить). Перегон тех же наборов на M1, я регрейжу. G1 уже даёт реальное снижение over-handoff — направление верное.
