# START HERE — сборка полной v2 на КОПИИ проекта. 2026-05-26

Для второго исполнителя (Claude-CLI) на КОПИИ репозитория. Архитектура согласована независимо Claude + Codex; референс закалён аудитом (fail-closed, guard-список).
Ты СОБИРАЕШЬ полную v2 как ПАРАЛЛЕЛЬНЫЙ путь за флагом, вживляя реальные вызовы модели. Продакшен-путь не трогаешь.

## Что читать (в этом порядке)
1. `D1_audit_backlog/ARCHITECTURE_root_flaw_2026-05-26.md` — зачем (диагноз: глупый слой понимал, умный переписывал).
2. `D1_audit_backlog/TZ_redesign_dialogue_contract_2026-05-26.md` — ПОЛНОЕ ТЗ, §1-§13 (контракт-план, факты, верификация, человечность, аудит Кодекса, инварианты).
3. `D1_redesign_reference/README_FOR_CODEX.md` + `reference/*.py` + `tests/test_pipeline.py` — РЕФЕРЕНС логики (13/13 на моке, LLM инъектируется).
4. `CLAUDE.md` проекта — правила брендов, P0, безопасность, «что нельзя без Дмитрия».

## Что построить
Поток (ТЗ §1 + §11/§12): P0 пре-гейт → LLM#1 контракт-план (подвопросы + слоты-с-источником + client_state, называет КЛЮЧИ не значения) →
ретривал значений из бренд-склада → LLM#2 черновик (свободно, стиль из gold/few-shot, только sourced-слоты) →
ЖЁСТКИЙ verifier → СЕМАНТИЧЕСКАЯ верность (fail-closed) → [fail] коррекция/фоллбэк → form-check → X2-тепло (ре-верификация) → финал.

## Жёсткие инварианты (НЕ нарушать)
1. Контракт называет ключи; значения только из склада. 2. Слот без источника не утверждать. 3. P0 детерминированный пре-гейт.
4. Жёсткий verifier + семантическая верность ОДНОВРЕМЕННО со свободой; семантика FAIL-CLOSED (сбой проверки → в менеджера, не PASS).
5. Бренд-скоуп жёсткий. 6. Нет факта → узкий честный хендофф, не сосед. 7. X2 — только форма, выход ре-верифицируется.
8. **(§13.7) В v2 draft_text меняют ТОЛЬКО: safety-коррекция/фоллбэк, X2-тепло (после проверок), sanitize. Всё прочее — finding или route, НЕ переписывание.**
9. Маршрут считает КОД после ретривала (есть факты + нет P0 + нет missing по отвечаемой части), а не LLM-`answerability`.

## Точки интеграции (вживить реальное)
understand_fn→LLM#1 (high reasoning); draft_fn→LLM#2; faithfulness_fn→дешёвый LLM-проход верности (структурные claims + лог «карточки доказательств»);
warmth_fn→X2. fact_store→client_safe_facts_<brand>; catalog→реальные fact_keys; style_examples→gold/few-shot по теме+бренду (углубить до 3-5).
История — НАСТОЯЩАЯ поролевая (клиент/бот по ходам). Verifier — переиспользовать REFUND_RE/бренд-токены(голое «мфти» для foton)/sanitize; `_is_soft_number` — заземлять проценты/части/месяцы.

## Guard-каскад в v2 (ТЗ §13)
ОСТАВИТЬ (безопасность, текст НЕ переписывают): payment_confirmation, brand_separation, input_policy, funnel_policy.
ОСТАВИТЬ как safety-fallback, НО их переписанный текст ОБЯЗАН проходить ре-верификацию v2: unstated_subject, unsupported_promise, unconfirmed_operational_specificity.
**autonomy_matrix — НЕ переносить целиком (правка Кодекса):** оставить ТОЛЬКО проверку разрешения маршрута (route/permission). Любые замены `draft_text` из старой
autonomy-matrix (`_live_status_manager_check_text`, `_promoted_verified_fact_text`) ЗАПРЕЩЕНЫ по §13.7 → должны стать finding/safety-fallback с ре-верификацией.
ОТКЛЮЧИТЬ в v2: conversation_intent_plan_guard, known_context_redundant_question_guard, answer_quality_rewriter, humanity_x2_rewriter (если не переиспользуешь как v2-warmth).
СПОРНО: high_risk_content (оставить блок, убрать шаблон-переписывание), humanity_guards (оставить вычистку служебного, убрать регенерацию/маршрут).

## Под-тумблеры (изоляция при отладке) и флаг
Всё за `TELEGRAM_DIALOGUE_CONTRACT_PIPELINE` (default OFF). Под-тумблеры: enforce_slot_evidence, semantic_faithfulness, form_warmth — чтобы при провале теста изолировать слабый компонент без пересборки.

## Что НЕ делать
Не трогать продакшен-путь и текущую безопасную базу (она едет в пилот). Не давать X2 добавлять смысл. Не оставлять старый переписывающий каскад поверх контракта.
Дорогие симуляторные прогоны — не вслепую: офлайн pytest → СТОП → ревью Claude → один прогон.

## Замер (делает Claude, независимо)
Флаг ON vs OFF на ОДНОМ полном наборе (round-5 + round-6), 1 прогон. Приёмка: fabrication=0, hard-gate=0, бренды чисты; ушёл ли класс мискласса
(perevod/recording/schedule/refund_presale); send-rate (и «готов отправить с мелкой правкой») vs старый путь. Регрейд Claude разнесёт по субагентам.
