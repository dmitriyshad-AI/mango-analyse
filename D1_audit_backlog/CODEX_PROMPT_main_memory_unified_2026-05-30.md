# Стартовый промт — MAIN Кодекс (память диалога, единое ТЗ). 2026-05-30.

> Скопировать в окно MAIN. Реализовать ПОСЛЕ мержа батча 5.1+5.2 (тот же боевой путь).

---

Ты — MAIN Кодекс.

**Прочитай:** `D1_audit_backlog/TZ_memory_unified_steps_1to4_2026-05-30.md` — полное ТЗ с готовыми
сниппетами. Доп. деталь по шагу 1a: `TZ_memory_wave1_step1a_feed_memory_to_prompt_2026-05-30.md`.

**Контекст (регрейд батча 5.1+5.2 по сырью):** все 11 выдумок имеют `retrieved_facts=пусто` и критик
(`faithfulness`) не вызывается без фактов. Значит калибровка критика (5/5.1) на эти кейсы не
распространяется — корень в розыске фактов не той темы и в том, что бот выдумывает при пустых фактах.
Лечит: гард пустых фактов + память (достройка вопроса темой).

**6 коммитов по порядку** (каждый самодостаточен, отдельный коммит):
0. Гард пустых фактов — при `answer_self` + нужны факты + retrieval пусто → `draft_for_manager`, не
   выдумывать. Точная точка и сниппет в ТЗ (расширение существующего `force_draft_for_manager`).
1a. Подать `dialogue_memory_view` в `build_draft_prompt` (готовый сниппет + хелпер в ТЗ).
1b. Достройка вопроса темой из `topic_focus` перед `retrieve_facts` (промпт понимания + страховка) — ядро.
2. `decide_route` и критик читают память (route_history + established_topic), с NEG-контролем.
3. Модельное наполнение памяти в `update_dialogue_memory_after_answer` (low effort, мелкая модель,
   ПОСЛЕ ответа, regex как fallback).
4. Смысловая сводка `_conversation_summary_short` тем же вызовом.

**Ограничения:**
- Тесты НЕ гонять (лимит) — после каждого коммита Клод 1 прогонит pytest+smoke в песочнике. В main не
  мержить до зелёного.
- Правило #1: каждую точку/имена подтвердить чтением.
- Каждый защитный шаг — с НЕГАТИВНЫМ контролем: не ослабить P0/бренд/факт-гарды и 4 типа правки 5.1.
- Не выбрасывать DialogueMemory и regex (regex = fallback). Модельное наполнение — только после ответа.
- Отчёт после каждого коммита: что изменено, тексты тестов.