# Backward Compatibility

## Runtime

No runtime wiring. Direct path, provider, profile flags, P0 floor/preblock and live behavior are unchanged.

## Report output

The report schema is extended with `product_existence_check`. Existing group names and headline totals remain compatible.

## Tests

Targeted report/catalog/overhandoff tests pass together.
