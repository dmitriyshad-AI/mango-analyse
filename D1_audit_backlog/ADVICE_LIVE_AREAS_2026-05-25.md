# Советы по областям, которые сам протестировать не могу (часть 3). 2026-05-25

Сюда вынесены мысли по модулям, где нужна живая среда/креды/аудио/БД или живая модель —
я их не гоняю, только читаю код на контракты и советую. Вернёмся позже.

## 1. Живые интеграции AMO / Tallanto / Mango Office
- Инвариант, который НАДО держать в тестах (готовые контрактные тесты уже в codex_tests/):
  бот/контур channels НЕ пишет в CRM; запись только из services/sync_amocrm + deal_aware/deal_writeback
  под dry_run/approval. Проверено ранее: write-методы только в clients/amocrm.py, гейт двойной.
- Совет: при включении живой записи — обязательный readback (записал → перечитал → сверил) и аудит-лог
  каждой записи с возможностью отката (amo_rollback уже есть). Идемпотентность по ключу сделки/события.
- Риск: дрейф схемы полей AMO (OPTIONAL-поля вроде «AI-интерес к скидке» попадают в payload только если есть в каталоге).
  Совет: контрактный тест на маппинг полей + алерт при неизвестном поле.

## 2. ASR / транскрибация / запись звонков
- ASR под approval-гейтом (asr_execution_approval_gate) — НЕ запускать без записи-одобрения (контракт в codex_tests/test_productization).
- Совет: держать ASR строго за гейтом Дмитрия (правило CLAUDE.md); любой авто-запуск — только после явного approval-record.
- Качество ASR (точность распознавания речи) — оценивается только на реальном аудио; это вне моих возможностей, нужен ручной/эталонный набор.

## 3. Scheduler / capture / tenant isolation
- Инварианты (контрактные тесты есть): capture идемпотентен по event_key; tenant isolation не отдаёт данные t1 в контексте t2;
  строки без tenant_id блокируются.
- Совет: добавить «канарейку» — периодический контрактный прогон tenant-isolation на проде (страховка от регрессии разделения арендаторов).

## 4. Генерация end-to-end (gpt-5.5)
- Живое качество ответов (тон, парафразы, «отправил бы») меряется ТОЛЬКО прогоном на gpt-5.5 у Кодекса (round-N).
- Мой офлайн ловит структуру/факты/безопасность, не живую речь. Это принципиальный предел — не закрыть без модели.
- Совет: метрика «отправил бы без правок» как главный KPI человечности; recall_probe — KPI извлечения; держать оба на каждом round.

## 5. Перформанс / миграции / деплой
- Не тестировал. Совет: перед пилотом — нагрузочный прогон пути генерации (латентность на ход), проверка, что
  retrieval+гварды не дают катастрофического времени; бэкап БД перед миграциями customer_timeline/product_db.

## Что из этого СТОИТ поднять в приоритет
- Контрактные тесты из codex_tests/ — прогнать в среде Кодекса как регрессионную страховку (это и есть часть 2).
- Перед публичным трафиком: readback+аудит-лог на записи в CRM; канарейка tenant-isolation; нагрузочный прогон генерации.
- Вернуться к качеству ASR и схеме AMO, когда дойдём до соответствующих направлений (разнос оплат 3A, реактивация).
