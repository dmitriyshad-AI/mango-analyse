# Регрейд по сырью — Блок 1.1 (recover estimates) + Блок 1a (partial-yield). Прогон D6 на 6788a2f5. 2026-06-04.

Ревьюер: Клод #1. Наборы: travel = levelA_estimate_acceptance (16 судья-строк), partial = block1_partial_yield (12).
Снимок v6.4_schedule, --parallel 4, memory/semantic codex. Оба summary: `needs_second_run=true` (near-bar, 1-run — направленческий).

## ВЕРДИКT (одной строкой): БЕЗОПАСНО, но НЕ к пилоту — over-handoff на гарантированных фактах сохраняется (~половина). Recover/partial-yield работают ЧАСТИЧНО.

Безопасность чиста: **0 выдумок** (оба набора), бренд не смешан, P0 маршрутизирован (p0×6, возврат→менеджер верно), ИИ не раскрыт, hard-gate держит, 0 битых транскриптов. Сводка D6 «travel FAIL=1 hard_gate» — артефакт раннера: по сырью в травеле FAIL нет, hard не нарушен.

## Победы
- Травел recover РАБОТАЕТ частично: `estimate_pre_handoff_estimate` сработал 14× (бот даёт оценку до хендоффа).
- Partial 7/12 PASS — на простых грунтованных частях отвечает.
- P0/возврат → менеджер (`foton_installment_plus_refund` ушёл верно — это НЕ баг).
- Выдумок 0, мета/бренд/ИИ чисто (кроме одной мета-течи, ниже).

## Что осталось и ПОЧЕМУ (раскладка остаточных уходов)
1. **Травел: `estimate_guard_failed` 12× убивает восстановленную оценку → хендофф.** Recover родил оценку, гард её заблокировал. `unpk_travel_lobnya`: ход1 хендофф, ход2 нерелевантная цена 49000/82000 (грунтована, но не про дорогу). Гард слишком злой к ОБЩЕЙ оценке дороги.
2. **Partial: over-handoff на СОСТАВНЫХ грунтованных частях.** `unpk_course_plus_camp` (есть факты про годовой курс физики 9кл — не дал, отвечал фрагментами ЛВШ + полный уход), `unpk_multi_three` (цена/дни/запись — дал одну олимп-группу, остальное уронил), `unpk_course_plus_teacher`. Partial-yield не вычленяет грунтованную под-часть, когда рядом camp/негрунт.
3. **Мета-течь:** `unpk_course_plus_camp` ход4 — «менеджер уточнит именно про **Есть ли сам годовой курс по физике для 9 класса**» — сырой подвопрос эхом в текст хендоффа.

## Узкие гапы — точный диагноз для следующей волны (на clean-линии c6b09326)
- **G1 estimate_guard:** пропускать ОБЩУЮ оценку дороги (минуты/км) с маркером «ориентировочно», НЕ роняя в хендофф. NEG: продуктовое число (₽/%/дата/длительность урока) гард по-прежнему блокирует даже с маркером.
- **G2 partial-yield на составных:** детектить грунтованную под-часть (курс есть / форматы / цены) и отвечать ею, даже если другая часть camp/негрунт; не уходить целиком. NEG: P0-часть → весь ход менеджеру; cross-brand → каноничная фраза.
- **G3 мета-течь:** детерминированно срезать эхо сырого подвопроса в тексте хендоффа (linter meta_leak). NEG: не ломать осмысленный хендофф.

## Рекомендация
Узкая волна G1+G2+G3 на clean-линии `c6b09326` (там v6.5), с NEG-контролями (number-gate, бренд, P0 не ослабить), затем перегон ТЕХ ЖЕ наборов на M1 — я регрейжу. Направленческий (1-run), но over-handoff — известный корневой гап, сигнал согласуется с прошлыми прогонами, не шум.
