# Отложенные потенциальные правки: resolve / analyze. 2026-05-25

ГЛАВНОЕ ПРАВИЛО (Дмитрий, 2026-05-25): уже обработано МНОГО звонков, качество хорошее.
Приоритет №1 — НЕ УХУДШИТЬ уже распознанное. Поэтому ниже — НЕ «чинить сейчас», а реестр на будущее,
с жёсткими условиями безопасной правки. Источник наблюдений: `DEEP_ANALYSIS_resolve_analyze_2026-05-25.md`
(+ logs/deep_resolve.md, deep_analyze.md). Сервисы НЕ запускались.

## Условия безопасной правки (для ВСЕХ пунктов ниже)
1. НЕ перепрогонять и НЕ перезаписывать уже готовые `analysis_json` / результаты resolve массово.
   Любая правка применяется только к НОВЫМ звонкам (или к выборочному backfill ТОЛЬКО по явному решению Дмитрия).
2. Новое поведение — за флагом/гардом, с возможностью отката; старый путь остаётся дефолтом до проверки.
3. Перед правкой — снять «золотой» срез текущих хороших карточек (N примеров) и сверять, что правка их НЕ меняет.
4. Это операционно-качественные пункты (P2/P3), НЕ безопасность клиента. Спешки нет.

## Реестр (resolve)
- R1 [P2] Путаница ролей manager↔client: метки из ASR, модель может их менять (resolve.py:1187–1200).
  Правка осторожно: жёстче фиксировать роль из ASR-метки; но проверить, что это не ухудшит уже хорошие слияния.
- R2 [P2] Тихий fallback LLM→правило при сбое модели (resolve.py:902–911) маскирует деградацию.
  Будущее: логировать/счётчик деградаций, не менять выход.
- R3 [P2] Lease-гонка без heartbeat (pipeline_claims.py:59–62) — риск двойной обработки при нескольких воркерах.
  Сейчас 1 воркер → не трогать; при масштабировании добавить heartbeat/SELECT…FOR UPDATE.
- R4 [P2] Нет явного таймаута к openai/ollama (у codex CLI таймаут есть). Добавить таймаут — операционно безопасно.
- R5 [P3] Имя стадии «resolve» вводит в заблуждение (это слияние ASR-вариантов, не привязка к CRM). Переименование — косметика, делать аккуратно (много ссылок).

## Реестр (analyze)
- A1 [P1 внутренний] Нет бренд-контроля в `analysis_json` (0 упоминаний brand/active_brand). Карточка может смешать
  Фотон/УНПК. Карточка manager-facing → не клиентский P0, но по CLAUDE.md CRM-тексты тоже требуют бренд-разделения.
  Правка ТОЛЬКО аддитивно: добавить флаг-предупреждение «возможно смешение брендов» в карточку, НЕ переписывать
  существующие; не блокировать пайплайн.
- A2 [P2] OpenAI-вызов без `timeout`, `max_tokens`, `seed`; temperature 0.1 (analyze.py:2078).
  Добавить timeout+max_tokens — операционно безопасно (не меняет смысл уже готовых карточек). seed — для детерминизма будущих.
- A3 [P2] PII в `analysis_json` (телефон/почта/ФИО) + fallback summary = первые 600 символов сырого транскрипта
  (analyze.py:1651, 1697–1703). Для менеджера норма; РИСК — если карточку переиспользуют в КЛИЕНТСКОМ слое без чистки
  (insights/sanitizers здесь не зовётся). Будущее: при любом клиентском переиспользовании — прогонять через sanitize_answer.
- A4 [P2] claim-гонка на не-SQLite/нескольких воркерах (UPDATE…WHERE id IN(SELECT…LIMIT) без FOR UPDATE, analyze.py:283).
  На текущем SQLite+1 воркер безопасно; учесть при масштабировании.
- A5 [P3] Ошибка экспорта файлов до commit может откатить корректный анализ в failed (analyze.py:2309).
  Будущее: вынести экспорт за пределы транзакции анализа.

## Что НЕ является проблемой (подтверждено)
- resolve и analyze в CRM/Tallanto НЕ пишут (resolve — codex `--sandbox read-only`; analyze — только локальная БД). Безопасность записи цела.
- Парсинг кривого JSON в analyze устойчив; ошибки не глотаются (failed/dead с backoff).

## Вывод
Ни один пункт не требует срочной правки и не ставит под угрозу уже обработанные звонки. Когда дойдём до правок —
двигаться по условиям безопасной правки выше (аддитивно, за флагом, новые звонки, золотой срез, без массового перепрогона).
