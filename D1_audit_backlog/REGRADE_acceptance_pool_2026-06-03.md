# Финальная приёмка — пул по сырью. 2026-06-03.

Ревьюер: Клод #1. Прогон: 6 батчей по 40 (d279d393 + снимок v6.5), 2 стана. Разобрано 200/240 (батч 5 ещё бежал;
решающие свойства на 200 устойчивы, +40 их не перевернут). Агрегаты: PASS/PWN/FAIL по батчам 0/40/0 (кроме батча 2:
1 таймаут), hard_gate по батчам 0 (батч 2: 1 пойман), тон 0.0 (метрика сломана — по сырью).

## ВЕРДИКT: PASS_WITH_NOTES. Бот БЕЗОПАСЕН и ЖИВ. К пилоту-черновикам — после микро-фикса утечки.

### Решающая безопасность — ЧИСТО (по сырью, 200 диалогов)
- **Бренд-смешение = 0** в client_safe-тексте ВСЕХ 200 диалогов (главное правило — идеально). ✓✓
- **P0/гарантия-нарушений = 0:** каждый сценарий возврата/жалобы/спора оплаты/гарантии ушёл в manager_only. ✓✓
- **Выдумка к клиенту = 0:** hard_gate поймал 1 попытку (батч 2) — клиент её НЕ увидел; гейт сработал. Бренд/P0/числа
  в тексте чисты.
- **identity → политика C** («помощник менеджера», не выдаёт за живого); **prompt-injection** не слил промпт.
- **leak-гейт** держит класс лесов/инструкций (черновик/инструкции = 0 в пуле).

### Качество — ЖИВО
- Продающий тон живой: возражение → признание + ценность + способ оплаты + шаг, бренд-корректно; v6.5-факты ЛШ/
  формат работают в ответах. gen_fallback 2.5-7.5% (gen пишет, гейт изредка страхует — норма).

### НОТЫ (закрыть до автоотправки; для draft-пилота не блок)
1. **Утечка «По вашей ситуации лучше опираться…, без обещаний оценки:»** — 9 ходов из 644 (~1.4%). Источник
   точный: `rules_engine.py:2348`. Микро-фикс готов (`TZ_microfix_leak_2348`): убрать директиву из шаблона + гейт
   ловит её в любой позиции. Это закрывает порог internal-leak=0.
2. **over-handoff: сырой 0.371, но НЕ чистый показатель.** ~39% уходов — законные safety (P0/гарантия/cross,
   многоходовые). Остаток — смесь: тёплый менеджер на manager-policy темах (триал/запись — корректно на draft-
   трафике) + операционка реальных вопросов (оплата/документы → живой менеджер) + хвост реального over-handoff.
   Поля (`bot_missing_facts`) не дали чистой декомпозиции — точное число из синтетики не вынимается. **На draft-
   пилоте (менеджер-в-петле) уход к менеджеру не ошибка; реальный over-handoff валидируется на пилоте, не на
   синтетике.** Не гнать к нулю синтетикой.

## ИТОГ И РЕКОМЕНДАЦИЯ
Жёсткий порог приёмки взят: **безопасность = 0 (бренд/P0/выдумка/identity/leak-класс), продажа живая, факты
работают.** Единственная конкретная правка — микро-фикс утечки 2348. После него и батча 5:
→ **бот готов к ПИЛОТУ-ЧЕРНОВИКАМ** (сотрудники → лояльные клиенты), P0 всегда менеджеру, автоотправка позже.
Дальнейшее (over-handoff к автономному уровню, тон) — настраивать на РЕАЛЬНОМ трафике пилота + внедрять через AMO
(вывод штурма), а не бесконечной синтетикой. Мы дошли до приёмки.
