> DONE 2026-07-27 10:45 | ветка main | codex

> TAKE 2026-07-27 10:41 | ветка main | codex

Ветка: main
Зоны: src/mango_mvp/utils/audio.py, tests/test_audio_probe.py, tasks/
Тест-команда: PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m pytest -q tests/test_audio_probe.py
Семантический-аудит: нет

# Python 3.13: необязательный audioop

Устранить падение текстового M1-экзамена на импорте `mango_mvp.utils.audio`,
если в Python нет удалённого модуля `audioop`.

## Требования

- Не добавлять зависимость и двоичные файлы.
- Не менять основной путь разделения стерео через ffmpeg.
- Запасной путь через afconvert должен завершаться `None`, если `audioop`
  недоступен, а не ломать импорт всего приложения.
- Добавить регрессионную проверку импорта без `audioop`.
- Прогнать точечный и полный pytest.

## Приёмка

- Импорт `mango_mvp.utils.audio` проходит при искусственно недоступном `audioop`.
- Точечный и полный pytest зелёные.
- Новых зависимостей нет.

## СТОП

- Остановиться, если исправление требует изменения обработки реальных аудиофайлов
  вне запасного пути или ломает существующие тесты звука.
