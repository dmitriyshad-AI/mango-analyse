# ТЗ-4: Перенос Волны 6 на main + бандл и парные задачи M1 (приоритет 1)

Контекст: в main (0e8553a0) кода Волны 6 НЕТ — `grep TELEGRAM_LLM_RETRIEVE src/mango_mvp/channels/subscription_llm.py` = 0. Код живёт в ветке `codex/wave6-llm-retriever` (база 7389d07a). Решение «включать ли В6» принимается по полному набору 89 на объединённом дереве (PLAN_mentor_waves_pilot_2026-06-09, поток Б2). Прогон заодно даст смысловую проверку ТЗ-2 (профиль пилота, legacy-фильтр, гейт срочных обещаний) по сырью — остаточный риск из твоего же отчёта ТЗ-2.

Сделать:
1. Перенести Волну 6 на main: rebase/merge ветки `codex/wave6-llm-retriever` на `main` (0e8553a0+). Default OFF. Конфликты хирургически (в main с тех пор пришли presale, профиль, legacy-фильтр, deadline-гейт — внимательно в зонах `_direct_path_wide_fact_pack` и преблока).
2. NEG на объединённом дереве: `TELEGRAM_LLM_RETRIEVE` выключен ⇒ поведение байт-в-байт прежнее; полный pytest.
3. Порядок преблока: при ON преблок P0 идёт ДО ретривера, при OFF — после сборки фактов (CODE_INVENTORY E:163). Если NEG на эквивалентность преблок-решений OFF/ON отсутствует — добавить.
4. Собрать бандл `mango_clean_<rev>` в Яндекс `OpenClaw/Actual Mango Tests/` и положить ДВЕ задачи в `tasks/_inbox_m1` по образцу `wave4_manual_run/*.task.yaml`, обе на одном бандле:
   - base: профиль `pilot_gold_v1`, `TELEGRAM_LLM_RETRIEVE=0`, набор `smoke_v2_acceptance` (89), судья v9, parallel 4;
   - on: то же + `TELEGRAM_LLM_RETRIEVE=1`.

Критерий приёмки ТЗ: мерж в main, полный pytest зелёный, NEG зелёные, бандл + 2 task.yaml лежат, отчёт в `_done` с ревизией.

Критерий решения по В6 (зафиксирован ДО прогона, из плана): `manager_only` ↓ ≥15% относительных И 0 новых жёстких (бренд/P0/число/ПДн) И fallback ≤5% И 4 известные выдумки — не от ретривера. Решение по регрейду сырья (Claude), не по сводке судьи.

НЕ делать: не включать флаг по умолчанию; не трогать тексты констант (отдельное ТЗ после утверждения диффа Дмитрием).
