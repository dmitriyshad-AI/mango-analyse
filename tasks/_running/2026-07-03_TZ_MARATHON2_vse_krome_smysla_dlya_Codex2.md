> TAKE 2026-07-02 21:42 | ветка codex/email-pipeline-restore | codex

Ветка: codex/email-pipeline-restore
Зоны: src/mango_mvp/customer_timeline/, src/mango_mvp/integrations/, src/mango_mvp/channels/subscription_llm_parts/direct_path.py, scripts/, tests/, docs/DECISIONS_LOG.md, tasks/, audits/_inbox/
Тест-команда: PYTHONPATH=src python3 -m pytest tests/ -q
Семантический-аудит: да

# ТЗ-МАРАФОН-2 v6 (режим цели, автономно): данные и каналы до полного образа результата на STAGING

Дата: 03.07.2026 (v6 ФИНАЛ — v5 + ревью-4 Codex: шапка выше — РОВНО в формате штатного preflight.py, первая строка файла; зоны расширены до scripts/ целиком; регистрация через task_move; порт call-increment из тега; семантик-аудит после сборки артефактов; money_kind через полный поиск). Исполнитель: Codex 2, режим цели, без пауз. Вне скоупа: качество «понимания смысла» ЯДРА БОТА (SemanticFrame/B1 — трек Д1).

## Пояснения к шапке
- `.codex_local/` — локальное хранилище данных/артефактов (staging/review/transfer), НЕ зона git-кода; в git не попадает.
- main НЕ трогать; порт-салвейджи — копированием кода, не merge веток/тегов.
- В зоне `src/mango_mvp/channels/subscription_llm_parts/` разрешён ТОЛЬКО direct_path.py и только по правилам блока 6; post_layers.py и policy_routing.py — НЕ трогать (наши версии со стражами ba48bcfa).
- В зоне `src/mango_mvp/customer_timeline/` блоку 6 дополнительно разрешён bot_safe_runtime_context.py (builder-функции с тега).
- **Регистрация задачи:** Foton-файл — внешний исходник; `task_move.py --take` на Foton-путь НЕ запускать, потому что скрипт переносит файл и удаляет источник. Безопасный порядок: сначала repo-local копия в `tasks/_inbox_codex/`, затем `task_move.py --take` на этой копии, затем `preflight.py` должен пройти зелёным (шапка выше — машинная).
- **Семантический-аудит** = субагент-аудитор после каждого блока (≤2 итераций) + skill `business-semantic-review` ПОСЛЕ СБОРКИ каждого CRM/Excel-артефакта (блоки 1.2-карточка, 7; проверка ГОТОВОГО артефакта, не намерения) с вердиктом в отчёте; финальная семантическая приёмка по mango-semantic-safety — у Fable на регрейде.
- **Audit pack:** после каждого значимого блока создавать `audits/_inbox/<block>_<timestamp>/` с `implementation_notes.md`, `changed_files.txt`, `test_output.txt`, `semantic_review.md` для CRM/бот/клиентских артефактов, `risk_review.md`, `backward_compatibility.md`; для CRM/writeback блоков отдельно фиксировать dry-run/readback/no-live-write evidence.

**НОВАЯ LLM-ПОЛИТИКА (владелец, 03.07 — отменяет прежнее «llm ожидаемо 0»):** LLM РАЗРЕШЁН И ОБЯЗАТЕЛЕН там, где задача = понимание/пересказ смысла (выжимки писем 1.4; человеко-формулировки возражений). Детерминизм остаётся полом для гейтов/идемпотентности/проверок — НЕ для смысла. `llm_calls_total` — метрика УЧЁТА (ожидаемо >0 в 1.4), НЕ стоп-условие; стопы только: прод/CRM-запись, ПДн/токены, видимость боту, деньги/P0/бренд. Идемпотентность с LLM обеспечивается КЭШЕМ (см. 1.4): «ре-ран не порождает новых llm-вызовов и не меняет закэшированное».

**PREFLIGHT (штатный конвейер):** закрыть/перенести старые `_running` задачи, обновить `PROJECT_NOW.md` штатно через `scripts/project_now.py`, зарегистрировать Марафон-2 через `task_move.py`.

## 0. Режим и стратегия базы (решение владельца 03.07)

**Прод-БД ЗАМОРОЖЕНА и в марафоне-2 НЕ трогается вовсе** (чтение только `mode=ro&immutable=1`). **Staging = будущий прод:** все новые данные и доработки — ТОЛЬКО в staging-копию (`.codex_local/staging/customer_timeline_staging.sqlite`, уже существует после марафона-1). Финал = **SWAP-пакет: CODEX ТОЛЬКО ГОТОВИТ ПАКЕТ И НИКОГДА НЕ ПРИМЕНЯЕТ** — подмена 3.5-ГБ базы это ОТДЕЛЬНОЕ РЕШЕНИЕ ВЛАДЕЛЬЦА вне марафона, после регрейда Fable (инструкция: стоп живого бота → бэкап старого прода → подмена файла (+чистые -wal/-shm) → smoke-чтение → старт; diff-опись; план отката). Аналогично CRM: только экспортные пакеты; live-запись пакета в AMO — условие ПРИМЕНЕНИЯ владельцем после регрейда Fable, не шаг Codex. AMO/Tallanto: ЧТЕНИЕ разрешено (read-only API, токены не логировать), ЗАПИСЬ = 0; для CRM — экспортные пакеты. Живой бот/профиль/флаги не трогать. M1-бандлы готовить — запускает человек. Субагенты: параллелить независимое (max reasoning), после КАЖДОГО блока — субагент-аудитор «ломатель» (≤2 итерации, остаток в «известные ограничения»). Лог решений вести (docs/DECISIONS_LOG.md + Foton-выжимка).

## 0-бис. EXCEL-КОНТРОЛЬ ВЛАДЕЛЬЦА (обязателен после КАЖДОГО блока)

После каждого блока обновлять книгу `.codex_local/review/marathon2_review.xlsx` (образец структуры — `2026-07-03_timeline_review_18_klientov.xlsx` от Fable): листы «Обзор клиентов» (18-20 фиксированных клиентов: имя/телефон/email/счётчики), «Таймлайн», «Звонки прод vs копия» (столбец «Изменилась?» — регресс-контроль), «Письма» (полный текст+контекст), «Деньги Сигналы Возражения», + лист нового блока (например «Семейная карта»: родитель→дети→привязка событий с уверенностью; «Оплаты Tallanto»: факт-платежи). Правила: русские заголовки, wrap_text, freeze первой строки; ПДн-версия ТОЛЬКО в `.codex_local/review/`; в Foton — обезличенная копия счётчиков (без имён/телефонов). Плюс к каждому блоку — метрики времени и счётчики до/после в отчёт.
**[Требование владельца 03.07 — БЕЗ ОБРЕЗОК]:** в Excel показывать ОСМЫСЛЕННЫЕ выжимки (summary из БД) ЦЕЛИКОМ и полные тексты ЦЕЛИКОМ (лимит ячейки Excel 32 767 знаков; если длиннее — резать по границе предложения с пометкой «…продолжение в БД»); тупые обрезки «первые N символов» ЗАПРЕЩЕНЫ. Сигналы — человекочитаемой формулировкой («сделка без движения 21 день», «2 входящих за неделю»), не сырым JSON. Возражения — после блока 1: нормализованная формулировка (тип + суть + цитата клиента целыми предложениями), не огрызок окна; до блока 1 сырые цитаты помечать «сырая цитата, чинится блоком 1».

## Блок 1 — ДОПИЛ марафона-1 (дефекты Д1-Д4 из консенсус-регрейда)

1.1 **Возражения со speaker (Д1, главный). [правка аудитора: диаризации в staging НЕТ]** Источник диаризации звонков = ВНЕШНЯЯ база `Mango analyse/stable_runtime/canonical_master_*/canonical_calls_master.db` (read-only; 65 974 звонка, transcript_client непустой у 65 086) — staging-события звонков хранят только слитый summary. Матчинг событие↔звонок: по canonical_call_id, при отсутствии — по контакту+дате(+summary); не сматчилось → `speaker=unknown`. Миграцией store добавить в `customer_objections_v1` колонки `speaker` (client|manager|mixed|unknown), `direction`, `confidence`; все 27 134 строки пересчитать: маркеры — ТОЛЬКО в репликах клиента (transcript_client). ⚠ СТОП-условие: пере-извлечение из слитого summary БЕЗ разделения говорящих ЗАПРЕЩЕНО (это воспроизведёт дефект Д1 под видом починки). **[v4→v5] Coverage-gate матчинга (ужесточён):** отдельная метрика «доля звонков staging, сматченных с canonical_calls_master.db» (по клиентам выборки и по всей базе); если coverage < 70% — блок «Возражения» в CRM-пакет НЕ СОБИРАЕТСЯ (карточки выходят без него, с пометкой «возражения: покрытие данных недостаточно»), доклад архитектору с разбором причин; speaker=unknown НИКОГДА не идёт в CRM-блок независимо от coverage. Письма: speaker по `direction` (inbound=клиент); из цитируемых хвостов не извлекать. Цитаты: не рвать слова, вычищать email-треды. Приёмка: выборка 30 price-возражений аудитором — ≥90% слова клиента; coverage-метрика в отчёте; в CRM-блок идут только speaker=client с высокой confidence.
1.2 **Сигналы после денег (Д2) + блок в карточку (Д3). [это ПОСТ-ШАГ блока 2 — исполнять после него]** Пересчёт строго через `derive_sg_v1_signals(..., purchases=customer_purchases_v1, as_of=…)`. `season_return_candidate` срабатывает только на покупках старше 180 дней: текущие AMO-покупки все свежие (янв–июл 2026) → сигнал появится лишь если Tallanto-выгрузки принесут покупки 2024–2025; если исторических покупок нет — 0 с обоснованием (НЕ дефект). В текст карточки — секция «Сигналы:» (тип, дата-основание, срок годности, человеко-формулировка: «клиент вернулся после паузы», «обещали перезвонить — просрочено»).
1.3 **Сверка d7-контракта (Д4).** Пофайловый дифф полей пакета против `write_deal_aware_amo_fields.py`/`build_amo_writeback_queue.py` (main) — отчёт-соответствие 1-в-1; расхождения чинить в пакете. `active_signals_count` — число, не строка.
1.4 **НАСТОЯЩИЕ выжимки писем (найдено владельцем 03.07: summary = механический префикс). Решение владельца = ВАРИАНТ А.**
Факт: Э2-enrich записал `summary = compact_text(full_text, 500)` — начало письма, НЕ пересказ (86%+ префиксы), ПЕРЕЗАПИСАВ настоящие LLM-выжимки пилотных 100 (восстановить из тест-БД `ingest_100_contentfix_*/customer_timeline_test.sqlite`); у 394 писем в summary — банковские реквизиты (в 81 ready-карточку НЕ утекли — гейт спас).
(а) **Объём СЕЙЧАС (вариант А):** письма контактов CRM-экспорта (уникальные customer_id из `batch_ready_*.jsonl` + `pilot_20_*.jsonl` = 99 контактов; отбор ТОЛЬКО из этих jsonl, НЕ по БД-флагу candidate_count — тот стоит у 4 168) + 18 ревью-клиентов ≈ 914 писем, из них ≥600 знаков ≈ 845 → LLM-пересказ функционалом `scripts/email_pipeline/summary.py` (промт пилота, prompt_version фиксировать). Короткие (<600) — summary = сам текст. **МАССА (27 100) — ЗАПРЕЩЕНА до доказательства качества на M1-прогоне** (бандл готовит Codex, запускает человек; решение о массе — владелец).
(б) **Кэш = идемпотентность (обязателен):** таблица `email_summary_cache_v1 (message_sha256 PK, summary_text, summary_sha256, prompt_version, model, llm_call_at)` — message_sha256 уникален у всех 30 093 писем (проверено, 0 дублей). Hit → детерминированный возврат без вызова; miss → 1 вызов + запись. Ре-ран = 0 новых вызовов, 0 изменений. Смена prompt_version = осознанный пересчёт.
(в) **ПРОВЕРКА ИСТОЧНИКА ДО ПРОГОНА:** полный текст может лежать в холодном хранилище (`record.record.extracted_text_path` / архив-БД stage2_enrich) — проверить доступность текстов для всех целевых писем ДО массового вызова; недоступные — в отчёт (не «0 выжимок молча»).
(г) Санитайзер выжимки: банковские реквизиты/карты в summary НЕ включать («реквизиты — в полном тексте»); полный текст НЕ трогать (память бота = всё).
(д) side-таблицу фактов писем (a2v3_mail_event_facts) создать/перенести в staging (сейчас ОТСУТСТВУЕТ — факты пилота остались в тест-БД).
(е) **[v4→v5] Анти-галлюцинация LLM-выжимки (расширена на бизнес-сущности):** если модель вернула fallback/пустоту ИЛИ выжимка содержит сущности, которых НЕТ в исходном письме — детерминированная сверка против полного текста по классам: суммы/числа, даты, номера (договоров/телефонов), БРЕНД (Фотон/УНПК — упомянут в выжимке, отсутствует в письме = подозрение), имена (ребёнка/контакта), названия курсов/предметов, утверждения о статусе оплаты/возврата и «следующем шаге», не опирающиеся на текст, — summary в память НЕ пишется: письмо в очередь `summary_review_needed` с причиной и классом сущности; счётчики ok/fallback/hallucination_suspect (по классам) — в отчёт и приёмку.
Приёмка: 30 писем аудитором — выжимка=пересказ (не префикс), 0 реквизитов, 0 выдуманных чисел; ре-ран-тест кэша; Excel-лист «Письма» с настоящими выжимками.

## Блок 2 — ДЕНЬГИ-ФАКТ: Tallanto-платежи [правка аудитора: скрипт файловый, три шага]

Скрипт `scripts/import_tallanto_payments_to_timeline.py` — ФАЙЛОВЫЙ (ест JSON-снимок с ключами `most_finances`/`most_abonements`; сетевых вызовов нет), и он НЕ пишет `customer_purchases_v1`. Поэтому:
(1) Препроцессор: обернуть локальные выгрузки (`Финансовая модель/03_выгрузки_tallanto/*/…_raw_finances.json` + `…_raw_abonements.json`, плоские списки; слить филиалы) в снимок `{most_finances:[…], most_abonements:[…]}`; `--allowed-root` покрывает снимок и staging. Если локальных выгрузок мало — свежая выгрузка через `crm_call.sh` (read-only, токен не логировать), с паузами на 429.
(2) Импорт: dry-run → отчёт → apply на STAGING (события `TALLANTO_PAYMENT`/`TALLANTO_ABONEMENT`). ⚠ Контроль: «прогнал успешно, но 0 событий» = НЕ выполнено (файловый скрипт на неверном формате молча даёт 0) — либо >0 событий, либо явное обоснование.
(3) Агрегат `money_kind` — ТОЧНЫЙ КОНТРАКТ (аудит: писателей purchases ДВА, оба с полным overwrite ON CONFLICT — чинить ОБОИХ): новый первичный ключ `customer_purchases_v1 = (tenant_id, customer_id, period, money_kind)`, `money_kind ∈ {plan, fact}`; backfill существующих 470 AMO-строк → `plan`; Tallanto-оплаты → `fact`; перетирание plan↔fact одной строкой ЗАПРЕЩЕНО (разные строки); синхронно править ОБОИХ писателей — `stage5_money_ingest.py` (:150/:365) И `a2_mail_ingest.py` (:985/:1022), их CREATE TABLE IF NOT EXISTS не мигрирует схему сам — нужна явная миграция staging-таблицы. **[v5→v6] ЧИТАТЕЛИ money_kind — НАЙТИ ВСЕХ ПОИСКОМ, не списком:** обязательный `rg customer_purchases_v1` по всему репо → обновить КАЖДОЕ найденное место (минимум ожидаются: derived_signals — вход purchases для sg_v1; crm_export_package — блоки «Деньги» карточки, fact приоритетен для «оплачено», plan — «план сделки»; d7-пакет; Excel-книга; но список из поиска — исчерпывающий); перечень найденных мест — в отчёт блока. Обязательный тест: «plan и fact НЕ затирают друг друга» (записать обе строки одного клиента → перечитать обе → повторный прогон → обе целы).
Порог: доля платежей без сопоставленного клиента > 30% → стоп и доклад (не копить висячие). Приёмка: сверка сумм по 10 клиентам с ИСХОДНЫМИ выгрузками (не с живым API), Excel-лист «Оплаты Tallanto».

## Блок 3 — СЕМЕЙНАЯ КАРТА (A1 образа; главный новый блок)

Фундамент: `customer_profile/child_identity_dedup_llm.py` (main) — НЕ переписывать, ДОСТРОИТЬ привязку. [Уточнение аудитора]: несмотря на имя `_llm`, ядро детерминистично: при `PROFILE_LLM_CHILD_RESOLVER=0` (дефолт) — чистое строковое сопоставление имён; ГОТОВЫХ результатов дедупа в staging-БД НЕТ → пере-прогнать детерминированный пол на staging-контактах; LLM-пути (Tier1/эскалацию) НЕ включать (llm_calls_total=0); внешний profile-build артефакт (customer_profiles*.sqlite) — допустимый вход, если найдётся.
3.1 Таблица `family_links_v1`: customer_id (родитель) → child_key (канонический ребёнок из дедупа) → grade/subjects → linked_deal_ids → linked_event_ids, `link_confidence` (high/medium/low), `link_reason` (имя в тексте события/сделки, класс, предмет, явное упоминание). Детерминированный матчинг: имя ребёнка/класс/предмет в subject/summary/сделке; LLM-эскалация НЕ в этом ТЗ (детерминизм-пол).
3.2 Правило «staging→активная память» (из образа): событие с неоднозначным ребёнком помечается `child_ambiguous=1` и в чанк-metadata; мусорные заявки (уже blocked identity) в активную память не идут (уже так — подтвердить тестом).
3.3 В карточку CRM — блок «Семья»: дети (имя/класс/предметы), какие сделки чьи; неоднозначное — с пометкой «уточнить». Excel-лист «Семейная карта» (20 клиентов: родитель, дети, привязка, уверенность) — владелец проверит руками.
Правило уверенности (ревью Codex): `high` — ТОЛЬКО при единственном ребёнке в семье ЛИБО однозначном совпадении уникального имени; братья/сёстры, однофамильцы, формулировки «X и Y» → `medium`/`ambiguous` + пометка «уточнить у менеджера», НИКОГДА не high. Приёмка: на 20 контрольных клиентах ≥80% событий с упоминанием ребёнка привязаны верно (выборочный аудит), 0 ложных «high» к чужому ребёнку; неоднозначные честно помечены.

## Блок 4 — КАНАЛЫ В ПАМЯТЬ (A3-часть 1, всё на staging)

4.1 Wappi/Telegram-история (ТЗ Claude#1 передано Fable — исполняется здесь): ПОРТ ИДЕИ из тега `archive/wappi-history-555a964` на ТЕКУЩУЮ email-ветку ребейзом (НЕ merge — база тега 21.06, main ушёл на +77 коммитов). ⚠ ВМЕСТЕ с зависимостью `src/mango_mvp/integrations/amo_wappi_auto_resolver.py` из того же тега (точный путь; top-level import на :53 — без модуля ModuleNotFoundError) ЛИБО импорт ленивым; порт сверять с ТЕКУЩИМ кодом ветки (не слепое копирование старого тега — API store/ingestion уточнить по месту); **авто-резолвер OFF с явным тестом-проверкой** (правило v1: личность сопоставляет человек; неоднозначность → пусто, не угадывать). ⚠ main-овский `channels/telegram_history.py` — ДРУГОЙ модуль (свои литералы, дефолт source_system="tallanto") — НЕ трогать, не путать. **Зафиксировать source_system-литерал импортёра ДО прогона** (аудит: в staging уже 1 230 telegram_message + 147 telegram_dialog — выбрать совместимый литерал/дедуп-стратегию, иначе дубли и слом идемпотентности; решение в отчёт). Транспорт: read-only, allowlist-эндпоинты. Источники = Telegram + Max (НЕ WhatsApp — не называть так). → staging (идемпотентно, бренд-политика как у почты; всё manager_only). Порог: несопоставленных >30% → стоп. **[v4] Удаление ветки `codex/wappi-history` — НЕ в марафоне:** разрушительные git-операции (`branch -D`) в автономный прогон не входят; после извлечения полезного + PASS-регрейда Fable — Codex только ФИКСИРУЕТ в отчёте «ветка готова к удалению» (предполёт пройден: тег цел = 555a964, ветка не вычекнута), само удаление — отдельная уборочная отмашка владельца (remote тем более не трогать). 4.2 Свежие звонки: **[v6]** `build_mango_call_timeline_increment.py` в ТЕКУЩЕЙ ветке ОТСУТСТВУЕТ — портировать из тега `archive/mango-call-increment-35fc5dd` (scripts/build_mango_call_timeline_increment.py, 450 строк, с адаптацией к текущим store/ingestion) ЛИБО реализовать эквивалент; инкремент новых звонков с даты снимка → staging. 4.3 AMO-инкремент: `amo_incremental.py` (main) → staging (read-only API AMO). Приёмка: счётчики+Excel-обновление; повторный прогон = 0 новых.

## Блок 5 — НОЧНАЯ СЛУЖБА (A3-часть 2): планировщик поверх готового nightly

`run_nightly_incremental`/`NightlyIncrementalConfig` (main, ручной one-shot) обернуть в службу: конфиг-цепочка источников (звонки→AMO→Tallanto→Wappi→почта-инкремент) с записью в STAGING (прод заморожен), чекпойнты/локи (single-writer соблюдён — писатель один, служба), публикация проверенного снимка (манифест: sha256+счётчики+версия+время). Хост-установку НЕ делать: подготовить launchd-plist + install/uninstall-скрипты + инструкцию (владелец решит, мак живого бота или M1 — развилка №1 образа). Тест: 2 подряд «ночных» прогона на staging — идемпотентно, снимок публикуется, время в отчёт. Excel-лист «Ночной прогон» (что добавилось).

## Блок 6 — ПАМЯТЬ→БОТ, подготовка live (A4; включение НЕ здесь)

Салвейдж `build_customer_memory_for_prompt` — источник: ТЕГ `archive/customer-memory-stage01-4d958dd` (иммутабельный реф; содержимое = ветке, оба 4d958dd), точный файл `src/mango_mvp/customer_timeline/bot_safe_runtime_context.py` (:218 builder, :401 scrub; вызов в direct_path.py) → объединить с нашим Э4а-каркасом. **Shadow-прогоны блока 6 гонять ТОЛЬКО по контактам с ГОТОВЫМИ LLM-выжимками (99 CRM-контактов + 18 ревью из блока 1.4)** — на остальных summary ещё механический, метрика контекстности будет зашумлена. ⚠ КРИТИЧЕСКОЕ ПРАВИЛО MERGE (аудитор): stage01 содержит СТАРЫЙ post_layers.py (до ba48bcfa) — слепой merge файла ОТКАТИТ всегда-вкл стражи (future-commitment guard + сохранение manager_only при downgrade) = запрещённое ослабление P0. Переносить из stage01 ТОЛЬКО builder-функции (build_customer_memory_for_prompt / scrub_customer_memory_text / рендер) копированием в НАШИ файлы; post_layers.py/policy_routing.py — только наши версии; shadow-флаг унифицировать на наш TIMELINE_MEMORY_* (OFF); пост-merge регресс-тест: оба стража ba48bcfa активны. Единый сборщик блока памяти: bot_safe_summary (слот-билдер уже в нашей ветке) + свежие rich-чанки + семейный блок из блока 3. Прогон симулятора на staging-памяти shadow и ON: сравнительный отчёт (контекстность/выдумки/P0/бренд по сырью) + лёгкий бандл M1 (запустит человек). Приёмка: fabrication hard-gate=0 (с verified_by_memory), P0/бренд целы, память OFF в дефолте; чек-лист включения на живом (пре-условия: A1-блок готов, регрейд Fable, «да» владельца).

## Блок 7 — ФИНАЛ: CRM-пакет v2 + SWAP-пакет

7.1 Пересборка CRM-экспорта v2: карточки с чистыми возражениями (1.1), НАСТОЯЩИМИ выжимками (1.4), секциями «Сигналы» (1.2) и «Семья» (3.3), деньгами план/факт (2); пилот-20 + партия; идемпотентность. **Гейты — явным перечнем (ревью Codex):** `raw_email_thread_artifact` — ОБЯЗАТЕЛЬНЫЙ blocker (главный: 17/99 у кандидатов; сквозной для ВСЕХ будущих CRM-пакетов); `weak_next_step` («уточнить у менеджера» / пустой шаг) — отдельная КАТЕГОРИЯ качества в манифесте (blocker для ПРИМЕНЕНИЯ пакета, не для сборки; сейчас 40/81); гейт текста (`_crm_text_quality_blockers`) прежний. **Формат — ПЛОСКИЙ d7-контракт:** поля field_id→value по каталогу `DEAL_AI_FIELDS`/`lead_field_catalog_cache.json`, совместимые с `write_deal_aware_amo_fields.py`/`build_amo_writeback_queue.py` 1-в-1 (не только JSON-блоб); `active_signals_count` — число. **Live-запись пакета в AMO — НЕ шаг марафона:** условие применения владельцем после регрейда Fable (semantic_pass). 7.2 **SWAP-пакет** (по решению владельца): инструкция подмены прод-файла на staging. Шаги: freeze-подтверждение → `wal_checkpoint(TRUNCATE)` на STAGING непосредственно перед копированием (staging можно — это не прод; если после чекпойнта был ещё writer — повторить; копировать только `.sqlite`) → стоп бота → бэкап прода с sha → подмена + удаление старых прод `-wal/-shm` → smoke-чтение (счётчики + 3 контрольных клиента; совместимость читалок бота с новой схемой подтверждена аудитором — новые колонки nullable) → старт бота → откат-план. Diff-опись prod-vs-staging финальная, чеклист «кто что жмёт». 7.3 Финальная Excel-книга + сводный отчёт марафона-2.

## Сквозные инварианты (нарушение = стоп)

Прод заморожен: запись/writable-open/checkpoint = 0. AMO/Tallanto запись = 0 (чтение read-only allowlist-эндпоинтами, токены не в логи/git). Живой бот/флаги/профиль = не трогать; формулировка «память готова» означает ТОЛЬКО shadow/staging, НИКОГДА live. Никакого `git add -A`; ПДн только `.codex_local` (Excel с именами/телефонами/email — ТОЛЬКО `.codex_local/review/`, НИКОГДА git/Foton). None-группы дублей NEVER. P0/бренд-гейты только ужесточать; `_bot_visibility`-семантика (bot_eligible_candidate) не откатывается. Деньги/P0 в клиентский ответ не текут (внутренние слои — можно). Идемпотентность каждого вливания: ре-ран = 0 новых записей И 0 новых LLM-вызовов (кэш 1.4б). llm_calls_total в каждом summary — метрика учёта (в 1.4 ожидаемо >0 — НЕ нарушение; вне смысловых задач — обоснование). Свежая ветка-работа поверх `codex/email-pipeline-restore`; main не трогать (порт-салвейджи — копированием кода в нашу ветку, НЕ мержем веток). Микро-расхождения — фиксируй в отчёте и продолжай; расхождения по прод/CRM-записи, ПДн, токенам, видимости боту, деньгам/P0/бренду = СТОП и доклад (это НЕ «микро» никогда).

## Определение «готово» марафона-2

Блоки 1-7 исполнены на staging с аудитами; Excel-книга наполнена по всем блокам (владелец может проверить руками); Tallanto-факты влиты; семейная карта построена с честными уверенностями; каналы догружены; ночная служба готова к установке (пакет+инструкция); память→бот готова к включению (shadow-зелёный, бандл M1 у человека); CRM-пакет v2 и SWAP-пакет собраны; все отчёты в Foton; ветка в origin.

---

## ПРОМТ ДЛЯ ЗАПУСКА CODEX 2 (после ревью аудитора Fable — вставить в новую сессию)

> Ты — Codex 2, исполнитель трека «Почта + Customer Timeline» проекта Mango. РЕЖИМ ЦЕЛИ, полностью автономно, по ТЗ-МАРАФОНУ-2 v6 (ФИНАЛ, вобрал ВСЕ ЧЕТЫРЕ твоих ревью): `/Users/dmitrijfabarisov/Claude Projects/Foton/2026-07-03_TZ_MARATHON2_vse_krome_smysla_dlya_Codex2.md` — ПЕРВЫЕ 4 СТРОКИ файла = машинная шапка РОВНО под твой preflight.py (Ветка/Зоны/Тест-команда/Семантический-аудит: да). Старт штатно: зарегистрируй задачу в tasks/_running через `task_move.py --take`, прогони `preflight.py` (должен быть зелёным), обнови PROJECT_NOW через project_now.py — и работай. Прочитай ТЗ целиком, включая «Пояснения к шапке» (.codex_local — хранилище данных, не зона кода; в subscription_llm_parts разрешён только direct_path.py; post_layers/policy_routing НЕ трогать). v6-дельта: зоны = scripts/ целиком; call-increment портировать из тега archive/mango-call-increment-35fc5dd (в ветке скрипта нет); business-semantic-review — ПОСЛЕ сборки каждого CRM/Excel-артефакта; читатели money_kind — полным `rg customer_purchases_v1`, не списком. Ключевое v5: SWAP и CRM только ГОТОВИШЬ — не применяешь; `branch -D` в марафон НЕ входит; coverage-gate звонков: <70% → блок «Возражения» в CRM-пакет НЕ собирается (пометка в карточках) + доклад; speaker=unknown в CRM никогда; анти-галлюцинация выжимок по ВСЕМ бизнес-сущностям (числа/даты/номера/БРЕНД/имена/курсы/статусы оплаты/шаги → summary_review_needed); money_kind: PK+(money_kind), чинить ОБОИХ писателей И перечисленных читателей (derived_signals, crm_export_package, d7-пакет, Excel) + тест «plan/fact не затирают»; wappi: точный путь src/mango_mvp/integrations/amo_wappi_auto_resolver.py, порт сверять с текущим кодом; плоский d7-контракт; family high только при уникальном ребёнке; память из тега archive/customer-memory-stage01-4d958dd (builder :218/:401, post_layers/policy_routing НЕ переносить); LLM-политика: пересказ смысла = LLM обязателен, кэш email_summary_cache_v1 по message_sha256, вариант А = 99 CRM-контактов+18 ревью ≈ 845 писем, масса 27К после M1-доказательства. Контекст марафона-1 — `2026-07-03_MARATHON_FINAL_TRANSFER_PACKAGE_Codex2.md` и регрейд `2026-07-03_REGRADE_MARATHON_E1_E5_konsensus_Fable_i_auditora.md`. ГЛАВНОЕ: прод-БД ЗАМОРОЖЕНА (никакой записи/writable-open; SWAP-пакет только готовится); всё — на staging `.codex_local/staging/`; AMO/Tallanto только чтение; живой бот не трогать («память готова» = только shadow/staging); после КАЖДОГО блока — обновление Excel-книги `.codex_local/review/marathon2_review.xlsx` (по образцу v2-книги: выжимки и тексты ЦЕЛИКОМ, без обрезок «N символов», сигналы человеко-формулировками) + субагент-аудитор (≤2 итераций). Порядок блоков: 1 (допил: возражения — диаризация из ВНЕШНЕЙ canonical_calls_master.db, из слитого summary извлекать ЗАПРЕЩЕНО, speaker/confidence-колонки; сверка d7-контракта; пересчёт сигналов — ПОСЛЕ блока 2 через derive_sg_v1_signals с purchases; блок «Сигналы» в карточку) → 2 (Tallanto-платежи в 3 шага: препроцессор формата снимка → импорт с контролем «>0 событий или обоснование» → money_kind plan|fact + refresh purchases через stage5_money_ingest; порог несопоставленных 30% = стоп) → 3 (семейная карта поверх ДЕТЕРМИНИРОВАННОГО пола child_identity_dedup (PROFILE_LLM_CHILD_RESOLVER=0, llm_calls=0): family_links_v1, привязка событий/сделок к детям, блок «Семья», Excel-лист) → 4 (каналы: wappi-history из тега ВМЕСТЕ с amo_wappi_auto_resolver-зависимостью; свежие звонки; AMO-инкремент; порог несопоставленных 30%) → 5 (ночная служба: пакет установки, сам не устанавливай) → 6 (память→бот: из stage01 переносить ТОЛЬКО builder-функции копированием — post_layers/policy_routing только НАШИ, иначе откат стражей ba48bcfa = запрещённое ослабление P0; регресс-тест стражей; shadow; включение НЕ делать) → 7 (CRM-пакет v2 + SWAP-пакет с чекпойнтом staging перед копией + финальная Excel-книга). Железные правила: git add -A НИКОГДА; ПДн только .codex_local (Excel с ПДн — review/, в Foton обезличено); None-группы NEVER; P0/бренд только ужесточать; main не трогать (салвейдж копированием в нашу ветку); идемпотентность всего; счётчик llm_calls_total; лог решений. СТОП-условия (не «микро»): любая запись в прод/CRM, ПДн/токен-утечка, видимость боту, деньги/P0/бренд-гейты. Начни с плана-чеклиста с оценкой объёма, работай до «Определения готово».
