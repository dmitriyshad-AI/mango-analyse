# Промпт для новой задачи Codex на M1

```text
Ты работаешь на Mac M1 над переносом конвейера звонков Mango. Репозиторий:
/Users/dmitrijfabarisov/Projects/Mango analyse

Источник правды - файлы репозитория, не чат и не старый внешний пакет. Сначала
прочитай целиком:
1. AGENTS.md
2. README.md
3. ARCHITECTURE.md
4. docs/PROJECT_NOW.md; если файл старше суток, запусти python3 scripts/project_now.py
5. docs/RUNBOOK.md
6. docs/DECISIONS_LOG.md
7. docs/m1_calls_handoff_20260801/README.md
8. tasks/_inbox_codex/2026-08-07_TZ_m1_calls_runtime_readiness.md
9. docs/M1_MANGO_CALLS_SPLIT_CUTOVER_RUNBOOK.md
10. tasks/_inbox_codex/2026-07-31_TZ_m1_calls_stage10_pilot.md

Цель: последовательно выполнить ТЗ готовности M1, не перескакивая через точки
ручного подтверждения. Не объявляй службу готовой по наличию файлов или зелёным
unit-тестам.

Начальный порядок:
1. Выполни git status --short --branch, git fetch origin и покажи HEAD и
   origin/main. Для кодовой Фазы 0 отсутствие `CUTOVER_CODE_SHA.txt` нормально:
   файл создаётся только после её PASS и вливания в main. Перед Фазой 1 он уже
   обязателен; если отсутствует, SHA различаются или дерево грязное, остановись.
   Старый CANONICAL_GIT_SHA.txt от 2026-08-01 игнорируй. Раздел старых логов в
   `docs/RUNBOOK.md`, указывающий на runtime внутри репозитория, относится только
   к схеме до переноса и не задаёт путь M1.
2. Проверь имя пользователя. Если это не dmitrijfabarisov, остановись: рабочие
   данные содержат абсолютные пути.
3. Создай отдельный чистый worktree и ветку codex/m1-calls-runtime-readiness.
   Зарегистрируй worktree. Возьми ТЗ только через scripts/task_move.py и запусти
   tz_lint, inventory_before_build и preflight.
4. Сначала выполни только Фазу 0 ТЗ: закрой кодовые гейты на синтетических и
   обезличенных данных. Не читай реальные секреты, аудио и транскрипты. Не
   запускай ASR, Resolve, Analyze, launchd и сетевые публикации.
5. Запусти точечные тесты, агент-ломатель и Claude-аудит. Исправляй корень
   подтверждённых проблем, не частные номера звонков.
6. Зафиксируй изменения одним коммитом и передай Дмитрию SHA и audit pack.
   CUTOVER_CODE_SHA формируется только после вливания этого коммита в main.
7. Фазу 1 выполняй после появления финального SHA. Установка пакетов разрешена
   только с явной переменной подтверждения. Интерактивный codex login выполняет
   Дмитрий; auth.json не копируй.
8. Для отсутствующих секретов, свежей CSV Tallanto и рабочих данных дай Дмитрию
   точные команды прямого scp/rsync из runbook. Не передавай их через Git,
   Яндекс Диск, чат или audit pack.
9. Реальные read-only микропробы, перенос данных, ручной контрольный цикл,
   --install и запуск службы выполняй только после отдельного подтверждения
   соответствующей фазы. Одно подтверждение не переносится на следующую фазу.
10. Google необязателен. Если оба Google-параметра пусты, это нормальная
    конфигурация. Если заполнен только один, статус BLOCKED.

Обязательные границы:
- на M1 работает только Process A;
- на основном Mac работает только Process B;
- одновременный Process A на двух компьютерах запрещён;
- один Whisper и один GigaAM, стадии последовательны;
- Resolve/Analyze используют изолированный Codex без plugins/MCP;
- рабочие базы, аудио и секреты не идут через Git или Яндекс Диск;
- Яндекс Диск получает только внутренние XLSX/TXT;
- никакой записи в AMO/CRM/Tallanto и никакого удаления старого runtime;
- не менять модели и версии ASR без отдельного сравнения качества;
- не использовать режим ultra.

После каждой фазы выведи:
- что доказано первоисточником;
- что не проверено;
- formal_pass, semantic_pass, business_pass, data_pass, runtime_pass;
- точную следующую команду для Дмитрия, если нужен ручной шаг.

До семи последовательных зелёных суток:
runtime_pass=false, pilot_ready=false, production_ready=false.
```
