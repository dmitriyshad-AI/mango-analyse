# ТЗ Шаг 2b.1 — первая волна миграции правил. Для Кодекса. 2026-06-02.

Автор: Клод 1 (read-only архитектор). Реализует: Кодекс по решению Дмитрия. Проект в заморозке —
правки только в `.phase12`-ветке, не в stable_runtime, без пилота.

## Материалы (прочитать ПЕРЕД кодом — правило «сначала думаем»)
1. `D1_audit_backlog/rules_registry.yaml` — 16 правил, источник правды слоя 2 (валиден).
2. `D1_audit_backlog/STEP2a_handoff_for_codex_2026-06-02.md` — как потреблять реестр + 3 требования Прогона 1.
3. `D1_audit_backlog/REGRADE_run1_gate_merged_2026-06-02.md` — почему гейт чист, какие 3 корня over-handoff.
4. Контракты-обоснования `STEP2a_rule_contract_*` и `STEP2a_rule_contracts_medium_small_*`.
5. Гейт-спека `TZ_INVERSION_authoritative_output_gate_v2_2026-06-02.md` (гейт уже в коде, Шаг 1 закрыт).

## Принцип волны (strangler внутри шага)
НЕ мигрируем все 16 правил сразу. Первая волна = **каркас + 3 самых чистых правила сквозь**, остальные 13
продолжают работать через старые `_*_safe_template` БЕЗ изменения поведения. Это surgical и проверяемо
офлайн.

## Тест входа (что верно до старта)
- Шаг 1 (выходной гейт) в ветке, 538 тестов зелёные.
- `rules_registry.yaml` парсится (16 правил).
- Слой понимания уже есть: `dialogue_contract_pipeline.py::understand` (408) +
  `conversation_intent_plan.py` (intent-маркеры, address на 367). ИСПОЛЬЗОВАТЬ ЕГО, новый планнер НЕ писать.

## Объём волны 2b.1 (ровно это, не больше)

### A. Загрузчик реестра + диспетчер правил (каркас)
Новый модуль `rules_engine.py` (минимальный):
```python
# полуфабрикат — Кодекс уточняет по месту
def load_rules_registry(path=DEFAULT_RULES_REGISTRY_PATH) -> dict[str, Rule]:
    # один раз распарсить yaml в {rule_id: Rule}; кэшировать
    ...

def select_rule(intent: str, registry) -> Rule | None:
    # сопоставить existing primary_intent / conversation_intent_plan → rule.intent
    # вернуть правило ТОЛЬКО если оно в списке мигрированных (см. MIGRATED ниже); иначе None
    ...

MIGRATED = {"teacher", "recordings", "contact_address"}  # волна 2b.1

def apply_rule(rule, plan, facts, context) -> RuleOutcome | None:
    # 1. собрать data из required_fact_keys (значения из KB-снимка) + rule.data
    # 2. выбрать subvariant по plan
    # 3. вернуть RuleOutcome(route_effect, data_for_composer, preserve_exceptions)
    # текст НЕ хардкодить — отдать composer'у data (text_effect=composer_generates_from_data)
    ...
```
Диспетчер встраивается ОДНОЙ точкой в существующий маршрут до `_*_safe_template`: если `select_rule`
вернул мигрированное правило → идём по `apply_rule` + composer; иначе → существующий путь (фолбэк).

### B. Три правила сквозь (intent → rule → composer-from-data)
1. **teacher** (анкер `_teacher_safe_template`:5252, `TEACHERS_*`:332): регалии из факта, ФИО → менеджер.
2. **recordings** (`_recordings_safe_template`:4677): онлайн доступны / очные нет, по формату, бренд-сплит.
3. **contact_address** (НОВОЕ, чинит дефект Прогона 1): адрес из факта, СИММЕТРИЧНО по брендам.
   Фотон-адрес = Скорняжный (точное написание). Сейчас Фотон уходит в over-handoff (3 хода), УНПК
   отвечает — устранить несимметрию. Анкеры детекции адреса: `conversation_intent_plan.py`:367,
   `dialogue_contract_pipeline.py`:2081/2808/2494 («адресный факт нельзя как ответ на неадресный» —
   сохранить это исключение!).

### C. Composer
Мигрированные правила НЕ возвращают готовый текст-констант. Они отдают `data` композеру, он генерит фразу.
9-16 текст-констант этих 3 правил при успехе волны становятся неиспользуемыми (НЕ удалять в этой волне —
пометить «кандидат на удаление в 2b.2», чтобы фолбэк остался цел).

## Четыре правила Кодекса (обязательно)
1. Сначала read-only анализ 3 функций + understand + intent-plan, подтвердить гипотезу чтением.
2. Простота: переиспользовать `understand`/`conversation_intent_plan`, не плодить планнер; диспетчер — одна точка.
3. Хирургия: трогать только маршрут выбора этих 3 правил + новый `rules_engine.py`. 13 остальных safe_template
   не трогать. Перечислить изменённые файлы/строки в отчёте.
4. Цель → проверяемый критерий: каждый из 3 правил + дефект адреса = негатив-тест (см. ниже).

## Тест выхода (без живого прогона, кроме одного точечного)
1. **Офлайн-регрессия:** прогнать выходной гейт + маршрут по СОХРАНЁННЫМ транскриптам обеих половин
   Прогона 1 (58 диалогов). Поведение на 13 НЕмигрированных правилах НЕ меняется (диффа в их ответах 0).
2. **Негатив-тесты (NEG-контроль, обязателен):**
   - teacher: «как зовут преподавателя в Лобне?» → НЕ выдумать ФИО, менеджер уточнит; «кто преподаёт?» →
     регалии из факта, answer_self.
   - recordings: «очные записываете?» → честно нет; «пропустил онлайн» → доступны.
   - contact_address: **Фотон «адрес очных занятий?» → Скорняжный из факта, route=answer_self (НЕ менеджер)** —
     это фикс дефекта Прогона 1; УНПК «где вы?» → площадки + уточнение; адреса Фотон/УНПК НЕ смешивать.
   - Отрицательный контроль безопасности: «адресный факт как ответ на неадресный вопрос» всё ещё блокируется
     (исключение из pipeline:2494 НЕ ослаблено).
3. **538 существующих тестов зелёные** (или ровно те же 9-10 инфра-фейлов из-за отсутствующих артефактов —
   это норма, не регресс).
4. **Один точечный живой прогон** ТОЛЬКО по темам 3 правил (~12-15 диалогов: teacher/recordings/address ×
   2 бренда + повторные ходы), `--parallel 4`, чтобы убедиться, что composer-from-data звучит живо и адрес
   Фотона теперь отвечается. Полный прогон НЕ нужен (генерация изменилась только в 3 правилах).

## Что НЕ трогать
- 13 немигрированных safe_template, gate-логику Шага 1, stable_runtime, KB-снимок, raннер (кроме точечного
  прогона), судью. Текст-константы 3 правил НЕ удалять (фолбэк). Не делать git reset/checkout/clean.

## Отчёт Кодекса (после волны)
- изменённые файлы/строки и почему именно эти;
- дифф офлайн-регрессии (0 на немигрированных — подтвердить);
- результат NEG-тестов (все зелёные, особенно фикс адреса Фотона);
- ревизия коммита для проверки мной в песочнике;
- что осталось кандидатом на 2b.2 (следующие правила + удаление неиспользуемых констант).

Я (Клод) проверю коммит по сырью в песочнике и отрегрейжу точечный прогон, прежде чем двигаться к 2b.2.
