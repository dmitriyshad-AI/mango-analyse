# Стартовый промт — Кодекс main (Волна пересборки 1, ред. 2). 2026-05-30.

> Скопировать в окно Кодекса main.

---

Ты — Кодекс main проекта Mango Analyse, владелец репозитория. Ветка
`codex/m1-intermediate-20260529-final`, HEAD `6de8d52d` (правки S4/C2 уже там; Claude #1 прогнал — 420 smoke зелёные).

**Прочитай по порядку:**
1. `/Users/dmitrijfabarisov/Projects/Mango analyse/CLAUDE.md`.
2. `D1_audit_backlog/TZ_codex_main_volna_peresborki_1_2026-05-30.md` (ред. 2) — проверенные факты внутри, заново не проверять.

**Контекст:** цель — максимальная автономия (бот отвечает сам, вечером; менеджер только по P0), не
скрываем ИИ. Ответы идут клиенту напрямую → щит критичен. Проверено: корень лишних уходов —
централизованный узел дословного покрытия `_claim_supported_by_facts` (7727), обслуживает promise- и
operational-гварды; плюс `_fresh_fact_texts` (7676) обнуляет факты.

**Эта волна = щит + одна главная правка:**
- Часть A — собрать/заморозить щит негативных тестов (бренд / P0 / выдумка / мета / ai-C) + негативный
  контроль под правку покрытия (смягчённая проверка не пропускает реальную выдумку/бренд).
- Часть B — заменить дословное `_claim_supported_by_facts` (вхождение подстроки) на семантическую
  проверку проверяемых элементов (числа/даты/сущности/scope), через существующий верификатор; смягчить
  обнуление `_fresh_fact_texts`.
- Router, инверсия умолчания, `unstated_subject`, `can_autonomously_replace` — НЕ в этой волне.

**Ограничения:**
- тесты не гонять (лимит); написать, прогон отложить; Claude #1 прогонит щит + smoke; в main не
  мержить до зелёного;
- правило #1: узел покрытия и потребителей подтвердить чтением; при расхождении — спросить, не молча;
- НЕ ослаблять блок реальных выдумок / бренда / P0;
- хирургично, без новых тяжёлых абстракций.

**Действуй:** Часть A (щит) → Часть B (семантическое покрытие) → отчёт: файлы/строки, тексты тестов
(вкл. негативный контроль), чем заменена дословная проверка.
