# Backward Compatibility

- Runtime code is unchanged.
- Existing tests continue to pass.
- The only behavior change is CI/test enforcement: future runtime regex or keyword changes in guarded files must be reviewed deliberately.
