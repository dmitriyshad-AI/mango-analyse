# Промт для D4 — ТЗ-21: вливание хвоста 3 439 + пересборка профилей (выдаёт Дмитрий)

Ты — D4. Регрейд прогона М1 «хвост 3 439» пройден (`D1_audit_backlog/existing_clients/REGRADE_tail3439_run_2026-06-13.md`, PASS — болезнь не рецидивирует, выдумок нет). Влей результаты и пересобери профили на чистой истории.

## Блок А. Вливание результатов в каноническую базу

1. Источник: `results_part1..4.jsonl.gz` из пакета `OpenClaw/Actual Mango Tests/analyze_tail_20260612/` (3 439 строк, sha-список `8680b545…`, подсказка `12718ea6…` — сверить перед вливанием).
2. Скрипт `scripts/import_tz19_analyze_tail_results.py` с `--apply` и ОБЯЗАТЕЛЬНЫМ `--backup-to` (бэкап канонической базы до записи, путь в отчёт). Сначала dry-run, числа в отчёт, потом apply.
3. Пишутся только поля вайтлиста выжимки; needs_review сохраняется как есть; blacklist-строки в этот вход не входят (их вливание — отдельно по ТЗ-20).
4. NEG: повторный --apply тех же результатов = 0 изменений (идемпотентность по call_id+версия подсказки); строки вне манифеста отклоняются; счётчики до/после (обновлено / отклонено / совпало) в отчёт; целостность базы (quick_check) после вливания.

## Блок Б. Пересборка профилей на обновлённой базе

1. Новая папка `product_data/customer_profiles/tz21_profiles_after_tail_<дата>/` (ignored); прежние срезы не трогать.
2. Сборщик профилей из main (нормализация дубль-детей включена). Микропроба 3–5 профилей перед полной сборкой.
3. В отчёт: метрики профилей до (tz16_profiles_v7) / после (tz21); заполненность ключевых полей; профили 2+ детей и merge_candidate; 5 обезличенных примеров; сколько звонков зоны теперь на v7-выжимках (должно вырасти на ~3 439).
4. NEG: повторная сборка идемпотентна; ПДн не в git.

## Границы и приёмка

Запись в AMO/Tallanto/CRM запрещена; этап Б карточек не начинать; настройки М1 не менять; blacklist-вливание сюда НЕ включать; фиче-ветка от main, коммиты по блокам, полный pytest, git чистый. Отчёт в `tasks/_done/`: путь бэкапа базы, счётчики вливания и пересборки, метрики до/после, llm_calls_total (ожидание 0 — без LLM), 5 примеров профиля.
