# Регрейд A Волна 1 — арм A-det (пол). По сырью. На d57bff06 (парент 13a243ab). 2026-06-03.

Ревьюер: Клод #1. **Вердикт: PASS (чистый A-det пол).**

## Что построено
- Блок `selling` в планнере (objection/exit_signal/anxiety/unmet_need/readiness), 0 новых LLM-вызовов
  (тест `..._without_extra_call`).
- A-det пост-процессинг `_apply_selling_variants` в rules_engine: на сигнал → продающий вариант денежного
  правила (ценность+оплата+шаг).

## Безопасность подтверждена по сырью (NEG-матрица)
- **Числа из факта:** Фотон installment → «6, 10 или 12 месяцев» из факта; УНПК → «10%»+«14%» (required-проверка);
  price → «29 750/47 250 ₽» из факта. Хардкода чисел нет (`_months_phrase`/`_period_pct` тянут из текста факта).
- **Бренд раздельно:** УНПК-путь только УНПК-условия (помесячно/семестр/год, 10/14%), без рассрочки Фотона.
- **P0 защищён НА ДЕТЕКЦИИ:** `_selling_signals` ставит objection/exit только `if not risk_signals` → реальный
  возврат/жалоба/спор → manager_only, продающий довесок НЕ срабатывает (`..._does_not_override_real_refund_p0`:
  route=manager_only, нет selling-флагов).
- **Trial-grounding:** exit без факта пробного → «пробн» НЕ в тексте (нейтральный шаг); с фактом → грунтован;
  cross-brand → None.
- **Только на сигнал:** обычный «сколько стоит» (objection=none) → без продающего довеска.
- Тесты: 84 целевых, полный 2433 passed / 9 failed (прежние infra .phase12) / 1 skipped.

## Архитектурная заметка (НЕ блокер, на след. шаг)
Детекция сигнала — **детерминированная (regex)**: `_has_price_objection_signal`/`_has_exit_signal`. Для A-det-
пола приемлемо и безопасно, НО keyword глух к подтексту («серьёзная сумма для семьи», «посоветуюсь с мужем»,
«а если не подойдёт»). Это ограничивает охват продажи явными формулировками — ровно то, ради чего планировали
модель. **План:** детекцию перенести в модельное понимание планнера (расширить understand(), 0 новых вызовов)
ОТДЕЛЬНЫМ шагом — после того как решим, нужен ли gen-композер (чтобы не смешивать два изменения и чисто
атрибутировать). Сейчас keyword переиспользуем как константный триггер.

## Итог + рекомендация
A-det пол готов и безопасен. Дальше — НЕ строить gen вслепую, а сперва **прогнать A-det по сырью** на наборе
сценариев возражение/уход (пол уже построен, замер дёшев). Это прямой ответ на вопрос Дмитрия «не топорно ли?»
на РЕАЛЬНОМ тексте. Если читается скриптово → строим gen-сверху (ТЗ
`TZ_stepA_wave1_armB_gen_compare`) + переносим детекцию в модель. Если приемлемо → пол можно катить, gen отложить.
Решает сырьё, не спор.
