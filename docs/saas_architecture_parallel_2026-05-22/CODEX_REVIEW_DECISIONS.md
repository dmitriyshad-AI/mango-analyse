# Решения Codex после ревью Claude

Дата: 2026-05-22

## Итог

Вердикт Codex после независимого ревью: `PASS_WITH_NOTES`.

Пакет можно использовать как архитектурный ориентир, но не как команду “строить SaaS сейчас”.

## Принятые замечания Claude

### 1. Зафиксировать текущую ступень

Принято.

Внесено:

- `README.md` - добавлен раздел “Текущий статус на 2026-05-22”.
- `SAAS_READINESS_GATES.md` - добавлена текущая ступень: `internal_pilot_ready` в процессе закрепления.

### 2. Ограничить storage-slice

Принято.

Внесено:

- `DIALOGUE_DECISION_STORAGE_SPEC.md` - явно указано, что сейчас реализуется только минимальный slice из раздела 8.

### 3. Добавить P0-протокол

Принято.

Внесено:

- создан `P0_INCIDENT_PROTOCOL.md`.

### 4. Не выделять ядро слишком рано

Принято.

Внесено:

- `IMPLEMENTATION_PHASES.md` - добавлено правило: не начинать фазу отделения ядра от Telegram до 5-7 рабочих дней стабильного пилота без P0.

### 5. Пометить будущий SaaS-контракт как не текущую реализацию

Принято.

Внесено:

- `TENANT_BRAND_ISOLATION_CONTRACT.md` - раздел будущего SaaS-контракта помечен как задача не раньше Фазы 8.

## Частично принято

### Обновить `CLAUDE.md` под актуальную базу знаний

Идея правильная, но в этом блоке не внедрялась, потому что задача ограничена архитектурным пакетом в `docs/saas_architecture_parallel_2026-05-22/`.

Компенсация:

- в `README.md` добавлено правило: актуальную базу знаний проверять по текущему manifest и bot pack, а не по старым документам.

Отдельная будущая задача: обновить общий проектный onboarding, если `CLAUDE.md` действительно указывает на устаревшую KB.

## Отклонённые замечания

Нет. Все замечания Claude либо приняты, либо отложены как отдельная задача вне границ этого пакета.

## Следующее практическое действие

Ближайший инженерный шаг после этого пакета:

1. persistent pilot storage для Telegram-диалогов;
2. daily feedback report;
3. P0-регистр и связка “ошибка -> тест/gate/KB”.

Выделение общего ядра `AiEmployeeCore` делать только после стабилизации пилота.
