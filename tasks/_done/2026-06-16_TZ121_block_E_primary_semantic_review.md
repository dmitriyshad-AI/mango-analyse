# TZ-121 Block E Primary Semantic Review

## Verdict

`PASS_WITH_NOTES`

## Что прошло

- Foton-морфология на реальном follow-up срезе восстановлена: `9/9` ручных `foton->unknown` false-negative теперь дают `foton`.
- Cross-brand / fail-closed случаи не ослаблены: `8/8` expected fail-closed остались `unknown`.
- Небрендовые слова `фотончики` и `Фотоний` не дают бренд Foton.
- Изменение ограничено офлайн customer timeline/import/triage; live-бот, AMO/Tallanto/CRM и записи в БД не затронуты.

## Неблокирующие риски

- Один старый `unpk->unknown` false-negative из follow-up не исправлен: это не цель блока E после регрейда, но его нельзя считать закрытым.
- Новый полевой приоритет Foton над одиночными `МФТИ`-следами безопасен для проверенного среза, но требует повторного контроля на будущих больших пересборках профилей.

## Новые регрессии / gate

- Добавлены тесты на:
  - Foton с побочным `МФТИ` в другом поле;
  - `Фотон и УНПК` / `Фотон МФТИ` как fail-closed;
  - `фотончики` и `Фотоний` как небрендовые формы;
  - follow-up runner без записи raw-фрагментов.

## Следующее действие

Переходить к блоку C только в shadow-режиме; primary для C не включать до регрейда.
