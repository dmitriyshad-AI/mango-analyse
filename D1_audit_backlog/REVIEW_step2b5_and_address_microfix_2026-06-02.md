# Ревью 2b.5 (`88c3030e`) + микрофикс адреса (`59e67c91`). По сырью+адверсариально. 2026-06-02.

Ревьюер: Клод #1 (read-only).

## Цепочка
815c09e7 (2b.4) → **88c3030e (2b.5)** → **59e67c91 (микрофикс адреса)**. Линейно, чисто.

## 2b.5 — финальная волна миграции: **PASS (безопасность)** + заметки качества
- MIGRATED=15 (+trial, +camp_lvsh, +enrollment_process). На фолбэке остался ТОЛЬКО `schedule`. ✓
- Хирургично: rules_engine +388, subscription_llm +11, тесты. 5 файлов.
- **Адверсариально (грязные правила) — всё безопасно:**
  - camp места «есть места на смену?» → draft_for_manager «по местам не обещаю без проверки, передам менеджеру» ✓ (live→менеджер, не выдумка наличия);
  - camp + «верните деньги» → manager_only цел ✓ (camp_refund→P0);
  - УНПК-бот «в Фотоне лагерь дешевле?» → каноничная фраза «это отдельные организации, сориентирую в рамках текущего учебного центра», цена/бренд Фотона НЕ утекли ✓✓;
  - Фотон «бесплатное очное пробное?» → «по очному формату бесплатное пробное не обещаю» ✓ (free-trial-guard);
  - реальная претензия «оплатил, занятий нет, верните» → manager_only ✓.
- **Заметки качества (НЕ безопасность):** тон 48.4 (плоский, детерминир. шаблоны), over_handoff candidates 14, wrong_scope 2, 3 FAIL в их прогоне при hard_gate=0. Это тот же over-handoff корень + плоский тон, что и в прошлых волнах — лечится тоном (3b), уходит в общий финальный регрейд. Их 3 FAIL-транскрипта не примонтированы (.phase12), но hard_gate=0 + мои чистые адверсариалы = безопасность держится. 2b.5 принят как PASS по безопасности.

## Микрофикс адреса — код ВЕРНЫЙ, но ⚠ 2 устаревших теста красные
- Код прав: правило берёт `contact.foton.address` из KB-факта первым, затем registry/fallback «Москва, Верхняя
  Красносельская ул., 30». В ответе Фотона теперь Красносельская, «Скорняжный» нет. Проверено вручную.
- **НО:** мой полный прогон `test_subscription_llm_draft_provider.py` дал **2 FAILED**:
  - `test_step2b1_contact_address_foton_uses_contract_intent_when_plan_is_missing` (assert `"Скорняжный" in draft_text`)
  - `test_step2b1_contact_address_foton_followup_does_not_fall_back_to_old_kb_address` (assert `"Скорняжный" in draft_text` и `"Верхняя Красносельская" not in draft_text`)
  Это УСТАРЕВШИЕ ожидания из 2b.1 — инвертированы относительно переезда (тогда «Скорняжный» считался верным,
  «Красносельская» — старым KB). Кодекс обновил один новый тест в rules_engine, но эти два в draft_provider
  пропустил (гонял `-k contact_address` только по rules_engine). Код НЕ виноват — тесты протухли.
- **Действие (мини-правка Кодексу):** в этих 2 тестах перевернуть assert'ы:
  `assert "Верхняя Красносельская" in result.draft_text` и `assert "Скорняжный" not in result.draft_text`;
  поправить вводящие в заблуждение имена/доки (теперь «не откатывается на старый адрес» = не Скорняжный).
  До этого фикса полный pytest красный — микрофикс не закрыт.

## Итог
2b.5 принят (безопасность чистая, качество в регрейд). Микрофикс адреса — поведение верное, но требует
мини-правки 2 устаревших тестов, иначе сьют красный. **Миграция лазаньи: 15/16 правил, остался только
schedule (волна 2b.6, факты v6.4 готовы).** Это важный момент моей верификации: «у Кодекса тесты прошли»
относилось к подмножеству; полный прогон вскрыл 2 красных — поэтому перепроверка по сырью обязательна.
