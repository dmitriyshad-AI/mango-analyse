# Backward Compatibility

- Existing report schema is extended with additive fields only.
- Runtime behavior is unchanged.
- Existing direct-path/provider code is unchanged.
- Active readiness remains `no_go`.
- Full candidate text is not exported, only hash/length/status.
