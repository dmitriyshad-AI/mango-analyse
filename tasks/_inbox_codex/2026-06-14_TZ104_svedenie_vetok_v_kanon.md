# Техническое задание ТЗ-104 — сведение готовых веток в канон (main)

- **Дата:** 2026-06-14. Заказчик: Дмитрий. Постановщик: Claude #1. Исполнитель: D1 (главный Кодекс, владеет боевым кодом и git).
- **Опора:** регрейды ТЗ-102 (PASS) и Graphify ТЗ-25 (PASS по качеству/безопасности); ревизия ТЗ-101.

## Бизнес-смысл (зачем)
Сейчас рабочий код «располз» по веткам: главная папка сидит на рабочей ветке, а готовые куски (детекция срочного обращения ТЗ-102, слой действий ТЗ-26, карта кода Graphify) не сведены в одну канон-ветку `main` (главную, эталонную). Из-за этого замеры рискуют идти не на том коде, а аудиторы — читать не то дерево (так уже было). Сведение в канон даёт чистую общую базу, на которой честно меряются и срочные обращения, и судья действий. Поведение клиента это НЕ меняет: все новые куски под флагами, по умолчанию выключены.

## Что важно знать про состояние (сверено по git 14.06)
- `main` = `ef230d3`.
- `codex/tz102-model-p0-direct` = `dbcb4a1`, на 2 коммита впереди main, и **уже содержит ТЗ-26** (`bd2e49c`, слой действий). То есть влив ТЗ-102 приносит и ТЗ-26 разом.
- `codex/tz25-graphify-structural` = `c5cd89a`, на 3 коммита впереди main (в основном новые файлы инструмента карты — пересекается с боевым кодом мало).
- Персоны симулятора (`codex/d6-autonomy-sim`) Кодекс-2 СЕЙЧАС правит (словарь действий `book_trial`→`send_materials` в рамках ТЗ-103). Их в этот влив НЕ берём — отдельной фазой после Кодекса-2.

## Этап 1 (сейчас) — влить боевой код и карту, оставить флаги OFF
1. Перейти на `main`, убедиться, что дерево чистое от ИЗМЕНЕНИЙ кода (untracked-файлы очереди и аудита не трогать — см. ниже).
2. Влить `codex/tz102-model-p0-direct` в `main` (приносит ТЗ-102 + ТЗ-26). После влива — полный `pytest` (ожидаем те же 3209 passed, 2 skipped).
3. Влить `codex/tz25-graphify-structural` в `main`. После влива — полный `pytest` ещё раз.
4. **Политика конфликтов: при любом неочевидном конфликте СТОП и доклад мне с текстом конфликта. Не разрешать догадкой.** Тривиальные (разные файлы / явное добавление) — разрешить, отметить в отчёте какие.
5. Пересобрать карту Graphify на новой вершине `main` (закрыть «карта устарела» из регрейда Graphify) штатной командой пересборки. Если окружение карты не поднимается у D1 — отметить и передать Кодексу-2, не блокировать влив.
6. Запушить `main` (появилась копия на сервере — этого сейчас нет).

## Чего НЕ делать на Этапе 1
- **Не вливать персоны/симулятор** (`d6-autonomy-sim`) — они в работе у Кодекса-2.
- **Не коммитить untracked-файлы** очереди и аудита (`tasks/_inbox_codex/*`, `D1_audit_backlog/*`) — это служебные папки, по нашему правилу не в git. Оставить как есть.
- **Не трогать исторические ветки** (waves, tz14–tz24, d1-humanity и пр.) — это история, не канон.
- **Флаги не включать**: `TELEGRAM_DIRECT_PATH_MODEL_P0` и `TELEGRAM_DEAL_ACTION_DECISION` остаются OFF и НЕ добавляются в `pilot_gold_v1`.

## Этап 2 (после Кодекса-2) — влить персоны
Когда Кодекс-2 закончит правку словаря персон (ТЗ-103, шаг 0) и закоммитит — влить его ветку с персонами в `main` отдельным шагом, снова полный `pytest`. Промт на Этап 2 дам тогда.

## Приёмка
- `main` содержит ТЗ-102 + ТЗ-26 + Graphify; полный `pytest` зелёный; `main` запушен.
- Флаги OFF, паритет боевого поведения сохранён.
- Карта Graphify пересобрана на вершине `main` (или явно передано Кодексу-2 с причиной).
- Отчёт в `_done`: какие ветки влиты, были ли конфликты и как разрешены, хэш новой вершины `main`. Ревью — Claude #1.
