# Semantic review

Verdict: `PASS_WITH_NOTES`

## What passed

Задача решает исходный риск: модель больше не получает датированную историю без признака, подтверждён ли следующий шаг.

Статус проброшен безопасно:

- `active`;
- `needs_manager_review`;
- `empty`.

Текст спорного шага из `metadata.next_step.display_text/action` не передаётся в prompt.

Active-only контекст не получает хедж-фразу `по прежним заметкам, актуальность уточню`, поэтому хорошие send-as-is кейсы не должны деградировать только из-за наличия даты.

Цены, даты и условия оставлены за блоком «Факты по вашему вопросу».

## Blocking issues

Нет.

## Non-blocking risks

Это инструкция для модели, не жёсткий переписчик ответа. Реальное снижение ошибки нужно подтвердить OFF/ON замером на 14/16/18 и проверкой active-кейсов 03/10/11/17.

## Missing checks

Не запускался D7-замер с моделью. Live-бот и боевые системы не трогались.

## Required regression checks

Добавлены тесты:

- статус доходит через runtime builder;
- статус доходит через read_api;
- измерительный контур получает `next_step_status`;
- prompt маркирует `needs_manager_review`;
- prompt не добавляет хедж-фразу на active-only.

## Recommended next action

Отдать пакет Claude #1 на регрейд, затем делать D7 OFF/ON переизмерение.
