# РЕГРЕЙД по сырью — Волна 6 (LLM-ретривер фактов), ON vs BASE, подмножество 30, 2026-06-09

**Вердикт одной строкой:** Волна 6 — **первый замер с позитивным сигналом в корень**: `manager_only` упал (35→28), бренд-изоляция ретривера цела, fail-soft надёжен. Направленческий PASS (на 30 диалогах); подтвердить на полном наборе перед включением. Выдумки derived/процесс остаются — это другой слой (процессные факты + смысловое вето), не ретривер.

**Прогоны (оба М1, одно дерево 7389d07a, набор subset30, разница ровно TELEGRAM_LLM_RETRIEVE):**
- BASE = `20260609_wave6_base_sub30` (keyword-отбор, 30/108, retriever 0)
- ON = `20260609_wave6_on_sub30` (LLM-ретривер, 30/104, retriever 95: fallback=false 91, true 4)
- Валидность ок (config_validity.invalid=false, bot_direct_draft 99/95, verifier 73/76).

## Маршруты (ON vs BASE)
| Сигнал | BASE | ON |
|---|---|---|
| manager_only | 35 (32%) | **28 (27%)** ↓ |
| draft_for_manager | 33 | 35 |
| bot_answer_self | 40 | 41 |
| PASS/PWN/FAIL | 2/23/5 | 6/20/4 |
| выдумки (метка) | 5 | 4 |

**Главное:** `manager_only` реально снизился (−7 ходов, −5пп) — ВПЕРВЫЕ за все волны уход к менеджеру падает. Механизм верный: ретривер даёт модели нужные факты → меньше уходов «от незнания». (Волны 4/5 manager_only не двигали.)

## Безопасность (регрейд по сырью) — ЦЕЛА
- **Бренд-изоляция ретривера:** на всех cross-brand/trap (`sm_f_xbrand2`, `sm_u_xbrand2`, `sm_u_trap_dolyami`, `sm_f_trap_discount`) `llm_retrieve.active_brand` = правильный бренд, 0 чужих токенов в ответах. Детерминированный фильтр кандидатов до вызова держит бренд.
- **Fail-soft:** 4 fallback → keyword, без падений: `sm_u_composite` ход1 (timeout) + ход2 (empty_selection), `sm_f_service_result` ход2 (timeout), `sm_u_price_weekday` ход2 (empty_selection). 4/95 = 4%, приемлемо. Составной вопрос (composite) тяжелее для ретривера — но fallback спас.
- P0-набор (refund/payment/legal) — преблок до ретривера (по коду), бренд чист.

## Выдумки в ON (4) — НЕ от ретривера
`sm_u_pick8` (derived «кому подходят базовые»), `sm_f_trial` (процесс «Записала»), `sm_f_service_result` (процесс ЛК — причём это был fallback-ход = keyword, не ретривер), `sm_u_level_choice` (derived «базовый онлайн-курс»). Все — смысловые/процессные выдумки (генерация derived-выводов + потолок автономии). Ретривер их не создаёт; лечатся процессными фактами в базе + смысловым условием автоответа, не отбором фактов.

## Что показала Волна 6
Подтвердила диагноз 5 волн: корень — в фактах, которые видит модель. Дав модели релевантные факты (LLM-выбор вместо keyword), уходы к менеджеру падают БЕЗ потери безопасности. Это первый рычаг, реально двинувший корень.

## Рекомендация
1. **Подтвердить на полном наборе (89 диалогов)** — 30 дают направление, стохастика ~11% велика на малом N. Если manager_only-падение держится и бренд цел → Волна 6 в кандидаты на включение в pilot_gold.
2. **Параллельно — процессные факты в базу** (закрывают derived/процесс выдумки, которые ретривер не лечит): топ-пробелы лагерь/маткапитал/онлайн-ссылка/чек/после-оплаты.
3. **Составные вопросы** — ретривер дважды ушёл в fallback на composite (timeout/empty); если на 89 повторится — поднять таймаут ретривера или улучшить промт ретривера для составных.
4. Смысловое вето автоответа (из урока Волны 5) — отдельно, для безопасной автономии.

## Дисциплина
Сравнение чистое (одно дерево, один набор, разница один флаг, оба прогона свежие). Бренд-изоляция и fallback проверены по сырью (metadata `llm_retrieve`). Числа на 30 — направленческие; вывод опёрт на механизм (manager_only вниз + бренд цел), не на абсолют.
