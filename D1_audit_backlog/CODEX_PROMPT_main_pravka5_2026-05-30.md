# Стартовый промт — MAIN Кодекс (правка 5: семантический критик). 2026-05-30.

> Скопировать в окно MAIN Кодекса. Это полуфабрикат — проверить и перенести, минимум работы.

---

Ты — MAIN Кодекс, репозиторий, HEAD `17a82794`.

**Прочитай:** `D1_audit_backlog/TZ_pravka5_semantic_critic_2026-05-30.md` — там готовые куски кода.

**Суть:** прогон 4b дал 14 семантических выдумок клиенту (онлайн вместо лагеря, 9 класс вместо 10) —
числовой verifier их не ловит. Усиливаем СУЩЕСТВУЮЩУЮ LLM-проверку верности по теме/scope. НЕ новый
слой.

**Две точечные правки (в ТЗ — готовый текст):**
1. `build_faithfulness_prompt` (dialogue_contract_pipeline.py ~575): заменить промпт на усиленный
   (добавлены вердикты `wrong_scope` / `contradicted`, требование совпадения темы, запрет подтверждать
   claim одной темы фактом другой).
2. `check_claim_faithfulness` (~636): добавить `wrong_scope`, `contradicted` в множество вердиктов,
   попадающих в `unsupported`. Одна строка.

**НЕ трогать:** repair-петлю (она уже использует `unsupported`), `claim_anchors_supported_by_fact`,
decide_route / 4b / маршрут / щит.

**Тесты добавить:** wrong_scope (онлайн про лагерь → draft_for_manager), contradicted (9 vs 10 класс),
негативный контроль (правильная тема → supported не сломать).

**Ограничения:** тесты не гонять (лимит) — Claude #1 прогонит faithfulness + smoke; в main не мержить
до зелёного. Правило #1: промпт и номер строки правки B подтвердить чтением (могли сместиться).
Отчёт: что изменено, тексты тестов.