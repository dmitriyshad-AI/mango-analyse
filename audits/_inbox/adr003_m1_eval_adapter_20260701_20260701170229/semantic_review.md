# Semantic Review

This block does not change customer-facing bot behavior. It only creates a reproducible measurement bundle.

Semantic caveat: the bundle includes baseline route/text and safety flags, but Wappi cases are not manually labelled with `expected_frame`. Therefore it must not be used to claim that SemanticFrame understands the cases correctly until a gold labelling pass is added.
