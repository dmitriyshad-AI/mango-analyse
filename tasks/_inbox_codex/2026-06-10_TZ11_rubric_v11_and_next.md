# ТЗ-11 (ФИНАЛ после аудита по сырью): рубрика v1.1, включение, pii-диалог, судья v9.1, пере-пара В6

Основание: ночные пары + перепроверка 6 судейских меток + аудит вторым архитектором ПО СЫРЬЮ. Ключевое уточнение аудита: деривативы («16,4%», «181 740 ₽») НЕ прошли мимо гейта — гейт сработал (`fact_grounding`→downgrade_keep_text, маршрут понижен, текст сохранён менеджеру по дизайну; `unsupported_product_number` и не мог сработать: free-ветка не в профиле, NUMBER_GATE_SCOPE_AWARE в профиле инертен — живёт только внутри неактивной free-ветки). Жёсткие В ОТПРАВЛЯЕМОМ = 0. Задача — классифицировать дериватив-класс адресно и погасить генерацию, не «чинить дыру», которой нет.

**Календарь (пилот блокируют: ТЗ-10 → части 1-2-3 → live-проба; часть 4 настоятельно до live):**
ТЗ-10 (KB r3, фиксы лжи — ДО smoke18, чтобы не перегонять) → части 1-2 → часть 3 (Дмитрий сказал ДА 10.06) → части 4-5 параллельно → live-проба (токен) → часть 6. **Решение Дмитрия 10.06: ТЗ-9 ОТЛОЖЕНО ради скорости** — пере-пара части 6 закладывается ВРУЧНУЮ; обязательная одна строка дисциплины: .ready-маркер = sha256 ОТ task.yaml (не от набора!), перепроверить руками перед закладкой. Новая гипотеза от Дмитрия: ТЗ-12 «калькулятор скидок кодом» (модель выбирает правило/параметры, код считает по формализованным правилам базы, результат с маркером) — проектировать ПОСЛЕ старта пилота по данным журнала.

## Часть 1. Рубрика v1.1 — анти-дериватив строка (тот же флаг TELEGRAM_ROUTE_RUBRIC)

```text
Запрещено вычислять новые числа: не выводи проценты, скидки, суммы и итоги из других цен («за два предмета выйдет…», «это получается N%»). Называй только числа, которые есть в фактах дословно или назвал сам клиент. Не подтверждай расчёты клиента («у меня выходит N, верно?») — точный расчёт и итог по нескольким предметам или со скидками подтвердит менеджер.
Избегай сравнительных оценок форматов/программ без факта («очно удобнее…») — вместо этого предложи признак выбора вопросом.
```
Golden ON пере-снять В ЭТОМ коммите (не смешивать с диффом включения). NEG: «семестр 49 000 и год 82 000 рядом» — легально, без отказа и findings (positive-control); «выходит 16,4%» — дериватив.

## Часть 2. Выделенный гейт-код `derived_product_number` (классификация, не блок)

Деньги/процент в тексте, отсутствующие в фактических поверхностях (детерминированная детекция существует — number_audit уже метит такие как money/no_match): новый код findings `derived_product_number`, политика `downgrade_keep_text`; в manager_checklist — адресная строка с числом («Проверьте 181 740 ₽ — вычислено ботом, в прайсе нет») вместо генерической. Код НЕ добавлять в `DIRECT_PATH_REPLACE_TEXT_GATE_CODES` (текст черновика не заменять фолбэком). NEG: дериватив в draft → finding+checklist-строка; число дословно из факта или из реплики клиента → без finding. Код нужен и метрикам части 6 (ведро fact_grounding шумное: 44 vs 39).

## Часть 3. Включение рубрики в профиль (ПОСЛЕ «да» Дмитрия)

`TELEGRAM_ROUTE_RUBRIC` в `DIRECT_PATH_PILOT_PROFILE_DEFAULT_ON_FLAGS` отдельным коммитом после 1-2. Прогнать smoke18 (перевести сценарии в jsonl: 18 штук, №12 — в переформулировке из аддендума «Если вдруг передумаем после оплаты — деньги вернёте?»; №8 проверяет анти-дериватив, №17 — pii части 4) + полный pytest; регрейд smoke18 — Claude.

## Часть 4. ПЕРЕРЕШЕНО Дмитрием 2026-06-10: не запрет, а ПРОВЕРКА ВЕРНОСТИ имени

Новая постановка: имя ребёнка, которое клиент САМ назвал в текущем диалоге, в ответах РАЗРЕШЕНО и приветствуется (персонализация — продажный плюс). Защита переворачивается: маскируется только имя, которого клиент в этом диалоге НЕ называл (галлюцинация / чужое имя / имя из памяти — последнее уже кроет П1). Механика: детерминированная сверка «личные имена в draft_text ⊆ имена из реплик клиента этого диалога» (с учётом склонений, как в П1); несовпадение → маска + имя в manager_checklist + finding. Строку миссии «без дословного повтора этих данных» смягчить: «имя ребёнка можно использовать, если клиент сам его назвал; телефон/ФИО целиком не дублируй». NEG: «клиент назвал Кирилл → "Записала Кирилла" проходит»; «клиент называл Кирилла, в ответе Артём → маска+finding»; «имя из памяти, не названное в диалоге → маска (П1)». Остальные страховки ниже (точечность замены, манифест менеджеру, омонимы) — в силе для случая маскирования.

### Прежняя постановка (для истории, НЕ исполнять):

Диагностика-сначала: механика окна существует (`_client_pii_echo_context` читает recent_turns) — выяснить, почему не покрыла night_lead (флаг? пустой memory view? маркеры) — ПЕРЕД новым слоем. Затем:
- Маскировать ТОЛЬКО `draft_text` (клиентский слой; после одобрения уходит клиенту). `manager_checklist`/`missing_facts`/слоты/amo-нагрузку НЕ трогать; при маскировании ДОБАВЛЯТЬ имя в manager_checklist (заявка остаётся самодостаточной менеджеру).
- Для диалоговых имён — точечная замена/перефраз («записала вас»), полнотекстовый replacement канонической фразой для ЭТОГО источника запрещён; replacement маршрутно-зависим: в self — без «менеджер свяжется» (не реинжектить уходы, не ломать метрику 2.7%).
- NEG: имя клиента из диалога не повторяется в draft_text; имя сохранено в manager_checklist; self после маскирования без «менеджер свяжется»; false-positive на имена-омонимы (Вера/Лев/Роман) — не маскировать вне явного именного контекста.

## Часть 5. Судья v9 → v9.1 (две калибровки)

(а) «менеджер свяжется/вернётся с ответом» БЕЗ срока — не made_a_promise; формулировать в judge-промпте «маршрутную уместность фразы меряет греп-метрика, не судья» (НЕ «политика разрешает» — иначе судья перестанет метить и варианты СО сроком). (б) Существование продукта для класса N проверять и по ценовым/продуктовым фактам, не только по предметной строке (ложный FAIL pick2kl). Константа `JUDGE_PROMPT_VERSION` → `judge_v9_1_*`, normalize принимает старый алиас; старые прогоны не пере-судить.

## Часть 6. Пере-пара «рубрика-ON vs рубрика-ON + Волна 6» (М1, после ТЗ-9 пп.2+4)

Один бандл нового дерева (bundle_sha256 в task.yaml, .ready = sha ОТ task.yaml — хелпером), разница один флаг `TELEGRAM_LLM_RETRIEVE`, судья v9.1 оба плеча. **Составной первичный критерий (предзарегистрирован):** (manager_only ↓≥15% отн. И ≥8 ходов) ИЛИ (доля черновиков с уходом-в-тексте ↓≥20% отн. — ночной эффект В6 был именно там, −33%, база 181-209 ходов крепче). Guardrails: 0 новых жёстких, fallback ≤5%, `derived_product_number` не растут. Self-долю репортить вторичной (гипотеза синергии), не критерием. Регрейд — Claude.

## Вопрос Дмитрию (вне ТЗ, для ТЗ-10/r3)

База рассогласована: прайс Фотона имеет «онлайн 1-4 класс» (19 000 ₽/семестр), предметная строка — «математика онлайн с 3-го». Что входит в онлайн 1-4 (какие предметы)? → дополнить subjects_by_class.

## Приёмка

Полный pytest; NEG частей 1-2-4 + positive-control; golden v1.1; smoke18-регрейд PASS (Claude); календарь соблюдён; отчёт в _done по частям с хешами.
