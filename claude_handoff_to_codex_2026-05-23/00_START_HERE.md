# START HERE — единая папка передачи Кодексу (2026-05-23)

Это один вход для всего, что Claude передаёт Кодексу по улучшению Telegram-ботов Фотона и УНПК.
**Статус: пока ничего не внедрено. Это план + готовые материалы + инструкции.** Реализация — после
подтверждения Дмитрия.

## Порядок чтения

1. **`MASTER_IMPLEMENTATION_ORDER.md`** — главный документ. Единый порядок всего, что накопилось
   (три ТЗ Кодекса + мой корпус + правки пакета), по фазам 0→3, с зависимостями. Начни отсюда.
2. **`AI_DISCLOSURE_POLICY_2026-05-23.md`** — канон политики «ты бот?» (вариант C, утв. Дмитрием).
   Применяется синхронно во всех местах в Фазе 0.
3. **`TZ_FOR_CODEX_apply_claude_corpus_and_fixes_2026-05-23.md`** — что взять из `01_corpus_files/`, чем
   заменить устаревшие копии в pack, что переписать в твоих файлах (метрика + гейт `revealed_ai` и т.д.).
4. **`REVIEW_sales_books_playbook_TZ.md`** — моё ревью ТЗ по книгам продаж + ответы на 5 вопросов и
   условия внедрения (дедуп, метрики, очередь).
5. **`01_corpus_files/`** — 5 валидированных файлов корпуса (тон/поведение, с политикой C).
6. **`README_MANIFEST.md`** — контрольные суммы файлов корпуса.

## Что это меняет (суть в 5 строках)

- Заменить устаревший снимок few-shot/gold в pack на мой свежий (там старый шаблонный тон).
- Внедрить политику «ты бот?» (вариант C) синхронно — иначе тест штрафует правильное поведение.
- Пересобрать загрязнённый holdout (47/83 — клоны тюнинга) и добавить негативы в p0-регрессию.
- Playbook по книгам — хорошая идея, но строить ПОСЛЕ фундамента и без дублирования корпуса/звонков.
- Сначала довести до пользы Фазу 0-1 и замерить, потом playbook (не строить всё впрок).

## Источники правды (чтобы не было трёх параллельных)

- Факты (цены/даты/скидки) — только актуальная КБ (`client-safe` + `valid_until`).
- Тон и поведение — мой корпус (`01_corpus_files/`) + playbook звонков (`03_calls`).
- Книги продаж — только каркас (рубрики/принципы), формулировки не выдумывать, брать из корпуса/звонков.
- Политика «ты бот?» — `AI_DISCLOSURE_POLICY_2026-05-23.md`.

## Жёсткие рамки (не нарушать)

Один бренд в ответе (Фотон не называет УНПК и наоборот); только текущие факты без будущих дат; P0 —
тёплый твёрдый хендофф без сбора данных; не грузить PII-CSV в промпты; не тюнить по holdout.
