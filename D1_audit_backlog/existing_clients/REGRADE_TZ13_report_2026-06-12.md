# Регрейд отчёта ТЗ-13 по сырью

Дата: 2026-06-12. Автор: Claude (архитектор трека). Сырьё: `product_data/customer_profiles/tz12_working_batch3/customer_profiles.sqlite` (91 МБ) и `customer_timeline.sqlite` (2,5 ГБ), запросы read-only (immutable). Отчёт исполнителя: `tasks/_done/2026-06-11_TZ13_D4_merge_and_followups_report.md`.

## Вердикт: PASS_WITH_NOTES

## Подтверждено запросами по сырью

1. Дубль-дети: профилей с 2+ детьми после пересборки = **4 471** (заявлено 4 471); маркеров-групп `child_slot_merge_candidate` = **817** (заявлено 817); слотов в маркерах суммарно = **2 077** (заявлено 2 077). Всего профилей 18 399, различных детских ключей 4 499.
2. Качество слияния: у всех 817 групп причина `normalized_child_name_match`; групп, сливающих БОЛЬШЕ одного нормализованного имени, — **0** (то есть «Тимур»+«Олег» не сливаются — NEG держится структурно).
3. Ретро-бренды: событий каналов ровно **46 984** = Telegram 6 950 + WhatsApp 40 034 (заявлено 6 950/40 034); у всех `metadata.brand=unpk`, событий с brand=unknown/пусто — **0**; куски для бота — те же 46 984 с брендом; итого 93 968 строк (заявлено 93 968). У WhatsApp `channel_shared=true` проставлен и в metadata, и тегом.

## Принято по отчёту (по сырью не проверяемо)

- Цифра «до» = 4 640: база пересобрана, прежнего состояния нет (build 22:53 поверх 17:43).
- Идемпотентность повторного `--apply` (changed=0): повтор — пишущая операция, сам не запускал.
- Зелёный pytest и чистый git — проверки исполнителя.

## Замечания (не блокеры)

1. **Формат тега бренда у кусков для бота различается**: Telegram — `brand:unpk`, WhatsApp — голый `unpk`. В `metadata.brand` всё единообразно, поэтому не блокер; но если выборка контекста пойдёт по тегу `brand:*`, WhatsApp выпадет. Рекомендация: микроправка-унификация тега (D4, довесок, с NEG «другие теги не тронуты»).
2. WhatsApp-события хранятся с `event_type=web_chat_message` (канал различим только по `metadata.channel=whatsapp`) — учитывать в будущих запросах/фильтрах, в т.ч. при пересборке профилей на v7.
