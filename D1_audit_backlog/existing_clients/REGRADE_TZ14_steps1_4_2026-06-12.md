# Регрейд ТЗ-14 (Шаги 1–4) по сырью

Дата: 2026-06-12. Автор: Claude (архитектор трека). Отчёт исполнителя: `tasks/_done/2026-06-12_TZ14_D4_steps1_4_final_report.md`. Проверено по сырью: git-дерево, CSV/журналы/summary всех шагов, код live_card, независимый смоук коннектора.

## Вердикт: содержание Шагов 1–4 — PASS_WITH_NOTES; МЕРЖ В ТЕКУЩЕМ ВИДЕ — БЛОКИРОВАН (чужие коммиты на ветке)

## Блокер мержа (не содержательный)

Ветка `codex/tz14-d4-crm-amo-tallanto` создана НЕ от main: под коммитами ТЗ-14 лежат два чужих коммита другого трека — `ff51bc06` (Wappi Max в draft_loop, 13:21) и `ea2c990a` (auto resolver draft_loop + отчёт «ТЗ-15», 15:25). Оба меняют запретные для D4 файлы живого пилотного контура (`draft_loop.py`, `amo_wappi_*`). Сами Шаги 1–4 этих файлов НЕ трогают (проверено по пофайловым диффам коммитов ТЗ-14), но мерж ветки как есть утащит чужой код в main в обход своего порядка приёмки. В отчёте D4 эти два коммита не упомянуты (упомянуты только незакоммиченные правки в дереве) — пробел отчёта.

Рекомендация: D4 пересаживает 6 коммитов ТЗ-14 (`1a8f149f`, `c7c52bb8`, `1d3ebd12`, `a6162d7e` + 2 отчётных) на чистую ветку от main и мержит её; судьбу `ff51bc06`/`ea2c990a` и «ТЗ-15» решает владелец того трека (главный диалог/Дмитрий) отдельно.

## Подтверждено по сырью

1. **Шаг 1**: счётчики совпадают с сырьём — контактов 13 895 (= строк jsonl), сделок 8 004; CSV: дубль-группы 29 (24 живые + 5 архив), общие телефоны 113, многодетные 537, неоднозначные 6; сумма contact_count = 60 = заявленным строкам. Read-only, пауза 1,05 с.
2. **Шаг 2**: 3 черновика (2 семьи + 1 общий телефон) в review-пакете; тексты безопасны — телефонов цифрами нет, имён на общем телефоне нет, оба бренда в одном тексте не встречаются, формат «Уточните, о ком разговор» соблюдён; журнал ровно 3 записи; повторный прогон идемпотентен (новых 0, скипов 3); флаги записи `enable_amo_notes=false`, `enable_amo_tasks=false`.
3. **Шаг 3А**: 20/20 карточек, у всех 6 строк проекта (Ученик/Семья/Договорённость/Возражения/Tallanto-статус/ИИ); сырой телефон 0, смешение брендов в строке 0, превышений длины 0, отметка ИИ везде; поле 2363933 textarea подтверждено в field_check; dry-run, записи нет.
4. **Шаг 4**: стоп-правила в коде на месте (shd → skip, 2+ контакта → нет карточки, notactive фильтруется, 10000 мест = безлимит не показывается, сверка active_brand); ФИО преподавателя покрыто тестом (`teacher_name` не попадает в карточку); существующие ключи ответа сохранены.
5. **Независимый смоук коннектора** (мой, через env-файл в Foton): amo_api_get users — PASS, токен жив после всех работ дня.

## Принято по отчёту (сам не гонял)

Полный pytest 3067 passed; целевые наборы шагов.

## Замечания (не блокеры)

1. `page_limit=10` вместо 50 из наряда (вероятно потолок коннектора) — в ~5 раз больше запросов; уточнить у D4 и зафиксировать причину.
2. Живых дублей 24 против исторической оценки ~150–250: определение Шага 1 строже (телефон + совпадение имени ученика + активная сделка/учёба); телефонов с 2+ контактами — 689. Не ошибка, но учитывать при ревью CSV.
3. Повторный (идемпотентный) прогон Шага 2 перезаписал summary/CSV первого прогона — тексты уцелели только в review-пакете и журнале; впредь повтор писать в отдельную папку.
4. Косметика текстов: артефакт «6 класс кл» (задвоение «класс») в карточках и примечаниях — поправить до этапа Б.
