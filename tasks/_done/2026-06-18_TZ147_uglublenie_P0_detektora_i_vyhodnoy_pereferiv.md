> DONE 2026-06-18 18:23 | ветка codex/tz147-p0-deep-output-carry | codex

> TAKE 2026-06-18 18:09 | ветка codex/tz147-p0-deep-output-carry | codex

# ТЗ-147 (со-проектировано с аудитором) — углубление P0-детектора + независимый выходной передерив. ПРИОРИТЕТ: безопасность+качество

- Дата: 2026-06-18. Постановщик: Claude #1 + аудитор. Исполнитель: **D1**. Регрейд — мой по сырью. Канон main `a079e914`.
- Принцип: P0-пол остаётся детерминированным; модель — ДОПОЛНИТЕЛЬНЫЙ улавливатель, НЕ разблокирует пол (ADR-001). Всё за флагами default OFF. LLM только через подписку Codex, без ключей.
- Проблема (аудит ТЗ-146, исполнено): класс «оплатил→нет доступа» пробит ~12/23 после 145 (синонимы активировали/заблокировали/подключили/выслали/назначили; разрыв предложения точкой «Оплата прошла. Доступа нет.»; дефисы «спи-сали»). Вход и выходной бэкстоп зовут ОДИН `codes_from_text` → общая слепая зона.

## Поправка к модели (подтверждено аудитором по коду)
- Чокпоинт один: `p0_recall_spec.codes_from_text` (p0_recall_spec.py:271). Правка чинит вход целиком, НО требует независимого выходного канала.
- Модельный P0-слой УЖЕ ON в пилоте (`DIRECT_PATH_MODEL_P0` в `DIRECT_PATH_PILOT_PROFILE_DEFAULT_ON_FLAGS`, support.py:98) и УЖЕ делает OR: `_direct_path_model_p0_signal` = `model_is_p0 OR floor_reason` (provider.py:2113). Усиливаем его, не создаём.
- Semantic output-verifier P0 НЕ проверяет сознательно (post_layers.py:4530) — НЕ вешать P0 на него.

## Слой (а) — детерминированное углубление `codes_from_text` (флаг `TELEGRAM_P0_DEEP_MATCH`, env-only, default OFF)
1. **Нормализатор для матчинга** (новая `_p0_normalize_for_match`, применять к КОПИИ; evidence не трогать): снять внутрисловные дефисы между кириллицей `re.sub(r"(?<=[а-яё])[-‐‑‒–—](?=[а-яё])","",t,flags=re.I)` («спи-сали»→«списали»); НЕ трогать дефис между пробелами/цифрами. Прогонять `normalized` через `*_RE.search`.
2. **Кросс-предложенческая склейка** (точечно, НЕ расширять gap глобально): новая `_payment_dispute_across_sentences(normalized)` — бить текст по `(?<=[.!?])\s+`, проверять пары соседних сегментов на (`_PAYMENT_MOVED_PATTERN` в одном) И (`_PAYMENT_RESULT_MISSING_PATTERN` в соседнем) в окне 2 сегмента. Ловит «Оплата прошла. Доступа нет.» без открытия generic-gap. Вызвать в `codes_from_text` через `or` в payment_dispute-блоке (~стр. 285).
3. **Словарь** (p0_recall_spec.py:103/107): в access-target добавить `активаци\w*|подключени\w*`; в `_PAYMENT_RESULT_MISS_PATTERN` добавить `не\s+(?:активир\w*|подключ\w*|выслал\w*|назначил\w*|открыл\w*)` и `заблокир\w*` — НО «заблокир/активир/подключ» срабатывают ТОЛЬКО в паре с `_PAYMENT_MOVED_PATTERN` (не соло). `_PAYMENT_MOVED_PATTERN`: добавить `оформил\w*\s+оплат\w*`.
4. **NEG-набор анти-передолова** (в `PAYMENT_DISPUTE_BENIGN_CASES` стр.167 + `test_p0_perifraz.py`): «Занятия завтра, в системе расписания пока нет» (нет payment-moved в паре); «Оплату ещё не вносил, доступ не появился — так и должно быть?» (negated payment); «Подключили новую платформу, удобно?»; «Ссылку выслали, спасибо!»; «Активировали аккаунт, всё работает»; «Списать абонемент за пропуск — нормально?»; дефис-контроль «Онлайн - очень удобно».

## Слой (б) — независимый выходной передерив (флаг `TELEGRAM_P0_OUTPUT_MODEL_CARRY`, default OFF)
- ВЫБОР: carry модельного сигнала в выходной гейт, БЕЗ повторного `codes_from_text` и БЕЗ доп. вызова модели. Модельный P0 уже в `metadata["direct_path_model_p0"]` (provider.py:2156); `_deal_action_final_p0` уже читает его первым (post_layers.py:1309-1312). Достроить `_authoritative_gate_findings` (post_layers.py:3389): сначала смотреть `metadata["direct_path_model_p0"].is_p0` и поднимать hard-P0-finding ДО строки `codes_from_text(client_message)`. Тогда выход = regex-пол ИЛИ модельный флаг (канал ≠ входной regex) — общая слепая зона снята.
- Дёшево-усиление: прогнать на выходе нормализованный (п.1) `codes_from_text` перед сырым — опционально, как дополнение.

## Риски (обязательно)
1. Сбой модельного слоя = fail-OPEN допустим: сигнал пуст, но `floor_reason` (regex-пол) держит P0 независимо (provider.py:2120). Приёмочный тест: «модель кидает Exception + текст с hard-regex → всё равно manager_only». Слой (б) доп. вызова НЕ добавляет (carry).
2. Над-хендофф: расширители парные (payment-moved + miss), NEG-паритет обязателен.
3. Оба флага default OFF.

## Приёмка
- `test_p0_perifraz.py` зелёный + 12 paid_no_access POS ловятся (цель 23/23 класса) + ~10 новых NEG не ловятся.
- Замер пара ON/OFF: микро-набор ~15 POS + ~12 NEG; over_handoff_rate по ходам не вырос; судья фиксирован; мой регрейд по транскриптам.
- Полный pytest зелёный. P0-пол детерминирован. Только Codex.

## Файлы
`p0_recall_spec.py` (96-107, 167, 271-285), `provider.py` (2113-2156), `post_layers.py` (1309, 3389), `tests/test_p0_perifraz.py`.
