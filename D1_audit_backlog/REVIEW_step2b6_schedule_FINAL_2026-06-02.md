# Ревью 2b.6 (`31437ee5`) — ФИНАЛ миграции лазаньи. По сырью+адверсариально. 2026-06-02.

Ревьюер: Клод #1. Вердикт: **PASS. Миграция лазаньи завершена — 16/16.**

## Структура
- Построен на микрофиксе адреса (`59e67c91`). MIGRATED=16 (+schedule), **фолбэк по реестру ПУСТ**.
- `DEFAULT_KB_SNAPSHOT_PATH` переключён на v6.4 (`kb_release_20260602_v6_4_schedule`). ✓
- Хирургично: rules_engine +325, subscription_llm +6, pipeline +2 (путь снимка), тесты. 7 файлов.

## Проверено по сырью
- **Адресные тесты починены:** Кодекс переименовал (включая семантически верное
  `...does_not_fall_back_to_old_skorznyazhny_address`) и перевернул assert'ы (Красносельская есть, Скорняжный
  нет). **Все 28 address+schedule тестов зелёные.** Полный pytest: в 9 инфра-падениях НЕТ ни одного
  address/schedule — то есть прежний красный сьют закрыт. (Подтверждает: «у Кодекса прошло» теперь = полный
  прогон, а не подмножество.)
- **Контакт-часы ≠ дни занятий — держится:** «в какие дни 7 класс физика?» при наличии только контакт-часов →
  draft_for_manager + явно «Контактные часы 10:00-18:00 не считаю расписанием, менеджер проверит класс/предмет/
  площадку». НЕ выдаёт 10-18 как дни. ✓
- **Адрес из расписания/правила = Верхняя Красносельская 30** (не Скорняжный). ✓
- Точечный прогон Кодекса: PASS 4/PWN 4/FAIL 0, hard_gate 0, **тон 67.9** (выше 2b.5 48.4 — расписание даёт
  более полные фразы).

## Вопрос Кодекса (wrong_scope=1): площадка в ответе про дни — РЕШЕНИЕ
УНПК-ответ про расписание содержал «Сретенка, 20», хотя клиент спрашивал про дни → судья пометил wrong_scope.
**Моё решение: оставить площадку.** Группа по определению = бренд×ПЛОЩАДКА×день×слот×предмет×класс; для очной
группы «где» неотделимо от «когда» — клиенту полезно знать и место. Это НЕ утечка и не ошибка (площадка верная,
бренд верный). wrong_scope здесь — чуть строгий чек судьи для расписания. Не режем. Если позже захотим строже
по тону — композер (3b) может опускать площадку, когда вопрос ЧИСТО про дни; но это не нужно для безопасности.
(Замечание для 3b, не правка сейчас.)

## Итог — ЛАЗАНЬЯ МИГРИРОВАНА (16/16)
Все доменные функции монолита разложены в декларативные правила `rules_engine` + выходной гейт. Фолбэк по
реестру пуст. Безопасность подтверждена на каждом шаге (бренд-разделение, P0, ПДн, выдумки, формат, адрес,
контакт-часы). Монолитные пути ОСТАВЛЕНЫ как страховка (кандидаты на снос в Шаге 4).

**Дальше (не миграция — доводка):**
- Шаг 3a — планнер берёт понимание (intent/subvariant) как единый вход правил.
- Шаг 3b — включить тёплый тон X2 на всём мигрированном (поднимет тон с ~48-68 и закроет over-handoff-остаток;
  wrong_scope-площадку тоже сюда).
- Шаг 4 — снос мёртвых монолитных путей (фолбэк пуст → их можно убирать).
- Плюс отдельно: калибровка входного p0_pregate на refund-hypo (требование #1 из Прогона 1).
- Мини-ТЗ: счётчик LLM-вызовов в раннере (видимость стоимости прогонов).

Перед Шагом 3 логичен один общий регрейд на среднем наборе (с включённым счётчиком), чтобы зафиксировать
базовую линию ПОСЛЕ полной миграции до правок тона.
