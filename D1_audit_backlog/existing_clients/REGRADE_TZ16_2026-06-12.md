# Регрейд ТЗ-16 по сырью

Дата: 2026-06-12. Автор: Claude (архитектор трека). Сырьё: git канона, `tz16_profiles_v7_20260612/customer_profiles.sqlite`, `rerun_tail_report.json`, `step4_blacklist_microprobe.json`. Отчёт исполнителя: `2026-06-12_TZ16_D4_profiles_v7_and_rerun_tail_final_report.md` (в дереве tz16).

## Вердикт: PASS_WITH_NOTES (мерж рекомендую; перед удалением дерева tz16 — перенести артефакты в канон)

## Подтверждено по сырью

1. **Git**: ветка `codex/tz16-d4-profiles-v7-rerun-tail` растёт ровно из актуального main (40c0a9fa); запретных файлов в диффе нет; replant-дерево удалено (в списке деревьев только канон + tz16); канонический `tz12_working_batch3` не тронут (время файла прежнее, 2,7 ГБ).
2. **Шаг 2, профили v7**: 18 399 профилей, 190 614 полей (117 823 активных, 72 791 вытеснено), профилей с 2+ детьми 4 411 (было 4 471 — чище имена), маркеров слияния 835 групп на 824 профилях — всё совпало с отчётом запросами к базе.
3. **Шаг 3, хвост**: вся арифметика сходится до единицы: зона 46 187 = 16 797 (v7) + 29 390 (старые); квадранты 56+0+20 749+3 439+5 146 = 29 390; разбивки по длительности и давности тоже. База звонков из stable_runtime читалась только на чтение, LLM-вызовов 0. Оценка полезного среза (старые ≥60 сек без blacklist): 3 439 звонков ≈ 4,7 ч на M1; весь хвост 29 334 ≈ 8 ч (по символам) — 40 ч (по звонкам), правда посередине ближе к нижней (хвост короткозвонковый).
4. **Шаг 4, микропроба**: артефакт подтверждает 5/5 `service_call` (ложный «автоответчик» ушёл), подсказка v7, llm_calls_total=5; контрольные 3 настоящих автоответчика остались автоответчиками (NEG в отчёте). Случай `target_product_present=true` — звонок 12617 (18+ минут), в санированном артефакте текста выжимки нет, смысл не проверить — оценка по малому батчу.

## Замечания

1. **Артефакты в дереве tz16**: `tz16_profiles_v7_20260612/` лежит в product_data рабочего ДЕРЕВА (git её игнорирует) — при удалении дерева срез пропадёт. Перед уборкой перенести папку в канон.
2. **Звонок 12617**: возможное приписывание продукта сервисному звонку (болезнь v6, ради которой делали v7). Полный перепрогон 77 — только после ревью малого батча с сохранёнными выжимками (вне git).
3. Канон по-прежнему стоит на старой ветке ТЗ-14 с чужими незакоммиченными правками — возврат на main за главным треком (повторное напоминание).

## Принято по отчёту

Полный pytest 3064; идемпотентность пересборки (сигнатуры); хэш источника до/после.
