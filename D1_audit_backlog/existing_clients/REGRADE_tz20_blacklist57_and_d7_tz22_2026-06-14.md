# Регрейд по сырью: TZ-20 (blacklist 57) + D7 ТЗ-22 (правки CRM-слоя)

Дата: 2026-06-14. Автор: Claude (архитектор, независимый регрейд). Метод: сырьё (результаты прогона, транскрипты из канонической базы, снимки) + прямое сравнение кода worktree↔main, НЕ по отчётам исполнителей.

---

## A. TZ-20 — добивка blacklist (57 звонков): PASS_WITH_NOTES

Сырьё: `tz20_blacklist77_results/` (worktree D4), канон-база (v6 «до» для тех же id), `blacklist_77.txt`.

Проверено лично:
- **57 записей, все `done`, все `v7`, model `gpt-5.4-mini`, failed 0, parse 0.** llm_calls=57 (только эти 57 и считались).
- **Выдумок нет:** target_product на коротком транскрипте (<800) — 0. Добавленных «пусто v6 → продукт v7» — 0 (консервативно). Наоборот, снято 5 ложных/широких v6-продуктов (лагерь/курсы).
- **Болезнь резко слабее v6**, но остаточный хвост есть. Длинных non_conversation (≥2500) — 5; прочитал транскрипты всех пяти:
  - 51275 — IVR + ASR-артефакт («субтитры создавал…», «перевожу на оператора») → non_conversation верно.
  - 49031, 63289 — административные диалоги (договор/карта партнёра, ЭДО/подпись), продаж нет → «non_conversation для анализа продаж» допустимо.
  - **58340 и 62856 — НЕВЕРНО:** это живые разговоры о записи (58340 — летняя выездная школа УНПК, даты августа; 62856 — запись на курсы, Сретенка, время). v7 обнулил продукт и увёл их в non_conversation. Класс неверен.
- **Защита есть:** все 11 non_conversation (включая эти 2) помечены `needs_review=long_non_conversation`. По правилу потребления non_conversation+needs_review **не дают полей в профиль** → неверный класс НЕ впрыснет неверные данные, молчаливой потери нет.
- **Контрольные автоответчики** (15717/16565/24790) детерминированно `non_conversation` без вызова модели (`used_llm=false`). ✓
- **Dry-run вливания безопасен:** режим `--blacklist-override` пускает только явные 77; чужих в override 0 (`rejected_non_blacklist_in_blacklist_override=0`); 5 not_done честно отклонены; только 5 разрешённых колонок; `apply=false`, записей в AMO/Tallanto/CRM/ASR нет; sha промпта совпал (12718ea6).

**Заметки (не блокеры):**
1. 2 из 57 (58340, 62856) — живые звонки о записи, ошибочно в non_conversation. Помечены needs_review → полей не дают. Перепрогон ради 2 пограничных не рекомендую (как и в batch15).
2. **Правило потребления обязательно при реальном вливании:** строки non_conversation+needs_review не считать «исправлено», в профиль полей не дают (by-design). Сохранять флаг.

**Вывод:** прогон 57 и dry-run пригодны. Реальное вливание blacklist (`--apply` + бэкап) можно делать по «да» Дмитрия с правилом потребления.

---

## B. D7 ТЗ-22 — правки текстового слоя CRM: PASS_WITH_NOTES

Сравнил код worktree `codex/tz22-d7-crm` ↔ main по каждому блоку. Все правки раундов аудита реализованы ТОЧНО:

- **A1 (бренд) — верно.** `tallanto_context.py`: `fail_closed=os.getenv(...,"0")` (default OFF, решение Дмитрия) с ИСПРАВЛЕННЫМ условием `if fail_closed and not active` (ловит и Фотон, scope=unknown), имя `build_tallanto_live_card`, `import os` добавлен. **Часть 1-бот реализована** (`run_telegram_public_pilot_bots.py`: `active_brand=brand` на вызове + проброс через readonly-обёртку в live-карту) — это ЕДИНСТВЕННАЯ защита брендов при выключенной страховке, она на месте.
- **A2 (гейт) — верно и проверено на снимке.** Детектор `_detect_service_test_markers` скоупится строго по `TARGET_CRM_TEXT_FIELDS`, regex сужен (без ветки «тестов+истори»), `class_id="Q-service-marker"`, severity P0. Замер на снимке 13 895 контактов: поймал **ровно 1** (76062310, smoke), **0 ложных**, 6 контактов с «тестов» в ручном «История общения» НЕ пойманы (scope верный).
- **B1/B2/B4 — верно.** Возражения >90 идут в `compact_objection` (не `fit_text`), флаг ON; итог авто-истории ограничен `MAX_AUTO_HISTORY_CHARS` через `_compact_without_ellipsis`, флаг ON; срезы по границе слова.
- **C1 — реализовано верно, но эффект на данных нулевой (см. заметку).** `child_slots_match`: слияние безымянных по (класс∧предмет) за `PROFILE_CHILD_MERGE_BY_TRAIT` (OFF), без фиктивного бренда, `import os` добавлен.
- **D1/D2 — за флагами OFF** (Tallanto/AMO batch + мок-счётчики). E2 (индекс телефона) — OFF, без production-миграции. E1 — единый `normalize_phone` (обёртки на utils). E3 — дата убрана из дефолта (`resolve_analysis_date`). E4 — метаданные в analysis_json (для будущих прогонов). E5 — `dead_code_candidates.md` без удаления.
- Полный pytest у D7: 3193 passed / 5 skipped.

**Заметка (важная, про мою постановку, не про код D7):** замер C1 на реальных профилях (tz21) — **0 слияний** под флагом. Причина: правило требует совпадения И класса, И предмета у обоих безымянных слотов, а фантомные слоты обычно несут лишь один признак (хеш от одного значения). C1 **безопасен** (ноль ложных слияний, именованные не тронуты), но проблему дубль-детей в нынешнем виде НЕ двигает. Это следствие моей консервативной постановки ТЗ. **Действие:** C1-флаг оставить OFF (вреда нет, пользы тоже нет); корень дубль-детей (создание безымянного child_key на каждый звонок) — отдельная задача проектирования, не точечный флаг.

**Вывод:** D7 пригоден к мерджу в main. Дефолты как решено (страховка OFF, B1/B2 ON, остальное OFF). Перед включением `CRM_TALLANTO_MODE=http` в бою — убедиться, что часть 1-бот работает (бренд реально доходит): это единственная защита брендов при выключенной страховке.

---

## Очередь дальше

1. Мердж D7 (ТЗ-22) в main (worktree → ff/merge), отдельным шагом (git-гигиена).
2. Реальное вливание blacklist (`--apply`+бэкап) по «да» Дмитрия, с правилом потребления.
3. Одна финальная пересборка профилей — после того как и хвост, и blacklist в базе (C1 включать смысла нет — 0 эффекта; E2 индекс — по желанию).
4. Корень дубль-детей — отдельная проектная задача (не флаг C1).
