# ТЗ — supervised KB-сборка v6.5: +факты ЛШ/формат, чистка 24 черновиков. Для Кодекса. 2026-06-03.

Автор: Клод #1. SUPERVISED (Claude верифицирует текст ПОСЛЕ сборки — не ночной авто, риск text-corruption и
бренд-смешения). Контент уже semantic-PASS: см. `KB_DRAFT_lesson_format_and_summer_schools_2026-06-03.md`
(подтверждён Дмитрием, цены финальны). Сборка ТОЛЬКО через `scripts/build_kb_release_v6_1_team_answers.py`
(применяет `release_manifest.yaml` из `_sources/`), НЕ старый билдер. См. `docs/KB_BUILD_RUNBOOK`.

## 1. ДОБАВИТЬ факты (из KB_DRAFT, бренд-раздельно, client-safe)
Интегрировать в `_sources`/manifest как client-safe факты с верным `active_brand` и темой:

**Формат занятия (оба бренда, одинаковый текст):**
- «Занятия проходят в формате семинара: теория и практика вместе. Преподаватель в мини-группе разбирает задачи с
  учениками, есть интерактив с вопросами. Бывает этап индивидуальной работы: пока все решают свои задания,
  преподаватель подходит к каждому на несколько минут.»

**Городская летняя школа — ФОТОН (только Москва):** B1 (смена 3–14 августа, Верхняя Красносельская 30, 5–10
классы, ФизМат «Формула Физтеха»/ИИ «AI Lab», 2 нед/10 дней, Предлёнка 09:45–11:45 со 2 дня) + B2 (тарифы
34 300 / 49 000 / 59 000, факультатив 16:30–18:00).

**Городская летняя школа — УНПК:** C1 Долгопрудный (смены 20–31 июля, 17–28 августа; питание) + C1b (1–4 классы
предзапись, площадка Пацаева) + C2 (тарифы 39 500 / 59 500 / 99 500) + C3 Москва (смены 6–17 июля ФизМат+ИИ,
20–31 июля только ФизМат; тарифы те же 39 500 / 59 500 / 99 500).

**Скидки ЛШ:** D1 (многодетные 10%, бывший ученик 5% по бренду, записался на след.год 10%, приведи друга 3 000/
5 000; не суммируются) + D2 (только УНПК: сотрудники МФТИ 10%).

Точные формулировки — из KB_DRAFT (брать дословно, они semantic-PASS). **Бренды НЕ смешивать:** Фотон-факты без
УНПК-смен/адресов и наоборот; общий адрес Москва (Верхняя Красносельская 30) разнесён сменами (Фотон 3–14 авг,
УНПК июль).

## 2. ПОЧИСТИТЬ источник: 24 факта «черновик для ситуации «X»:»
В `_sources`/исходных client-safe фактах есть 24 записи (12 foton + 12 unpk) с ВНУТРЕННИМ префиксом «черновик для
ситуации «X»:» прямо в client_safe_text (ситуации: возражение о неудобном времени / о стоимости курса / о
стоимости лагеря / вопрос о связи брендов / кто отвечает клиенту). **Снять префикс «черновик для ситуации «...»: »,
оставить текст ПОСЛЕ двоеточия** (он клиентский, напр. «Это отдельные организации…»). Грепнуть «черновик для
ситуации» в источнике, починить все.

## 3. Тест выхода (Кодекс)
- Сборка `quality_passed=true`, `semantic_pass=true`, blocking findings 0; гейты целостности зелёные.
- Новый релиз `kb_release_..._v6_5_*` + bot_pack/employee_pack; снимок `kb_release_v3_snapshot.json`.
- В отчёте: сколько фактов добавлено/изменено, путь релиза, ревизия.
- НЕ менять снимок, захардкоженный в коде (`DEFAULT_KB_SNAPSHOT_PATH`), пока Claude не проверил — обновление пути
  снимка отдельным согласованным шагом.

## 4. Верификация Claude (ПОСЛЕ сборки — обязательно, анти-corruption)
Я грепну client_safe_text ВСЕХ добавленных/изменённых фактов по сырью:
- числа ЛШ верны (Фотон 34 300/49 000/59 000; УНПК везде 39 500/59 500/99 500), даты смен, формат;
- бренд не смешан (Фотон без УНПК-токенов и наоборот);
- 24 черновика: префикс снят, текст после двоеточия цел, не битый;
- нет абсурда от мис-экстракции (как «2026 раз в неделю» в прошлом);
- прогоню semantic-safety на новых фактах.
Только после моего PASS снимок v6.5 идёт в код/прогоны.

## Что НЕ делать
НЕ старый билдер; НЕ смешивать бренды; НЕ выдумывать формулировки (брать из KB_DRAFT); цены/даты строго как
подтверждено; не git reset; не менять `DEFAULT_KB_SNAPSHOT_PATH` до моего PASS.

После v6.5 + leak-гейт + тёплый хендофф → повторный cfg3 → финальная приёмка.
