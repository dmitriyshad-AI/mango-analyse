# Промт для D4 — ТЗ-19 (выдаёт Дмитрий)

Ты — D4, исполнитель трека «действующие клиенты». Выполни полностью ТЗ-19: `tasks/_inbox_codex/2026-06-12_TZ19_D4_tail_bundle_blacklist_batch_ingest_prep.md`. Три блока: А — бандл «хвост 3 439» на Яндекс + задача в шину М1 (это невыполненный Блок 2 прошлого промта — в этот раз обязателен); Б — малый батч 15 blacklist-звонков с сохранением выжимок на ревью (LLM только здесь, ровно 15); В — скрипт вливания хвоста (код и тесты на синтетике, БЕЗ запуска вливания).

Порядок: А → Б → В (Б и В можно параллельно после А, до 2 субагентов, дорожки не пересекаются по файлам).

Дисциплина: фиче-ветка от main; каждый блок — отдельный коммит с тестами и NEG; полный pytest перед отчётом; `git status` чистый (ПДн!); сверь sha256 подсказки между бандлом и батчем — они обязаны совпасть.

Жёсткие границы (нарушение = стоп): запись в AMO/Tallanto/CRM запрещена; полный перепрогон 77 не запускать; вливание не запускать; настройки М1 не менять; транскрипты и выжимки — только в ignored-папках и data/ бандла на Яндексе.

Не виси на вопросах: мелочи решай безопасным дефолтом с пометкой в отчёте; число манифеста ≠ 3 439 — стоп и доклад (не «подогнать»).

Отчёт в `tasks/_done/` по списку приёмки ТЗ-19. Критерий завершения: бандл на Яндексе + задача `.task.yaml`+`.ready` в `_inbox_m1`, 15 строк батча в отчёте, выжимки доступны для ревью, скрипт вливания закоммичен с тестами, полный pytest зелёный, git чистый. До этого — не останавливаться.
