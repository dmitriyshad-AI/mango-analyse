# Risk Review

- Live bot/Wappi не трогались.
- Профиль и live-флаги не менялись.
- P0 floor/preblock не менялись.
- Мораторий regex fixtures не менялись.
- Изменение gold не меняет поведение бота, только измеритель.
- Сырые v4 transcripts добавлены только как локальный git traceability artifact; они взяты из synthetic/scripted eval и содержат маски `[phone]`/`[fio]`, но не должны уходить в публичные каналы.
- Свежий M1-прогон всё ещё нужен: локальный recheck проверяет метрику на сохранённой телеметрии, но не проверяет хрупкость prompt на новом запуске.
