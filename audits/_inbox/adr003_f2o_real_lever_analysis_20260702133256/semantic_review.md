# Semantic Review

Semantic pass только для диагностического отчёта.

Смысловой вывод:

- над-хендофф в текущем наборе не лечится простым route-only понижением;
- большинство true too-cautious строк требует утверждения факта
  (существование/формат/цена/адрес/платформа);
- часть строк опасно-соседняя и не должна попадать в active candidates;
- в текущем наборе есть 29 negative controls настоящих live/operational
  запросов, их нельзя понижать;
- чистых harmless ack/status route-only кандидатов нет (`0`).

Нельзя писать, что автономность готова к включению. Доказан только следующий
фокус работы: SemanticFrame должен лучше отделять safe reference от missing/live
facts, а runtime должен нести точный fresh client-safe факт.
