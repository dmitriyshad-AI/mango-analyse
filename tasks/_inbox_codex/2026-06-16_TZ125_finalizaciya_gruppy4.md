# ТЗ-125 (после аудитора) — финализация Группы 4 в каноне (влив D primary + хвосты)

- **Дата:** 2026-06-16. Заказчик: Дмитрий. Постановщик: Claude #1 (+ аудитор по коду). Исполнитель: D3 (владеет веткой D). Цель — закрыть Группу 4 в main полностью и чисто.
- **Состояние:** B/E/C primary + A shadow уже в main (tz121). Остался D primary (роли в звонках, офлайн transcribe.py) на чистой ветке `codex/tz118-d-primary-clean` (commit `b00b3bd`). Бренд-фикс цен уже в main (`6b93b770`).

## Что сделать (с учётом находок аудитора)
1. **Источник D = `b00b3bd` (clean).** Clean = базовый codex_selective primary БЕЗ отклонённого segment_guard (а он и так был force-off → поведение и метрика 94.62% сохраняются). Зафиксировать это в отчёте; перепрогон НЕ нужен, но письменно обосновать «segment_guard удалён, был off → метрика не меняется».
2. **Дифф строго офлайн.** `git diff --name-only origin/main..codex/tz118-d-primary-clean` обязан быть = `services/transcribe.py` + `tests/test_tz118_transcribe_d_primary.py`. Если в диффе есть post_layers/живой путь — СТОП/показать (дивергенция).
3. **Утечка токена (безопасность).** `_prepare_role_assignment_codex_home` копирует `~/.codex/auth.json` в persistent-папку `codex_home_role_assignment_neutral`. Убедиться: эта папка в .gitignore И НЕ попадает в бандл M1, И чистится после прогона. Секрет не должен утекать в артефакты.
4. **Конфиг-поле.** Код читает `mono_role_low_info_filter_mode` через `getattr(..., "mark")`, но поля нет в `config.py`. Добросить поле в config.py (default `"mark"`) для управляемости (или явно задокументировать, что зашито).
5. **Отчёт и closure-док.** D-primary отчёт перенести/пересоздать в `tasks/_done`; в closure-отчёте обновить ссылку на D с `ce836b0` на `b00b3bd`; зафиксировать итоговую матрицу: **B=primary / E=primary / C=primary / D=primary(clean) / A=shadow**.
6. **C/E и ключи.** Подтвердить, что C-primary в офлайне идёт через Codex, а путь с `OPENAI_API_KEY` в `question_catalog` — мёртвый/неактивный (политика «без ключей»). Если активен — отдельно флагнуть.
7. **НЕ вливать грязные сёстры.** Ветки `codex/tz118-group4-primary-d` и `codex/tz116-offline-understanding` трогают живой путь/другие офлайн-модули — НЕ мержить, явно пометить «оставить/закрыть без влива».

## Чек-лист перед «Группа 4 закрыта»
- Полный pytest на ветке влива зелёный (ожидаемо ~3297+; недостающие vs dirty — это segment-guard тесты, не регрессия — подтвердить).
- Дифф офлайн-only (п.2).
- Регрессия B/E/C: `test_tz121_*` зелёные после влива D.
- Инвариант: ни одной записи в AMO/Tallanto/CRM; живой пилот не задет; все primary — офлайн по явному режиму.
- Мой финальный регрейд диффа → мерж в main.
