# Промт MAIN — фикс одного щит-теста 4a. 2026-05-30.

> Скопировать в окно MAIN Кодекса.

---

Claude #1 прогнал 4a (`a695d391`) в песочнике: **431 passed, 1 failed.**

**4a структурно верна и безопасна** (НЕ трогать код): `decide_route` собран, переворот выключен
(`allow_default_autonomy=False`), все прежние 430 тестов зелёные → поведение 1:1, регрессий нет.

**Единственное падение — баг ОЖИДАНИЯ в новом щит-тесте**
`test_pravka4_router_veto_shield_keeps_all_manager_routes` (~строка 2948):

- для cross-brand тест требует флаг `cross_brand_safe_template_applied`;
- реально на этом входе (черновик САМ смешал бренды: «У Фотона и УНПК одинаковые условия») срабатывает
  блокировка черновика → флаги `cross_brand_client_text_blocked` + `brand_separation_guarded`, маршрут к
  менеджеру, бренд не утёк. Это корректная защита, просто другой её механизм (не safe-template).

**Фикс — в ТЕСТЕ, не в коде:** ослабить assert на бренд — принимать ЛЮБОЙ бренд-вето-флаг
(`cross_brand_safe_template_applied` ИЛИ `cross_brand_client_text_blocked` ИЛИ `brand_separation_guarded`).
Суть щита уже проверена первым assert (маршрут не автономный) — главное, что бренд не утёк и ушло к
менеджеру. Код `decide_route` и бренд-логику не трогать.

Тесты не гонять (лимит) — Claude #1 прогонит. После фикса 4a закрыта, щит зелёный → можно начинать 4b
(переворот умолчания через `allow_default_autonomy`).