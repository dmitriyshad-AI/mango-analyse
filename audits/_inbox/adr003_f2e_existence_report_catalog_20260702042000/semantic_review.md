# Semantic Review

## Verdict

PASS_WITH_NOTES.

## Что прошло

Отчёт стал смыслово безопаснее: он больше не считает частичную похожесть текста KB доказательством. Для `kb_exact` теперь требуется результат `exists` или `not_offered` из отдельного proof-layer.

На реальном 36ea110 выявлены 2 текущих handoff, где gold говорит safe self и каталог даёт точный proof ЛВШ 5 класса:

- `wappi_pair_missing_72h_001#1`;
- `wappi_pair_missing_72h_003#1`.

После аудиторского BLOCKED добавлены регрессии на две критичные дыры:

- регистрация/набор/заявка не доказывают существование продукта;
- мусорный шаблон `число + произвольное слово + класс` не парсится как grade.

## Что НЕ разрешено этим результатом

- Не разрешено включать active route demotion.
- Не разрешено клиенту обещать места, запись, бронирование или оплату.
- Не разрешено менять P0/money/danger rows.

## Остаточные риски

- Есть 1 already-self existence/format без exact KB evidence (`cf142_fabrication_foton_camp_laptop`). Это не новый риск F2e, но сигнал: перед active надо либо добавить точный факт, либо оставить блок.
- Два найденных handoff-кандидата сейчас `manager_only`, а не `draft_for_manager`; для active нужен отдельный policy-step и отдельный регрейд.
