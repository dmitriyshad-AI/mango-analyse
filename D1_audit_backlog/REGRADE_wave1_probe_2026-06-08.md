# РЕГРЕЙД по сырью — проба Волны 1 (Б1+Б5), A/B на одной машине (2026-06-08)

**Вердикт:** Волна 1 — **PASS по сырью.** Целевые эффекты достигнуты, оба FAIL при ON ложные/внешние, безопасность цела. Готова к мержу в `main` ПОСЛЕ приёмки прямого пути.

**Прогоны (один мак, worktree, изолированный CODEX_HOME одинаков для обеих сторон — чистый A/B):**
- A = OFF `20260608_wave1_probe_gpt_OFF_isolated` (флаги волны OFF = паритет 6351de27)
- B = ON `20260608_wave1_probe_gpt_ON_isolated` (`TELEGRAM_NUMBER_GATE_SCOPE_AWARE=1` + `TELEGRAM_VERIFIER_HANDOFF_CLAIMS=1`)

## Целевые эффекты (OFF → ON)

| Сигнал | OFF | ON |
|---|---|---|
| `unsupported_product_number` (гейт рубит) | 10 | 4 |
| `wrong_scope` (ловит чужой скоуп) | 0 | 2 |
| Верификатор проверил ходов | 53 | 75 |
| Поймал `invented_generalization` | 0 | 2 |
| `manager_only` (глухой уход) | 25 | 19 |
| `draft_for_manager` | 22 | 26 |
| flip уход→ответ / ответ→уход | — | 9 / 5 |
| Реальные выдумки чисел (faithfulness) | 0 | 0 |
| P0-ветки | 8/2/1/4 | 8/2/1/4 |

- **Б1** перестал рубить верный перефраз (`unsupported_product_number` 10→4) и начал ловить чужой скоуп (`wrong_scope` 0→2).
- **Б5** закрыл pure_handoff-дыру: верификатор +22 хода, поймал 2 `invented_generalization`, которые раньше скипались.
- Маршрут чище: `manager_only` −6, +9 ходов стали отвечать (бот перестал зря уходить).

## Разбор 2 FAIL при ON (по сырью — оба НЕ регресс Волны 1)

1. **`sm_f_expensive` — ложный `fabrication`.** Бот на прямой вопрос «сколько человек в группе» ответил «очно 6–12 человек» — это ВЕРНЫЙ факт: `fact:v3:foton:online_platform.offline_group_size`, `structured_value.raw_value = "6-12 человек"`. Судья ошибся: `number_audit` ложно сматчил «12» с фактом расписания (`group_start_date…`), матча по размеру группы не нашёл → no_match. Бот не выдумал.
2. **`sm_f_night_lead` — внешний.** pii_echo (ФИО+телефон). Уже починен в `main` (`e42597be`); ветка Волны 1 на `6351de27` без pii-фикса, поэтому всплыл. Уйдёт при мерже Волны 1 поверх main.

## Заметки (для Волны 2, не блокеры)

- `number_audit` (судейский) ложно привязывает число к не тому факту (диагностический шум судьи, не проблема бота). Б1-гейт здесь отработал верно: «6-12» есть в фактах → не пометил.
- Верификатор на `invented_generalization` реагирует `annotate` (текст остаётся, менеджеру помечено). Для черновикового пилота — ок. Для Волны 2 (расширенный вход → больше качественных обобщений) — следить за нагрузкой; ширину Волны 2 калибровать умеренно.

## Рекомендация

- Волну 1 мержить в `main` цепочкой ПОСЛЕ приёмки прямого пути (смоук 100). После мержа `sm_f_night_lead` pii-фейл исчезает.
- Проба Волны 1 чистая (выход держит числа Б1 + ловит обобщения Б5) → можно вписывать ширину Волны 2 (умеренную, по заметке выше).
