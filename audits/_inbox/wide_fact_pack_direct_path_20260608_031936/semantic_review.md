# Semantic Review

Verdict: `PASS_WITH_NOTES`.

Audience:

- Customer-facing Telegram draft text generated by direct path.
- Manager-facing draft review metadata and M1 measurement reports.

What passed:

- Wide fact pack filters to one active brand and excludes internal/forbidden/expired facts.
- Direct prompt separates exact facts from adjacent facts and tells the model not to use out-of-scope numbers.
- Product numbers remain governed by existing output gate and number grounding; the wide pack is not a permission bypass.
- A4 avoids repeating the same generic hard-gate replacement text in direct path.
- `pilot_gold_v1` gives one named acceptance configuration instead of a loose set of flags.
- Fail-fast catches misconfigured direct-path runs with zero direct model calls in the first 4 completed dialogs, including named `pilot_gold_v1`.

Non-blocking risks:

- The wide pack broadens context. This should reduce missing-fact over-handoff, but may increase adjacent fact temptation. Current mitigation is exact/adjacent split plus final output gate; live raw transcript review is still required.
- Category and scope heuristics are intentionally conservative but not a semantic planner. Some useful facts can still land in adjacent rather than exact.
- Historical judge v9 re-judging was not run in this implementation pass.

Missing checks:

- No fresh M1 smoke run yet on GPT/Claude with the final smoke set.
- No manual review yet of model-written direct-path drafts with v6.6 + gold pack under `pilot_gold_v1`.

Required regressions/gates added:

- Brand-isolated wide pack for both brands.
- Client-safe field serialization only; no internal text, stale address, or unrelated license facts in pricing pack.
- Expired client-safe facts excluded by `valid_until`.
- Schedule pack stays within limits and keeps an exact block.
- Scope conflict is marked adjacent, not exact.
- Direct-path hard-gate replacement text deduplicates against previous bot text.
- `pilot_gold_v1` enables direct path and real-manager gold examples without extra direct/gold env flags.
- Runner fail-fast detects 4 configured direct-path dialogs with zero model calls.

Recommended next action:

- Build the clean bundle and run the three M1 smoke tasks once the smoke set path and SHA are available.
