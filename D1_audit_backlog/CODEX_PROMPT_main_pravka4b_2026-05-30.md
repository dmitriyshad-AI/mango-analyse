# Стартовый промт — MAIN Кодекс (правка 4b: переворот умолчания). 2026-05-30.

> Скопировать в окно MAIN Кодекса.

---

Ты — MAIN Кодекс, репозиторий, HEAD `431ee0e6` (4a закрыта: decide_route + щит 18 вето зелёные,
432 теста по прогону Claude #1).

**Прочитай:** `D1_audit_backlog/TZ_pravka4b_flip_2026-05-30.md` — это ТЗ, в нём всё.

**Суть:** включить переворот умолчания. Механика уже в `decide_route` (subscription_llm.py:6593), но
guard (1434) вызывает её без флага → выключено. Включить управляемым флагом (стоп-кран, default off),
пробросив `allow_default_autonomy=_default_autonomy_flip_enabled(context)` в вызов decide_route.

**Это самый опасный шаг — бот отвечает клиенту напрямую.** Поэтому СЕРДЦЕ задачи — негативный контроль:
прогнать щит-тест с флагом=True, все 18 вето-категорий ОБЯЗАНЫ остаться маршрутом к менеджеру. Переворот
меняет только «осторожное умолчание без вето и с покрывающим фактом».

**Условия НЕ ослаблять** (autonomy_ready = тема в матрице 23 безопасных; has_covering_fact). Маршрутную
логику не трогать — только включить флаг.

**Ограничения:** тесты не гонять (лимит) — Claude #1 прогонит щит (флаг True) + smoke + тест автоответа.
В main не мержить до зелёного И прогона M1. Флаг управляемый, default off. Правило #1 — проброс
подтвердить чтением.

**Отчёт:** где флаг и как пробрасывается; тексты негативного (18 вето при True) и позитивного
(автоответ) тестов.