# Микро-ТЗ — исправить адрес Фотона (переезд). Для Кодекса. 2026-06-02.

Автор: Клод #1. Подтверждено Дмитрием: Фотон ПЕРЕЕХАЛ. Старый адрес «Скорняжный» → новый
**«Москва, Верхняя Красносельская ул., 30»**. Старый адрес клиенту больше НЕ выдавать.

## Контекст
KB v6.4 уже правильна: факт «Фотон: адрес и место занятий — Верхняя Красносельская ул., 30». НО мигрированное
правило `contact_address` (2b.1, `_apply_contact_address_rule`) держит хардкод-фолбэк «Москва, Скорняжный» —
это устаревший адрес, бот может выдать его клиенту. Реестр `rules_registry.yaml` и `CLAUDE.md` я уже поправил.

## Правка (хирургическая)
1. В `_apply_contact_address_rule` (rules_engine.py) фолбэк-строку Фотона `"Москва, Скорняжный"` заменить на
   `"Москва, Верхняя Красносельская ул., 30"`.
   ЛУЧШЕ (если несложно): брать адрес Фотона из KB-факта `contact.foton.address` (он уже верный — Красносельская
   30), а не из хардкода — тогда переезды чинятся в KB, а не в коде. Если быстрее — просто заменить строку.
2. Источник правды — обновлённый `D1_audit_backlog/rules_registry.yaml`:
   `contact_address.data.foton.address = "Москва, Верхняя Красносельская ул., 30"`.

## Обязательный НЕГАТИВ-контроль (правило #4)
- ПОЗИТИВ: Фотон «где занятия / какой адрес?» → ответ содержит «Верхняя Красносельская», route=answer_self.
- НЕГАТИВ: ответ Фотона про адрес НЕ содержит «Скорняжный» (старый адрес не выдаётся клиенту). Это контрольный
  отрицательный тест — без него правка не закрыта.

## Проверить попутно (внутренний факт)
В KB есть `fact:v3:unpk:legal_entities_entities_1_used_for_2` — «УНПК: ЛШ городская АНО июль (Москва
Скорняжный)», помечен `internal_only / forbidden_for_client / manager_only_route`. Клиенту не уходит. Но раз
Фотон съехал со Скорняжного — проверить с Дмитрием/РОПом, актуален ли адрес ЛШ городской УНПК на Скорняжном
(возможно тоже устарел). НЕ клиентский, низкий приоритет, но зафиксировать в approval_queue.

## Рамки
Хирургично: только адресная строка правила + тест. KB v6.4 не пересобирать (она уже верна). stable_runtime,
гейт, другие правила не трогать. Не git reset/checkout/clean.

## Отчёт
Изменённая строка + результат НЕГ-теста (Скорняжный не выдаётся, Красносельская выдаётся) + ревизия коммита
для моей проверки.
