# Техническое задание ТЗ-109 (финал) — единое ТЗ на исправления по аудиту, только реальное

- **Дата:** 2026-06-14. Заказчик: Дмитрий. Постановщик: Claude #1. Прошло круг аудитора-архитектора + координацию с треком «действующие клиенты». **Заменяет ТЗ-107 и ТЗ-108.**
- **Опора:** аудит Клод-кода (ТЗ-101, PASS_WITH_NOTES) + независимый D3 + сверка, мой регрейд по сырью. Все file:line выверены аудитором по канону `main` (8bf69f5b).
- **Принцип:** только реальный вред / реальная безопасность-корректность. Идентичность и общий бренд-резолвер — НЕ дублируем здесь, переиспользуем/координируем с треком «действующие клиенты» (см. границы).

## Дисциплина (жёстко)
- Правки КАЧЕСТВА/поведения — за новым флагом default OFF + NEG-тест (паритет при OFF) + мой регрейд перед включением.
- Правки БЕЗОПАСНОСТИ (auth, sanitizer) — **fail-closed по умолчанию, БЕЗ выключенного флага** (иначе дыра открыта); где нужен dev — отдельный явный dev-escape, не общий «default OFF».
- Каждый пакет — своя ветка от `main`, свой коммит, отчёт в `_done`, полный `pytest` зелёный. Флаги в `pilot_gold_v1` не добавлять до замера.

## Бизнес-смысл
Чиним точечное реальное: бот может перепутать ребёнка (→ неверная цена/программа в ответе), анализ звонков молча теряет данные, последняя линия защиты пропускает текст, внутренний API отдаёт права «директора», откат AMO не восстанавливает. По убыванию реального вреда, безопасно, не трогая здоровый живой путь.

---

## ПАКЕТ 1 — безопасность/корректность: два подтверждённых бага (БЕЗ флага, fail-closed по умолчанию)
- **1a. auth fail-open → fail-closed.** `amocrm_runtime/auth.py:188-190` — пустые ключи → контекст роли Director любому. Фикс: боевой путь fail-closed без флага; пустые ключи + не-localhost + без явного dev-escape (`MANGO_API_DEV_CONTEXT=1` или детект `request.client.host`) → 401; dev-контекст только под dev-escape/localhost. Заодно (тот же PR, дёшево): сузить CORS `*`+`allow_credentials` (`amocrm_runtime/main.py:36`) и host-default `0.0.0.0` (`config.py:146`) под dev/прод-список. **Тест:** пустые ключи+localhost+dev → dev-контекст; пустые ключи+не-localhost → 401.
- **1b. откат AMO: «успех» только `restored`.** Убрать `dry_run_ready` из множества успешных В ОБЕИХ точках: `deal_aware/amo_rollback.py:222` (`load_successful_rollback_keys`) И resume-путь `:323`. Иначе после сухого прогона `--apply`+resume молча пропускает восстановление. **Тест:** dry-run пишет `dry_run_ready` → последующий `--apply`+resume реально откатывает (не skipped).

## ПАКЕТ 2 — sanitizer fail-CLOSED (безопасность, по умолчанию для клиентского режима)
- **Где:** `insights/sanitizers.py:234` (`fixpoint_not_reached`).
- **Фикс:** в клиентском (client-safe/customer) режиме при `fixpoint_reached=False` возвращать пустоту/блок (нет авто-выдачи), уводить в менеджерский черновик — **по умолчанию, без выключенного флага** (это последняя линия ПДн/бренда). Ветку `fixpoint_reached=True` (`:222-230`, где решает `_status_for_text`) НЕ трогать. **Тест:** текст, не достигший фикс-точки в клиентском режиме → блок; стабильный текст → как раньше (паритет).
- **Ловушка:** блокировать ТОЛЬКО при `fixpoint_reached=False`, иначе зарубишь валидные ответы → массовый ложный хендофф.

## ПАКЕТ 3 — безопасный дефолт маршрута direct-path (защита-в-глубину, флаг)
- **Где:** `channels/subscription_llm_parts/provider.py:2135` (нет route → `bot_answer_self_for_pilot`). По сырью это перехватывается выходным гейтом (дыры нет), фикс — защита-в-глубину.
- **Фикс (флаг `TELEGRAM_DIRECT_DEFAULT_MANAGER`, default OFF):** при ON отсутствие route → безопасный менеджерский черновик. NEG паритет; позитив: пустой route → менеджер.

## ПАКЕТ 4 — Tallanto live-card: убрать адрес как бренд + fail-closed (аддитивно к уже влитому)
> Координация: проброс `active_brand` в этот файл уже сделан (D7, ТЗ-22 A1, влито). Наши правки аддитивны — согласовать редактирование одного файла.
- **Где:** `amocrm_runtime/tallanto_context.py:61-71` (бренд по адресным токенам `красносель/менделеево/сретенка`, `foton/фотон` не распознаётся → unknown) и `:267` (`CRM_LIVE_CARD_BRAND_FAILCLOSED` default OFF).
- **Фикс:** убрать адресные токены из бренд-скоринга филиала, завести явную ветку `foton/фотон`; `CRM_LIVE_CARD_BRAND_FAILCLOSED` → fail-closed по умолчанию. **Тест:** филиал Фотона → `foton` (не unknown/shared); пустой/непроверенный бренд → карточка не выдаётся.

## ПАКЕТ 5 — non_conversation становится advisory (тихая потеря данных, флаг; ОБА детектора)
- **Где (ДВА независимых детектора, накрыть оба):** pre-LLM `services/analyze.py:2303` (`should_force_non_conversation` → ранний возврат заглушки) И post-LLM `services/analyze.py:1786-1822` (`_detect_call_type=="non_conversation"` → зануление products/grade/ФИО/next_step).
- **Фикс (флаг `TELEGRAM_NON_CONVERSATION_ADVISORY`, default OFF):** при ON вердикт «не-разговор» — рекомендация (`needs_review`/тег), факты LLM НЕ обнуляются без подтверждения модели/консенсуса; накрыть и ранний форс (2303), и зануление (1799-1822). При OFF — прежнее. Отдельный пайплайн обработки звонков (не бот).
- **Ловушка:** тронуть только одну точку = потеря данных останется; это РАЗНЫЕ детекторы.

## ПАКЕТ 6 — идентичность ребёнка в живой памяти → ПЕРЕИСПОЛЬЗОВАТЬ ядро LLM-резолвера (дизайн уточнён регрейдом tz24)
> Координация: НЕ строить второй резолвер. Переиспользовать ядро `customer_profile/child_resolver_llm.py` (трек «действующие клиенты», ТЗ-30). Сам резолвер и `builder.py` здесь НЕ править.
- **Проблема:** `channels/dialogue_memory.py:1083-1089` грубым регексом решает привязку ребёнка (`child_2 if "доч"/"девоч"/"младш"`), provenance-выход `:521-522` идёт ДО любого LLM. Регекс кормит контекст бота → перепутанный ребёнок.
- **ВАЖНО (по регрейду tz24, меняет дизайн):** точка входа `apply_llm_child_resolver_to_fields(...)` — ПАКЕТНАЯ, синхронная, codex-CLI таймаут 180с, требует секрет-файл стоп-листа. Звать ИНЛАЙН на каждый ход живого бота НЕЛЬЗЯ (медленно/хрупко). Поэтому:
  - **(основное) офлайн-разрешение:** `child_key` разрешается при сборке профиля (трек действующих клиентов), живой `dialogue_memory` ЧИТАЕТ готовый `child_key` и перестаёт перезаписывать его регексом; регекс — advisory только для нового/неразрешённого упоминания;
  - **(или) тонкая обёртка** с коротким таймаутом + fail-soft на текущий регекс.
  Смысловое ядро (`build_child_resolver_cases` + промт + `validate_child_resolver_response`) переиспользуется как есть.
- **Фикс (флаг `TELEGRAM_CHILD_IDENTITY_MODEL`, default OFF):** при ON живой путь использует разрешённую моделью идентичность (готовый `child_key`), регекс — только advisory; жёсткого пере-ключевания слота регексом нет. При OFF — паритет.
- **Точка врезки (по ловушке):** там, где собираются `known_slots` для контекста (`telegram_pilot_context_builder.py:131-142`) — читать готовый `child_key`; НЕ в post-answer enrichment (поздно) и НЕ через `memory_llm_fn` (живой раннер его не передаёт — `run_telegram_public_pilot_bots.py:619-625`).
- **Зависимости:** (1) влить `codex/tz24-dubli-deti` в main (регрейд PASS); (2) `PROFILE_LLM_CHILD_RESOLVER=1` (офлайн) — включать ПОСЛЕ просмотра Дмитрием очереди low-confidence на реальной выборке (остаточный риск: снят авто-страж имён).
- **Тест:** NEG (OFF = слоты бит-в-бит, регекс как сейчас); позитив (готовый `child_key` читается; сын + «младший» не перепутан).

## ПАКЕТ 7 — минимальная гигиена секретов/ПДн (код) + ротация (операция Дмитрия)
> Большую безопасность сейчас не делаем. Только дешёвый минимум.
- **Операция Дмитрия (вне кода):** ротация живых ключей из `.env` + перенос в `~/.mango_secrets`; осознать `TASK_CONTAINER_CODEX_SANDBOX=danger-full-access` + `DIRECTOR_AUTO_RUN_ENABLED=true`.
- **Код-фиксы (с тестами, без флага — усиление):** убрать захардкоженные ПДн `scripts/finalize_messages30_tail.py:13-29`; bearer не в argv `curl` (`existing_clients/amo_step1_snapshot.py:177`); не хранить сырой OAuth `last_secrets_payload` (`amocrm_runtime/amo_integration.py:439`); вызвать неиспользуемый `validate_no_secret_leak` (`build_telegram_public_pilot_feedback_report.py:373`); закрыть неаутентифицированную debug-имперсонацию аллоулистом оператора (`scripts/run_telegram_public_pilot_bots.py:487-512`).

## ПАКЕТ 8 — долг (НИЗКИЙ). Разбит по итогам D3: Часть A+C сейчас, Часть B запаркована.
- **Часть A (делаем):** удалить мёртвые tallanto-дубли (`amocrm_runtime/tallanto_deal_ranking.py`, `tallanto_premature_close.py`, `tallanto_matching.py`) — 0 импортёров подтверждено (Claude #1 + D3). Грепом перепроверить (вкл. тесты), удалить + связанные мёртвые тесты, полный pytest.
- **Часть C (делаем):** задокументировать legacy-лазанью (`policy_routing/rules_engine/answer_quality_rewriter`) как замороженную/недостижимую в пилоте (заметка в `AGENTS.md` или рядом; живой путь = direct path `provider.py:918`).
- **Часть B (ЗАПАРКОВАНА):** удаление `monolith.py` сейчас НЕ делаем. Скрипты паритета не подтверждают покрытие, т.к. split-модули ушли ВПЕРЁД от monolith (слой действий ТЗ-26 добавил определения/экспорты, которых в monolith нет) — это рассинхрон инструмента паритета, НЕ риск живого кода. Отдельная низкопри-задача: обновить паритет-инструмент под текущие split-модули → затем снести monolith + фолбэк в `__init__.py` + 3 скрипта паритета.
- Бренд-нормализаторы (единый `brand_resolver`) — профильный трек «действующие клиенты», НЕ здесь. Не трогать живой `_active_brand` (`support.py:589-600`) без флага+регрейда.

## КООРДИНАЦИЯ — отдать в трек «действующие клиенты» (НЕ аудит-трек)
- **Идентичность КЛИЕНТА в таймлайне (MEDIUM, offline-ингест, кормит историю бота):** `customer_timeline/ids.py:101-121` — `source_ref` в каноническом `customer_id` → история дробится по источникам; `customer_timeline/ingestion.py:1169-1184` — пустой маркер = `STRONG_UNIQUE` (fail-open слияния). Это identity-домен того же трека — решать там единым подходом «модель решает, детерминизм проверяет», за флагом+NEG. Здесь зафиксировано, чтобы не потерять.
- **`builder.py` (offline-склейка детей) и `child_resolver_llm.py`** — их трек (ТЗ-30), здесь не править.

## Порядок (диалоги остановлены — порядок за нами)
1 (auth/rollback) → 4 (tallanto адрес+fail-closed) → 2 (sanitizer) → 3 (default route) → 5 (non_conversation) → **влить `tz24-dubli-deti`** → 6 (идентичность ребёнка на готовом резолвере) → 7 (гигиена) → 8 (долг). Конфликтов по файлам между пакетами нет.

## Явные ИСКЛЮЧЕНИЯ (не делаем)
- Legacy-каскад `policy_routing/rules_engine/answer_quality_rewriter` — заморожен, в пилоте недостижим (call-graph). Только при реактивации.
- Бренд как «живая дыра» — переоценка: живую защиту держат direct-path преблок `post_layers.py:1116` + выходной гейт `cross_brand/brand_leak` `post_layers.py:835-836` (НЕ `apply_brand_separation_guard` — он в пилоте не на пути). Пакет 4 = укрепление, не аврал.
- Полный security-overhaul, шифрование БД, N+1 за флагами — сознательный долг вне эшелона.

## Приёмка
- Безопасность (1,2): fail-closed по умолчанию, dev-escape покрыт тестом, полный `pytest` зелёный.
- Поведение (3,5,6): флаг default OFF, NEG-паритет доказан, позитив на ON; мой регрейд по сырью перед включением.
- Пакет 4: тесты бренда филиала; Пакет 7: тесты на гигиену. Каждый пакет — отчёт в `_done`, ревью Claude #1.

---

## ДОПОЛНЕНИЕ (раунд 2 — триаж замечаний Кодекса по полноте; Пакет 1 уже сделан и принят)
Кодекс справедливо указал на неполноту. Разобрал каждый пункт по сырью/аудитам — не всё «must-fix», часть уже покрыта, часть — отложенная Дмитрием безопасность:

- **ДОБАВЛЕН Пакет 10 — безопасные дефолты запуска публичного бота.** `scripts/run_telegram_public_pilot_bots.py:319` `TELEGRAM_PILOT_AUTONOMY_ENABLED` default True; по нашему стоячему решению должно быть OFF/approved_list_only. Перевернуть дефолт на безопасный (автономия по умолчанию выкл, явный opt-in), kill-switch не зависит от этого. **Это пилот-политика → подтверждение Дмитрия.** REAL, был пропущен — согласен с Кодексом.
- **ДОБАВЛЕНО в Пакет 7 (БЕЗОПАСНОСТЬ — приоритет за Дмитрием, он сознательно отложил «большую безопасность»):**
  - внешний вывод ПДн без маски — `services/export_ai_office.py:112-234` (сырые phone/ФИО/`raw_analysis` POST-ом наружу), `amocrm_runtime/deal_dossier.py:262`, `services/llm_response_cache.py:65` (без TTL/шифр). **HIGH.** Маскировать перед egress. Был под-описан в Пакете 7 — расширяю.
  - OAuth callback без nonce/replay-защиты — `amocrm_runtime/routers/integrations.py:169` (`/secrets`,`/callback` без require_api_key и без state-nonce). **MEDIUM.** Проверять серверный state-nonce. Был пропущен — согласен.
- **LOW/долг (Пакет 8):** question_catalog — LLM-путь мёртв (`QUESTION_CATALOG_LLM_ENABLED` нигде не ON, работает регекс-заглушка) + redaction. Offline-каталог, не клиентский слой → в долг, не в эшелон.
- **УЖЕ ПОКРЫТО (не пропуск):** customer timeline identity (`source_ref` в id; «нет маркера = STRONG_UNIQUE») — в разделе КООРДИНАЦИЯ выше, отдано в трек «действующие клиенты».
- **БОЛЬШИНСТВО «опасных скриптов» = ложная тревога** по моему регрейду D3 (cleanup аудио — в Корзину + хеш-гейт + abort; `pkill` сужен до точной команды). Реальный (неаутентифицированная debug-имперсонация) уже в Пакете 7.

**Итог триажа:** реально пропущено и добавлено — Пакет 10 (дефолт автономии) + расширение Пакета 7 (внешний вывод ПДн HIGH, OAuth callback nonce). Безопасность (ПДн/OAuth) — отдельный приоритетный звонок Дмитрия (он отложил большую безопасность). Остальное — уже покрыто/координация/ложная тревога/долг.

## Согласие по порядку (правка от Кодекса)
Кодекс предложил Пакет 2 (sanitizer fail-closed) следующим — согласен: это короткий и реальный safety-финал после auth/rollback, важнее защиты-в-глубину (Пакеты 3/4). Новый порядок: **1 (done) → 2 (sanitizer) → 3 (default route) → 4 (tallanto) → 5 (non_conversation) → влить tz24-dubli-deti → 6 (идентичность) → 10 (дефолт автономии, после решения Дмитрия) → 7 (гигиена+ПДн+OAuth, после приоритета Дмитрия) → 8 (долг).**
