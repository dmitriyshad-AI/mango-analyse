# ТЗ интеграции для Кодекса — два слоя: (A) довести замену, (B) слой извлечения фактов

Дата: 2026-05-25. Автор: Claude (Cowork). Режим: Кодекс ВНЕДРЯЕТ готовые результаты Claude;
если с чем-то не согласен — пишет «не согласен с X, потому что Y», НЕ переписывает молча.

## Принцип процесса (важно — round-5 показал почему)
- Где Claude дал рабочий модуль + зелёные тесты — **копировать дословно** и только подключать,
  НЕ переписывать заново. round-5 провалился ровно на «переписал по-своему»: потерялись
  held_state, фикс дизъюнкции, осталась старая ветка scope.
- Любую правку сверяем по диффу + офлайн-тестам ДО живого прогона.
- «ЗАМЕНИТЬ, не ДОБАВИТЬ»: старые ветки удалять, не оставлять спящими.

## Источник правды (эталон Claude, все тесты зелёные офлайн)
`D1_semantic_roles_reference/` — reference/{semantic_roles, decision_policy→answer_plan,
held_state, fact_retrieval}.py + tests/. Прогон на 2026-05-25:
юнит 35/35, многоход 11/11, извлечение 10/10, триаж round-5 0 провалов, сим v8 0 аномалий,
статич. аудиты 0/0. Recall-проба round-5 (база) = 0/8.

---

## ЧАСТЬ A — довести начатую замену (Кодекс уже сделал большую часть)
Кодекс на checkpoint добавил held_state, контекст+дизъюнкцию в semantic_roles, начал замену
scope-каскада, починил онлайн-пробное, guard на предмет, +тесты (301 passed). Осталось:

A1. Сверить свои held_state.py и semantic_roles.py с эталоном Claude: они должны проходить
    МОИ тесты `test_semantic_roles.py` (35) и `test_multiturn.py` (11). Если расходятся —
    выровнять по эталону (взять мой модуль дословно). Held должен нести и тему ИЗВЛЕЧЕНИЯ
    (`active_fact_scope`, `active_topics`, `required_fact_keys`) — см. обновлённый held_state.
A2. ЗАВЕРШИТЬ удаление старого каскада `conversation_intent_plan._fact_scope_constraints`
    (был «начат»): scope только из ролей; старые marker-if удалить. Анти-крот: число
    marker-if в функции ~0; старых веток нет.
A3. Подтвердить: офлайн-набор Claude зелёный после выравнивания (перенести мои tests/*).

## ЧАСТЬ B — слой ИЗВЛЕЧЕНИЯ фактов (recall-first) — это и есть «большая архитектурная правка»

Корень потолка (доказано, см. CEILING_FINDINGS): факты ЕСТЬ в базе, но не попадают в
confirmed_facts (recall round-5 = 0/8). Тонкость: точность (не пускать чужую область) Кодекс
уже усилил — НЕ зажимать её дальше за счёт recall. Чинить recall, точность сохранить.

Эталон: `reference/fact_retrieval.py` (select_confirmed_facts) + расширенный `held_state`.

B1. Подключить отбор фактов через select_confirmed_facts на стадии сборки confirmed_facts
    (там, где сейчас scope-фильтр + кап 10):
    - факт-ОТВЕТ (совпал с required_fact_keys по СМЫСЛОВОМУ алиасу, не точному префиксу)
      ГАРАНТИРОВАННО в выдаче, даже если кап 10 его срезал бы;
    - сопоставление ключей — по `_KEY_ALIASES` (discounts↔discount/payment_options,
      locations↔address, matkap↔sfr и т.п.);
    - факт без scope НЕ отбрасывать только из-за отсутствия scope (смягчить
      `fact_scope_spec.fact_scopes_allowed:138`);
    - факт ЧУЖОЙ области (в blocked_scopes: residential_lvsh при city, olympiad при regular)
      по-прежнему исключать — точность.
B2. Тема извлечения держится через held: на голом follow-up брать
    `held.retrieval_context()` (active_fact_scope/active_topics/required_fact_keys), а не
    обнулять. На каждом ходу: update_held(..., fact_scope=plan.fact_scope,
    required_fact_keys=plan.required_fact_keys).
B3. Калибровка хендоффа: если факт-ответ доступен (есть в confirmed_facts) — отвечать, не
    уходить в менеджера. manager_only — только реальный P0 или действительно неизвестное.
B4. Ориентир vs точное: ориентир-факты (диапазоны, «обычно», «ориентир до 15 раб. дней»)
    можно называть с мягкой оговоркой; живой проверки требует только клиент-специфика
    (его место/его дата). Снимок уже помечает СФР как ориентир — дать боту это произносить.
B5. Пробелы рантайм-базы (вторично): добавить/выверить «ориентир срока СФР» и «где смотреть
    запись урока» (личный кабинет/ссылка) в client-safe факты, если их нет.
B6. Заземление НЕ ослаблять: запрет называть числа вне confirmed_facts оставить как есть —
    чинить ВВЕРХ по потоку (recall), иначе незаземлённые числа = выдумки.

---

## Приёмка
Офлайн (перенести мои tests/, поправить импорты на mango_mvp.channels.*):
- test_semantic_roles 35/35; test_multiturn 11/11; test_fact_retrieval 10/10;
  triage_round5 0 провалов; sim_full_v8 0 аномалий; audit_static 0/0.
Живой round-5 (тот же holdout v8_holdout_fresh_round5_2026-05-25.jsonl):
- **recall_probe ≥ 90%** (база 0%) — главный новый порог слоя извлечения;
- hard-gate 0 (0 p0_mishandled, 0 brand_leak, 0 fabrication);
- доля manager-маршрутов на безопасных темах падает (база 64%);
- ignored_question / over_handoff падают;
- presale-возврат (02,14) не в P0; мультитема (06,20) — оба вопроса; scope не из соседнего
  продукта (12,19); 0 непрошеного предмета (17);
- рост «отправил бы» (база round-5 12.5%).

## Очередность
A (довести замену) → checkpoint-коммит зелёного офлайн-среза → B (извлечение) → round-5.
Сначала напиши: что из A уже совпадает с эталоном, с чем НЕ согласен и почему. Потом код.
