> DONE 2026-07-27 04:36 | ветка main | codex

> TAKE 2026-07-27 04:06 | ветка main | codex

Ветка: main
Зоны: src/mango_mvp/channels/subscription_llm_parts/policy_routing.py, tests/test_adr003_semantic_reading_trace.py, scripts/, tests/, docs/, tasks/
Тест-команда: PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m pytest -q tests/test_adr003_semantic_reading_trace.py tests/test_customer_timeline_maintenance.py tests/test_wappi_replay_m1_adapter.py
Семантический-аудит: да

# ТЗ: актуальное состояние, первый шаг модельного понимания и безопасная подготовка M1/Timeline

## Цель

Не пересекаться с четырьмя активными Claude-диалогами по Wappi, Tallanto,
почте, AMO и звонкам. Обновить владельческий документ по фактам `main`, убрать
обязательное словарное разрешение перед уже полученным уверенным модельным
намерением, подготовить минимальную воспроизводимую проверку на M1 и проверить,
нужен ли новый код для сжатия Timeline.

## Границы

- Не менять рабочую Customer Timeline и `stable_runtime`.
- Не удалять данные, базы, ветки и внешние артефакты.
- Не писать в AMO, Tallanto, CRM, Wappi и не запускать live.
- Не менять контуры первых четырех Claude-диалогов.
- P0, бренд, ПДн, неподтвержденные числа и финальный защитный пол не ослаблять.

## СТОП

- Любое пересечение с файлами первых четырех Claude-диалогов.
- Любое падение P0/бренд/ПДн/числовых защит или полного набора тестов.
- Необходимость менять рабочую Timeline, live или внешнюю систему.
- Пакет M1 требует клиентских текстов без штатной очистки ПДн.

## Приемка

1. Уверенное модельное намерение может исправить или задать non-P0 тему без
   предварительного совпадения ключевых слов; низкая уверенность и явный вопрос
   о местах сохраняют прежние ограничения.
2. Есть тесты для отсутствующего словарного сигнала, низкой уверенности,
   выключенного режима и жесткого ограничения по местам.
3. Пакет M1 переиспользует существующие runner/manifest и имеет явный бюджет
   вызовов; тяжелый прогон локально не запускается.
4. По Timeline не создается второй механизм: либо переиспользуется существующий
   staging-only maintenance, либо фиксируется доказанный минимальный gap.
5. Документ
   `2026-07-26_MANGO_tekuschee_sostoyanie_i_plan_do_AI_sotrudnika.docx`
   обновлен по текущему `main`, визуально проверен и не обещает готовность без
   реального сбора и смысловой приемки.

## Результат

- Экспериментальное понимание non-P0 темы без словарного разрешения доступно
  только при явном `TELEGRAM_INTENT_MODEL_LED=1`; обычный профиль до M1 не
  изменён.
- Существующий парный runner расширен этим флагом. Отчёт требует хотя бы одно
  реальное применение нового пути и сохраняет список таких ходов для ручного
  смыслового просмотра.
- Полный локальный набор: `4844 passed, 2 skipped`.
- Новый компрессор Timeline не создан. Рабочая база и частичная staging-база не
  менялись: существующий безопасный maintenance достаточен, но его применение
  заблокировано до двух полных ночных сборок и приёмки 30 семей.
- Владельческий DOCX переписан по сырью последней ночи, текущему `main` и
  независимым архитектурному и бизнес-аудитам; визуально проверены все страницы.
