# Semantic Review

Formal pass: да.

Semantic pass for report-only decision: да, с ограничением. Новый отчёт не делает вывод "можно отвечать клиенту"; наоборот, он снижает риск ложного GO:
- отделяет реальные текущие handoff-строки от строк, где бот уже отвечает сам;
- исправляет ложный `wrong_brand` из-за duplicate `fact_key`;
- не позволяет факту про классы лагеря стать ответом на вопросы про питание, медсестру, охрану, живые места или доступ после оплаты.

Клиентский текст не генерировался и не менялся, поэтому отдельный semantic review клиентских ответов не применим.

Остаточный риск: source-axis классификация внутри отчёта всё ещё эвристическая. Она используется только как blocker/диагностика, не как permission к активному поведению.
