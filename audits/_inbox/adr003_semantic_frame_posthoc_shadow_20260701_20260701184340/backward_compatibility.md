# Backward Compatibility

- Existing `TELEGRAM_SEMANTIC_FRAME_SHADOW` behavior remains unchanged.
- New post-hoc flag is default OFF and not profile-aware.
- Live bot was not touched and no external writes were performed.
