# ТЗ-131 (после аудитора) — Этап 1 прокачки Кодекса: операционный фундамент

- **Дата:** 2026-06-16. Заказчик: Дмитрий. Постановщик: Claude #1 (+ фокус-аудитор). Исполнитель: **D1**. Идёт ПОСЛЕ ТЗ-130 (0-pre).
- **Зоны:** `scripts/`, `docs/`, `tasks/`, `tests/`, `.gitignore`, репо-корневой `AGENTS.md`. **Тест-команда:** `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m pytest -q tests/test_project_now.py tests/test_audit_pack_pii.py tests/test_preflight.py`. Семантический-аудит: нет.
- **Цель:** Кодекс работает по генерируемому паспорту состояния, живой очереди, доказывает результат аудит-пакетом с ПДн-фильтром, не заходит в запретные зоны (preflight). Это ИНСТРУМЕНТАРИЙ — поведение бота НЕ меняется.

## База и что СУПЕРСИДИТСЯ
Код-полуфабрикаты бери из `Foton/codex_upgrade/03_TZ_codex_self_upgrade_etap1.md`, Приложение Б (project_now.py=Б1, task_move.py=Б2, task_stale_report.py=Б3, make_audit_pack.py=Б4, preflight.py=Б5) и Приложение А (BLOCKERS.yaml, .gitignore, блоки AGENTS.md). **НО scope — по ЭТОМУ файлу.** В файле 03 УСТАРЕЛО и НЕ выполнять: «Шаг 0» (обновление CLI + правка ~/.codex) — ушёл в отдельный Этап 0-env; «Шаг 0а реестр worktree» — уже сделан в ТЗ-130; стартовый синк очереди на «TZ14/TZ16» — мусор, см. Шаг 2.

## Гейт входа (проверено — выполнен)
ТЗ-130 закрыт: главная папка на `main` (ee45e45), `docs/worktrees_registry.md` создан, worktree=14. Если реестра нет — СТОП, сначала закрыть 0-pre.

## Жёсткие границы
1. **Ноль записи в `~/.codex` и в `stable_runtime/`** (вкл. `stable_runtime/.../codex_home/AGENTS.md` — это боевой рантайм). Этап 1 правит ТОЛЬКО репо-корневой `AGENTS.md`. Три AGENTS.md — разные файлы, не путать.
2. Ничего не удалять (файлы/ветки/worktree). 
3. **НЕ пересобирать M1-бандл из канона во время Этапа 1** (идёт флаговый прогон — не подхватить полуфабрикат).
4. Поведение бота, прогоны, шину M1 — не трогать.

## Фаза 0 — спайки (только ЧТЕНИЕ + отчёт SPIKE_REPORT, ноль изменений)
- **С1.** Формат профилей: секции `[profiles.<имя>]` в config.toml ИЛИ файл `~/.codex/<имя>.config.toml`? По докам + `codex --help`, подтвердить, что профиль переопределяет `personality`/`model`. (Вход для Этапа 0-env.)
- **С2.** Hooks: блокируют или уведомляют, дословная схема событий. (Вход для Этапа 2.)
- **С3.** Задаётся ли модель/reasoning субагенту явно. (Вход для Этапа 2.)
- **С4.** Прочитать `stable_runtime/.../codex_home/config.toml` (подтвердить, что прод пинит модель+personality) и `~/.codex/AGENTS.md` — **только чтение.** Зафиксировать: временные `mango_codex_home_*` копируют из `~/.codex` → любая будущая правка ~/.codex рискует протечь в живой контур (это для Этапа 0-env, не сейчас).

## Шаг 1. Паспорт состояния
- `scripts/project_now.py` (Б1, только чтение git/очереди) → генерит `docs/PROJECT_NOW.md` (в .gitignore). Единственный ручной вход — `docs/BLOCKERS.yaml` (А1).
- Чистка репо-корневого `AGENTS.md`: снести протухший блок «Current Main Priority» (ссылается на ТЗ от 15.05), внести блоки «Очередь задач» / «Preflight» / «Шапка ТЗ» / замену правила субагентов (А3/А4).
- Критерий: `python3 scripts/project_now.py` < 10 сек; PROJECT_NOW кажет верную ветку (main)/задачи/блокеры; дифф AGENTS.md приложен.

## Шаг 2. Очередь задач
- `scripts/task_move.py` (Б2: `--take|--done|--fail "причина"`) + `scripts/task_stale_report.py` (Б3). Создать `tasks/_running/.gitkeep` и `tasks/_failed/.gitkeep`.
- Блок «Очередь» в AGENTS.md: `_inbox_codex → _running → _done/_failed`; шина M1 (Яндекс) — НЕ трогать.
- **Стартовая синхронизация (правка аудитора):** очередь де-факто заброшена с 14.06 — `_inbox_codex` заморожен на старье ≤TZ23, реальная работа давно идёт через `_done`+worktree. Поэтому: в `_running` положить **ТОЛЬКО сам файл ТЗ-131** (через `--take`), а финальным действием — `--done`. **Старые TZ1–TZ23 из `_inbox_codex` НЕ трогать** (их разбор — отдельный триаж Дмитрия по stale-отчёту). Не синкать очередь на содержимое inbox.

## Шаг 3. Генератор аудит-пакета с ПДн-фильтром
- `scripts/make_audit_pack.py` (Б4). **Сверить CLIENT_PATHS по факту репо** (кандидаты: `product_data/knowledge_base/`, `src/mango_mvp/channels/`, `client_safe`, `telegram`) — клиентские тексты для авто-`--semantic`. ПДн-паттерн: телефоны РФ + email (ФИО сознательно не маскируем). 
- Критерий: на тест-наборе с телефоном/email — замаскировано; манифест пишется последним.

## Шаг 4. Preflight
- `scripts/preflight.py` (Б5). **Обязательные правки (аудитор):**
  1. Читать реестр из `docs/worktrees_registry.md`.
  2. **Пропускать записи `prunable`/`detached`/`locked`** из `git worktree list --porcelain` (полуфабрикат Б5 этого НЕ делает — дописать), иначе 14 ложных СТОПов на самом ТЗ.
  3. Закрыть два TODO в Б5: список запретных live-write зон из AGENTS.md; безопасный `--collect-only` прогон тест-команды с env-префиксом.
  4. Проверка «грязные ⊆ зоны ТЗ» (переходный снимок не нужен — дерево чистое после 0-pre).
- Порядок: `project_now.py` (Шаг 1) генерит PROJECT_NOW ДО preflight (preflight проверяет его свежесть <24ч).
- Критерий: preflight прогнан на самом ТЗ-131; синтетика — файл вне «Зоны:» → стоп; запретная зона в «Зоны:» → стоп; **зарегистрированные/prunable worktree НЕ дают ложный стоп.**

## Приёмка
1. SPIKE_REPORT (С1-С4).
2. `project_now.py` <10с, верная ветка; `_running`/`_failed` существуют.
3. ПДн-фильтр сработал (телефон/email замаскированы); CLIENT_PATHS сверены по репо.
4. preflight: стоп на файле вне зон И отсутствие ложного стопа на реестровых/prunable worktree.
5. Полный pytest зелёный + тест-команда из шапки.
6. Файл ТЗ-131 перенесён `task_move.py --done` в `_done` последним действием (очередь стартует без рассинхрона).
7. Мой регрейд (диффы scripts/AGENTS.md, прогон preflight/project_now, проверка ноль-записи в ~/.codex и stable_runtime) → закрытие.

## Что НЕ входит (следующие этапы)
- Этап 0-env (CLI + правка ~/.codex + рестарт ботов) — отдельным ТЗ в тихое окно, после спайков С1/С4.
- Этап 2 (стражи/hooks, роли-субагенты, скиллы, фон) — после приёмки Этапа 1, по С2/С3.
