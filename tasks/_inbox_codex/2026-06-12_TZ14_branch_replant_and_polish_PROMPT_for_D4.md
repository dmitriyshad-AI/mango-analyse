# Промт для D4: пересадка ветки ТЗ-14 + мерж + полировка (выдаёт Дмитрий)

Ты — D4. По регрейду ТЗ-14 (`D1_audit_backlog/existing_clients/REGRADE_TZ14_steps1_4_2026-06-12.md`) содержание Шагов 1–4 принято (PASS_WITH_NOTES), но мерж ветки `codex/tz14-d4-crm-amo-tallanto` как есть БЛОКИРОВАН: она создана не от main — под твоими коммитами лежат два чужих коммита другого трека (`ff51bc06` Wappi Max, `ea2c990a` auto resolver + отчёт «ТЗ-15»), меняющие запретные для тебя файлы живого пилотного контура. Задача — пересадить свою работу на чистую основу, смержить и закрыть полировку.

## 1. Пересадка (сначала, отдельным шагом)

- Новая ветка от АКТУАЛЬНОГО main. Перенеси на неё ровно 6 своих коммитов (`1a8f149f`, `c7c52bb8`, `1d3ebd12`, `a6162d7e` + 2 отчётных) переносом коммитов (cherry-pick), порядок сохранить.
- Проверка после пересадки: пофайловый дифф новой ветки к main НЕ содержит `channels/`, `pilot_context_assembly`, `draft_loop`, `amo_wappi_*` и `scripts/run_amo_wappi_draft_loop.py` — только файлы ТЗ-14.
- Чужие коммиты и старую ветку НЕ удалять и НЕ переписывать — их судьбу решают Дмитрий и владелец того трека. Незакоммиченные правки в дереве (draft_loop и др.) не трогать.

## 2. Мерж

Одобрение Дмитрия = выдача этого промта. Новую ветку — в main отдельным merge-коммитом. Приёмка: полный pytest на main зелёный; дифф main до/после мержа = только файлы ТЗ-14; `git status` без новых артефактов (ПДн!).

## 3. Полировка (каждый пункт — отдельный коммит с тестом и NEG)

1. Косметика текстов: убрать артефакт «класс кл» (задвоение слова «класс») в карточках Шага 3 и примечаниях Шага 2. NEG: класс без задвоения («7 класс») не ломается.
2. Тег бренда WhatsApp-кусков для бота (из регрейда ТЗ-13): унифицировать к виду `brand:unpk` (сейчас Telegram `brand:unpk`, WhatsApp голый `unpk`) — идемпотентный ретро-скрипт по `tz12_working_batch3`. NEG: другие теги и события других каналов не тронуты; повторный прогон = 0 изменений.
3. Повтор-прогоны сканов: идемпотентный перезапуск пишет в ОТДЕЛЬНУЮ папку, артефакты первого прогона не перезаписываются. NEG: повтор не меняет файлы первой папки.
4. В отчёт: причина `page_limit=10` вместо 50 из наряда (потолок коннектора? твой выбор?) — одной строкой с фактом-подтверждением.

## 4. Отчёт

В `tasks/_done/`: хэши новой ветки и merge-коммита, вывод проверки диффа, счётчики полировки, полный pytest. Запись в AMO/Tallanto по-прежнему ЗАПРЕЩЕНА; этап Б — только после ревью Дмитрием пакета 20 семей.
