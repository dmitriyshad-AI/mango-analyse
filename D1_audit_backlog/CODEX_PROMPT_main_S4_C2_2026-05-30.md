# Стартовый промт — Кодекс main (правки S4 + C2). 2026-05-30.

> Скопировать в окно Кодекса main.

---

Ты — Кодекс main проекта Mango Analyse, владелец репозитория. Текущая ветка
`codex/m1-intermediate-20260529-final`, HEAD `6f849cff` (в main НЕ слита).

**Прочитай по порядку:**
1. `/Users/dmitrijfabarisov/Projects/Mango analyse/CLAUDE.md` — правила проекта, 4 правила,
   бренд-разделение, semantic gate, что нельзя без Дмитрия.
2. `D1_audit_backlog/TZ_codex_main_S4_C2_fix_2026-05-30.md` — ТЗ на две узкие правки.

**Контекст:** финальный прогон прогнан на M1, Claude #1 регрейдил по сырью. Держится безопасность
(бренд/P0/мета = 0, эталоны P0 → менеджер). Осталось 2 реальные выдумки в клиентском слое: S4
(онлайн-цена курса в ответе про лагерь) и C2 (нет скидки на 3-й предмет, хотя правило «второй и
последующий»).

**Жёсткие ограничения этой сессии:**
- полный pytest НЕ гонять (лимит подписки). Отрицательные тесты написать, прогон отложить — это
  долг; **в main НЕ мержить, пока тесты не прогнаны**;
- правки хирургические; не трогать P0-латч / бренд / верификатор / тёплый слой / судью;
- правило #1: функции в ТЗ — гипотеза Claude #1, подтверди чтением перед правкой; при расхождении
  спроси Дмитрия, не правь молча;
- общий over_handoff и пересборку `subscription_llm.py` НЕ трогать (отдельная / послепилотная работа).

**Действуй:** подтверди корень чтением → внеси две правки → напиши отрицательные тесты (без прогона)
→ отчёт: точные файлы/строки, тексты тестов, явный долг по отложенному прогону, чистое tracked-дерево.
