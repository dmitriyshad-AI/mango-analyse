# Готовые промты для 5 диалогов Кодекса (с учётом замечаний Кодекса)

Все задачи и стратегия — в `Claude Cowork Space/`. Перед действиями пересверить номера строк (дерево «грязное»).
Набор для отладки уже готов: `03_corpus/v8_dev_set_2026-05-24.jsonl`.

---

## ГЛАВНЫЙ (D1) — промт (сначала мнение по всему плану, потом задача)

«В `Claude Cowork Space/` лежит полный пакет от Claude: аудит проекта, стратегия, план по 5 диалогам и задачи.
Прежде чем кодить — прочитай и дай мнение.

1. Прочитай `00_START_HERE.md`, `01_NEXT_STEP_AND_PARALLEL_PLAN.md`,
   `02_dialogue_tasks/CROSS_DIALOGUE_STRATEGY.md` и задачи всех диалогов (D1-D5).
2. Дай честное мнение по плану в целом: согласен ли с разбивкой «один диалог — своя зона файлов» и правилами;
   согласен ли заморозить SaaS (D3); реалистична ли последовательность «3 фикса + замер, потом ширина»; как
   тебе единый артефакт критичных тем для синхронизации с D4; что из отложенных модулей включать раньше/позже.

Затем твоя задача (D1, `02_dialogue_tasks/D1_MAIN_TZ.md`):
- Закрыть 3 пункта из проверки билда 48935d60: (1) отлов критичных тем — добавить `мошенн`, `незаконн`,
  `жал(?:об|у|ова)` в ЕДИНЫЙ классификатор + проверочный набор; (2) довести консолидацию — новый классификатор
  не должен быть обёрткой над старыми `_has_*_signal`, нужен один источник; оформить единый артефакт
  `p0_recall_spec` (жёсткий список + проверочный и безобидный наборы), на который сошлётся D4; (3) убрать
  шаблоны-заглушки на зелёных темах ПОЭТАПНО, по одной теме под флагом (жёсткие шаблоны для критичных тем/
  передачи менеджеру/брендов оставить); число шаблонов до/после должно УПАСТЬ.
- Потом замер: `v8_targeted16` (сигнал) → разбор ПО КЛАССАМ → правки → один прогон закрытой проверки
  `03_corpus/v8_holdout_clean_2026-05-24.jsonl` (только оценка, НЕ подгонять) → полный v8. Отладку гонять на
  `03_corpus/v8_dev_set_2026-05-24.jsonl` и `targeted16`. Перед закрытой проверкой перезапусти ботов.
- Второй проход модели — под флагом, на мягкие промахи, с тёплыми примерами `03_corpus/few_shot_warm_answers`,
  после него снова защитные фильтры; сначала в симуляции.
- Историю клиента (`customer_timeline`) подключить ПОСЛЕ замера.
`channels/` трогаешь только ты. Сначала пришли мнение по плану, потом начинай Часть 1.»

---

## D2 — Звонки + Mango API

«Задача — `Claude Cowork Space/02_dialogue_tasks/D2_calls_mango_api_TZ.md`. Кратко: рутинная дозагрузка/
обработка новых аудио по Mango API + лёгкая гигиена. В крупную переработку звонкового контура НЕ вкладываться —
по аудиту он достаточно зрелый. `stable_runtime` — только чтение; любые запись/распознавание/повторный анализ —
только с моим отдельным подтверждением. Дополнительно: отчёт реального покрытия против условного (сколько
исключено списками, сколько помечено «вручную» — чтобы «0 пропусков» не прятал рост исключений). Зафиксируй
риск: список для бота нельзя подключать к поиску по базе без смыслового слоя поверх простых правил (вся защита
личных данных/денег — простые правила со списком имён). Зона — `services`/`quality`/`maintenance`/скрипты;
`channels/` не трогать.»

---

## D3 — Улучшения + SaaS

«Задача — `Claude Cowork Space/02_dialogue_tasks/D3_improvements_saas_TZ.md`. Сейчас — ЗАМОРОЗИТЬ, не строить.
Сначала собери карту зависимостей: какие тесты и какие скрипты обработки/Mango импортируют `productization`
(по моему замечанию — бот не зависит, но тесты/скрипты могут). Заморозь только реально изолированный
SaaS-каркас (внешний интерфейс/сервер, планировщик, supervisor, appliance-loop, обвязка арендаторов, песочница
звука, архиватор почты): пометь «отложено» и сними с обязательного прогона. Ядро захвата
(`mango_office_client`/`product_db`/`repository`/`contracts`/`capture`) и звонковый контур оставь рабочими и в
прогоне — звонки не сломать. Зафиксируй SaaS как поздний этап в одном плане; `TZ_FULL_VISION` пометь
историческим. Ничего НЕ удалять. Зона — `productization/` + настройки прогона + документы.»

---

## D4 — Исторические каналы → база знаний

«Задача — `Claude Cowork Space/02_dialogue_tasks/D4_historical_channels_kb_TZ.md`. Сначала зафиксируй границы
изменяемых файлов — в дереве уже висят удаления старых релизов базы из параллельных потоков, не смешивай с ними,
работай отдельным коммитом. Две части: (1) гигиена базы — прогнать настоящую проверку (smoke/Stage6) по v6.3
(сейчас релиз «проверка не запускалась»), свести сборку базы к ОДНОМУ источнику (убрать правки цен прямо в коде
сборщика поверх YAML), точечные правки фактов из `01_strategy/KB_QUESTIONS_AND_EDITS.md`; (2) обогащение из
почты/телеграма/звонков ТОЛЬКО через очередь одобрения (кандидаты → статусы → одобрение), без авто-добавления
фактов, без личных данных в подсказку, без дословных ответов менеджеров. Критичные темы НЕ заводи отдельным
списком — ссылайся на единый артефакт `p0_recall_spec` из главного диалога. Зона — `knowledge_base`/данные базы;
`channels/` не трогать.»

---

## D5 — Телеграм-воронка

«Задача — `Claude Cowork Space/02_dialogue_tasks/D5_tg_funnel_TZ.md`. Спроектировать путь лида до продажи как
ПРИНЦИПЫ + примеры (для ответа модели), НЕ как код-ветки/шаблоны на каждую тему. Собрать: карту пути лида,
матрицу уточняющих вопросов (1–2 по делу), набор приёмов на возражения, правила этичного влияния (польза →
доверие → шаг; жёсткие запреты на искусственный дефицит, давление виной, недоказанные «все выбирают»/авторитет).
ВАЖНО (моё замечание): отдавать не только текст, а машинно-читаемый файл по схеме (brand, topic, situation,
example, forbidden, safe_next_step, requires_fact_ids, source, dedup_ref) — иначе главный диалог не сможет
подключить это к ответу модели. Формулировки — из тёплого набора `03_corpus/` и звонков, НЕ выдумывать; цены/
даты — только из базы; один бренд; политика «ты бот?» честно по запросу. Итог — файлы
`product_data/bot_sales_playbook_*`, которые главный диалог подключит по одной теме под флагом с замером.
`channels/` не трогать.»
