# Регрейд по сырью: TZ-19 (инструменты) + TZ-21 (вливание хвоста + пересборка профилей)

Дата: 2026-06-14. Автор: Claude (архитектор, независимый регрейд). Метод: сверка по самой канонической базе и базам профилей (read-only, `mode=ro&immutable=1`) + по исходным результатам хвоста, НЕ по отчётам D4. Гейт перед мерджем застрявшей работы в main.

## Вердикт: PASS (мердж в main одобряю) — с одной заметкой-наблюдением по Block B

---

## TZ-21 Block A — вливание хвоста 3 439 в каноническую базу: PASS

Проверено лично:
- **Все 3 439 id хвоста присутствуют в базе с выжимкой, все на `v7`. Отсутствующих 0.** (точечные запросы по `canonical_call_id`).
- **Целостность содержимого:** на 58 выборках сравнил `analysis_json` в базе с исходным результатом из `results_part*.jsonl.gz` (поля summary / target_product / needs_review / history_summary / next_step) — **0 расхождений**. Текст при вливании не побился и не обрезался.
- **Итог v7 в базе = 26 118** — совпадает с отчётом (`22 679 → 26 118`, дельта +3 439).
- **Бэкапы до записи на месте:** `...backup_before_tz21_tail_20260613` и `...backup_before_tz21_tail_idempotence_20260613`.
- **Болезнь v6 не рецидивирует:** длинных живых разговоров (≥2500 символов), ошибочно свёрнутых в `non_conversation`, — **0**. Классы: sales 2509 / service 840 / existing_client_progress 56 / technical 32 / non_conversation 2. parse_fail 0. (полностью совпало с прежним регрейдом прогона).
- **Выдумок продукта нет:** кандидатов «продукт при коротком транскрипте (<800 симв)» — 4 (10216/12231 — летний лагерь; 372/2997 — годовые курсы), все реально обсуждались (сверено ранее).
- **needs_review = 738** сохранён в выжимках как есть.
- **Импорт-скрипт безопасен** (`scripts/import_tz19_analyze_tail_results.py`): белый список колонок записи (только `analysis_json/analysis_status/has_analysis_json/analysis_json_chars/last_error` — ПДн/телефон не трогаются); `--apply` требует `--backup-to` и не перетирает существующий бэкап; двойная защита blacklist (отказ при пересечении манифеста с blacklist + построчный reject); пропуск по совпадению содержимого (идемпотентность); живых записей в AMO/Tallanto/ASR нет, LLM-вызовов 0.
- **Идемпотентность вливания** подтверждена повторным apply в отчёте (updated=0, skipped_same=3439) — согласуется с тем, что D4 сейчас видит «уже влито».

## TZ-21 Block B — пересборка профилей: PASS_WITH_NOTES

Проверено по базе `tz21_profiles_after_tail_20260613/customer_profiles.sqlite`:
- profiles **18 399** (без изменений), fields_total **190 670**, active (`superseded_by=''`) **117 702**, superseded **72 968** — все совпали с отчётом.
- Дедуп-метрики: профили с 2+ детьми **4411 → 4410**, merge-маркеры **835 → 841** — совпали.
- **Идемпотентность пересборки:** `customer_profiles.sqlite` и `customer_profiles_idempotence.sqlite` совпадают по (профили, поля, активные). Источник `tz12_working_batch3` не пересоздан.
- Зона v7 звонков выросла на +3 439 (Block A это и обеспечил).

**Заметка-наблюдение (не блокер):** активных полей стало на **121 меньше** (117 823 → 117 702). По моей сверке это просадка коммерческих полей при ре-анализе хвоста более строгой v7 (target_product и objection чуть упали), частично компенсированная ростом `child_name`. Это ожидаемое поведение v7 (честнее пустые поля + needs_review-строки дают меньше полей by-design), не потеря данных и не дефект вливания. **Действие:** при обзоре карточек CRM (трек D7) держать на виду, что часть карточек стала «суше» — это норма, не баг.

## TZ-19 — инструменты: PASS

- Импорт-скрипт — см. выше (безопасен).
- Дифф `builder.py` (main..ветка) — мелкий и безобидный: добавлен помощник `connect_read_only` (открытие SQLite read-only под путём с пробелом) + правки шапки; **функции склейки детей не тронуты** (важно: не конфликтует с C1 из D7).
- Прочее (calls-review-table, tail-bundle-builder) — отчётно-инструментальное; полный pytest по отчёту 3100 passed / 2 skipped (сверка кода-результата на данных выше — чистая).

## Что это разблокирует

Работа TZ-19/TZ-21 (8 коммитов на `codex/tz19-calls-review-table`) пригодна к мерджу в main. После мерджа: канон получит импорт-скрипт (нужен TZ-20) и безобидную правку builder; хвост уже в базе. Дальше — worktree от обновлённого main для D7 и TZ-20.

**Не вливать в этом шаге:** реальное вливание blacklist (отдельно, после регрейда 57) и финальную пересборку профилей (один раз, в конце, на ветке с флагами E2/C1 из D7).
