# Таргет-набор под зоны риска. 2026-05-26 (eval-only, holdout)

Назначение: точечно проверить, что недавние правки не хрупкие и не сломались на вариациях.
НЕ замена полному round-5/6 — это узкий «дымовой» набор по зонам, которые мы трогали.
Файл: `targeted_riskzones_2026_05_26.jsonl` (1 simulator_spec + 1 judge_spec + 9 персон).
Холдаут: `do_not_tune_against=true`, `do_not_use_as_fewshot=true`. Один прогон, без тюнинга под него.

## Что покрываем (9 персон, Фотон 5 / УНПК 4)

Зона 1 — возврат (фикс маршрута `manager_only`, чтобы возврат не подменялся «правилами поведения»):
- `rz_foton_refund_prepay_rules_01` — возврат заранее, до оплаты;
- `rz_unpk_refund_paid_dispute_02` — реальный спорный возврат после оплаты;
- `rz_foton_refund_sneaky_info_03` — прощупывает условия возврата «просто интересно».

Зона 2 — выходные / частичный ответ (soft_guidance, без выдумки точных дней):
- `rz_unpk_weekend_partial_04`, `rz_foton_weekend_partial_05`.

Зона 3 — очная цена (регресс hard_verification, цена должна отвечаться автономно):
- `rz_foton_offline_price_06` (44 600 / 74 500), `rz_unpk_offline_price_07` (49 000 / 82 000).

Зона 4 — частота 1 vs 2 раза в неделю (KB-фикс, не путать продукты):
- `rz_unpk_frequency_1_vs_2_08`.

Зона 5 — тёплый слой не протаскивает выдумку/гарантии:
- `rz_foton_warmth_trust_guarantee_09` (тревожный родитель + запрос гарантии результата).

## Как прогонять (после внедрения тона и перевалидации базы)

Те же параметры, что и в прошлой растяжке, только `--scenarios` на этот файл:
- `TELEGRAM_DIALOGUE_CONTRACT_PIPELINE=1`
- `TELEGRAM_DRAFT_X2_REWRITE=1`, `TELEGRAM_DRAFT_X2_REWRITE_MODE=all_eligible`
- bot reasoning `high`, judge reasoning `medium`
- снимок — пересобранный v6.3 (`quality_passed=true`)

## Что проверяю при регрейде (Claude)

Каждое спорное число — grep по актуальному снимку v6.3; каждое утверждение — против
`retrieved_facts` хода (не называет факт вне достанного). По зонам — success/fail в самих персонах.
Особое внимание: зона 1 (возврат не отвечается поведением), зона 3 (очная цена автономна),
зона 5 (тёплый слой не добавил выдумку/гарантию).
