# Техническое задание ТЗ-106 — настоящий замер закрытия real_006 (флаги ON, A/B к базовой линии)

- **Дата:** 2026-06-14. Заказчик: Дмитрий. Постановщик: Claude #1. Исполнитель: Кодекс-2 (владеет динамическим раннером и станцией M1).
- **Опора:** регрейд ТЗ-102 (PASS, фикс в каноне за флагом OFF), регрейд ТЗ-103 (PASS, базовый прогон с флагом model_p0 ВЫКЛЮЧЕН воспроизвёл провал real_006).

## Бизнес-смысл (зачем)
Мы починили распознавание срочного обращения моделью (ТЗ-102), но проверили только «проводку» юнит-тестами. Этот замер на стенде отвечает на главный вопрос ДО включения в бою: реально ли бот теперь зовёт менеджера на спорную оплату и перестаёт продавать поверх спора. Это прямое подтверждение нашей приоритетной метрики — качества — на самом болезненном случае (real_006).

## Что это за замер (точно)
Чистое A/B на ОДНОЙ машине M1 (правило сравнения прогонов — только одна машина):
- **A (база):** прогон ТЗ-103 `tz103_action_judge_m1_20260614_125652` — слой действий ON, **model_p0 OFF**.
- **B (этот замер):** ТОТ ЖЕ набор 12 персон (`autonomy_unpk_real_001..012`), всё то же, **плюс единственное отличие** — `TELEGRAM_DIRECT_PATH_MODEL_P0=1`.
Так дельта изолирует эффект ровно фикса ТЗ-102.

## Конфигурация прогона
- Набор: тот же, что в базе A (не менять — иначе сравнение грязное).
- Машина: M1, `--parallel 4`, снимок фактов v6.5, профиль `pilot_gold_v1`.
- Флаги через окружение: `TELEGRAM_DIRECT_PATH_MODEL_P0=1` И `TELEGRAM_DEAL_ACTION_DECISION=1` (оба ON). Больше боевых защит не трогать.
- В сводку дампить `effective_flag_profile` (фактический профиль) — как в ТЗ-103. Запрет флаг-ключей в персонах остаётся (валидатор ТЗ-103).

## Приёмка — закрытие real_006 (главное)
- **`autonomy_unpk_real_006`, ход 1:** route = `manager_only`; модель пометила `is_p0=true`; текст клиенту = чистая передача менеджеру БЕЗ консультативной продажи и без вываливания общих контактов как ответа; `p0_latched=true` после хода.
- **`autonomy_unpk_real_006`, ход 2** (повтор просьбы про менеджера + вопрос про гибкую оплату «математика 3 класс»): удержан латчем как P0 — бот НЕ выдаёт скидки/варианты оплаты, остаётся на «спор решает менеджер».

## Приёмка — отсутствие нового вреда (обязательно)
- **Ноль ложных срочных на нейтральных:** персоны `real_001..005, 007..012` НЕ должны массово стать `manager_only`/P0. Дать таблицу маршрутов по диалогам A против B — измениться должен только real_006 (и только если где-то есть настоящий P0).
- Судья действий: жёсткие барьеры по-прежнему `{}`, опасных ходов 0.
- Бренд-чистота и отсутствие новых выдумок (сверю сам).

## Поставка
Папка прогона в `audits/_inbox/...` (4 файла как обычно) + `effective_flag_profile` в сводке + таблица дельты маршрутов A против B по диалогам. Ревью — Claude #1 (регрейд по сырью: прочитаю ходы 1–2 real_006 и дельты по нейтральным; сравнение строго A(M1) против B(M1)).

## Чего НЕ делаем
- Флаг в боевой профиль `pilot_gold_v1` НЕ добавляем — только замер.
- Набор не меняем (чистота A/B). 
- Это НЕ замер полноты действий по истине — для него нужен золотой набор с ручной разметкой (отдельный ТЗ-107 после разметки Дмитрия).
