# ТЗ-116 (финал, после аудитора) — Группа 4: офлайн-понимание (CRM/анализ) → модель, детерминизм верифицирует

- **Дата:** 2026-06-15. Заказчик: Дмитрий. Постановщик: Claude #1. Исполнитель: **D4** (трек «действующие клиенты / CRM / анализ»). Прошло круг аудитора (триаж по сырью).
- **Честно (по триажу):** Группа 4 — это в основном **«включить и замерить УЖЕ построенные LLM-пути»** (они есть, выключены флагом) + пара реальных офлайн-фиксов. Это ОФЛАЙН-слой (CRM/анализ), не ответ клиенту — другая цель эпохи. Каждый пакет за флагом default OFF, замер на офлайн-наборе, мой регрейд.
- **Принцип:** смысл решает модель, детерминизм верифицирует (точный AMO `status_id`, числа, бренд-токены — жёсткие инварианты, не ослаблять).
- **Пути:** все под `src/mango_mvp/...`.

## ИСКЛЮЧЕНО из Группы 4 (по триажу — это НЕ офлайн)
- `insights/sanitizers.py` (маска имён регексом), `insights/tone_score.py`, `insights/phase2_detectors.py` — **ЖИВОЙ путь бота** (зовутся из `channels/subscription_llm_parts/provider.py`, `post_layers.py`). Их fail-closed/миграция — отдельный бот-трек, НЕ Группа 4. НЕ трогать здесь.
- `ingestion.py` «нет маркера = STRONG_UNIQUE» — **уже исправлено** в коде (`STRONG_UNIQUE if phone/email else INFERRED`). Пункт снят.
- Бренд-логика в `channels/dialogue_contract_pipeline.py`/`humanity_rewriter.py` — живой путь, не здесь.

## ПАКЕТ A (ПРИОРИТЕТ) — deal_aware: включить и замерить LLM-контур качества сделки
- **Факт:** LLM-путь уже построен. `amocrm_runtime/config.py:271` `CRM_ANALYSIS_MODE` (default `heuristic`; есть `llm_shadow`/`llm_primary`); модуль `amocrm_runtime/deal_llm.py`; в `llm_primary` LLM ПЕРЕБИВАЕТ эвристику (`amocrm_runtime/deals.py:817-822`). Запись в AMO под двойным гейтом (`crm_amo_deal_writeback_safe_mode` default True + ручной `apply_writeback`).
- **Задача (не «строить», а включить+замерить+калибровать):** прогнать `llm_shadow` → сверить с эвристикой на офлайн-наборе → перевести в `llm_primary` под safe_mode. Регекс-поля `deal_text_builder.py` (next_step/topic/budget) и `deal_state_classifier.py` — в **advisory** (подсказка модели, не арбитр). Пороги `deal_attribution.py` (0.76/0.69) — откалибровать на размеченных исходах или задокументировать. Эвристика в `deals.py:732-736` — оставить как блокер-сигнал, не как вето над LLM.
- **Не ослаблять:** AMO `status_id`-инвариант, safe_mode, ручной apply_writeback. Кормит AMO-запись (не бот). Флаг/режим + замер + регрейд.

## ПАКЕТ B — outcome_linker (единственный офлайн-кусок инсайтов)
- **Где:** `insights/outcome_linker.py:120-174` — won/lost по счёту слов БЕЗ отрицания («не оплатил» → может дать paid), магические скоры 0.82/0.86/0.76. Офлайн (импортёры — productization/product_api, не бот).
- **Задача:** исход классифицирует модель (с учётом отрицания); регекс-счёт — вторичная подсказка. Флаг + замер. (`sanitizers/tone/phase2` сюда НЕ входят — живой бот.)

## ПАКЕТ C — question_catalog: офлайн-пересборка на LLM (или задокументировать)
- **Факт:** LLM-путь реален с фолбэком (`question_catalog/classifier.py:144-177`), гейт `QUESTION_CATALOG_LLM_ENABLED` default False; `classify_question` зовётся ТОЛЬКО офлайн (`codex_full_run.py`/`rebuild_from_predictions.py`), бот берёт лишь загрузчик таксономии — бота НЕ задевает.
- **Задача:** либо включить LLM-путь для офлайн-пересборки каталога + ключ модели, либо задокументировать регекс как осознанный офлайн-инструмент и закрыть «ловушку флага». Решить по факту использования каталога. Флаг + замер на размеченных вопросах.

## ПАКЕТ D — transcribe: включить LLM-распределение ролей + замерить
- **Факт:** `services/transcribe.py:1634` `MONO_ROLE_ASSIGNMENT_MODE` default `off`; LLM-режимы `openai_selective`/`ollama_selective` уже построены (`:1656-1679`, `_assign_roles_with_openai:1482`). Офлайн.
- **Задача:** включить `openai_selective` + замерить на размеченных моно-звонках; словарь cue — низкоуверенная подсказка/фолбэк. Флаг + замер.

## ПАКЕТ E — узкий офлайн-остаток identity/brand (НЕ дублировать трек)
- **Уже сделано треком (НЕ трогать/не дублировать):** `customer_profile/child_resolver_llm.py` (модельная идентичность ребёнка + бренд-на-ребёнка), `customer_timeline/canonical_readonly_import.py` (STRONG_UNIQUE/INFERRED/**AMBIGUOUS**, cross-source дубли), STRONG_UNIQUE-дефолт в `ingestion.py` (исправлен).
- **Реальный остаток:** офлайн `canonical_readonly_import.py:1406-1412` `infer_brand` ловит только точные токены → варианты/кириллические артефакты → `unknown`. Задача: расширить ЭТОТ резолвер (кириллица-aware), `unknown`→fail-closed; НЕ плодить второй и НЕ трогать живую бренд-логику. `source_ref` в `customer_id` — низкий приоритет (срабатывает лишь без phone/email).

## Дисциплина / приёмка
- Каждый пакет: флаг/режим default OFF + замер на офлайн-наборе (модель vs детерминизм) + мой регрейд. Жёсткие инварианты (status_id, числа, бренд-токены, safe_mode) не ослаблять. Живой путь бота (`channels/subscription_llm_parts/*` и импортируемое им: sanitizers/tone/phase2) НЕ трогать. Ветки от main; полный pytest зелёный; отчёты в `_done`.

## Порядок (по реальному влиянию)
**A (deal_aware → качество записи в AMO, дешёвый высокоотдачный шаг: shadow→сверка→primary) → C (офлайн-каталог) → D (роли в звонках) → B (outcome_linker) → E (infer_brand).**

## Чего НЕ делаем
- Не трогаем живой путь бота и его инварианты; не дублируем child_resolver/canonical import; не «строим» LLM там, где он уже есть (включаем+мерим); не чиним уже исправленное (STRONG_UNIQUE).
