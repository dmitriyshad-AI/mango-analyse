# Промт для D4 — ТЗ-16 (выдаёт Дмитрий)

Ты — D4, исполнитель трека «действующие клиенты». Задача — полностью выполнить ТЗ-16: `tasks/_inbox_codex/2026-06-12_TZ16_D4_profiles_v7_and_rerun_tail.md` (4 Шага: уборка replant-дерева, пересборка профилей на v7-выжимках, расчёт хвоста зоны перепрогона, анти-«автоответчик» микроправка). Не останавливайся, пока всё не выполнено и не отчитано.

Порядок работы:

1. **Сначала думаем.** Прочитай ТЗ-16 целиком, свой отчёт вливания v7 (`tasks/_done/2026-06-12_analyze_v7_import_report.md`, там твоя заметка о 77 ошибках) и регрейд ТЗ-14 (`D1_audit_backlog/existing_clients/REGRADE_TZ14_steps1_4_2026-06-12.md`). План коммитов — первым сообщением.

2. **Порядок Шагов:** 1 → 2 → (3 и 4 параллельно, до 2 субагентов с максимальным уровнем рассуждения — дорожки не пересекаются по файлам). Перед пересборкой Шага 2 — микропроба на 3–5 профилях, потом полная.

3. **Дисциплина:** фиче-ветка от main; каждый Шаг — отдельный коммит с тестами и NEG; полный pytest перед отчётом; `git status` чистый (ПДн!); `tz12_working_batch3` не менять — контроль хэшем до/после.

4. **Жёсткие границы (нарушение = стоп, не «обойти»):** запись в AMO/Tallanto/CRM запрещена; этап Б карточек не начинать; ASR/R+A не запускать; LLM-вызовы — ТОЛЬКО микропроба 5 звонков Шага 4; полный перепрогон 77 — только после отдельного «да» Дмитрия; главную папку (старая ветка, чужие незакоммиченные правки draft_loop) не трогать и не чистить.

5. **Не виси на вопросах.** Мелкие неясности решай безопасным дефолтом с пометкой в отчёте. Полная остановка только при: красном полном pytest не из-за твоих правок, риске ПДн, необходимости пишущей операции в CRM.

6. **Отчётность:** один финальный отчёт в `tasks/_done/`: коммиты по Шагам, все счётчики ТЗ-16 (метрики профилей до/после, цифры хвоста, итог микропробы), перечень NEG, `llm_calls_total` по ролям, 5 обезличенных примеров профиля.

Критерий завершения: все 4 Шага закоммичены, полный pytest зелёный, отчёт в `_done`, git чистый, replant-дерево удалено. До этого — не останавливаться.
