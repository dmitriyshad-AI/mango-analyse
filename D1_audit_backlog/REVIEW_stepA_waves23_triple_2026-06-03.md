# Регрейд тройной волны A (Волны 2-3 + Покрытие + Нить). По сырью. 2026-06-03.

Ревьюер: Клод #1. Коммиты последовательны на gen-стеке `7adc534c`:
`291b04ce` → `06cdc6e0` → `e4e39d01`. **Вердикт: PASS (все три, детерминированный NEG крепкий).**
Тон/живость gen — в кумулятивном прогоне M1 (нужна модель).

## Commit 1 — `291b04ce` SELLING_SIGNALS_FULL (anxiety + unmet_need + readiness)
Детекция в understand() (+keyword-пол), gen/det. Default OFF (`TELEGRAM_A_SELLING_SIGNALS_FULL`). Подтверждено
ассертами:
- **unmet_need free-text НЕ утекает клиенту:** используется как ТРИГГЕР, текст suffix-степа из ФАКТОВ
  (`_selling_unmet_need_step(all_facts, brand)`); тест: `"физика тяжело" not in text`.
- **anxiety:** «лицензия на образовательную деятельность» в тексте, но `"Л035" not in text` (номер не светится);
  без обещания результата.
- **unmet_need:** `"исправим" not in text`, `"пят" not in text` (нет «исправим на пятёрку»); предложение из факта.
- **readiness:** флаг есть, но `"куда платить"/"оплат" not in text` (шаг из факта, не эхо вопроса, не выдумка оплаты).
- **P0/risk** → ни одного selling-флага; нейтральная реплика → все сигналы none/false (не гиперактивно).
- бренд один (`"УНПК" not in` foton-тексте). Gen-композиция добивается железным `_verify` (бренд/давление/
  гарантия/число).

## Commit 2 — `06cdc6e0` TELEGRAM_A_COVERAGE (составные price)
Составные ценовые (онлайн+очно, несколько предметов). KB не трогал. Default OFF. Подтверждено:
- **сумму НЕ выдумывает:** `"Итоговую сумму ..." in text`, при этом `"59 500" not in text`, `"50 575" not in text`
  (не считает total — компоненты из факта + честный хвост к менеджеру). Консервативно — верно (нет арифметики-выдумки).
- компоненты из факта (`"29 750 ₽" in text`); нет факта → `"₽" not in text` + менеджер-хвост.
- бренд раздельно (`"Фотон" not in` unpk-тексте); cross-brand составной → None.

## Commit 3 — `e4e39d01` TELEGRAM_A_THREAD (перенос продающих слотов)
Узко: subject/grade/format/product/active_brand между ходами. Default OFF. Подтверждено:
- слоты переносятся на эллипсисе ТОЛЬКО под флагом (off → None; on → восстановил цену «29 750/47 250 ₽»);
- **явная смена темы вытесняет старое:** `explicit_switch → None`; реализация — текущий слот из сообщения
  перебивает память, память берётся только если `not _text_explicitly_mentions_selling_slot(...)`;
- **бренд из канала/контекста, не переопределяется** (`active_brand == "foton"` из context, тест
  `without_brand_override`);
- P0 не ослаблен: нить только НАПОЛНЯЕТ слоты для price/selling-правил, которые сами уважают P0 (selling только
  `if not risk_signals`); маршрут нить не трогает.

## Тесты
точечные 45/46/48, расширенно после C3 322, полный 2446 passed / 9 failed (прежние infra .phase12) / 1 skipped.

## Итог
Тройная волна детерминированно безопасна (free-text leak, выдуманные суммы, бренд-смешение, P0-пропуск, обещания
— закрыты). Готово к кумулятивному прогону: Клод собирает бандл M1 (оверлей на e4e39d01 + набор сценариев + v6.4 +
раннер с 3 флагами) → Конфиг 1/2/3 → регрейд по дельте (тон/живость/доля fallback/нить по сырью).
