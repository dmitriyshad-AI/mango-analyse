# ТЗ-16 (D4): профили на v7-выжимках + хвост зоны перепрогона + анти-«автоответчик»

Дата: 2026-06-12. Постановка: Claude (архитектор трека). Номер 15 пропущен намеренно: имя «ТЗ-15» уже занято несанкционированным отчётом auto resolver на старой ветке — не смешивать.
Основание: HANDOFF нити 4–6; ТЗ-14 смержено в main (f5a57281, полировки до 92e5ef10, регрейд пройден).

## Шаг 1. Уборка replant-дерева

Проверить, что отчёт `40c0a9fa` и полировки в main канона, затем удалить рабочее дерево `Mango-analyse-tz14-replant` штатной командой git worktree. ГЛАВНУЮ папку не трогать: она стоит на старой ветке с незакоммиченными правками чужого трека (draft_loop) — возврат её на main НЕ твоя задача.

## Шаг 2. Пересборка профилей клиентов на v7-выжимках

Вход: каноническая база звонков с влитыми v7-выжимками (22 679 шт., бэкап от 12.06). Сборщик профилей — механика ТЗ-12/13 из main (нормализация дубль-детей включена).
Выход: НОВАЯ папка `product_data/customer_profiles/tz16_profiles_v7_<дата>/` — `tz12_working_batch3` не перезаписывать и не менять.
В отчёт: профилей всего; заполненность ключевых полей до (tz12) / после (tz16); профили с 2+ детьми и merge_candidate до/после; конфликты superseded; счётчик звонков со старыми выжимками (blacklist 77 — допустимо по решению Дмитрия); 5 обезличенных примеров профиля.
NEG: повторная сборка идемпотентна; `tz12_working_batch3` байт-в-байт не изменён (контроль по хэшу до/после); ПДн не в git (gitignore-тест).

## Шаг 3. Хвост зоны перепрогона (только посчитать)

Зона трека: 46 187 звонков активных клиентов. Прогнано v7: 22 756 (за 12 мес, ≥60 сек). Посчитать read-only: размер пересечения; сколько звонков зоны осталось со старыми выжимками; разбивка хвоста по давности и длительности; оценка объёма второго среза (число звонков, прикидка времени конвейера из Яндекс-пакета analyze_rerun_20260611).
САМ ПЕРЕПРОГОН НЕ ЗАПУСКАТЬ — отдельное решение Дмитрия по цифрам.

## Шаг 4. Анти-«автоответчик» микроправка подсказки v7

По твоей заметке о причине 77 смысловых ошибок (длинный живой разговор сведён к «автоответчику»): микроправка подсказки v7 — при длинном транскрипте с диалоговыми репликами не классифицировать как автоответчик.
Проверка: микропроба на 5 звонках из blacklist_77 — ЕДИНСТВЕННЫЕ разрешённые LLM-вызовы этого ТЗ. Полный перепрогон 77 — только после отдельного «да» Дмитрия (цифры микропробы в отчёт).
NEG: 3 настоящих коротких автоответчика из канона по-прежнему классифицируются как автоответчики.

## Границы

Запись в AMO/Tallanto/CRM запрещена; этап Б карточек НЕ начинать (ждём ревью Дмитрия по пакету 20 семей); ASR/Resolve+Analyze не запускать; main не откатывать; правки — фиче-ветка от main, каждый Шаг отдельный коммит с тестами и NEG.

## Приёмка

Полный pytest зелёный + все NEG; отчёт в `tasks/_done/` со всеми счётчиками и `llm_calls_total` по ролям (ожидание: только микропроба Шага 4); `git status` чистый; `tz12_working_batch3` не изменён.
