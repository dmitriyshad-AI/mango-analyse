# Backward compatibility

Код и схема БД не менялись.

Влияние на существующее поведение:

- генератор bot-safe выжимок не изменён;
- `scan_bot_safe_context_pii` не изменён;
- правила видимости брендов не изменены;
- `next_step` не изменён;
- исходные чанки и `allowed_for_bot=0` не трогались;
- боевая БД не трогалась.

Совместимость: полная.
