# Промт MAIN — фикс конструкции 4b-теста (код не трогать). 2026-05-30.

> Скопировать в окно MAIN Кодекса.

---

Claude #1 прогнал `f0daa8fe` в песочнике. **4b-код правильный и безопасный:**
- щит 18 вето при `allow_default_autonomy=True` — зелёный;
- 432 smoke зелёные (поведение 1:1 при флаге off, изменение no-veto ветки ничего не сломало);
- прямые проверк `decide_route`: флаг+факт+safe-тема → `bot_answer_self_for_pilot`; флаг off / без
  факта / вне матрицы тем → `draft_for_manager`. Переворот включается флагом и держит все границы.

**Падает только твой новый тест** `test_pravka4b_default_autonomy_flip_is_flagged_and_bounded` (3185) —
**баг конструкции теста, НЕ кода.**

Корень (подтвердил прямым вызовом `_apply_v2_guard_chain`): позитивный кейс берёт черновик
«…курс стоит 49 000 ₽», а context несёт только флаг `client_safe_fact_verified=True` — БЕЗ реального
факта на «49 000». Поэтому `unsupported_promise` корректно ветирует непокрытое число → `manager_only`
(flags: `unsupported_promise_detected`, `price_redacted`). Тест ждал `bot_answer_self`. То есть переворот
правильно НЕ обходит вето.

Контроль (я проверил): тот же кейс с черновиком БЕЗ непокрытого числа
(«По подтверждённым данным сориентирую по программе.») → `bot_answer_self_for_pilot`. Переворот работает.

**Фикс — в ТЕСТЕ (код decide_route / guard НЕ трогать):** позитивный кейс должен использовать либо
черновик без непокрытых чисел (чистая безопасная фраза / verified-шаблон), либо дать реальный
покрывающий факт на «49 000» (`confirmed_facts`), чтобы `unsupported_promise` не срабатывал и был виден
именно переворот. Границы и негативные кейсы теста оставить.

Тесты не гонять (лимит) — Claude #1 перепроверит. После зелёного **4b закрыта** → собираю финальный
bundle для M1.