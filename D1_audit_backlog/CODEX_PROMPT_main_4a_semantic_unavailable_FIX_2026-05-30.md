# Промт MAIN — фикс semantic_unavailable (последняя красная категория 4a). 2026-05-30.

> Скопировать в окно MAIN Кодекса.

---

Claude #1 прогнал `e78cad33` в песочнике: **veto_category починен** (force_manager_only проходит).
431 passed, 1 failed — падение сместилось на следующую категорию. Я проверил ВСЕ оставшиеся разом
(вызвал тест-хелпер напрямую), чтобы закрыть за один заход:

- `semantic_unavailable` — **КРАСНАЯ**. Вход: draft «Передам информацию по курсу», `facts={}`,
  `faithfulness_fn=lambda: "not json"` (судья сломан). Ожидается `route="draft_for_manager"`,
  `fallback_reason="semantic_check_unavailable"`. **РЕАЛЬНО:** `route="bot_answer_self"`, `fallback_reason=""`.
- `no_draft_fn` — зелёная (`draft_for_manager` / `no_draft_fn`), НЕ трогать.
- Остальные 16 категорий — зелёные.

**Это не косметика, а маршрут:** при недоступном судье бот дал автоответ вместо ухода к менеджеру —
потенциальная дыра fail-safe. Критично до 4b (бот отвечает клиенту напрямую: при сбое судьи в проде
выдаст непроверенный ответ).

**Разберись (правило #1):** почему `semantic_unavailable` не fail-safe'ит на этом входе. Вероятная
причина: нейтральный черновик без claim/чисел не вызывает semantic-check → проверка пропускается →
`bot_answer_self`. Реши:
- **рекомендую (безопаснее):** недоступность судьи ВСЕГДА → `draft_for_manager` с
  `fallback_reason="semantic_check_unavailable"`, независимо от наличия claim;
- либо, если автоответ на черновик без claim осознанно безопасен — тогда баг в тесте (вход должен
  содержать claim, чтобы semantic-check реально вызвался) — но это надо обосновать.

**Негативный контроль:** нормальный путь (судья доступен, факт покрывает) по-прежнему даёт автоответ —
не сломать. force_manager_only / unknown_brand veto_category остаются зелёными.

Это ПОСЛЕДНЯЯ красная категория щит-теста 4a. После фикса 4a-щит полностью зелёный → можно 4b.
Тесты не гонять (лимит) — Claude #1 прогонит весь щит + smoke.