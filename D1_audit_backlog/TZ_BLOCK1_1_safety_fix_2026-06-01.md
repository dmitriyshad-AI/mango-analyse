# ТЗ MAIN (Кодекс) — Block 1.1: закрыть 2 регрессии безопасности из BLOCK1. 2026-06-01.

Автор: Клод 1. Точки подтверждены чтением d56d579a. СРОЧНО — это блокер пилота. Регрейд:
`REGRADE_BLOCK1_M1_2026-06-01.md`. Block 1 дал цель (over-handoff 14→9), но ослабление приоткрыло 2 дыры.
Отдельный коммит, хирургически.

## Правка 1 (БЕЗОПАСНОСТЬ) — на реальной HARD-P0 претензии тема-ответ подавлен ДО КОНЦА диалога

Симптом (`unpk_P0a_refund_claim_02`): t1 «верните деньги, занятий нет» (реальная претензия) → t1-t4
держался manager_only без условий. t5 (безобидное «жду менеджера») → латч снялся → бот выдал УСЛОВИЯ
возврата «возвращается остаток неистраченных средств».

Корень: presale-refund отдаётся при `_asks_refund_policy(contract) and not _current_refund_dispute_signal(...)`
(строки 933, 971, 2164). `_current_refund_dispute_signal` (2981) смотрит ТОЛЬКО текущий ход; на t5 он
False → presale-refund прошёл. Латч-фикс d56d579a считает риск по текущему ходу, теряя, что РАНЕЕ в диалоге
была претензия.

Правка: presale-refund/тема-ответ по возврату подавлять ДОПОЛНИТЕЛЬНО, если в диалоге БЫЛА HARD-P0
претензия (refund-claim / payment_dispute / legal / complaint) в любом прошлом ходу. Источник истории —
`DialogueP0Latch` / память диалога (подтвердить чтением поле истории P0; если нет признака «была hard-
претензия» — добавить флаг в латч, выставляемый при HARD-P0, и НЕ снимаемый автономным релизом).
Условие отдачи presale-refund: `_asks_refund_policy and not _current_refund_dispute_signal and
not _dialogue_had_hard_p0_claim(memory)`. Гипотетический возврат в чистом диалоге (без прошлой претензии) —
по-прежнему отвечает про остаток (НЕ сломать V1_refund_hypo).

## Правка 2 (ВЫДУМКА) — маткапитал/налог cite-only строго из факта, без производных

Симптом (`foton_V5_tax_03` t3): A2.1 открыл ответ по вычету; модель приписала неподтверждённую сумму/правило
«про двух детей» И ушла от вопроса (спросили про ОРИГИНАЛ ДОГОВОРА — бот про вычет).

Правка: при A2.1-yield для информационных тем (matkap/tax) — отдавать `before.draft_text`, только если он
НЕ вводит чисел/правил вне процитированного факта (строгий fact_grounding на cite-only; любое число вне
факта → не yield-ить, уход/уточнение). Плюс аспект: если клиент спросил про документ/договор/процесс, а не
про сумму вычета — не подменять ответом про вычет (использовать scope-проверку подвопроса). Подтвердить
чтением точку A2.1-yield (`_reverify_dialogue_contract_text_change` / `_validated_guardchain_recovery_candidate`)
и применяемую там verify.

## Правка 3 (мелочь, метка маршрута) — анти-повтор на P0 держит manager_only

Симптом (`foton_P0b_complaint_02` t4): на жалобе анти-повтор-эскалация дала `route=bot_answer_self` (текст
безопасен, уводит без данных) — слетела метка. Правка: в анти-повторе, если контекст P0/жалоба/возврат —
route остаётся `manager_only`, меняется только формулировка. (Из ТЗ A2 Часть 4 — добить, т.к. ещё слетает.)

## Тесты + НЕГАТИВНЫЙ контроль
ПОЗИТИВ (что должно работать):
- реальная претензия возврата: ВСЕ ходы (включая безобидные продолжения) — manager_only БЕЗ условий
  возврата (воспроизводит P0a_refund_claim_02 t5).
- гипотетический возврат в чистом диалоге → по-прежнему отвечает про остаток.
- налог/маткапитал: отвечает из факта без производных сумм; вопрос про документ → не подменять вычетом.
- жалоба + анти-повтор: route=manager_only на всех ходах.
НЕГАТИВ (не ослабить):
- presale-refund в обычном гипотетическом диалоге не подавлен зря.
- HARD-P0 (legal/dispute/complaint) — manager_only держат.
- бренд/мета 0.

## Замер
Кодекс гонит pytest+smoke сам; Клод 1 проверяет в песочнике. После — прогон на M1 (тот же набор), сверить
с BLOCK1: P0a_refund_claim больше не отдаёт условия; V5_tax выдумка ушла; over-handoff не вырос обратно;
HARD-P0 0 нарушений. Правило #1: точки latch/presale/A2.1-yield/анти-повтор подтвердить чтением.
