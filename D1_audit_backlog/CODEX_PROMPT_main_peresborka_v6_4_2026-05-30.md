# Стартовый промт — MAIN Кодекс (легализация v6.4). 2026-05-30.

> Скопировать в окно MAIN Кодекса. НЕ срочно — после правки 4, перед пилотом.

---

Ты — MAIN Кодекс, репозиторий. Прочитай:
1. `CLAUDE.md` (раздел про сборку KB: только `build_kb_release_v6_1_team_answers.py`).
2. `D1_audit_backlog/TZ_main_peresborka_v6_4_2026-05-30.md`.

**Контекст (проверено Claude #1 по git):** бот читает снимок v6.3; v6.4 не пересобрана в снимок и
собрана ЗАПРЕЩЁННЫМ `build_kb_release_v3_from_claude_handoff.py` с бизнес-логикой в Python
(refund client-safe, structured metadata, факт q15 с маршрутом `bot_answer_self`). `release_manifest.yaml`
не обновлён → чистый `v6_1` потеряет эту логику.

**Суть:** легализовать v6.4 — перенести бизнес-правки из Python в `release_manifest.yaml`/`_sources` YAML
(смысл не менять, только слой) → собрать разрешённым `v6_1` → сверить с прогнанным артефактом
(`_kb_v6_4_artifact_for_m1_20260529`) → semantic gate → переключить `DEFAULT_KB_SNAPSHOT_PATH` на новый
снимок → запрещённый скрипт пометить deprecated.

**Негативный контроль:** internal (юр.лица) не стал client-safe; бренд-раздел цел; `semantic_pass=true`.

**Ограничения:** сборку/semantic gate можно отдать Claude #1 в песочник, если лимит мал. Если перенос
велик — сообщи Дмитрию (альтернатива — зафиксировать долг, пилот на v6.3). Отчёт: что перенесено,
сверка с артефактом, semantic_pass, что переключено.