> DONE 2026-07-29 00:01 | ветка main | codex

> TAKE 2026-07-28 23:57 | ветка main | codex

Ветка: main
Зоны: src/mango_mvp/crm_card_aggregator.py, src/mango_mvp/services/analyze.py, tests/test_analyze.py, tests/, tasks/
Тест-команда: PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m pytest -q tests/test_crm_card_aggregator.py tests/test_analyze.py tests/test_analyze_xa_safe_pack.py
Семантический-аудит: нет

# P18: удалить мёртвые вычисления и обходной prompt-метод

## Цель

Удалить два вычисления, результаты которых не читаются, две вызываемые только
ими функции, а также приватную обёртку `_analysis_user_prompt`, используемую
только одним тестом. Тест должен проверять живой `_analysis_prompt_context`.

## Доказательства

- AST-аудит и `rg` подтверждают точные вызывающие.
- Удаляемые функции не входят в `__all__`, не подменяются и не вызываются динамически.
- `_analysis_prompt_context` остаётся единственным владельцем обоих prompt-полей.

## Приёмка

- CRM payload и prompt-контекст до/после совпадают.
- Ноль ссылок на три удалённых имени.
- Точечные и полный pytest зелёные.

## СТОП

- Обнаружен дополнительный вызывающий или побочный эффект.
- Красный тест.

## Результат

- Удалены 2 бесполезных вычисления, 2 обслуживающие функции и 1 приватная prompt-обёртка: 26 строк нетестового кода.
- Тест переведён на единственный живой владелец prompt-контекста.
- Точечные тесты: 59 passed; полный pytest: 5031 passed, 2 skipped.
- Добавлено строк нетестового кода: 0; удалено: 26.
- Новых файлов: 0; флагов: 0; зависимостей: 0.
- Сохранение совместимых обёрток отвергнуто: им не найдено ни одного живого потребителя.
