> DONE 2026-09-06 20:16 | ветка codex/ai-passport-group-quality-20260906 | codex

> TAKE 2026-09-06 17:33 | ветка codex/ai-passport-group-quality-20260906 | codex

> FAIL 2026-09-06 17:27 | ветка codex/ai-passport-group-quality-20260906 | codex | причина: Остановка до кода: Claude code-plan PASS_WITH_FIXES, preflight STOP; нужно решение Дмитрия о сужении приёмки до экспорта истории в 150 строк либо бюджете до 250 строк. Код и JSON паспортов не созданы, Problem-ID не закрыт.

> TAKE 2026-09-06 17:07 | ветка codex/ai-passport-group-quality-20260906 | codex

Ветка: codex/ai-passport-group-quality-20260906
Зоны: scripts/build_ai_student_passport_export.py, tests/test_ai_student_passport_export.py, tasks/, docs/worktrees_registry.md
Тест-команда: PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m pytest -q tests/test_ai_student_passport_export.py
Семантический-аудит: да
Feature-ID: feature.customer_timeline.student_passport_export
Problem-ID: problem.customer_timeline.student_passport_truncation_attribution
Исход: attempt_complete
Изменение: extend
Ключевые-символы: list_events_by_customer,split_thread_context,load_cache_rows,build_customer_dossier
Ключевые-слова: student passport,full communication history,child attribution,manager summary,email summary cache,event_child_attribution_v1

# Полезные паспорта одной группы без повторной выгрузки
## Допуск плана до кода: точная команда

Для этого code-plan review проверяется достаточность плана, не существование
будущего tests-файла. Штатный scripts/preflight.py:498-503 имеет аргумент
--skip-collect-only и передаёт run_collect=not args.skip_collect_only. Поэтому
точная команда ДО создания кода/тестов: python3 scripts/preflight.py --tz <этоТЗ>
--inventory <новыйinventory> --claude-receipt <новыйreceipt> --graph <существующаякарта>
--skip-collect-only. Этот штатный флаг отключает только преждевременный collect,
не inventory, receipt, зоны или остальные гейты. После реализации collect и
synthetic tests выполняются обязательно, затем независимый review результата.
Код и тесты до допуска писать нельзя; коммит до финального review Дмитрия
запрещён. Предложение сначала написать/закоммитить код нарушает границы владельца.
Первый preflight STOP был ONLY "Claude verdict is not PASS", а не collect-only:
при более ранних ошибках collect не выполняется (:484). В возобновлённой попытке
добавился drift status_fingerprint inventory; он пересчитывается штатно перед
новым pack/receipt и отдельно проверяется preflight, не игнорируется.
Не блокировать допуск на несуществующей проблеме отсутствия этого CLI-флага.

## Цель и границы

Исправить представление указанной владельцем текущей группы Tallanto (13 строк
в сохранённой Google-таблице). Получатель: менеджер и учебный филиал. Критерий:
понятная сводка, полноценная хронология, конкретный ребёнок, текущий учебный год.
Не менять writer/его HEAD/службы/CRM, не писать в SQLite, не запускать ASR,
полный LLM-экзамен, массовое извлечение почты или новые полные API-выгрузки.

## Источники и повторное использование

В .codex_local/passport_group_20260906/ сохранены GOOGLE_BEFORE.json (13 паспортов,
контакты, Tallanto_ID) и HISTORY_BEFORE.json (616 строк истории, без расшифровок).
Полные исходники остаются в прежней таблице и закрытом M1 compact от30.08,
канонической Calls DB. Рабочую изменяемую r5 НЕ читать параллельно writer.
Свежесть каждого источника показывать отдельно, не выдавать старый snapshot
за сегодняшние данные. Контакты/состав группы подтверждены предыдущим live05.09.
OWNER_DATA_PERMISSION.md фиксирует прямое разрешение владельца передавать Claude
полные клиентские данные; секреты не включать. Клиентские записи только в
.codex_local, в git только общий код/синтетические тесты/документация.

Graphify -> сырой код -> canonical inventory -> Claude pack/receipt -> preflight
по mango-development-process. Не переписывать процесс ради этой задачи.
Найденная причина представления: локальный JS chronology использовал
good.slice(0,8) и short(s,n=290). Старые временные генераторы уже отсутствуют;
не восстанавливать их дефектные ограничения. Важные функции уже существуют:
src/mango_mvp/customer_timeline/store.py (постраничная лента),
src/mango_mvp/customer_timeline/bot_safe_runtime_context.py (образец child join),
scripts/email_pipeline/summary.py (очистка цепочки),
scripts/email_pipeline/orphan_driver.py (готовый summary cache).

## Минимальное решение

Варианты: ручная правка ячеек; очередной временный скрипт; сохраняемый небольшой
сборщик поверх существующих читателей. Выбрать третий: повторяемое обновление
не должно снова требовать расследования. Новый файл явно разрешён этим ТЗ;
если inventory найдёт подходящий действующий сборщик, расширить его вместо нового.
Сохраняемый сборщик: не более150 обычных строк; настоящие safety-проверки отдельно.
Вариант 1 принят владельцем как граница кода, не отмена полного бизнес-результата.
Две операции: (1) сбор источников и доказательств; (2) Claude + Codex по полным
приватным данным всех13 формируют и независимо проверяют сводку, учебный профиль,
ситуацию/риски и следующее действие. Не перекладывать заполнение на Дмитрия.
Проверенный semantic payload содержит фиксированный as_of и source_digest;
сборщик принимает только совпадающий пакет целиком, устаревший отвергает.
Эвристический генератор смысла не создавать. Будущий повторяемый skill сохраняет
обе операции; skill пока не создаётся. Google-write/readback делает Дмитрий.
Не создавать новую модель семьи, импортёр, generic платформу и зависимости.

1. История не обрезается на полуслове и не ограничивается8/12событиями. При
   лимите ячейки делить на законченные части/строки, сохранять ссылки и все события.
2. Доказанные события конкретного ребёнка отдельно от семейного контекста и
   неопределённых событий. Использовать имеющиеся child attribution high/medium,
   не присваивать общий телефон ребёнку. Нельзя писать «разговоров не было», если
   причина в недостаточной привязке. Старый Google Q является предупреждением
   прежней проекции, не запретом владельца; решение требует исходных доказательств.
3. Полные конспекты звонков; письма из готовой качественной выжимки/очищенной
   головы, без подписей/MIME дублей. Смысл цитат сохранять с отличием автора.
   «Квитанция отправлена» не равняется подтверждённой оплате. Исходник сохранять.
4. Поля: контакты; сводка менеджеру; полная история; семья/ребёнок; статус2026/27
   с группой и датой проверки; учебный профиль (цели/уровень/пробелы/интересы);
   текущая ситуация и риски; следующее действие; источники/качество. Скрытые ID
   сохранить для обновления. Убрать дублирующую старую историю и шаблон ответственного
   из представления, не удалять исходные данные/чужие вкладки.
5. Сводка и действие обновляются вместе с историей. Отличать факт, слова родителя,
   диагностику и рекомендацию. Не создавать поручение из старого обещания.

## Приёмка

Тесты: длинный конспект; >12событий; несколько частей без потерь; два ребёнка
с одним телефоном; отсутствующая привязка не равна отсутствию истории; письмо
с цитатой/подписью и сохранённой просьбой; учебный год/отмена/запись!=посещение;
новое событие обновляет зависимые поля; повтор даёт тот же экспорт без дублей.
Сначала1паспорт -> все13, независимая смысловая проверка по источникам.
Не автозелёнить неизвестный уровень или спорного ребёнка. Генератор готовит
локальный JSON/пакет, Google-запись и readback делает основной исполнитель MCP.
Сохранить до/после, найденные ограничения, команды и последовательность успешного
повтора в аудите для будущего skill. Skill сейчас не создавать до успешного повтора.

## СТОП

Любая клиентская отправка/CRM/Tallanto/SQLite запись; потеря исходных событий;
отсутствующее доказательство личной принадлежности выдаётся как точный факт.
Не передавать токены/пароли. Не менять HEAD активного writer.

## План для read-only проверки до кода

Это проверка достаточности плана, не утверждение о выполненной приёмке.
Один вызов Claude Opus: architect-auditor, breaker, business-auditor, cleaner;
четыре ракурса одного процесса, не четыре независимых процесса. Клиентские
файлы и базы в этот canonical code-plan context не включать и не открывать.
Смысловой аудит конкретного результата будет отдельно по разрешённому локальному
пакету после реализации. Код процесса и PII gate не менять.

Точный внешний формат: исходные сохранённые gridData диапазоны объединяются
по startRow/startColumn; скрытые Tallanto_ID/Passport_key сохраняются. Данные
двух диапазонов истории не считать дублями. Исходные JSON остаются неизменными.
Для истории: готовый list_events_by_customer с next_cursor, без обрезки числа
событий, цикл до None, только замороженный источник без WAL/writer.
Второе RO-чтение событий без владельца: только по доказанному конечному списку
event_id/source_id/исходных ссылок, извлечённых из точной identity-связи/конфликта
или предоставленного evidence. Широкий SELECT всех customer_id IS NULL запрещён.
Superseded сохранять только в raw_sources с active=false, не возвращать в живую
историю. Старые HISTORY_BEFORE строки без точного ключа не объявлять новыми
точно связанными событиями: сохранять отдельно как неопределённые источники,
с координатой исходной строки и без потери текста. Не обещать полноту всей семьи
без доказанных family links. Старые служебные и неактивные источники отделены.
Кэш письма читать только по sha целевых событий; load_cache_rows изучен как
владелец схемы, но целиковый SELECT его API здесь не подходит. Очищенная голова
через split_thread_context, остальные части сохраняются в raw для смысловой
проверки; не выкидывать просьбу после подписи/цитаты молча. Полные конспекты
звонков брать по точному исходному ID из разрешённой Calls DB, не по похожести
ФИО: analysis_json.history_summary с проверкой source identity/даты, а не
transcript_client (это одна сторона, не полный разговор). Недоступный полный
конспект явно отмечать; snapshot текст сохранять. Историю внешнего листа без доказанной личной связи показывать как
неопределённый контекст, не терять и не повышать телефон до child evidence.

Связь: exact tallanto_student_id -> существующие family_links_v1 с точным
tallanto_student_ids -> matched high/medium event attribution. Старый Google Q
не заменяет проверку доказательств; pending_attribution, identity-конфликт и
явный конфликт брендов metadata/record/payload запрещают личную атрибуцию. Таблицы и
семейный граф не менять. Доказанное семейное событие не равно событию ребёнка.
build_customer_dossier изучен как существующий владелец менеджерского dossier;
его семейная сводка не заменяет требуемую индивидуальную атрибуцию. Использовать
готовые нижние читатели из owner_map через узкий адаптер, не править владельцев
вне разрешённых scripts/tests и не дублировать существующую модель семьи.
Dossier с его LIMIT и усечением НЕ источник полной истории/полных конспектов.

Вывод: локальные паспорта и отдельные полные записи/части истории со ссылками.
Любая нехватка полного текста или источника явно помечается. Статус группы
только на дату старого подтверждённого среза; запись не означает посещение,
название продвинутой группы не означает доказанную диагностику. Сводка и
следующее действие пересчитываются из нового набора доказательств одновременно;
старые обещания не превращаются в текущие поручения. Учебные выводы требуют
источника; неизвестное не автозаполнять догадкой.

Бюджет: сбор данных/parts и приём проверенного semantic payload <=150 обычных
строк на stdlib и готовых helpers. Новые смысловые поля делает отдельная операция
Claude + Codex, не кодовая эвристика. Уровень/цель/оплата/посещение неизвестны,
если соответствующего личного доказательства нет; полный бизнес-результат
остаётся обязательным. as_of фиксирован входным параметром; source_digest связан
с полным собранным raw+источниками/контактами/датами, не с часами запуска.
Изменение источника отвергает устаревший semantic payload целиком.
При отклонении пересобирается смысловая операция Claude + Codex; выпуск пустого
смыслового паспорта не считается результатом. Сырой draft/export нужен для
проверки и не считается полной приёмкой. Операционный bot-safe scope может
отличаться от полного внутреннего raw-экспорта: это явно отражается в quality,
без повышения bot/client видимости исходных событий.

Разногласия с прежним Claude review зафиксированы владельцем: пункт7 не означает
новый preflight ПОСЛЕ кода ДО его написания. Плановый preflight проходит сейчас,
после кода - независимый review результата. Rebase/amend текущей ветки сами по
себе не меняют чужой ref, но таких команд не будет. Обычная обсуждаемая развилка
не причина переводить active-задачу в failed. Предыдущий FAIL исторический;
эта попытка возобновлена владельцем, остаётся active пока можно продолжать.
Результат этой попытки: исполняемый export, synthetic tests, local JSON13,
отдельно сформированные/проверенные semantic поля и точные неоднозначности.
Нельзя финализировать как готово без собранных данных. До/после Google остаются
за основным исполнителем; финальный коммит локального блока Дмитрий отдельно
разрешил 06.09 после завершения своего preflight. Прочитать только точечные исходники,
не ревьюить унаследованный r5 diff как дифф паспортов.

## Результат локальной попытки 06.09.2026

Следующий шаг: основной исполнитель выполняет Google-write/readback принятой по Godel final PASS версии и предоставляет receipt; затем merge по решению Дмитрия.

Данные собраны, не заготовки: 13 паспортов и 1576 активных событий; 9 superseded
только в raw, 616 прежних строк сохранены отдельно. 431 полный canonical/increment
конспект оставлен без сокращений. Дополнительно 3 августовских звонка одной строки
проверены по предоставленному main первичному Google CellData, телефону, времени
и полному тексту; scope остаётся family_context, точные слова о ребёнке выделены
в смысловых полях. Остальные 12 источников и проекций не изменились.
182 законченных конспекта писем: 47 проверенных по источнику кэшированных,
72 по тексту, 63 по доступной служебной записи с явным ограничением полноты.
35 служебных конспектов независимого Russell включены без повторного Claude.
Все 13 сводок, профилей, ситуаций и действий сформированы Claude + Codex;
замечания main/Anscombe внесены, спорные личные выводы и служебные фразы убраны.

Расчёт только в собственной .codex_local/passport_group_20260906/:
- PASSPORTS_13_SOURCE_ACCEPTED.json: полные источники и доказательства.
- SEMANTIC_FINAL.json: принятые локально смысловые поля и конспекты событий.
- PASSPORTS_13_RENDER.json: полный связанный экспорт.
- PASSPORTS_13_ROWS.json: облегчённый пакет 13 строк для проверки main.
- FINAL_AMBIGUITIES.json, FINAL_VALIDATION.json: точные границы и проверки.
- GOOGLE_WRITE_CONTRACT.json и GOOGLE_PASSPORT_VALUES/GOOGLE_HISTORY_VALUES.json:
  данные для существующего MCP основного исполнителя, не новый адаптер.
  Таблица A5:W18 (14x23), хронология A5:S1581 (1577x19), включая заголовки.
  Для хронологии нужно минимум 1581 строк; прочие листы/данные не затрагивать.

as_of: 2026-09-06T15:01:48.182317+00:00.
source_digest: a5a7a9977e31998a18cab2be975223e68d8f41fcc8b7e50dca79ef08dd69cc31.
Исходная r5 после получения замороженного среза больше не читалась. Основной
исполнитель предоставил дополнительные Google-доказательства; этот блок не
выполнял Google/API/CRM write, ASR, R+A и не менял DB identity.

formal_pass: 282 tests PASS, включая мораторий и соседние читатели. Повторный
экспорт reused=13/13, 0.4054s; SHA и mtime выходного файла неизменны. 150 обычных
строк +196 safety +36 пустых/комментариев; новый исполняемый файл 1, тестовый 1,
feature flags 0, зависимости 0. Принятый плановый Claude receipt PASS не менялся;
замечания post-code сохранены и разобраны отдельно, не переименованы в PASS.

Локальный аудит: audits/_inbox/ai_passport_group_quality_20260906T140900Z/.
После Godel исправлены три согласованные находки: документы не равны занятиям
и не меняют отправителя, пожелание точной карточки не теряется, направление
двух писем соответствует собственному тексту. Один унаследованный AI-конспект
исправлен по полной расшифровке; оригинал retained. Исправление допускается только
с хешами исходного события/primary evidence/готового текста и без изменения
личной принадлежности. 430 остальных полных call summaries неизменны.
Адресный Godel-recheck завершён PASS, 06.09.2026, версия 20:06 МСК; main подтвердил
принятие и разрешил финальный коммит. Первичный отчёт и его дополнение сохранены
в PD/INDEPENDENT_FINAL_BUSINESS_REVIEW.md. Проверенный ROWS SHA:
1bb5b4022ab0c5a1bd550ea1916cda8d89f07b0ab0b33fd9778ab18610a3493f.
Новые 13 LLM проверки не запускались. Прежние замечания Claude сохранены в аудите,
их raw verdict не подменён; финальная выбранная версия принята main по Godel.

Локальная попытка завершается как attempt_complete, не problem_closed: независимая
смысловая проверка PASS, но Google/readback и merge не подтверждены. GOOGLE_*
payloads после принятия не изменены; H5 основной исполнитель переименует в
«Статус в группе текущего учебного года». Receipt Google ещё не получен.
Ветка и worktree сохраняются до
успешного листа и merge; удаление только по отдельному разрешению владельца.
